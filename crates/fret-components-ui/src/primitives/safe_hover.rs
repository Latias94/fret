//! Safe hover corridor helpers for nested menus (Radix-aligned outcomes).
//!
//! This is a deterministic, geometry-only helper inspired by Floating UI's `safePolygon`, and is
//! used to implement Radix-like submenu ergonomics.

pub use crate::headless::safe_hover::safe_hover_contains;

