[package]
name = "fret-demo-web"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]

[target.'cfg(target_arch = "wasm32")'.dependencies]
fret-demo = { path = "../fret-demo" }
fret-launch = { path = "../fret-launch" }
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"
