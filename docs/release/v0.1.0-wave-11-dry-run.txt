# Fret v0.1.0 Wave 11 dry-run status

Date verified: 2026-04-01.

Commands run:

- `cargo publish --dry-run -p fret-bootstrap`

Status:

- `fret-bootstrap`: failed (awaiting earlier waves on crates.io)

Failure excerpts:

## `fret-bootstrap`

```text
Updating crates.io index
   Packaging fret-bootstrap v0.1.0 (./ecosystem/fret-bootstrap)
    Updating crates.io index
error: failed to prepare local package for uploading

Caused by:
  no matching package named `fret-app` found
  location searched: crates.io index
  required by package `fret-bootstrap v0.1.0 (./ecosystem/fret-bootstrap)`
```

Interpretation:

- Wave 11 is packaging-clean, but local `cargo publish --dry-run` still depends on earlier waves being visible on crates.io.
- Registry-gap failures are expected when later waves depend on crates that have not been published yet.
