---
type: Work Progress
title: Select public surface is closed to Pass
timestamp: 2026-07-07T20:05:14Z
tags:
  - shadcn
  - select
  - ui-gallery
  - public-surface
status: verified
---

# Summary

Closed the shadcn-aligned Select single-select lane to `Pass` and fixed a stale docs-surface import
ordering assertion.

# Truth

- `docs/shadcn-declarative-progress.md` now marks `select` as `Pass`.
- `apps/fret-ui-gallery/tests/select_docs_surface.rs` now expects the current rustfmt import order
  in the copyable usage snippet.
- `docs/audits/shadcn-select.md` now cites focused docs-surface, recipe/headless, web-golden, and
  matrix packet gates.
- Base UI multiple/object-value examples remain explicitly scoped as a separate future
  public-surface workstream.

# Verification

- `cargo nextest run -p fret-ui-gallery --test select_docs_surface`
- `cargo nextest run -p fret-ui-gallery --test ui_authoring_surface_default_app select_page_uses_typed_doc_sections_for_app_facing_snippets`
- `cargo nextest run -p fret-ui-shadcn --lib select::tests`
- `cargo nextest run -p fret-ui-kit --lib select_item_aligned_items_height_uses_larger_listbox_or_scroll_extent`
- `cargo nextest run -p fret-ui-headless --lib vertical_keeps_natural_items_height_when_leading_label_forces_top_clamp`
- `cargo nextest run -p fret-ui-shadcn --features web-goldens --test web_vs_fret_layout web_vs_fret_layout_select_scrollable_trigger_size`
- `cargo nextest run -p fret-ui-shadcn --features web-goldens --test web_vs_fret_overlay_placement web_vs_fret_select_cases_match_web_fixtures`
- `cargo nextest run -p fret-ui-shadcn --features web-goldens --test web_vs_fret_overlay_chrome web_vs_fret_select_overlay_chrome_cases_match_web_fixtures`
- Select matrix packet check: all 7 validation gates are `pass`, with no live-measurement,
  mismatch, or blocked status counts.
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_surface_policy.py`
- `cargo fmt --all --check`
- `python3 tools/check_layering.py`
- `python3 tools/report_largest_files.py --top 30 --min-lines 800`
- `python3 $HOME/.codex/skills/engineering-wiki-memory/scripts/wiki_memory.py validate --root docs/knowledge/engineering`
  (passes with existing historical warnings)
- `git diff --check`
