---
type: Work Progress
title: Dialog public surface is closed to Pass
timestamp: 2026-07-07T20:22:34Z
tags:
  - shadcn
  - dialog
  - ui-gallery
  - public-surface
status: verified
---

# Summary

Closed the shadcn-aligned Dialog public surface to `Pass` by fixing stale docs-surface assertions,
updating the header layout invariant to the current two-layer stack structure, and adding a
Dialog-specific misc-overlay placement gate.

# Truth

- `docs/shadcn-declarative-progress.md` now marks `dialog` as `Pass`.
- `apps/fret-ui-gallery/tests/dialog_docs_surface.rs` now matches the current copyable import order
  and records the actual docs order: upstream docs path through `RTL`, then the in-tree
  `Nested Combobox` modal-boundary proof before `API Reference` and explicit Fret follow-ups.
- `ecosystem/fret-ui-shadcn/src/dialog.rs` keeps the DialogHeader wrapping invariant focused on
  behavior: the stack shell and inner vertical flex root both fill width and use `min-w-0`.
- `ecosystem/fret-ui-shadcn/tests/web_vs_fret_overlay_placement/misc_overlays/fixtures.rs` now has
  `web_vs_fret_misc_overlays_dialog_cases_match_web_fixtures`, so Dialog placement evidence is not
  blocked by unrelated misc overlay fixture drift.
- `docs/audits/shadcn-dialog.md` cites the rerunnable Dialog-specific validation gates.

# Verification

- `cargo nextest run -p fret-ui-gallery --test dialog_docs_surface`
- `cargo nextest run -p fret-ui-shadcn --lib dialog_header_inner_stack_is_fill_width_for_wrapping_text`
- `cargo nextest run -p fret-ui-shadcn --lib dialog::tests`
- `cargo nextest run -p fret-ui-gallery --test dialog_diag_surface`
- `cargo nextest run -p fret-ui-shadcn --features web-goldens --test web_vs_fret_overlay_chrome dialog::fixtures`
- `cargo nextest run -p fret-ui-shadcn --features web-goldens --test web_vs_fret_overlay_placement misc_overlays::fixtures::web_vs_fret_misc_overlays_dialog_cases_match_web_fixtures`
- `cargo nextest run -p fret-ui-shadcn --features web-goldens --test radix_web_overlay_geometry radix_web_dialog_open_geometry_matches_fret`
- `cargo check -p fret-ui-shadcn`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_surface_policy.py`
- `cargo fmt --all --check`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_layering.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/report_largest_files.py --top 30 --min-lines 800`
- `PYTHONDONTWRITEBYTECODE=1 python3 $HOME/.codex/skills/engineering-wiki-memory/scripts/wiki_memory.py validate --root docs/knowledge/engineering`
  (passes with existing historical warnings)
- `git diff --check`

# Known Context

- The full misc-overlay placement aggregate can still be blocked by unrelated component fixtures.
  Use the focused Dialog placement gate above for this component's pass evidence.
- Dialog has no parity-matrix packet in the current `shadcn-component-parity-matrix-v1` artifacts, so
  the Pass evidence is anchored to the focused component, gallery, chrome, placement, and Radix
  geometry gates instead.
