---
type: Work Progress
title: Engineering Memory Log
tags: fret,engineering-memory
timestamp: 2026-06-30
---

# Engineering Memory Log

## 2026-06-30

- Created the first engineering memory bundle for the Fret architecture convergence goal.
- Recorded that the repository had no dirty changes to commit before planning began.
- Synthesized four audit lanes into `docs/plans/2026-06-30-001-refactor-ui-framework-architecture-plan.md`: repository boundaries, GPUI/Zed comparison, framework consumer/DX, and performance architecture.
- Key conclusion: Fret should continue toward `ViewId`, `ViewBoundary`, `notify`, prepaint frame products, stable handles, dirty graph, scene chunks, and source-policy gates rather than inventing a second runtime.
- Started execution on branch `feat/ui-framework-convergence`.
- Completed U1 contract freeze in commit `020bb34a37 docs(architecture): freeze ui convergence contract`; verified JSON, workstream catalog, layering, and whitespace checks.
- U2 source-policy checker exploration completed in subagent `019f15f9-d73a-7983-adf1-22de5ccc58b8`: add a heuristic `tools/check_surface_policy.py` with focused tests rather than broad repo-wide `fret_ui` / `fret_core` denies.
- Completed U2 in commit `84f60d8355 feat(tools): add ui surface policy gate`; added `tools/check_surface_policy.py`, focused unit tests, pre-release wiring, dependency-policy docs, and removed a raw `fret_ui::AnyElementIterExt` first-hour snippet.
- U2 verification passed: `python3 -m unittest tools/test_check_surface_policy.py`, `python3 tools/check_surface_policy.py`, `python3 tools/check_layering.py`, `python3 tools/check_consumption_profiles.py`, `python3 -m py_compile tools/check_surface_policy.py tools/test_check_surface_policy.py`, and `git diff --check`.
- U3 scaffold/app-ladder exploration completed in subagent `019f15fa-144d-7043-a574-2409e52848d1`: first slice should add/promote a public `workbench-lite` scaffold from the API workbench prototype, add a focused settings dialog recipe, and document existing `commands_keymap_basics`, `data_table_basics`, and `mutation_toast_feedback_basics`; workspace/canvas/node graph should stay advanced until public wrappers exist.
- Closed read-only audit subagents `019f1440-5447-75e2-b450-6700e61df8c6` and `019f1440-7fc5-7330-83d1-b51dae3f56f3`. Their reusable findings are: U4/U7/U8 should address layout dirty scans, subtree invalidation allocations, paint-cache rebase costs, text/layout cache and atlas churn; U6/U9 should address over-wide shadcn raw surface, giant menu/select files, table state/test monoliths, docking diagnostics vocabulary in runtime, and large docking/node/chart/plot tests or state objects.
- Completed U3 first slice locally: added `fretboard new workbench-lite`, generated a public app-facade workbench with command palette button, settings dialog, content pane, status bar, simulated submit flow, and stable `TEST_ID_*` selectors. Updated `docs/first-hour.md`, `docs/examples/README.md`, `docs/crate-usage-guide.md`, and `apps/fret-examples/README.md`.
- U3 verification passed after one transient crates.io `xml-rs` download failure was retried successfully: `cargo fmt --all --check`, `cargo nextest run -p fretboard scaffold`, `python3 tools/check_surface_policy.py`, `python3 tools/check_layering.py`, `python3 tools/check_consumption_profiles.py`, and `git diff --check`.
- U4 read-only explorer `019f1627-edfc-7001-be3e-8ef1d98c076e` recommends starting with observability, not `StableNodeHandle`: add counters for identity seeded/fallback resolve, parent pointer repair, GC reachability, dirty frontier breadth, dispatch snapshot cache hit/miss/build/invalidation, and observation churn; keep fallback scans temporarily but gate them toward zero after warmup.
- U4 first implementation slice adds frame stats for identity seeded hits/stale seeds/fallback scans, dispatch snapshot cache hit/miss/build/invalidation, parent pointer repair passes/repairs, and stale GC reachability/removal pressure. The diagnostic schema is propagated through `fret-bootstrap` and `fret-diag` perf-key registry, with `fret-ui-kit` focus restore call sites updated because live element-to-node resolve now mutates debug counters.
- U4 first-slice verification passed: `cargo check -p fret-ui`, `cargo check -p fret-bootstrap`, `cargo check -p fret-ui-kit`, `cargo nextest run -p fret-ui seeded_live_node_resolution_ignores_stale_detached_node_entry seeded_reusable_node_resolution_reuses_detached_seed_when_no_live_attached_node_exists seeded_live_node_resolution_records_seed_hit_without_fallback_scan dispatch_snapshot_cache_reuses_forest_across_frames_until_structure_changes set_children_same_children_repairs_parent_pointers_and_reconnects_dirty_descendant_layout --no-fail-fast`, `cargo nextest run -p fret-ui-kit resolve_restore_focus --no-fail-fast`, `cargo nextest run -p fret-diag registered_perf_keys_are_unique registered_perf_key_units_match_names registered_perf_key_contract_keeps_stats_and_gate_keys_additive registered_perf_key_inventory_doc_is_in_sync full_registered_perf_key_registry_covers_consumed_debug_stats_fields --no-fail-fast`, `cargo nextest run -p fret-bootstrap --lib`, `cargo fmt --all --check`, `python3 tools/check_consumption_profiles.py`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, and `git diff --check`.
- U4 follow-up explorers `019f167a-2cb3-7dd0-bffe-889f9be0f2ff` and `019f167a-817e-7ba3-9dbd-35815bc40c4b` narrowed the next steps: finish dirty frontier/observation churn stats first, then make U5's smallest ViewId-first slice a `DirtyViewFrontier` wrapper around the current dirty boundary set. They explicitly advised not to start U5 by moving dispatch snapshots, paint recording, or scene chunks.
- U4 second implementation slice adds frame stats and perf keys for dirty frontier layout/paint/hit-test breadth, dirty boundary max/layout-start/contained-candidate counts, and model/global observation-index edge add/remove/mask-change churn. `ObservationIndex::record()` stays the no-stats hot path; `record_with_stats()` is used only while UI debug stats are enabled.
- U4 second-slice verification passed: `cargo check -p fret-ui`, `cargo check -p fret-bootstrap`, `cargo check -p fret-diag`, `cargo nextest run -p fret-ui view_cache_runs_contained_relayout_for_invalidated_boundaries empty_model_observation_record_removes_previous_node_entry model_observation_record_reports_mask_changes_without_edge_churn empty_global_observation_record_removes_previous_node_entry global_observation_record_reports_mask_changes_without_edge_churn --no-fail-fast`, `cargo nextest run -p fret-diag registered_perf_keys_are_unique registered_perf_key_units_match_names registered_perf_key_contract_keeps_stats_and_gate_keys_additive registered_perf_key_inventory_doc_is_in_sync full_registered_perf_key_registry_covers_consumed_debug_stats_fields --no-fail-fast`, `cargo nextest run -p fret-bootstrap --lib`, `cargo fmt --all --check`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, `python3 tools/check_consumption_profiles.py`, `git diff --check`, and `python3 tools/perf/audit_perf_baselines.py --matrix docs/workstreams/ui-perf-zed-smoothness-v1/ui-perf-contract-matrix.md --strict`.
- U4 second slice committed as `df0d6620ff feat(ui): expose dirty frontier diagnostics`.
- U5 read-only explorer `019f16b7-57cb-70f2-9232-a5e7a760e802` confirmed the first dirty-frontier slice should replace raw `UiTree::dirty_boundaries` ownership with `DirtyViewFrontier { views: HashSet<ViewId> }`, keep explicit v1 boundary-node bridge methods, preserve existing debug schema names, and update ADR 0165/0327 alignment without upgrading status.
- U5 first implementation draft replaces raw dirty-boundary ownership with `DirtyViewFrontier`, updates contained relayout/pruning/debug reads to iterate `ViewId` or v1 boundary-node bridges, adds wrapper coalescing/bridge coverage, and updates ADR implementation alignment evidence. It intentionally leaves dispatch snapshots, paint recording, interaction/prepaint caches, and scene chunks untouched.
- U5 BoundaryFrameProducts slice committed as `ac1aa7ba27 refactor(ui): group boundary frame products`; it groups boundary-owned dirty/prepaint/scene-fragment/paint-cache state under `ViewBoundaryState::frame_products` and keeps dispatch snapshots, `PreviousFramePaintRecording`, scene chunks, tree-level `PaintCacheState`, and public APIs out of scope. Canvas tests now invalidate the actual canvas boundary node when asserting same-key repaint behavior, instead of requiring a low-level root paint invalidation to force descendant repaint.
- U5 BoundaryFrameProducts verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, focused `cargo nextest run -p fret-ui boundary_frame_products_own_boundary_dirty_prepaint_scene_and_paint_cache_state canvas_prepaint_output_is_visible_to_canvas_paint canvas_scene_fragment_is_boundary_owned_and_keyed_by_prepaint_key canvas_prepaint_can_prepare_text_scene_fragment_before_paint prepaint_output_store_is_keyed_by_cache_root_prepaint_key prepaint_output_is_owned_by_view_boundary_state_and_removed_with_node paint_cache_entry_is_boundary_owned_for_view_cache_roots paint_cache_side_store_entry_migrates_when_node_becomes_view_boundary view_cache_runs_contained_relayout_for_invalidated_boundaries dispatch_snapshot_cache_reuses_forest_across_frames_until_structure_changes hit_test_works_with_view_cache_root_and_prepaint_reuse_under_render_transform outside_press_observer_works_with_view_cache_root_and_prepaint_reuse modal_barrier_blocks_underlay_pointer_events_even_when_modal_root_is_pointer_transparent --no-fail-fast`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, `python3 tools/check_consumption_profiles.py`, and `git diff --check`.
- Note: a broad `cargo nextest run -p fret-ui --no-fail-fast` still reports unrelated existing failures in anchored scroll visual bounds, combobox active-descendant retained windowing, scroll/layout/text measurement, and scroll-into-view tests. These are not used as this slice's completion gate; future broad stabilization should address them separately before claiming package-wide green.
- U5 interaction replay entry slice committed as `8572864d49 refactor(ui): move interaction replay entries to boundaries`; it removes `Node::interaction_cache`, stores cache-root interaction replay entry metadata in `BoundaryFrameProducts::interaction_cache`, and propagates `interaction_cache_owner` through boundary diagnostics and `fret-diag` cache-root summaries. ADR implementation alignment now records `Node::interaction_cache` as a deleted runtime path and updates ADR 0167 evidence for boundary-owned cache-root range metadata.
- U5 interaction replay entry verification passed: `cargo fmt --all --check`, `git diff --check`, `cargo check -p fret-ui --all-targets`, `cargo check -p fret-bootstrap`, `cargo check -p fret-diag`, `cargo nextest run -p fret-ui prepaint_interaction_cache_entry_is_owned_by_view_boundary_state prepaint_interaction_cache_replays_for_clean_view_cache_root prepaint_interaction_cache_replay_translates_records_when_cache_root_moves prepaint_interaction_cache_root_move_invalidates_stale_root_only_hit_path prepaint_hook_runs_for_view_cache_root_even_when_reusing_interaction_cache hit_test_works_with_view_cache_root_and_prepaint_reuse_under_render_transform focus_traversal_uses_prepaint_cache_for_clean_nodes outside_press_observer_works_with_view_cache_root_and_prepaint_reuse --no-fail-fast`, `cargo nextest run -p fret-diag bundle_stats_preserves_cache_root_boundary_summary --no-fail-fast`, `cargo nextest run -p fret-bootstrap --lib --no-fail-fast`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, and `python3 tools/check_consumption_profiles.py`.
- U5 dispatch snapshot explorer `019f17b3-ddef-7370-a1d6-4d023b21b95f` concluded that `UiDispatchSnapshot` should not be forced into single-boundary `BoundaryFrameProducts`: its key is window + active layer roots + barrier, invalidation is window/layer-forest scoped, and input/focus snapshots can span multiple roots. The recommended next true boundary-owned frame-product candidate is `HitTestBoundsTrees` / hitbox inputs because it is already per layer root and built from prepaint interaction records.
- U5 dispatch snapshot owner-state pre-slice committed as `51551ae554 refactor(ui): name dispatch snapshot frame product state`; it moves raw `UiTree` dispatch snapshot generation/cache fields behind `DispatchSnapshotFrameProductState`, keeps dispatch routing behavior unchanged, updates ADR 0327 alignment to distinguish window/layer-forest ownership from per-boundary hitbox/input follow-ons, and extends the focused cache test to assert generation/cache entries are owned by the named frame-product state.
- U5 dispatch snapshot owner-state verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo nextest run -p fret-ui dispatch_snapshot_cache_reuses_forest_across_frames_until_structure_changes --no-fail-fast`, `git diff --check`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, and `python3 tools/check_consumption_profiles.py`.
- U5 hit-test bounds owner audit subagent `019f17f1-0549-72a3-abba-e3108c26da81` confirmed the correct cut: move only the reusable per-layer bounds index state to boundary products, keep `frame_id`, `clip_stack`, `leaves`, `search_stack`, `level`, and `next_level` as tree-level transient scratch, and avoid creating empty boundaries on stable-frame reuse.
- U5 hit-test bounds implementation slice moves the reusable hit-test bounds tree index from tree-wide `HitTestBoundsTrees.layers` into `BoundaryFrameProducts::hit_test_bounds`. `HitTestBoundsTrees` now acts as a frame/build/query scratch controller, stable-frame reuse updates an existing boundary product only, low-record/non-candidate rebuilds disable stale products, and `debug.boundaries[]` plus `fret-diag` cache-root summaries now report `hit_test_bounds_owner`.
- U5 hit-test bounds verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo check -p fret-bootstrap --features launch,ui-app-driver,diagnostics`, `cargo nextest run -p fret-ui tree::tests::bounds_tree --no-fail-fast`, `cargo nextest run -p fret-ui prepaint_interaction_cache_entry_is_owned_by_view_boundary_state prepaint_interaction_cache_replays_for_clean_view_cache_root prepaint_interaction_cache_replay_translates_records_when_cache_root_moves prepaint_interaction_cache_root_move_invalidates_stale_root_only_hit_path prepaint_hook_runs_for_view_cache_root_even_when_reusing_interaction_cache hit_test_works_with_view_cache_root_and_prepaint_reuse_under_render_transform outside_press_observer_works_with_view_cache_root_and_prepaint_reuse focus_traversal_uses_prepaint_cache_for_clean_nodes --no-fail-fast`, `cargo nextest run -p fret-diag bundle_stats_preserves_cache_root_boundary_summary --no-fail-fast`, `cargo nextest run -p fret-bootstrap --features launch,ui-app-driver,diagnostics ui_diagnostics::cache_root_boundary_tests::boundary_diagnostics_are_built_from_boundary_stats_with_cache_root_outcomes --lib --no-fail-fast`, `git diff --check`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, and `python3 tools/check_consumption_profiles.py`.
- U5 input/dispatch snapshot audit subagent `019f181e-fe0a-72c0-ba7c-79cf3a60ccf3` concluded these are not boundary-owned products today: `UiInputArbitrationSnapshot`, `UiDispatchSnapshot`, focus/capture, command availability caches, and layer policy flags are window/layer-forest scoped. It recommended either a command routing correctness pre-slice using `UiDispatchSnapshot.parent` or a separate `hit_test_path_cache` boundary-product candidate, but not migrating dispatch/input snapshots into `BoundaryFrameProducts`.
- U5 semantics audit subagent `019f181f-9af6-7741-93ae-7c5dd821f1fb` concluded the final `SemanticsSnapshot` and AccessKit tree update must remain window-owned because roots, barriers, focus/capture, and relation normalization are global. The safe boundary-owned cut is only reusable clean semantics subtree products/ranges.
- U5 semantics subtree implementation slice moves reusable boundary semantics subtree products into `BoundaryFrameProducts::semantics`. `refresh_semantics_snapshot` still builds one window-level `SemanticsSnapshot`, but clean boundary roots can replay from their boundary-owned product before falling back to previous whole-snapshot ranges. Publishing uses the new snapshot's subtree ranges to refresh each boundary product, and `debug.boundaries[]`, `fret-bootstrap`, and `fret-diag` now expose `semantics_subtree_owner`.
- U5 semantics subtree verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo check -p fret-bootstrap --features launch,ui-app-driver,diagnostics`, `cargo check -p fret-diag`, `cargo nextest run -p fret-ui semantics_subtree_reuse_product_is_owned_by_view_boundary_state semantics_snapshot_reuses_clean_subtrees_between_dirty_refreshes semantics_snapshot_rebuilds_clean_descendants_when_dirty_ancestor_transform_changes semantics_snapshot_includes_visible_roots_and_barrier --no-fail-fast`, `cargo nextest run -p fret-a11y-accesskit --no-fail-fast`, `cargo nextest run -p fret-diag bundle_stats_preserves_cache_root_boundary_summary --no-fail-fast`, `cargo nextest run -p fret-bootstrap --features launch,ui-app-driver,diagnostics ui_diagnostics::cache_root_boundary_tests::boundary_diagnostics_are_built_from_boundary_stats_with_cache_root_outcomes --lib --no-fail-fast`, `git diff --check`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, and `python3 tools/check_consumption_profiles.py`.
- U5 hit-test path cache audit subagent `019f1853-0640-79d2-8e19-8411b52ef258` concluded `hit_test_path_cache` must not become a `BoundaryFrameProducts` field: reuse is scoped by window input routing state, active layer roots, modal barrier, pointer occlusion, event type, and secondary hit-test context. The recommended implementation cut is a named owner-state wrapper with clear/suspend/update APIs.
- U5 hit-test path routing cache implementation slice replaces raw `UiTree::hit_test_path_cache: Option<HitTestPathCache>` ownership with `HitTestPathRoutingCacheState` in `crates/fret-ui/src/tree/hit_test.rs`. Secondary hover/occlusion hit tests now suspend cache publication instead of taking and restoring the raw field directly, and tests lock that suspended queries cannot replace the primary pointer routing cache entry.
- U5 hit-test path routing cache verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo nextest run -p fret-ui hit_test_path_routing_cache_clear_api_drops_entry suspended_hit_test_path_routing_cache_query_does_not_replace_primary_entry hit_test_layers_cached_reuses_path_and_respects_layer_order hit_test_layers_cached_rejects_stale_path_when_higher_z_sibling_moves_under_pointer hit_test_layers_cached_checks_transformed_higher_z_siblings_before_reuse pointer_move_dispatch_rejects_stale_path_when_higher_z_sibling_moves_under_pointer hit_test_works_with_view_cache_root_and_prepaint_reuse_under_render_transform prepaint_interaction_cache_root_move_invalidates_stale_root_only_hit_path --no-fail-fast`, `cargo nextest run -p fret-ui hit_test_path_cache_reuse_policy_covers_high_frequency_events modal_barrier_blocks_underlay_pointer_events_even_when_modal_root_is_pointer_transparent outside_press_observer_works_with_view_cache_root_and_prepaint_reuse pointer_occlusion --no-fail-fast`, `git diff --check`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, and `python3 tools/check_consumption_profiles.py`.
- U5 command routing audit explorer `019f1883-7496-7011-bf5e-1f7b0e6ca2be` found retained-parent command routing dependencies in `command_availability_from_node`, `publish_window_command_action_availability_snapshot_for_command_set`, `dispatch_command`, and action-route fallback availability. It recommended a correctness pre-slice that uses the existing window/layer-forest `UiDispatchSnapshot.parent` without migrating snapshots into `BoundaryFrameProducts`.
- U5 remaining-owner audit explorer `019f1883-c202-7be2-81ac-3597a09ef050` ranked command/focus routing caches as the next best owner-state grouping candidate, classified input snapshots and paint recording as window/layer-forest owner-states, and warned not to move node-local measurement caches or final window semantics into boundaries.
- U5 command routing implementation slice makes pending command source starts, command dispatch bubbling, direct command availability bubbling, action-route fallback availability, and command action availability snapshot publication use the current `UiDispatchSnapshot` membership / `UiDispatchSnapshot.parent` / `UiDispatchSnapshot::is_descendant` instead of retained parent pointers for focused/default/action-route paths. Tests cover direct availability, actual dispatch, pending source routing under focus barriers, and cross-surface snapshot publication with deliberately stale retained parents outside the dispatch snapshot.
- U5 command routing verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo nextest run -p fret-ui command_availability_uses_dispatch_snapshot_parent_not_retained_parent dispatch_command_bubble_uses_dispatch_snapshot_parent_not_retained_parent dispatch_command_ignores_pending_source_node_outside_dispatch_snapshot action_availability_snapshot_uses_dispatch_snapshot_parent_not_retained_parent --no-fail-fast`, `cargo nextest run -p fret-ui dispatch_command_records_programmatic_source_by_default dispatch_command_consumes_pending_pointer_source_metadata dispatch_command_bubbles_from_pending_source_element_when_focus_is_none dispatch_command_prefers_pending_source_element_over_stale_focus dispatch_command_source_element_ignores_stale_detached_node_entry dispatch_command_falls_back_to_default_root_when_focus_in_other_layer command_availability_walks_focus_chain command_availability_falls_back_to_default_root_when_focus_in_other_layer --no-fail-fast`, `cargo nextest run -p fret-ui action_availability_snapshot_matches_no_focus_dispatch_subtree_fallback action_availability_no_focus_subtree_fallback_scans_each_node_once_per_command action_availability_no_focus_subtree_fallback_honors_ancestor_blocking action_availability_snapshot_uses_explicit_action_route_fallback_root action_availability_snapshot_publishes_focus_traversal_gating action_availability_snapshot_reuses_focus_traversal_within_frame action_availability_snapshot_refreshes_focus_traversal_on_next_frame dispatch_event_publishes_action_availability_snapshot --no-fail-fast`, `git diff --check`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, and `python3 tools/check_consumption_profiles.py`.
- U5 command routing cache owner-state slice groups command availability revision, command action availability snapshot signature, focus traversal availability cache, and command availability interest cache under `CommandRoutingSnapshotState`. The owner is intentionally window/layer-forest scoped, not `BoundaryFrameProducts`, because the cached inputs include active focus layers, barrier roots, dispatch snapshot generation, window command registry revisions, menu-bar state, and action-route fallback roots.
- U5 command routing cache owner-state verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo nextest run -p fret-ui action_availability_snapshot_caches_declarative_interest_within_publication action_availability_snapshot_reuses_declarative_interest_across_same_frame_refine action_availability_snapshot_keeps_interest_cache_for_scroll_hit_test_only_invalidation action_availability_snapshot_reuses_focus_traversal_within_frame action_availability_snapshot_refreshes_focus_traversal_on_next_frame action_availability_snapshot_skips_recompute_when_inputs_are_unchanged action_availability_snapshot_dedupes_same_pending_refine_but_post_layout_republishes action_availability_snapshot_ignores_pointer_arbitration_only_changes action_availability_no_focus_subtree_fallback_reuses_subtree_interest_across_commands --no-fail-fast`, plus layering, surface-policy, consumption-profile, memory validation, and `git diff --check` gates.
- U5 dispatch retained-parent fallback audit explorer `019f18e8-c8d7-74a1-986a-9034c4467802` found that hover/event-chain/focus fallbacks mostly already use `UiDispatchSnapshot` when present and should not be globally deleted yet, but touch drag scroll/virtual-list owner lookup in `crates/fret-ui/src/tree/dispatch/window.rs` still walked retained `node.parent`. Paint/text/input owner audit explorer `019f18eb-afc8-7910-83b4-8d90e2a6f07c` concluded `PreviousFramePaintRecording`, `PaintCacheState`, window input/arbitration/text snapshots, layout frame roots, and text widget `last_*` caches are window/layer-forest or widget-local owner-states, not `BoundaryFrameProducts`.
- U5 touch-drag routing implementation slice makes touch `PointerDown` scroll/virtual-list owner lookup use `UiDispatchSnapshot.parent` for the hit-leaf ancestor walk instead of retained parent pointers. The new regression `touch_pan_scroll_target_resolution_uses_dispatch_snapshot_parent_when_hit_leaf_parent_is_stale` breaks the pressable retained parent after layout while preserving child edges, then verifies touch pan still scrolls and clears pressable state.
- U5 touch-drag routing verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo nextest run -p fret-ui touch_pan_scroll_steals_capture_from_pressable_and_clears_pressed_state touch_pan_scroll_target_resolution_uses_dispatch_snapshot_parent_when_hit_leaf_parent_is_stale touch_pan_scroll_live_target_resolution_ignores_stale_detached_node_entry --no-fail-fast`, `cargo nextest run -p fret-ui cursor_icon_query_remains_correct_with_stale_parent_pointers key_down_dispatches_capture_then_bubble_under_stale_parent_pointers hover_region_is_resilient_to_stale_parent_pointers_during_hover_updates outside_press_branch_containment_uses_child_edges_not_parent_pointers --no-fail-fast`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, and `python3 tools/check_consumption_profiles.py`.

## 2026-07-01

- U5 paint replay owner-state slice names the former tree-level `PaintCacheState` as `WindowPaintReplayState`, moves the `UiTree` field to `window_paint_replay`, and keeps `BoundaryFrameProducts::paint_cache` scoped to boundary entry metadata. Replay generation, previous-frame `Scene` recording, hit/miss/replayed-op counters, and recording ingestion remain window/layer-forest owned; `paint_cache_policy`, `ingest_paint_cache_source(...)`, and paint-cache entry APIs keep their public/behavioral vocabulary.
- U5 paint replay owner-state verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo nextest run -p fret-ui paint_cache_replays_ops_when_plain_node_translates_from_boundary_entry_store paint_cache_entry_is_boundary_owned_for_view_cache_roots paint_cache_side_store_entry_migrates_when_node_becomes_view_boundary window_paint_replay_ingests_scene_and_clears_when_recording_invalidates window_paint_replay_preserves_text_blob_side_index paint_cache_rebases_descendant_entries_after_ancestor_replay paint_cache_hit_test_only_invalidation_replays_when_cache_key_matches --no-fail-fast`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, `python3 tools/check_consumption_profiles.py`, engineering memory validation, and `git diff --check`.
- U6 root policy vocabulary audit explorer `019f197d-45fe-7f82-b88f-6d7ff9848f2f` confirmed the safest first slice is the `ResizablePanelGroupStyle` root export/source-policy transitional classification. Dismiss/focus/roving names are higher risk because they are public module APIs or deeply consumed behavior contracts for overlays, focus handoff, menus, and selectors.
- U6 implementation draft removes the `fret_ui::ResizablePanelGroupStyle` root export, exposes only the explicit `fret_ui::resizable_panel_group::ResizablePanelGroupStyle` mechanism path, keeps `BoundResizablePanelGroup` and layout internals `pub(crate)`, updates `fret-ui-kit` and `fret-ui-shadcn` imports, and flips the source-policy unit test so root re-exporting the style is rejected instead of classified.
- U6 root resizable chrome export verification passed: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools.test_check_surface_policy`, `python3 tools/check_surface_policy.py`, `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `cargo check -p fret-ui-kit`, `cargo check -p fret-ui-shadcn`, `cargo check -p fret-workspace`, `cargo nextest run -p fret-ui resizable_chrome_style_is_not_root_exported retained_widget_authoring_exports_are_compat_feature_gated --no-fail-fast`, `cargo nextest run -p fret-ui resizable_panel_group_style_defaults_keep_handles_visible resizable_panel_group_keyboard_arrow_keys_nudge_when_handle_child_is_focused resizable_panel_group_drag_updates_fractions_model declarative_resizable_panel_group_updates_model_on_drag resizable_panel_group_registers_viewport_roots_for_panels --no-fail-fast`, `cargo nextest run -p fret-ui-shadcn resizable_panel_group_defaults_use_theme_gap_and_hit_thickness resizable_handle_semantics_stamps_splitter_role_and_orientation resizable_handle_semantics_omits_value_steps_when_value_is_unknown resizable_handle_with_handle_increases_hit_thickness resizable_handle_grip_is_paint_only resizable_vertical_axis_flips_handle_orientation resizable_panel_group_respects_caller_height web_vs_fret_layout_resizable_geometry_matches_web_fixtures --no-fail-fast`, `python3 tools/check_layering.py`, `python3 tools/check_consumption_profiles.py`, `python3 tools/report_largest_files.py --top 30 --min-lines 800`, root-import `rg` search, engineering memory validation, and `git diff --check`.
- Note: broad `cargo nextest run -p fret-ui-shadcn resizable --no-fail-fast` currently fails only `snapshot_resizable_splitter_semantics`, where the checked-in snapshot expects an empty handle wrapper bounds of `6x180` while the current actual wrapper reports `0x0`; the focused U6 gate excludes that existing snapshot drift because this slice changes imports/root exports and not resizable rendering or semantics.
- U6 scroll vocabulary audit explorers `019f19a8-e921-7751-ac01-4575becdd6c4` and `019f19a9-2a2f-7741-bd71-2c603a0c9430` confirmed `DismissReason` / autofocus / roving hooks are module-public and heavily consumed, not root exports. The safest next breakable API was `UiTree::set_layer_scroll_dismiss_elements`, because it exposed strategy vocabulary directly on the root-exported `UiTree` mechanism surface.
- U6 scroll observer slice renames the runtime layer API and storage from `scroll_dismiss_elements` to `scroll_observer_elements` / `set_layer_scroll_observer_elements`, updates the kit overlay registration and focused test, adds a source-policy check rejecting future public `scroll_dismiss` mechanism members, and updates ADR 0129 plus implementation alignment to state that the runtime only observes scroll targets while component-layer dismiss hooks own close behavior.
- U6 scroll observer verification passed: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools.test_check_surface_policy`, `python3 tools/check_surface_policy.py`, `cargo nextest run -p fret-ui layer_scroll_observer_ignores_stale_detached_node_entry --no-fail-fast`, `cargo check -p fret-ui --all-targets`, `cargo check -p fret-ui-kit`, `cargo nextest run -p fret-ui-kit tooltip_scroll tooltip_dismiss popover_dismiss modal dismissible_popover --no-fail-fast`, `cargo fmt --all --check`, `python3 tools/check_layering.py`, `python3 tools/check_consumption_profiles.py`, and `git diff --check`.
- U6 layer-interaction vocabulary slice renames the runtime/public `fret-ui` action hook surface from `Dismiss*` / `DismissibleLayer*` to `LayerInteraction*`: `LayerInteractionReason`, `LayerInteractionRequestCx`, `OnLayerInteractionRequest`, `OnLayerInteractionPointerMove`, `LayerInteractionRoot`, `render_layer_interaction_root_with_hooks`, and `ElementContext::layer_interaction_*`. `fret-ui-kit::primitives::dismissable_layer` now owns the Radix/Dismiss aliases (`DismissReason`, `DismissRequestCx`, `OnDismissRequest`, `OnDismissiblePointerMove`) for component policy, and first-party kit/shadcn/material3/editor/imui/example consumers were migrated to that alias rather than importing old runtime names.
- U6 layer-interaction source policy now rejects public `DismissReason`, `DismissRequestCx`, `OnDismissRequest`, and `OnDismissiblePointerMove` declarations in `crates/fret-ui/src`, preserving the mechanism vocabulary boundary. ADR 0074, ADR 0069, ADR 0129, `docs/action-hooks.md`, `docs/component-authoring-contracts.md`, and `docs/adr/IMPLEMENTATION_ALIGNMENT.md` record the runtime-vs-component naming split.
- U6 layer-interaction verification passed: `cargo fmt --all --check`, `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools.test_check_surface_policy`, `python3 tools/check_surface_policy.py`, `python3 tools/check_layering.py`, `python3 tools/check_consumption_profiles.py`, `cargo check -p fret-ui --all-targets`, `cargo check -p fret-ui-kit --all-targets`, `cargo check -p fret-ui-shadcn --all-targets`, `cargo check -p fret-ui-material3 --all-targets`, `cargo check -p fret --all-targets`, `cargo check -p fret-ui-editor --all-targets`, `cargo check -p fret-imui --all-targets`, `cargo check -p fret-ui-gallery --all-targets`, `cargo check -p fret-examples --all-targets`, focused `cargo nextest run -p fret-ui dismissible layer_scroll_observer_ignores_stale_detached_node_entry escape_dismiss dock_drag --no-fail-fast`, `cargo nextest run -p fret-ui-kit dismissible_popover non_modal_overlay tooltip dock_drag pointer_capture --no-fail-fast`, `cargo nextest run -p fret-ui-shadcn mechanism_harness_focus_restore_recipe_cases_match_oracles --no-fail-fast`, `cargo nextest run -p fret-ui-material3 select_inside_dialog_closes_inner_popover_before_modal_dialog autocomplete_inside_dialog_escape_closes_inner_popover_before_modal_dialog field_overlays_inside_modal_bottom_sheet_close_before_sheet search_view_and_dropdown_menu_arbitrate_sibling_popovers --no-fail-fast`, and `git diff --check`. Two earlier broad selector attempts selected zero tests (`fret-ui-shadcn focus_restore_mechanism_harness`, `fret-ui-material3 material3_overlay_interactions`) and were replaced by the explicit test names above.

- U6 focus handoff vocabulary slice renames the runtime/public `fret-ui` auto-focus hook surface to mechanism names: `FocusHandoffRequestCx`, `OnOpenFocusHandoff`, and `OnCloseFocusHandoff`. `fret-ui-kit::primitives::focus_scope` now owns the Radix auto-focus aliases (`AutoFocusRequestCx`, `OnOpenAutoFocus`, `OnCloseAutoFocus`), and first-party kit/shadcn/editor consumers were migrated off `fret_ui::action::*AutoFocus*` imports. Source policy now rejects future public `AutoFocusRequestCx`, `OnOpenAutoFocus`, and `OnCloseAutoFocus` declarations in `crates/fret-ui/src`.
- U6 focus handoff verification passed: `cargo fmt --all --check`, `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools.test_check_surface_policy`, `python3 tools/check_surface_policy.py`, `python3 tools/check_layering.py`, `python3 tools/check_consumption_profiles.py`, `cargo check -p fret-ui --all-targets`, `cargo check -p fret-ui-kit --all-targets`, `cargo check -p fret-ui-shadcn --all-targets`, `cargo check -p fret-ui-material3 --all-targets`, `cargo check -p fret --all-targets`, `cargo check -p fret-ui-editor --all-targets`, `cargo check -p fret-imui --all-targets`, `cargo check -p fret-ui-gallery --all-targets`, `cargo check -p fret-examples --all-targets`, `cargo nextest run -p fret-ui-kit auto_focus focus_restore non_modal_overlay modal dismissible_popover --no-fail-fast`, `cargo nextest run -p fret-ui-shadcn --lib dialog_open_auto_focus dialog_close_auto_focus popover_open_auto_focus popover_close_auto_focus context_menu_keyboard_open_auto_focus context_menu_close_auto_focus sheet_open_auto_focus sheet_close_auto_focus drawer_open_auto_focus drawer_close_auto_focus alert_dialog_open_auto_focus alert_dialog_close_auto_focus menubar_keyboard_open_auto_focus --no-fail-fast`, `cargo nextest run -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles --no-fail-fast`, `cargo nextest run -p fret-ui-material3 select_inside_dialog_closes_inner_popover_before_modal_dialog autocomplete_inside_dialog_escape_closes_inner_popover_before_modal_dialog field_overlays_inside_modal_bottom_sheet_close_before_sheet search_view_and_dropdown_menu_arbitrate_sibling_popovers --no-fail-fast`, old runtime auto-focus import `rg`, and `git diff --check`. An initially attempted broad `cargo nextest run -p fret-ui-shadcn on_open_auto_focus on_close_auto_focus focus_restore context_menu_open_auto_focus context_menu_close_auto_focus --no-fail-fast` was interrupted after it compiled the entire shadcn integration-test fleet for ~15 minutes; use `--lib` or explicit `--test` targets for this crate's focused gates.

- U6 internal layer-interaction cleanup renames the remaining runtime `dismissible` source/module/helper/test names to `layer_interaction`: `declarative/host_widget/event/dismissible.rs` became `layer_interaction.rs`, the declarative interaction tests moved to `interactions/layer_interaction.rs`, `dismissible_has_pointer_move_handler` became `layer_interaction_has_pointer_move_handler`, and source policy now rejects future public `dismissible*` mechanism members in `crates/fret-ui/src`. Component-layer `fret-ui-kit::primitives::dismissable_layer` remains the Radix/Dismiss alias surface.
- U6 internal layer-interaction cleanup verification passed: `cargo fmt --all --check`, `cargo check -p fret-ui --all-targets`, `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools.test_check_surface_policy`, `python3 tools/check_surface_policy.py`, `python3 tools/check_layering.py`, `python3 tools/check_consumption_profiles.py`, `cargo nextest run -p fret-ui layer_interaction layer_scroll_observer outside_press escape_dismiss --no-fail-fast`, `rg` checks for old runtime dismissible helper/module/test IDs, and `git diff --check`.
- U6 roving/typeahead closeout audits the remaining `Roving*` / `Typeahead` runtime vocabulary and records it as mechanism retention rather than another blind rename: `RovingFlex` / `Roving*` hook contexts stay in `crates/fret-ui` because the runtime only discovers composite items, forwards key/typeahead facts, requests focus for a returned index, and calls component hooks. APG/Radix navigation policy, disabled-item behavior, entry fallback, matching buffers, and selection writes remain component/headless policy in `ecosystem/fret-ui-kit`. Source-policy tests now lock the split: `RovingFocus` / `Typeahead` root exports are rejected while mechanism members inside `crates/fret-ui/src` are allowed. ADR 0066, ADR 0074, `docs/action-hooks.md`, and `docs/adr/IMPLEMENTATION_ALIGNMENT.md` record the retention/deletion boundary, and stale alignment anchors for the renamed `layer_interaction.rs` path were corrected.
- U6 roving/typeahead closeout verification passed: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools.test_check_surface_policy`, `python3 tools/check_surface_policy.py`, `cargo fmt --all --check`, `python3 tools/check_layering.py`, `python3 tools/check_consumption_profiles.py`, engineering memory validation, and `git diff --check`.
- U7 renderer explorer `019f1a3d-5f31-7211-8493-1920832b8c49` recommends the first U7 slice be `renderer scene/upload observability floor`: add geometry upload bytes/write counts per stream plus a scene-encoding miss-reason histogram before changing retained scene chunks or dirty upload writes. The explorer flagged `SceneRecording::push`, `Scene::swap_storage`, `PreviousFramePaintRecording`, encode stack/effect boundaries, render-plan compilation, and partial `geometry_upload` writes as high-risk paths that should not be the first cut.
- U7 renderer scene/upload observability slice adds renderer `GeometryUploadPerfSnapshot` and `SceneEncodingCacheMissHistogramSnapshot`, maps them through bootstrap UI frame stats and `fret-diag` stats/perf JSON/repeat summaries, regenerates the frame-stats perf-key registry, and updates ADR/perf contract docs. It deliberately keeps flat scene compatibility and full stream writes while making upload and scene-cache pressure visible for later retained chunk and dirty range work.
- U7 observability verification passed: `cargo check -p fret-render-wgpu --all-targets`, `cargo check -p fret-diag --all-targets`, `cargo check -p fret-bootstrap --lib --tests`, focused `cargo nextest` coverage for renderer scene-cache diagnostics and `fret-diag` JSON/registry tests, `cargo nextest run -p fret-bootstrap --lib --no-fail-fast`, `cargo fmt --all --check`, `python3 tools/check_layering.py`, `python3 tools/check_surface_policy.py`, `python3 tools/check_consumption_profiles.py`, strict perf baseline audit, and `git diff --check`.
- U7 scene chunk compatibility bridge slice adds `fret_core::scene::SceneChunk` as a portable retained scene fragment carrier for ops, text-blob side index, and chunk fingerprint. `CanvasSceneFragment` and code-editor row retained fragments now store/replay chunks while renderer input remains flat `Scene`; renderer chunk encode reuse and dirty GPU range uploads remain future U7 slices.
