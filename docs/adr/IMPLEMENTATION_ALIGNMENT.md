# ADR Implementation Alignment Matrix

This document tracks whether each ADR is implemented and whether the current implementation aligns with the ADR contract.
It is **non-normative**: the ADR itself remains the source of truth; this file is a practical audit checklist.

## Legend

- **Aligned**: core mechanism exists; behavior matches ADR intent; evidence paths are listed.
- **Aligned (with known gaps)**: largely aligned, with gaps explicitly captured (usually in the ADR itself).
- **Partially aligned**: some mechanisms exist, but important outcomes are missing or incomplete (notes specify gaps).
- **Not implemented**: ADR exists, but the corresponding framework surface is not implemented yet.
- **Not audited**: not reviewed recently; do not assume alignment.
- **N/A (superseded)**: ADR is superseded; do not implement new work against it.

## Summary

- Last updated: 2026-01-26
- ADR count (numbered): 205

- Aligned: 87
- Aligned (with known gaps): 63
- N/A (superseded): 2
- Not audited: 18
- Not implemented: 5
- Partially aligned: 30

## Matrix

| ADR | ADR Status | Implementation Alignment | Notes |
| --- | --- | --- | --- |
| [`1158-state-driven-style-resolution-v1.md`](1158-state-driven-style-resolution-v1.md) | Proposed | Aligned (with known gaps) | Adds a minimal “state → style” primitive for ecosystem component libraries: `WidgetStates` and `WidgetStateProperty<T>` (last-match wins), plus a small color derivation fallback (`ColorFallback::ThemeTokenAlphaMul`). Evidence: `ecosystem/fret-ui-kit/src/style/{state.rs,tokens.rs,tests.rs,slots.rs}`, `docs/shadcn-style-override-patterns.md`, `ecosystem/fret-ui-shadcn/src/{button,checkbox,combobox,input,item,navigation_menu,radio_group,select,slider,switch,tabs,toggle,toggle_group}.rs`, `ecosystem/fret-ui-shadcn/src/{dropdown_menu,menubar}.rs`, `ecosystem/fret-ui-material3/src/{button,checkbox,icon_button,radio,switch,tabs,text_field}.rs`, `docs/workstreams/state-driven-style-resolution-v1.md`. Gaps: adoption is incomplete outside the shadcn core control set; some components still resolve state styling ad-hoc and have no public `*Style` override surface. |
| [`1159-ecosystem-style-override-surface-v1.md`](1159-ecosystem-style-override-surface-v1.md) | Proposed | Aligned (with known gaps) | Defines a stable per-component `*Style` override surface for ecosystem component libraries, with partial per-state overrides via `Option<WidgetStateProperty<Option<T>>>` and shallow right-biased `merged()` semantics. Evidence: `docs/shadcn-style-override-patterns.md`, `ecosystem/fret-ui-shadcn/src/{button,checkbox,combobox,input,item,navigation_menu,radio_group,select,slider,switch,tabs,toggle,toggle_group}.rs`, `ecosystem/fret-ui-material3/src/{button,checkbox,icon_button,radio,switch,tabs,text_field}.rs`, `ecosystem/fret-ui-kit/src/style/slots.rs`. Gaps: Material3 and future ecosystems still need to adopt the public `*Style` surface broadly (tracked in `docs/workstreams/material3-style-api-alignment-v1.md`). |
| [`0192-retained-windowed-surface-hosts.md`](0192-retained-windowed-surface-hosts.md) | Proposed | Not implemented | Defines a sliver-style retained host boundary that can attach/detach composable item subtrees based on a prepaint-derived window (ADR 0190) without rerendering the parent cache root. Current state: fixed-height paint-driven windowed surfaces exist as an ecosystem helper (`ecosystem/fret-ui-kit/src/declarative/windowed_rows_surface.rs`), but fully composable virtualization still relies on render-driven `VirtualList` (window changes trigger cache-root rerender via `crates/fret-ui/src/declarative/host_widget/layout/scrolling.rs`). |
| [`0193-ephemeral-prepaint-items-v1.md`](0193-ephemeral-prepaint-items-v1.md) | Proposed | Partially aligned | A prepaint phase exists and can run per cache root via `Widget::prepaint(PrepaintCx)` (`crates/fret-ui/src/tree/prepaint.rs`, `crates/fret-ui/src/widget.rs`). Diagnostics bundles export bounded `debug.prepaint_actions` so prepaint-driven invalidation/scheduling requests are explainable (`crates/fret-ui/src/tree/mod.rs`, `ecosystem/fret-bootstrap/src/ui_diagnostics.rs`). Gaps: there is no general “ephemeral items” API or prepaint-derived window computation yet; VirtualList window derivation remains render-driven and still requires cache-root rerender when the visible set changes (tracked in ADR 0190/0192). |
| [`0191-declarative-liveness-roots-and-gc-under-view-cache-reuse.md`](0191-declarative-liveness-roots-and-gc-under-view-cache-reuse.md) | Accepted | Aligned (with known gaps) | The global “skip sweep under reuse” stopgap has been removed (workstream MVP2-cache-005). GC reachability now explicitly unions liveness roots (layer roots + view-cache reuse roots) and retained child edges (`UiTree` + `WindowFrame`) in `crates/fret-ui/src/declarative/mount.rs`, and cache-hit frames refresh liveness via per-reuse-root subtree membership lists (ADR 0191). When diagnostics are enabled, bundles can export liveness-root and sweep-attribution context (`debug.all_layer_roots`, `element_runtime.{node_entry_root_overwrites,view_cache_reuse_root_element_samples}`, plus `removed_subtrees` fields like `trigger_element_in_view_cache_keep_alive` and `root_root_parent_sever_*`) and cache-root child-swap attribution (`debug.cache_roots[*].children_last_set_*`) via `ecosystem/fret-bootstrap/src/ui_diagnostics.rs`. Known gaps: best-effort debug-path resolution can be `null` when identities are pruned within the GC lag window; detach attribution is best-effort (only where parent `set_children(..)` writes are recorded); and membership list completeness still relies on the element-runtime node mapping remaining authoritative under nested cache roots. |
| [`0190-prepaint-windowed-virtual-surfaces.md`](0190-prepaint-windowed-virtual-surfaces.md) | Accepted | Partially aligned | VirtualList has a layout-derived window model and can keep wheel scrolling transform-only while the visible window remains stable (`crates/fret-ui/src/declarative/host_widget/layout/scrolling.rs`, `crates/fret-ui/src/declarative/host_widget/event/scroll.rs`). Out-of-band window-affecting scroll handle changes (e.g. `scroll_to_item`) are surfaced to view-cache reuse decisions to avoid cache-hit stale output (`crates/fret-ui/src/declarative/mount.rs`, `crates/fret-ui/src/declarative/tests/view_cache.rs`). Nested cache roots inside VirtualList are validated for semantics correctness under `scroll_to_item` when `contained_layout` is opt-in (`crates/fret-ui/src/declarative/tests/view_cache.rs`). Diagnostics bundles can export virtual-surface window telemetry (`ecosystem/fret-bootstrap/src/ui_diagnostics.rs` via `UiTreeDebugSnapshotV1.virtual_list_windows`). Gaps: the runtime does not yet compute windows during `prepaint` nor provide a general “ephemeral items” API; VirtualList still derives visible items during declarative render and uses rerender when the window changes. |
| [`0182-prepaint-interaction-stream-and-range-reuse.md`](0182-prepaint-interaction-stream-and-range-reuse.md) | Proposed | Partially aligned | A minimal prepaint v0 exists and records an “interaction stream” with cache-root range reuse for clean `ViewCache` subtrees. Prepaint currently caches hit-test relevant metadata per node (inverse transforms + clip + layer hit-test model), and hot paths reuse it for hit-testing and event coordinate mapping when nodes are clean and inspection is inactive. Evidence: `crates/fret-ui/src/tree/layout.rs` (`prepaint_after_layout` call site), `crates/fret-ui/src/tree/prepaint.rs` (`prepaint_after_layout`, `apply_interaction_record`), `crates/fret-ui/src/tree/hit_test.rs` (`prepaint_hit_test` fast path), `crates/fret-ui/src/tree/dispatch.rs` (`build_mapped_event_chain`), tests in `crates/fret-ui/src/tree/tests/{prepaint,hit_test,outside_press}.rs`. Gaps: the interaction stream does not yet carry cursor requests / outside-press observers / accessibility semantics; routing still performs tree-level hit-test traversal (it does not dispatch directly from the recorded stream); there is no standardized multi-stream frame recording contract surface beyond v0 experiments. |
| [`0187-preferences-command-policy.md`](0187-preferences-command-policy.md) | Proposed | Aligned | `app.preferences` exists as a standard command with a default macOS keybinding, and remains app-owned (no runner-native behavior). Golden-path `UiAppDriver` exposes an optional `on_preferences` hook. Evidence: `crates/fret-app/src/core_commands.rs`, `ecosystem/fret-bootstrap/src/ui_app_driver.rs`, example app handling `apps/fret-ui-gallery/src/driver.rs`. |
| [`0190-execution-and-concurrency-surface-v1.md`](0190-execution-and-concurrency-surface-v1.md) | Accepted | Aligned | Runner-provided `Dispatcher` contract exists in `crates/fret-runtime/src/execution.rs` and is implemented for desktop and wasm in `crates/fret-launch/src/runner/{desktop,web}/dispatcher.rs` (with shutdown + hot-reload fences). Driver boundaries drain inboxes via `InboxDrainRegistry` in `crates/fret-launch/src/runner/{desktop/mod.rs,web.rs}`. Ecosystem ergonomics live in `ecosystem/fret-executor/src/lib.rs` (Inbox + cancel-on-drop task handles). Example migrations use the surface without backend deps: `ecosystem/fret-markdown/src/mathjax_svg_support.rs` and `apps/fret-examples/src/markdown_demo.rs`. Tests cover shutdown/hot-reload fencing and deterministic wake/drain ordering (see unit tests in `crates/fret-launch/src/runner/desktop/dispatcher.rs`, `ecosystem/fret-executor/src/lib.rs`, and `ecosystem/fret-markdown/src/mathjax_svg_support.rs`). |
| [`0186-macos-about-panel-effect.md`](0186-macos-about-panel-effect.md) | Proposed | Aligned | Adds `Effect::ShowAboutPanel` and a macOS runner mapping to the standard native About panel (`orderFrontStandardAboutPanel:`), while keeping About command-driven via bootstrap mapping for `app.about`. Evidence: `crates/fret-runtime/src/effect.rs`, `crates/fret-launch/src/runner/desktop/{mod.rs,macos_menu.rs}`, `ecosystem/fret-bootstrap/src/ui_app_driver.rs`. |
| [`0185-menu-roles-system-menus-and-os-actions.md`](0185-menu-roles-system-menus-and-os-actions.md) | Accepted | Partially aligned | `MenuRole` and `SystemMenuType::Services` are modeled in `fret-runtime::MenuBar`, and macOS runner supports Services system submenu mapping + role-first standard menu hooks: `crates/fret-runtime/src/menu.rs`, `crates/fret-launch/src/runner/desktop/macos_menu.rs`, `ecosystem/fret-workspace/src/menu.rs`. `menubar.json` v2 can express roles/system menus: `crates/fret-runtime/src/menu.rs`, template `apps/fretboard/src/config.rs`. `OsAction` is modeled in `CommandMeta` and can be attached by apps, and macOS uses it to choose standard selectors for edit actions (`cut:`/`copy:`/`paste:`/`selectAll:`/`undo:`/`redo:`): `crates/fret-runtime/src/commands.rs`, `crates/fret-launch/src/runner/desktop/macos_menu.rs`, command metadata for `edit.undo/edit.redo`: `crates/fret-app/src/core_commands.rs`, example usage `ecosystem/fret-node/src/ui/commands.rs`. Gaps: OS runners still dispatch through command/effects and only implement limited “richer native behaviors” so far; checked/radio items and other native semantics remain unstandardized. |
| [`0188-window-command-enabled-service.md`](0188-window-command-enabled-service.md) | Proposed | Aligned | Window-scoped per-command enabled overrides exist as a data-only service, and are honored by keyboard shortcut dispatch + OS menubar gating (Windows `WM_INITMENUPOPUP` and macOS `validateMenuItem:`): `crates/fret-runtime/src/window_command_enabled.rs`, `crates/fret-ui/src/tree/shortcuts.rs`, `crates/fret-launch/src/runner/desktop/{windows_menu.rs,macos_menu.rs}`, `crates/fret-launch/src/runner/desktop/mod.rs`, test `crates/fret-ui/src/tree/tests/command_enabled_service.rs`. |
| [`0189-foreign-ui-embedding-isolated-surfaces.md`](0189-foreign-ui-embedding-isolated-surfaces.md) | Proposed | Aligned | Kernel contracts already support isolated embedding via `RenderTargetId` + `SceneOp::ViewportSurface` (ADR 0007) plus viewport input forwarding (ADR 0025/0147). Ecosystem-level helpers reduce boilerplate while keeping focus/IME/a11y isolated as intended by this ADR: `crates/fret-core/src/scene.rs`, `crates/fret-launch/src/runner/viewport_target.rs`, `ecosystem/fret-ui-kit/src/declarative/viewport_surface.rs`, `ecosystem/fret-kit/src/interop/embedded_viewport.rs`, demos `apps/fret-examples/src/todo_interop_kit_demo.rs` and `apps/fret-examples/src/todo_foreign_iced_style_demo.rs`. |
| [`0184-linux-menubar-strategy.md`](0184-linux-menubar-strategy.md) | Proposed | Aligned | Linux desktop runner treats OS menubar as unsupported (in-window fallback) and currently no-ops `Effect::SetMenuBar` on non-Windows/macOS platforms: `docs/adr/0184-linux-menubar-strategy.md`, `crates/fret-launch/src/runner/desktop/mod.rs`. |
| [`0183-os-menubar-effect-setmenubar.md`](0183-os-menubar-effect-setmenubar.md) | Proposed | Aligned (with known gaps) | Effect seam exists (`Effect::SetMenuBar`) and native menu mappings exist for Windows (Win32 `HMENU` + `WM_COMMAND`) and macOS (`NSMenu`): `crates/fret-runtime/src/effect.rs`, `crates/fret-launch/src/runner/desktop/{windows_menu.rs,macos_menu.rs}`, runner handling `crates/fret-launch/src/runner/desktop/mod.rs`. Runner-side gating uses window-scoped `InputContext` snapshots (`WindowInputContextService`), with a small app-provided seam for non-focus predicates (v1: Undo/Redo via `WindowCommandAvailabilityService` + `edit.can_undo/edit.can_redo`): `crates/fret-runtime/src/{window_input_context.rs,window_command_availability.rs,when_expr.rs}`, `crates/fret-ui/src/tree/{paint.rs,dispatch.rs,commands.rs}`. Linux OS integration is intentionally deferred (ADR 0184; in-window fallback). Remaining gaps: richer menu item semantics (checked/radio/native roles) and macOS standard menu conventions (roles/system menus + native behaviors; ADR 0185) are not fully standardized yet. Demo wiring: `apps/fret-ui-gallery/src/driver.rs`. |
| [`0181-interactivity-pseudoclasses-and-structural-stability.md`](0181-interactivity-pseudoclasses-and-structural-stability.md) | Proposed | Partially aligned | Some components already follow the “structurally stable hover chrome” pattern (e.g. recent code-view fixes), and the runtime supports mechanism primitives like `HoverRegion`, `Opacity`, and `InteractivityGate`. Gaps: pseudoclass semantics are not locked as a contract; hover transitions are not uniformly enforced as paint-only; there is no debug enforcement/attribution for “hover caused layout invalidation” offenders. GPUI reference: `repo-ref/zed/crates/gpui/src/elements/div.rs` (hover style refinement + hover-edge `cx.notify(current_view)`). |
| [`0180-dirty-views-and-notify-gpui-aligned.md`](0180-dirty-views-and-notify-gpui-aligned.md) | Accepted | Partially aligned | v1 "cache-root-first" dirty-view semantics are implemented: `EventCx::notify()` flows through a dedicated invalidation source (`UiDebugInvalidationSource::Notify`) and marks the nearest cache root as `view_cache_needs_rerender`, disabling view-cache reuse for that root (prevents stale cached replay). Model/global observation invalidation also marks cache roots dirty, so data changes cannot be hidden behind view-cache reuse. Dirty cache roots propagate to ancestor cache roots for nested-boundary correctness. A per-window dirty-view (dirty cache root) set is tracked and redraw requests are coalesced per tick in the UI runtime; diagnostics can list dirty views (debug-only). Evidence: `crates/fret-ui/src/widget.rs` (`EventCx::notify`), `crates/fret-ui/src/tree/dispatch.rs` (notify-driven redraw scheduling), `crates/fret-ui/src/tree/mod.rs` (`dirty_cache_roots`, `request_redraw_coalesced`, `invalidation_source_marks_view_dirty`, view-cache reuse gate), `crates/fret-ui/src/tree/tests/view_cache.rs` (`view_cache_notify_propagates_to_ancestor_cache_roots`, `view_cache_uplifts_observations_to_nearest_root_and_invalidates_ancestor_roots`), `ecosystem/fret-bootstrap/src/ui_diagnostics.rs` (`UiTreeDebugSnapshotV1.dirty_views`). Gaps: there is not yet an explicit `cx.notify(view_id)`/entity-first `ViewId` surface; prepaint/interaction caching exists (ADR 0182), but the system is not yet fully stream-driven end-to-end. |
| [`0178-canvas-viewport-transform-hit-testing-and-spatial-index-v1.md`](0178-canvas-viewport-transform-hit-testing-and-spatial-index-v1.md) | Proposed | Aligned (with known gaps) | Toolkit building blocks exist in `ecosystem/fret-canvas/src/spatial.rs` (grid + backrefs + backend wrapper with optional `rstar`) and `ecosystem/fret-canvas/src/view/*` (`PanZoom2D` and mapping helpers), and are used by `ecosystem/fret-node/src/ui/canvas/spatial.rs`. Gaps: the ADR’s proposed `CanvasViewport2D` naming and a dedicated budget helper surface are not standardized yet; there is no shared instrumentation surface for candidate/refine counts (beyond ad-hoc widget diagnostics). |
| [`0177-fret-canvas-modules-and-feature-strategy.md`](0177-fret-canvas-modules-and-feature-strategy.md) | Proposed | Aligned (with known gaps) | `fret-canvas` is organized as a portable substrate with feature-gated integration surfaces: module layout in `ecosystem/fret-canvas/src/lib.rs`, feature gates in `ecosystem/fret-canvas/Cargo.toml` (notably `ui`, `kurbo`, `rstar`, placeholder `declarative`). Gaps: cache observability counters are not implemented; call-site key hygiene is not standardized via a shared key builder helper. |
| [`0176-canvas-cache-policy-and-hosted-resource-caches.md`](0176-canvas-cache-policy-and-hosted-resource-caches.md) | Proposed | Aligned (with known gaps) | Declarative `Canvas` now has numeric cache policy knobs (`CanvasProps.cache_policy`) and hosted caches evict by keep-frames + LRU budgets: `crates/fret-ui/src/element.rs`, `crates/fret-ui/src/canvas.rs`, wired in `crates/fret-ui/src/declarative/host_widget/paint.rs`. Retained caches exist in `ecosystem/fret-canvas/src/cache/{path_cache,svg_cache}.rs` (`PathCache`/`SvgCache`) and retained call sites now reuse `PathCache` in `ecosystem/fret-node/src/ui/canvas/paint.rs`, `ecosystem/fret-plot/src/retained/layers.rs`, `ecosystem/fret-chart/src/retained/canvas.rs`. Cache observability counters are available via `CacheStats` (`ecosystem/fret-canvas/src/cache/mod.rs`) but are not wired into diagnostics bundles yet. Gaps: `SvgCache` call-site migration is only partial (e.g. `ecosystem/fret-bootstrap` and `ecosystem/fret-asset-cache` are migrated; other sites may remain). |
| [`0175-unified-authoring-builder-surface-v1.md`](0175-unified-authoring-builder-surface-v1.md) | Proposed | Not implemented | Building blocks exist (typed tokens + `ChromeRefinement`/`LayoutRefinement`, and per-component `refine_style/refine_layout` patterns in `ecosystem/fret-ui-shadcn`), but there is not yet a single “golden path” builder surface that composes chrome + layout + cross-cutting authoring fields consistently across shadcn components and non-shadcn primitives. `StyledExt` exists in `ecosystem/fret-ui-kit/src/styled.rs` and is re-exported, but shadcn components do not uniformly implement the trait integration needed for `.styled()` to be usable across the surface. |
| [`0174-ui-diagnostics-snapshot-and-scripted-interaction-tests.md`](0174-ui-diagnostics-snapshot-and-scripted-interaction-tests.md) | Proposed | Partially aligned | Versioned, structured diagnostics export exists behind `fret-bootstrap` `diagnostics`: per-window ring buffers for events + snapshots, on-demand dumps to `target/fret-diag/<unix_ms>/bundle.json` with `target/fret-diag/latest.txt` pointer (`ecosystem/fret-bootstrap/src/ui_diagnostics.rs`, wired in `ecosystem/fret-bootstrap/src/ui_app_driver.rs`; trigger via `fretboard diag poke` in `apps/fretboard/src/diag.rs`). Bundle includes `UiTree` debug stats/layers/hit-test, optional `ElementRuntime` window snapshot (`crates/fret-ui/src/elements/runtime.rs`), view-cache/invalidation stats, and an exported semantics snapshot (ADR 0033) when enabled (including optional `test_id` for stable automation selectors). A minimal file-triggered script harness exists (JSON `UiActionScriptV1` + `script.touch`) with `Click` (selectors: `RoleAndName`, `RoleAndPath`, `TestId`, `GlobalElementId`, `NodeId`), `TypeText`, `WaitFrames`, `WaitUntil`, `Assert`, and `CaptureBundle` steps (`ecosystem/fret-bootstrap/src/ui_diagnostics.rs`, fed via `fretboard diag script` in `apps/fretboard/src/diag.rs`). GPUI-style picking ergonomics now exist in debug form: an inspect overlay (hover + picked bounds) and a picking short-circuit that can consume pointer-down while selecting, with `UiTree.inspection_active` set before layout to disable view caching during inspection (`ecosystem/fret-bootstrap/src/ui_app_driver.rs`, `ecosystem/fret-bootstrap/src/ui_diagnostics.rs`). Continuous inspect can be toggled via file-triggered config (`inspect.json` + `inspect.touch`) using `fretboard diag inspect ...`. Gaps: selector vocabulary is still minimal (no rich descendant/path matching beyond strict parent chain), and the bundle still lacks effect/perf capture beyond basic UI/view-cache stats. |
| [`0173-docking-tab-bar-variable-width-tabs.md`](0173-docking-tab-bar-variable-width-tabs.md) | Proposed | Aligned (with known gaps) | Docking tab bars now support variable-width geometry via a shared `TabBarGeometry` that provides prefix-sum hit-testing, insertion marker positioning, and scroll bounds: `ecosystem/fret-docking/src/dock/tab_bar_geometry.rs` (`TabBarGeometry::variable`, `dock_tab_width_for_title`). DockSpace caches per-node tab widths (`tab_widths`) and uses them for scroll clamping, hit-testing, and internal-drag hover insert indices: `ecosystem/fret-docking/src/dock/space.rs` (`tab_bar_geometry_for_node`, `dock_drop_target_via_dnd`). Painting and the drop overlay consume the cached widths to stay consistent with hit-testing: `ecosystem/fret-docking/src/dock/paint.rs`. Gaps: `min_tab_w_px` currently equals legacy `DOCK_TAB_W` (short titles do not shrink yet), and there is no user-configurable policy surface for min/max widths. |
| [`0172-headless-dnd-v1-contract-surface.md`](0172-headless-dnd-v1-contract-surface.md) | Proposed | Aligned (with known gaps) | `fret-dnd` exists as a headless toolbox with activation constraints (`ecosystem/fret-dnd/src/activation.rs`), deterministic rect collisions (`ecosystem/fret-dnd/src/collision.rs`, `ecosystem/fret-dnd/src/rect_index.rs`), modifiers (`ecosystem/fret-dnd/src/modifier.rs`), auto-scroll request computation (`ecosystem/fret-dnd/src/scroll.rs`), and a minimal sortable insertion semantic (`InsertionSide`) (`ecosystem/fret-dnd/src/sortable.rs`). `fret-ui-kit` validates before/after insertion in its sortable recipe (`ecosystem/fret-ui-kit/src/recipes/sortable_dnd.rs`). Docking consumes collision selection and derives tab insert indices via insertion-side semantics (`ecosystem/fret-docking/src/dock/space.rs`, tests: `ecosystem/fret-docking/src/dock/tests.rs`). Gaps: multi-rect droppables and keyboard sensors are not part of v1; UI integration ownership remains app/ecosystem-specific (registry/controller not yet standardized). |
| [`0171-workspace-shell-tabs-and-pane-layout.md`](0171-workspace-shell-tabs-and-pane-layout.md) | Proposed | Aligned (with known gaps) | Workspace chrome building blocks live in `ecosystem/fret-workspace/src/lib.rs` with a tab model + snapshots (`ecosystem/fret-workspace/src/tabs.rs`) and a versioned pane layout snapshot (`ecosystem/fret-workspace/src/layout.rs`). `fret-kit` re-exports this as a user-facing entry point (`ecosystem/fret-kit/src/lib.rs`). Gap: focus + command routing for “active pane” is app-owned today (no shared arbiter yet). |
| [`0001-app-effects.md`](0001-app-effects.md) | Accepted | Aligned | Effects queue + redraw coalescing: `crates/fret-app/src/app.rs` (`push_effect`, `flush_effects`); bounded fixed-point draining: `crates/fret-launch/src/runner/desktop/mod.rs` and `crates/fret-launch/src/runner/web.rs`. |
| [`0002-display-list.md`](0002-display-list.md) | Accepted | Aligned | `SceneOp` contract: `crates/fret-core/src/scene.rs`; renderer preserves op iteration order: `crates/fret-render/src/renderer/render_scene/encode/mod.rs`. |
| [`0003-platform-boundary.md`](0003-platform-boundary.md) | Superseded | N/A (superseded) | Superseded ADR; do not implement new work against this contract. |
| [`0004-resource-handles.md`](0004-resource-handles.md) | Accepted | Aligned | Stable IDs: `crates/fret-core/src/ids.rs`; effect-driven registration: `crates/fret-runtime/src/effect.rs` (`ImageRegisterRgba8`, `ImageUnregister`); runner/renderer handles at flush point: `crates/fret-launch/src/runner/desktop/mod.rs`. |
| [`0005-retained-ui-tree.md`](0005-retained-ui-tree.md) | Accepted | Aligned | Retained tree + bounds/hit-test routing: `crates/fret-ui/src/tree/mod.rs` and `crates/fret-ui/src/tree/dispatch.rs` (hit-test, capture, focus fallback). |
| [`0006-text-system.md`](0006-text-system.md) | Accepted | Aligned | Text boundary trait: `crates/fret-core/src/text.rs` (`TextService`); renderer implementation: `crates/fret-render/src/text.rs` + `crates/fret-render/src/renderer/services.rs`; UI emits `SceneOp::Text`: `crates/fret-ui/src/declarative/host_widget/paint.rs`. |
| [`0007-viewport-surfaces.md`](0007-viewport-surfaces.md) | Accepted | Aligned | `RenderTargetId` + `SceneOp::ViewportSurface`: `crates/fret-core/src/ids.rs`, `crates/fret-core/src/scene.rs`; registry: `crates/fret-render/src/targets.rs`. Declarative embedding: `ViewportSurfaceProps` + `cx.viewport_surface(...)` (`crates/fret-ui/src/element.rs`, `crates/fret-ui/src/elements/cx.rs`). Input forwarding helpers: core mapping (`crates/fret-core/src/input.rs`), declarative `viewport_surface_panel` (`ecosystem/fret-ui-kit/src/declarative/viewport_surface.rs`), and retained helper `handle_viewport_surface_input` (`crates/fret-ui/src/retained_bridge.rs`). |
| [`0008-threading-logging-errors.md`](0008-threading-logging-errors.md) | Accepted | Aligned | Main-thread runner loop + `pollster` init: `crates/fret-launch/src/runner/desktop/app_handler.rs`; structured logging via `tracing` across crates (e.g. `crates/fret-launch/src/runner/desktop/mod.rs`). |
| [`0009-renderer-ordering-and-batching.md`](0009-renderer-ordering-and-batching.md) | Accepted | Aligned | Renderer encodes ops in order: `crates/fret-render/src/renderer/render_scene/encode/mod.rs`; adjacency batching without reordering via `ordered_draws` + quad batch flush: `crates/fret-render/src/renderer/render_scene/encode/state.rs`. |
| [`0010-wgpu-context-ownership.md`](0010-wgpu-context-ownership.md) | Accepted | Aligned | Host-provided `WgpuContext`: `crates/fret-render/src/lib.rs`; runner supports provided vs created context: `crates/fret-launch/src/runner/common.rs`; surfaces created from `context.instance`: `crates/fret-render/src/lib.rs` (`create_surface`). |
| [`0011-overlays-and-multi-root.md`](0011-overlays-and-multi-root.md) | Accepted | Aligned | Overlay roots + modal blocking: `crates/fret-ui/src/tree/layers.rs` (`push_overlay_root_ex`, `blocks_underlay_input`, `visible_layers_in_paint_order`); hit-test across roots: `crates/fret-ui/src/tree/hit_test.rs`; tests: `crates/fret-ui/src/tree/tests/hit_test.rs`. |
| [`0012-keyboard-ime-and-text-input.md`](0012-keyboard-ime-and-text-input.md) | Accepted | Aligned (with known gaps) | Shortcut deferral for text input in `crates/fret-ui/src/tree/dispatch.rs`. IME effects are wired on desktop and web: `Effect::ImeAllow` / `Effect::ImeSetCursorArea` are handled by the runners (`crates/fret-launch/src/runner/desktop/mod.rs`, `crates/fret-launch/src/runner/web.rs`) and applied via `fret-runner-winit` window state (`crates/fret-runner-winit/src/lib.rs`). Gap: web IME cursor-area positioning is best-effort and may be a no-op depending on backend support/capabilities. |
| [`0013-docking-ops-and-persistence.md`](0013-docking-ops-and-persistence.md) | Accepted | Aligned | Dock ops + persistence: `crates/fret-core/src/dock_op.rs`, `crates/fret-core/src/dock_layout.rs`, `crates/fret-core/src/panels.rs`; runner applies via `Effect::Dock`: `crates/fret-launch/src/runner/desktop/mod.rs`; demo persistence: `apps/fret-examples/src/docking_arbitration_demo.rs`. |
| [`0014-settings-and-configuration-files.md`](0014-settings-and-configuration-files.md) | Accepted | Aligned | Strongly typed settings file: `crates/fret-app/src/settings.rs` (`SettingsFileV1`). Layered loading + OS config dirs are implemented in `crates/fret-app/src/config_files.rs` (`default_user_config_dir`, `LayeredConfigPaths`, `load_layered_settings`). Bootstrap uses the layered defaults (`.fret/settings.json` + user `settings.json`) via `with_default_config_files`: `ecosystem/fret-bootstrap/src/lib.rs`. Change propagation is available as an optional polling watcher (`ConfigFilesWatcher`) wired by `ecosystem/fret-bootstrap/src/lib.rs` (`with_config_files_watcher`) and handled in the UI driver event loop: `ecosystem/fret-bootstrap/src/ui_app_driver.rs` (`handle_config_files_watcher_timer`). Repo defaults keep `.fret/` mostly out of VCS while allowing project-scoped config files (e.g. `.fret/settings.json` / `.fret/keymap.json` / `.fret/menubar.json` / `.fret/layout.json`) to be committed. |
| [`0015-frame-lifecycle-and-submission-order.md`](0015-frame-lifecycle-and-submission-order.md) | Accepted | Aligned | Runner render pipeline + submission ordering: `crates/fret-launch/src/runner/desktop/app_handler.rs` (`RedrawRequested` records engine + UI and submits engine cmd buffers before UI cmd); `TickId` vs `FrameId`: same file (`about_to_wait` vs frame increment). |
| [`0016-plugin-and-panel-boundaries.md`](0016-plugin-and-panel-boundaries.md) | Accepted | Aligned (with known gaps) | Stable panel identity exists (`crates/fret-core/src/panels.rs`, `crates/fret-core/src/dock.rs`); ecosystem panel registry service exists (`ecosystem/fret-docking/src/dock/panel_registry.rs`). App-owned plugin registry exists in `crates/fret-app/src/plugins.rs` (`Plugin`, `PluginRegistrar`, `PluginHost`, `install_plugins`), and the bootstrap golden path wires an install hook via `ecosystem/fret-bootstrap/src/lib.rs` (`BootstrapBuilder::with_plugins`). Gap: there is not yet a standardized cross-crate panel factory/registration surface for plugins (beyond the docking registry service) and no versioned settings schema contribution mechanism. |
| [`0017-multi-window-display-and-dpi.md`](0017-multi-window-display-and-dpi.md) | Accepted | Aligned | Logical px window events: `crates/fret-core/src/input.rs` (`WindowScaleFactorChanged`, `WindowMoved`, `WindowResized`); metrics service: `crates/fret-core/src/window.rs`; runners apply: `crates/fret-launch/src/runner/desktop/mod.rs` and `crates/fret-launch/src/runner/web.rs` (`apply_window_metrics_event`). |
| [`0018-key-codes-and-shortcuts.md`](0018-key-codes-and-shortcuts.md) | Accepted | Aligned | Physical key codes as `KeyCode` (aligned with `keyboard-types::Code`): `crates/fret-core/src/input.rs`; winit mapping uses physical keys: `crates/fret-runner-winit/src/lib.rs` (`map_physical_key`). |
| [`0019-scene-state-stack-and-layers.md`](0019-scene-state-stack-and-layers.md) | Accepted | Aligned | Scene state ops + debug validation: `crates/fret-core/src/scene.rs` (`PushTransform/Opacity/Layer/Clip` + `validate`); renderer encodes stack into draw state: `crates/fret-render/src/renderer/render_scene/encode/ops.rs`. |
| [`0020-focus-and-command-routing.md`](0020-focus-and-command-routing.md) | Accepted | Aligned | Shortcut→command as effects: `crates/fret-ui/src/tree/shortcuts.rs` (`Effect::Command`, repeatable gating via `CommandMeta`); widget-scope dispatch: `crates/fret-ui/src/tree/commands.rs`; runner drains and calls driver handlers: `crates/fret-launch/src/runner/desktop/mod.rs`. |
| [`0021-keymap-file-format.md`](0021-keymap-file-format.md) | Accepted | Aligned (with known gaps) | Keymap parsing + platform/when + sequences support: `crates/fret-runtime/src/keymap.rs` (v1 + v2 sequences). Disk IO helpers live in `crates/fret-app/src/keymap.rs` (`keymap_from_file_if_exists`). Layered path defaults (user config dir + `./.fret/`) live in `crates/fret-app/src/config_files.rs` (`LayeredConfigPaths::{user_keymap_json,project_keymap_json}`) and are used by the bootstrap golden path to layer user + project keymaps: `ecosystem/fret-bootstrap/src/lib.rs` (`with_layered_keymap`, `with_default_config_files`). Applying disk layers preserves baseline keybindings and avoids duplicate accumulation: `crates/fret-app/src/keymap.rs` (`apply_layered_keymap`). Conflict detection exists (`Keymap::conflicts`) and is surfaced as diagnostics in `crates/fret-app/src/config_files.rs` (`LayeredKeymapReport`). Command-provided default keybindings can be installed into the app keymap via `crates/fret-app/src/keymap.rs` (`install_command_default_keybindings_into_keymap`) and are used by the bootstrap golden path: `ecosystem/fret-bootstrap/src/lib.rs` (`with_command_default_keybindings`). Gaps: plugin default bindings remain app-owned, and there is no standardized conflict reporting/UI yet (ADR 0021 section 7). |
| [`0022-when-expressions.md`](0022-when-expressions.md) | Accepted | Aligned | Parser/validator/evaluator: `crates/fret-runtime/src/when_expr.rs` (unknown identifiers evaluate false via `eval_ident_bool`). |
| [`0023-command-metadata-menus-and-palette.md`](0023-command-metadata-menus-and-palette.md) | Accepted | Aligned (with known gaps) | Command metadata + scopes: `crates/fret-runtime/src/commands.rs` (`CommandMeta`, `CommandScope`), stored on app: `crates/fret-app/src/app.rs`; menu model: `crates/fret-runtime/src/menu.rs`; standard `app.command_palette` command ID exists (hidden by default) with platform-specific default keybindings: `crates/fret-app/src/core_commands.rs` (`COMMAND_PALETTE`, `register_command_palette`). Command-provided default keybindings can be installed into the app keymap via `crates/fret-app/src/keymap.rs` (`install_command_default_keybindings_into_keymap`) and are used by the bootstrap golden path: `ecosystem/fret-bootstrap/src/lib.rs` (`with_command_default_keybindings`, `with_default_config_files`). Command palette surface exists and can derive entries from the host command registry (with `when` gating + best-effort shortcut display) via `ecosystem/fret-ui-shadcn/src/command.rs` (`command_entries_from_host_commands*`, `CommandDialog::new_with_host_commands`). The components gallery demo wires the registry + open command into the palette: `apps/fret-examples/src/components_gallery.rs` (`build_app`, `handle_command`). Gaps: end-to-end palette invocation policy (open/close state ownership) remains app-owned. |
| [`0024-undo-redo-and-edit-transactions.md`](0024-undo-redo-and-edit-transactions.md) | Deferred | Partially aligned | Reusable app-owned undo substrate exists in `ecosystem/fret-undo` (`UndoService`, `UndoRecord`, `ValueTx`, `CoalesceKey`) and is exercised by viewport tool interactions (gizmo drag begin/update/commit/cancel + coalescing) in `apps/fret-examples/src/gizmo3d_demo.rs`. Domain-local, op-based undo/redo also exists for the node-graph editor (`GraphTransaction` + `GraphHistory`) in `ecosystem/fret-node/src/ops/{mod.rs,history.rs}`. Gaps: no standardized cross-domain `EditOp` schema, no multi-effect transaction bundling (e.g. selection + transform + dirty) as a single edit unit, and no persistence/serialization policy is implemented yet (consistent with ADR scope being editor-app). |
| [`0025-viewport-input-forwarding.md`](0025-viewport-input-forwarding.md) | Accepted | Aligned | Core event + mapping: `crates/fret-core/src/input.rs` (`ViewportInputEvent`), `crates/fret-core/src/viewport.rs` (`ViewportMapping`); UI emits `Effect::ViewportInput`: `ecosystem/fret-docking/src/dock/space.rs`; runner drains into driver hook: `crates/fret-launch/src/runner/desktop/mod.rs`. |
| [`0026-asset-database-and-import-pipeline.md`](0026-asset-database-and-import-pipeline.md) | Deferred | Not implemented | No editor/project asset database or importer pipeline exists in-repo. Existing "assets" crates are explicitly renderer/UI-asset focused: `ecosystem/fret-asset-cache` provides renderer-facing image/SVG caches, and `ecosystem/fret-ui-assets/src/lib.rs` clarifies it is *not* an editor/project asset pipeline. |
| [`0027-framework-scope-and-responsibilities.md`](0027-framework-scope-and-responsibilities.md) | Accepted | Aligned | Layering matches: `fret-core` stays platform/render-agnostic while `wgpu` lives in `crates/fret-render`; policy-heavy surfaces live under `ecosystem/` (see `AGENTS.md`, `docs/dependency-policy.md`). |
| [`0028-declarative-elements-and-element-state.md`](0028-declarative-elements-and-element-state.md) | Accepted | Aligned | Declarative mount contract: `crates/fret-ui/src/declarative/mount.rs` (`render_root`); stable IDs: `crates/fret-ui/src/elements/id.rs` + hashing (`crates/fret-ui/src/elements/hash.rs`); cross-frame state: `crates/fret-ui/src/elements/access.rs` (`with_element_state`). |
| [`0029-text-pipeline-and-atlas-strategy.md`](0029-text-pipeline-and-atlas-strategy.md) | Accepted | Aligned (with known gaps) | Cosmic-text shaping/raster + explicit caching keys: `crates/fret-render/src/text.rs` (`TextSystem`, `TextBlobKey` includes `TextConstraints.scale_factor` + wrap/overflow + `font_stack_key`). Atlas split matches ADR: `mask_atlas_texture: R8Unorm` vs `color_atlas_texture: Rgba8UnormSrgb` and `SwashContent::{Mask,Color,SubpixelMask}` routing into separate atlases (`mask_atlas`/`color_atlas`): `crates/fret-render/src/text.rs` (plus `subpixel_mask_to_alpha` test). Subpixel X positioning is preserved by using `CacheKey::new(..)` returned integer coords: `crates/fret-render/src/text.rs`. Output is coverage-bitmaps (not SDF) sampled in WGSL + premul blending: `crates/fret-render/src/renderer/shaders.rs` (`TEXT_SHADER`, `TEXT_COLOR_SHADER`) + `crates/fret-render/src/renderer/pipelines/text.rs` (`PREMULTIPLIED_ALPHA_BLENDING`). Default generic-family config is app/runner-owned and participates in cache invalidation via `font_stack_key`: `crates/fret-core/src/text.rs` (`TextFontFamilyConfig`), `crates/fret-launch/src/runner/common.rs` and `crates/fret-launch/src/runner/desktop/mod.rs` (propagate global change → `renderer.set_text_font_families`). Gaps: no explicit `PlatformTextSystem` split yet; no gamma/contrast tuning knobs in shaders; Y-subpixel variants + richer per-script fallback customization remain follow-ups (as noted in ADR). |
| [`0030-shape-rendering-and-sdf-semantics.md`](0030-shape-rendering-and-sdf-semantics.md) | Accepted | Aligned (with known gaps) | `fret-core` keeps SDF out of the contract (`SceneOp::Quad` + `PushClipRect/PushClipRRect` in `crates/fret-core/src/scene.rs`) while `fret-render` implements rounded rects via analytic SDF in WGSL: `crates/fret-render/src/renderer/shaders.rs` (`QUAD_SHADER`) with derivative-based AA (`fwidth` + `smoothstep`, explicitly referencing ADR 0030). Border semantics match the ADR lock-in: per-edge widths and **inside** alignment, with inner radii derived by subtracting adjacent border widths: `crates/fret-render/src/renderer/shaders.rs` (`// Border alignment: inside...`). Corner radii are clamped for degenerate rects and max radius (`min(w,h)/2`) via `clamp_corner_radii_for_rect`: `crates/fret-render/src/renderer/util.rs`, used by quad/clip encoders (`crates/fret-render/src/renderer/render_scene/encode/draw/quad.rs`, `crates/fret-render/src/renderer/render_scene/encode/clip.rs`) and covered by tests (`crates/fret-render/src/renderer/tests.rs`). Clip evolution is implemented as “scissor fast path + shader clip when needed”: `crates/fret-render/src/renderer/render_scene/encode/clip.rs` (axis-aligned rect → scissor-only; transformed/rrect → clip stack node). Gap: no first-class shadow primitive (`SceneOp::Shadow` or equivalent) is wired yet; dashed borders remain intentionally component-level (no dashed-quad contract). |
| [`0031-app-owned-models-and-leasing-updates.md`](0031-app-owned-models-and-leasing-updates.md) | Accepted | Aligned | App-owned model store + borrow-friendly updates are implemented via `ModelStore` and typed handles in `crates/fret-runtime/src/model.rs` and wired into `fret_app::App` as the owner (`App.models: ModelStore` and `impl ModelHost for App`): `crates/fret-app/src/app.rs`. Updates use a lease that temporarily moves the value out of the store (`lease_shared` takes `entry.value`, returning `ModelLease<T>`; `end_lease_shared` restores it) so update closures can mutate model state without borrowing the store (GPUI-style): `crates/fret-runtime/src/model.rs`. Unwind safety is handled by always ending the lease on both Ok and panic paths (`catch_unwind` + `end_lease` + `resume_unwind`) in both the host-level and store-level APIs (`ModelHost::read/update_model`, `ModelStore::read/update`): `crates/fret-runtime/src/model.rs`. Re-entrancy/nested updates are constrained by the lease marker: attempting to lease the same model while already leased returns `ModelUpdateError::AlreadyLeased` (with debug diagnostics), matching the ADR rule that same-id re-entrant update is forbidden while different models remain allowed: `crates/fret-runtime/src/model.rs`. Change tracking is revision-based (`entry.revision` increments on dirty end_lease) and model removal/updates mark ids as changed for invalidation (`mark_changed_locked`, `take_changed_models`): `crates/fret-runtime/src/model.rs`, surfaced through `App::take_changed_models`: `crates/fret-app/src/app.rs`. |
| [`0032-style-tokens-and-theme-resolution.md`](0032-style-tokens-and-theme-resolution.md) | Accepted | Aligned | Token registry + resolution + linearized colors: `crates/fret-ui/src/theme.rs`, `crates/fret-ui/src/theme_registry.rs` (includes sRGB/HSL/OKLCH parsing to linear + revision). |
| [`0033-semantics-tree-and-accessibility-bridge.md`](0033-semantics-tree-and-accessibility-bridge.md) | Accepted | Aligned | Portable schema: `crates/fret-core/src/semantics.rs`; snapshot build w/ overlay barrier root: `crates/fret-ui/src/tree/mod.rs` (`refresh_semantics_snapshot`); AccessKit mapping + action decoding: `crates/fret-a11y-accesskit/src/lib.rs`; winit adapter: `crates/fret-runner-winit/src/accessibility_accesskit_winit.rs`. |
| [`0034-timers-animation-and-redraw-scheduling.md`](0034-timers-animation-and-redraw-scheduling.md) | Accepted | Aligned | Scheduling effects: `crates/fret-runtime/src/effect.rs` (`RequestAnimationFrame`, `SetTimer`, `CancelTimer`); RAII continuous frames: `crates/fret-ui/src/elements/runtime.rs`; runner timers + bounded draining: desktop `crates/fret-launch/src/runner/desktop/mod.rs`, web `crates/fret-launch/src/runner/web.rs` (`MAX_EFFECT_DRAIN_TURNS=8`). |
| [`0035-layout-constraints-and-optional-taffy-integration.md`](0035-layout-constraints-and-optional-taffy-integration.md) | Accepted | Aligned | Hybrid explicit bounds + internal Taffy: `crates/fret-ui/src/declarative/host_widget.rs` (Flex/Grid caches + measurement hook), `crates/fret-ui/src/declarative/taffy_layout.rs`. |
| [`0036-observability-tracing-and-ui-inspector-hooks.md`](0036-observability-tracing-and-ui-inspector-hooks.md) | Accepted | Partially aligned | Structured tracing is pervasive (e.g. `crates/fret-launch/src/runner/desktop/mod.rs`); UI inspector toggles + per-frame debug stats exist: `crates/fret-ui/src/tree/mod.rs` (`inspection_active`, `UiDebugFrameStats`); standardized renderer metrics surface is not yet implemented (beyond ad-hoc snapshots like `crates/fret-render/src/renderer/types.rs`). |
| [`0037-workspace-boundaries-and-components-repository.md`](0037-workspace-boundaries-and-components-repository.md) | Accepted | Aligned (with known gaps) | Workspace layout matches the intended “core vs ecosystem vs apps” split: `Cargo.toml` (`members = ["crates/*","ecosystem/*","apps/*"]`, `default-members = ["crates/*","ecosystem/*"]`). The wiring/integration crate exists as `crates/fret-launch` (runner depends on platform+render+ui) and the framework facade stays framework-only (`crates/fret/src/lib.rs`, `crates/fret/Cargo.toml` features; no `ecosystem/*` re-exports). Component incubation is in-tree under `ecosystem/` (e.g. `ecosystem/fret-ui-kit`, `ecosystem/fret-ui-shadcn`, `ecosystem/fret-icons*`) consistent with the ADR’s “MVP45 incubation before extracting to fret-components”. Dependency-direction checks exist as a dedicated script (`tools/check_layering.ps1`, referenced by `docs/dependency-policy.md`) and can be run locally. Gaps: the “separate fret-components repository” extraction is not done yet; CI runs ADR numbering + layering checks via `.github/workflows/consistency-checks.yml` (see `.github/workflows/ci.yml` for fmt/clippy/nextest). |
| [`0038-engine-render-hook-and-submission-coordinator.md`](0038-engine-render-hook-and-submission-coordinator.md) | Accepted | Aligned | Engine frame recording + target updates: `crates/fret-launch/src/runner/common.rs` (`EngineFrameUpdate`, `RenderTargetUpdate`); runner applies updates before UI submits: desktop `crates/fret-launch/src/runner/desktop/app_handler.rs`, web `crates/fret-launch/src/runner/web.rs`. |
| [`0039-component-authoring-model-render-renderonce-and-intoelement.md`](0039-component-authoring-model-render-renderonce-and-intoelement.md) | Accepted | Aligned (with known gaps) | Authoring traits exist in `crates/fret-ui/src/element.rs` (`IntoElement`, `RenderOnce`); derive-macro ergonomics (`fret-macros`) not implemented yet. |
| [`0040-color-management-and-compositing-contracts.md`](0040-color-management-and-compositing-contracts.md) | Accepted | Aligned (with known gaps) | Linear shading + conditional output encoding: `crates/fret-render/src/renderer/shaders.rs` (`encode_output_premul`, `output_is_srgb`); swapchain prefers sRGB: `crates/fret-render/src/surface.rs`; viewport target encoding via view format + `RenderTargetColorSpace`: `crates/fret-render/src/targets.rs`. Gap: render target alpha semantics (opaque vs premul) not modeled yet. |
| [`0041-drag-and-drop-clipboard-and-cross-window-drag-sessions.md`](0041-drag-and-drop-clipboard-and-cross-window-drag-sessions.md) | Accepted | Aligned (with known gaps) | App-scoped internal drag session: `crates/fret-runtime/src/drag.rs` + stored on app (`crates/fret-app/src/app.rs`); internal drag routing override: `crates/fret-ui/src/drag_route.rs` + dispatch rules in `crates/fret-ui/src/tree/dispatch.rs`; external file drag/drop events + tokenized reads: `crates/fret-core/src/input.rs` (`ExternalDrag*`, `ExternalDropData`), effects in `crates/fret-runtime/src/effect.rs` (`ExternalDropReadAll*`, `ExternalDropRelease`), runner wiring in `crates/fret-launch/src/runner/desktop/app_handler.rs` + `crates/fret-platform-web/src/wasm.rs`. Gap: external drag initiation (`Effect::StartExternalDrag`) is still deferred; `DragSessionId`/phase/pointer id are not modeled yet. |
| [`0042-virtualization-and-large-lists.md`](0042-virtualization-and-large-lists.md) | Accepted | Aligned | VirtualList container + scroll model: `crates/fret-ui/src/element.rs` (`VirtualListProps`, `VirtualListState`), implementation in `crates/fret-ui/src/declarative/host_widget/layout.rs`; tests: `crates/fret-ui/src/declarative/tests/virtual_list.rs`; demos: `apps/fret-examples/src/virtual_list_stress_demo.rs`. |
| [`0043-shortcut-arbitration-pending-bindings-and-altgr.md`](0043-shortcut-arbitration-pending-bindings-and-altgr.md) | Accepted | Aligned | AltGr is first-class on `Modifiers`: `crates/fret-core/src/input.rs`; winit normalizes AltGr (clears `ctrl/alt` while `alt_gr` is held): `crates/fret-runner-winit/src/lib.rs` (`map_modifiers`, `is_alt_gr_key`); keymap v2 parsing + sequence matching: `crates/fret-runtime/src/keymap.rs` (`KeysAny`, `Keymap::match_sequence`); pending multi-stroke shortcuts + replay + timeout + `TextInput` suppression: `crates/fret-ui/src/tree/shortcuts.rs` + `crates/fret-ui/src/tree/dispatch.rs`. |
| [`0044-text-editing-state-and-commands.md`](0044-text-editing-state-and-commands.md) | Accepted | Aligned | UTF-8 byte offsets clamped to char boundaries: `crates/fret-ui/src/text_edit.rs` (`utf8::*`); core `text.*` command vocabulary: `crates/fret-ui/src/text_edit.rs`, `crates/fret-ui/src/text_surface.rs`, widgets `crates/fret-ui/src/text_input/widget.rs` + `crates/fret-ui/src/text_area/widget.rs`; IME cursor area effect: `Effect::ImeSetCursorArea` emitted by text widgets. |
| [`0045-text-geometry-queries-hit-testing-and-caret-metrics.md`](0045-text-geometry-queries-hit-testing-and-caret-metrics.md) | Accepted | Aligned | Geometry query hooks in `crates/fret-core/src/text.rs` (`TextService::{caret_x,hit_test_x,caret_rect,hit_test_point}`); renderer implementation: `crates/fret-render/src/text.rs` + `crates/fret-render/src/renderer/services.rs`; widgets use caret rect/hit testing: `crates/fret-ui/src/text_input/widget.rs`, `crates/fret-ui/src/text_area/mod.rs`. |
| [`0046-multiline-text-layout-and-geometry-queries.md`](0046-multiline-text-layout-and-geometry-queries.md) | Accepted | Aligned | Affinity + hit-test result types: `crates/fret-core/src/text.rs` (`CaretAffinity`, `HitTestResult`); multiline caret/hit-test implementation: `crates/fret-render/src/text.rs` (`caret_rect`, `hit_test_point`); multiline widget uses affinity for selection/IME: `crates/fret-ui/src/text_area/mod.rs`. |
| [`0047-virtual-list-data-source-and-stable-item-keys.md`](0047-virtual-list-data-source-and-stable-item-keys.md) | Accepted | Aligned | Stable keys (`ItemKey = u64`): `crates/fret-ui/src/lib.rs`; `items_revision` + key caching policy: `crates/fret-ui/src/element.rs` (`VirtualListOptions`, `VirtualListKeyCacheMode`); scroll anchor preserved across reorder via key + offset: `crates/fret-ui/src/elements/cx.rs` (`virtual_list_with_layout_and_keys`); virtualizer integration + `sync_keys(items_revision)`: `crates/fret-ui/src/virtual_list.rs`; tests: `crates/fret-ui/src/declarative/tests/virtual_list.rs`; demos: `apps/fret-examples/src/virtual_list_stress_demo.rs`. |
| [`0048-inspector-property-protocol-and-editor-registry.md`](0048-inspector-property-protocol-and-editor-registry.md) | Accepted | Partially aligned | Example-layer protocol types exist in `apps/fret-editor` (`PropertyPathSegment::{Field,Index,Key}` + `PropertyPath`: `apps/fret-editor/src/property.rs`; tree schema: `apps/fret-editor/src/inspector_protocol.rs`; phased edits: `apps/fret-editor/src/property_edit.rs`; edit parsing/request plumbing: `apps/fret-editor/src/inspector_edit.rs`). Editor registry resolution is implemented as “most specific path prefix wins; ties by last-registered order”: `apps/fret-editor/src/inspector_protocol.rs` (`InspectorEditorRegistry::resolve_kind`, `register_path_prefix`). Gaps: path/type_tag are not currently serializable (no `serde` derives; uses `Arc<str>` / `&'static str`), edit requests model `targets: Vec<u64>` rather than a stable `SelectionId`, value/meta coverage is minimal, and the ADR’s Implementation Notes section still references a removed `fret-editor` crate layout (under the old `crates/` tree) and a non-existent `inspector_edit_layout.rs`. |
| [`0049-viewport-tools-input-capture-and-overlays.md`](0049-viewport-tools-input-capture-and-overlays.md) | Deferred | Partially aligned | Viewport input forwarding + capture is implemented in the docking layer: pointer down inside the active viewport panel emits `Effect::ViewportInput` and arms capture; while captured, move/up are forwarded even when leaving bounds via clamped UV mapping: `ecosystem/fret-docking/src/dock/space.rs` (`viewport_capture`, `request_pointer_capture`, `viewport_input_from_hit_clamped`) + mapping helpers in `ecosystem/fret-docking/src/dock/viewport.rs` and `crates/fret-core/src/viewport.rs`. Overlay rendering hook exists and runs after the viewport surface is placed (so overlays paint above the engine target): `DockViewportOverlayHooks` in `ecosystem/fret-docking/src/dock/mod.rs` and call site in `ecosystem/fret-docking/src/dock/paint.rs` (after `SceneOp::ViewportSurface`). Example demos register overlay hooks and expose viewport input for debugging: `apps/fret-examples/src/docking_demo.rs`, `apps/fret-examples/src/docking_arbitration_demo.rs` (`viewport_input`). Example editor-layer prototypes for tool state and overlay shapes exist but are not wired into a stable tool routing loop yet: `apps/fret-editor/src/viewport_tools.rs`, `apps/fret-editor/src/viewport_overlays.rs`. Gap: the ADR’s “central tool manager routing + Begin/Update/Commit/Cancel phases + undo coalescing boundaries” is not implemented end-to-end (mostly data structures today). |
| [`0050-theme-config-schema-and-baseline-tokens.md`](0050-theme-config-schema-and-baseline-tokens.md) | Accepted | Aligned | Baseline typed tokens + default theme + semantic alias keys (shadcn bridge): `crates/fret-ui/src/theme.rs` (includes tests). |
| [`0051-model-observation-and-ui-invalidation-propagation.md`](0051-model-observation-and-ui-invalidation-propagation.md) | Accepted | Aligned | Changed model/global propagation: `crates/fret-app/src/app.rs` (`take_changed_models`) + `crates/fret-ui/src/tree/mod.rs` (observation registries and invalidation); tests: `crates/fret-ui/src/tree/tests/models.rs`. |
| [`0052-ui-host-runtime-boundary.md`](0052-ui-host-runtime-boundary.md) | Accepted | Aligned | Host boundary trait surface: `crates/fret-runtime/src/ui_host.rs`; default host impl: `crates/fret-app/src/ui_host.rs`; integrated bridge crate: `crates/fret-ui-app`. |
| [`0053-external-drag-payload-portability.md`](0053-external-drag-payload-portability.md) | Accepted | Aligned | Tokenized external drag events: `crates/fret-core/src/input.rs` (`ExternalDragFiles { token, files: Vec<ExternalDragFile { name }>`), token type `crates/fret-core/src/ids.rs` (`ExternalDropToken`); effect-driven reads + release: `crates/fret-runtime/src/effect.rs` (`ExternalDropReadAll*`, `ExternalDropRelease`); desktop runner bridges reads via platform provider: `crates/fret-launch/src/runner/desktop/mod.rs`; web provider reads `File` objects into bytes under limits: `crates/fret-platform-web/src/wasm.rs`. |
| [`0054-platform-capabilities-and-portability-matrix.md`](0054-platform-capabilities-and-portability-matrix.md) | Accepted | Aligned (with known gaps) | Runtime `PlatformCapabilities` + stable string keys: `crates/fret-runtime/src/capabilities.rs` (includes key kind validation tests) + threaded into `InputContext` (`crates/fret-runtime/src/input.rs`) and `when` evaluation (`crates/fret-runtime/src/when_expr.rs`); runners clamp requested→available and log startup caps: `crates/fret-launch/src/runner/desktop/mod.rs`, `crates/fret-launch/src/runner/web.rs`. Docking tear-off is gated by capability signals, and Linux Wayland sessions degrade tear-off + window-under-cursor to keep multi-window docking predictable: `crates/fret-launch/src/runner/desktop/mod.rs` (`linux_is_wayland_session`, `backend_platform_capabilities`) + `ecosystem/fret-docking/src/runtime.rs` (tear-off request degradation) with unit tests in both modules. Gap: capabilities are logged, but there is not yet a dedicated HUD/inspector surface to display them (ADR 0036 follow-up). |
| [`0055-frame-recording-and-subtree-replay-caching.md`](0055-frame-recording-and-subtree-replay-caching.md) | Accepted | Aligned | Per-window paint cache keyed by `(bounds.size, scale_factor, theme_revision)`: `crates/fret-ui/src/tree/paint_cache.rs` (`PaintCacheKey`) + replay-time translation: `crates/fret-ui/src/tree/paint.rs` (`replay_ops_translated`); cache ingestion swaps ops storage (no per-frame copy): `UiTree::ingest_paint_cache_source` in `crates/fret-ui/src/tree/mod.rs`; debug counters: `crates/fret-ui/src/tree/mod.rs` (`UiDebugFrameStats`) + tests `crates/fret-ui/src/tree/tests/paint_cache.rs`; renderer-side scene encode reuse via stable `Scene::fingerprint()`: `crates/fret-core/src/scene.rs` + `crates/fret-render/src/renderer/render_scene/render.rs`. |
| [`0056-component-size-and-density-system.md`](0056-component-size-and-density-system.md) | Accepted | Partially aligned | Prototype exists in `ecosystem/fret-ui-kit/src/sizing.rs`; `density modes` follow-up not implemented yet (see ADR). |
| [`0057-declarative-layout-style-and-flex-semantics.md`](0057-declarative-layout-style-and-flex-semantics.md) | Accepted | Aligned | Typed `LayoutStyle` + `Length` (`Auto`/`Fill`/`Px`) and defaults are fit-content: `crates/fret-ui/src/element.rs` + `crates/fret-ui/src/declarative/layout_helpers.rs` (`clamp_to_constraints`); Taffy-backed `Flex` container (grow/shrink/basis/min/max/wrap/gap/justify/align, plus positioned children): `crates/fret-ui/src/declarative/host_widget/layout/flex.rs` + `crates/fret-ui/src/declarative/taffy_layout.rs`; `Row`/`Column` are thin wrappers lowered to `Flex`: `crates/fret-ui/src/declarative/mount.rs`; `Spacer` is a flex item that grows by default: `crates/fret-ui/src/element.rs`; tests: `crates/fret-ui/src/declarative/tests/layout.rs` (row align/justify + flex wrap). |
| [`0058-typography-v1-textstyle-weight-lineheight-tracking.md`](0058-typography-v1-textstyle-weight-lineheight-tracking.md) | Accepted | Aligned | Expanded `TextStyle` contract (`FontWeight`, `line_height`, `letter_spacing_em`): `crates/fret-core/src/text.rs`; renderer includes fields in text cache keys and shaping (line height + tracking): `crates/fret-render/src/text.rs` (e.g. `TextBlobKey` bits + `attrs.letter_spacing(...)` / `attrs.metrics(...)`) with coverage tests in the same module; UI text elements plumb `TextStyle` and provide theme-driven default line height: `crates/fret-ui/src/declarative/host_widget/layout.rs` (`ElementInstance::Text`). |
| [`0059-text-overflow-ellipsis-and-truncation.md`](0059-text-overflow-ellipsis-and-truncation.md) | Accepted | Aligned | Core contract exists (`TextOverflow::{Clip,Ellipsis}` and `TextConstraints.overflow`): `crates/fret-core/src/text.rs`. Declarative authoring exposes overflow on text elements (`TextProps.overflow`, `StyledTextProps.overflow`, `SelectableTextProps.overflow`) and forwards it into `TextConstraints` during measure: `crates/fret-ui/src/element.rs`, `crates/fret-ui/src/declarative/host_widget/measure.rs`. Renderer implements single-line ellipsis when `wrap=None` and `max_width` is set, trimming glyphs to fit and appending the “…” glyphs with `start=end=cut_end` to keep caret/selection tables representable; also includes a subpixel-rounding guard to avoid spurious ellipses: `crates/fret-render/src/text.rs` (`layout_text`, `want_ellipsis`, `ellipsis_text = "…"`) with a direct unit test: `crates/fret-render/src/text.rs` (`ellipsis_overflow_truncates_single_line_layout`). |
| [`0060-shadows-and-elevation.md`](0060-shadows-and-elevation.md) | Accepted | Aligned | Low-level shadow primitive is implemented as `ShadowStyle` and plumbed through `ContainerProps { shadow: Option<ShadowStyle> }`: `crates/fret-ui/src/element.rs`. Baseline “no-blur elevation” is implemented by painting layered quads with alpha falloff via `paint_shadow` (softness clamped, spreads in layout-space `Px`): `crates/fret-ui/src/paint.rs`, and it is invoked before the container background/border quad in container paint: `crates/fret-ui/src/declarative/host_widget/paint.rs`. Theme integration follows the ADR token keys (`component.shadow.{xs,sm,md,lg}.*`) and semantic shadow color key (`shadow`): `ecosystem/fret-ui-kit/src/declarative/style.rs`; shadcn surfaces apply these helpers (e.g. dialog/popover/sheet). Tests assert ordering and basic bounds expansion: `crates/fret-ui/src/declarative/tests/layout.rs` (`container_paints_shadow_before_background`). |
| [`0061-focus-rings-and-focus-visible.md`](0061-focus-rings-and-focus-visible.md) | Unknown | Aligned | Focus-visible heuristic in `crates/fret-ui/src/focus_visible.rs`; updated during dispatch in `crates/fret-ui/src/tree/dispatch.rs`. |
| [`0062-tailwind-layout-primitives-margin-position-grid-aspect-ratio.md`](0062-tailwind-layout-primitives-margin-position-grid-aspect-ratio.md) | Unknown | Aligned | Implemented in `LayoutStyle` + Taffy mapping: `crates/fret-ui/src/element.rs`, `crates/fret-ui/src/declarative/taffy_layout.rs`, `crates/fret-ui/src/declarative/host_widget/layout/grid.rs`. |
| [`0063-rounded-clipping-and-soft-clip-masks.md`](0063-rounded-clipping-and-soft-clip-masks.md) | Accepted | Aligned | Rounded clip ops exist in the scene contract: `crates/fret-core/src/scene.rs` (`SceneOp::PushClipRRect`, `PopClip`); declarative paint selects `PushClipRRect` when `Overflow::Clip` + non-zero radii: `crates/fret-ui/src/declarative/paint_helpers.rs` (and tests: `crates/fret-ui/src/declarative/tests/layout.rs`); renderer encodes rounded clips via a clip stack (`push_clip_rrect`): `crates/fret-render/src/renderer/render_scene/encode/ops.rs` + `crates/fret-render/src/renderer/render_scene/encode/clip.rs`; hit-testing supports rounded clip masks via `Widget::clip_hit_test_corner_radii` and `crates/fret-ui/src/tree/hit_test.rs` (plus tests in `crates/fret-ui/src/tree/tests/hit_test.rs`). |
| [`0064-overlay-placement-contract.md`](0064-overlay-placement-contract.md) | Accepted | Aligned | Pure anchored overlay placement solver lives in `crates/fret-ui/src/overlay_placement/solver.rs` (exported via `crates/fret-ui/src/overlay_placement/mod.rs`) with regression tests in `crates/fret-ui/src/overlay_placement/tests.rs`; used by declarative `Anchored` layout: `crates/fret-ui/src/declarative/host_widget/layout.rs` and tested in `crates/fret-ui/src/declarative/tests/anchored.rs`. External conformance gates (web geometry vs Fret) cover Radix/shadcn overlay placement outcomes: `ecosystem/fret-ui-shadcn/tests/web_vs_fret_overlay_placement.rs`, `ecosystem/fret-ui-shadcn/tests/radix_web_overlay_geometry.rs`. |
| [`0065-icon-system-and-asset-packaging.md`](0065-icon-system-and-asset-packaging.md) | Accepted | Aligned | Semantic `IconId` + `IconRegistry` (renderer-agnostic) in `ecosystem/fret-icons/src/lib.rs` (includes aliases + missing-icon fallback); curated vendor packs with `register_icons`: `ecosystem/fret-icons-lucide/src/lib.rs`, `ecosystem/fret-icons-radix/src/lib.rs`; component rendering bridges icons to `SvgSource` and supports preload-to-`SvgId` optimization: `ecosystem/fret-ui-kit/src/declarative/icon.rs`; runtime SVG element and source types: `crates/fret-ui/src/element.rs` (`SvgIconProps`) + `crates/fret-ui/src/svg_source.rs` + scene emission in `crates/fret-ui/src/declarative/host_widget/paint.rs` (`SceneOp::SvgMaskIcon`). |
| [`0066-fret-ui-runtime-contract-surface.md`](0066-fret-ui-runtime-contract-surface.md) | Accepted | Aligned (with known gaps) | Runtime substrate is concentrated in `crates/fret-ui` (elements, layout vocab, routing, scroll/virtualization, overlay placement, etc.) and policy-heavy surfaces live in `ecosystem/fret-ui-kit` / `ecosystem/fret-ui-shadcn`; retained widget plumbing is crate-private by default (`crates/fret-ui/src/lib.rs` has `pub(crate) mod widget`) with an opt-in bridge behind `unstable-retained-bridge` (`crates/fret-ui/src/lib.rs`), matching the `runtime vs component` boundary. Gap: `Stable/Experimental/Compatibility` tiers are not enforced as an explicit public API taxonomy yet (still policy-by-convention + Cargo features). |
| [`0067-overlay-policy-architecture-dismissal-focus-portal.md`](0067-overlay-policy-architecture-dismissal-focus-portal.md) | Accepted | Aligned (with known gaps) | Runtime overlay mechanisms exist as a minimal substrate: per-window overlay roots + modal barrier semantics (`push_overlay_root_ex`, `remove_layer`, `blocks_underlay_input`): `crates/fret-ui/src/tree/layers.rs` + deterministic routing across roots (ADR 0011); policy layer is centralized in `ecosystem/fret-ui-kit/src/window_overlays/` (see module docs in `ecosystem/fret-ui-kit/src/window_overlays/mod.rs`) and Radix-named facades live under `ecosystem/fret-ui-kit/src/primitives/` (`portal.rs`, `focus_scope.rs`, `presence.rs`, `dismissable_layer.rs`, etc.); dismissal + focus restore + auto-focus hook behaviors are covered by tests: `ecosystem/fret-ui-kit/src/window_overlays/tests.rs` and shadcn parity tests in `ecosystem/fret-ui-shadcn` (e.g. dialog/popover/alert dialog/dropdown menu focus restore). Auto-focus hooks are modeled as `OnOpenAutoFocus` / `OnCloseAutoFocus` + `AutoFocusRequestCx` (`crates/fret-ui/src/action.rs`) and are orchestrated by `ecosystem/fret-ui-kit/src/window_overlays/render.rs`. Gap: full `focus outside` and nested modal/portal semantics beyond current controller types remain evolving; treat as policy-level and lock via additional tests as new surfaces ship. |
| [`0068-focus-traversal-and-focus-scopes.md`](0068-focus-traversal-and-focus-scopes.md) | Accepted | Aligned (with known gaps) | `focus.next/focus.previous` in `crates/fret-ui/src/tree/commands.rs`; tests in `crates/fret-ui/src/tree/tests/focus_scope.rs` and `crates/fret-ui/src/tree/tests/scroll_into_view.rs`. |
| [`0069-outside-press-and-dismissable-non-modal-overlays.md`](0069-outside-press-and-dismissable-non-modal-overlays.md) | Accepted | Aligned | Outside-press observer pass in `crates/fret-ui/src/tree/mod.rs`; observer dispatch invariants + pointer-occlusion-aware routing in `crates/fret-ui/src/tree/dispatch.rs`; overlay policy wiring in `ecosystem/fret-ui-kit/src/window_overlays/render.rs` (consume outside pointer-down, disable outside pointer events, and pointer-move observers for menu safe-hover corridors); tests in `crates/fret-ui/src/tree/tests/outside_press.rs` + `ecosystem/fret-ui-kit/src/window_overlays/tests.rs` + shadcn menu tests. |
| [`0070-virtualization-contract.md`](0070-virtualization-contract.md) | Accepted | Aligned | Stable contract exists in `crates/fret-ui/src/virtual_list.rs` (`VirtualListMetrics`, `VirtualRange` with inclusive indices, `VirtualItem`, `default_range_extractor`, `visible_range`, `scroll_offset_for_item`) with unit tests in the same module. Declarative element layer consumes the contract (`VirtualListProps` in `crates/fret-ui/src/element.rs`, element builders in `crates/fret-ui/src/elements/cx.rs`). |
| [`0071-text-input-multiline-composition-contract.md`](0071-text-input-multiline-composition-contract.md) | Accepted | Aligned | Multiline IME composition semantics + byte-offset semantics outputs are implemented in `crates/fret-ui/src/text_area/widget.rs` with regression tests in `crates/fret-ui/src/text_area/tests.rs` (preedit shown inline; semantics `value` includes preedit; `text_selection`/`text_composition` are byte offsets). Newline normalization at the framework boundary is implemented in `crates/fret-ui/src/text_edit.rs` (e.g. `normalize_newlines`). Shortcut arbitration while composing (Tab focus traversal suppressed, etc.) is covered by tests in `crates/fret-ui/src/tree/tests/semantics_focus_shortcuts.rs`. |
| [`0072-docking-interaction-arbitration-matrix.md`](0072-docking-interaction-arbitration-matrix.md) | Accepted | Partially aligned | Overlay policy closes/suspends non-modal overlays during dock drags: `ecosystem/fret-ui-kit/src/window_overlays/render.rs` (`dock_drag_affects_window`: closes popovers by setting `open` model to `false`, suppresses hover overlays) with regression coverage in `ecosystem/fret-ui-kit/src/window_overlays/tests.rs` (`dock_drag_closes_non_modal_overlays_for_entire_window`, `dock_drag_does_not_restore_closed_non_modal_overlays_on_drag_end`). Dock drag input arbitration (Escape cancels drag without dismissing overlays) is locked by `crates/fret-ui/src/tree/dispatch.rs` and tested in `crates/fret-ui/src/tree/tests/dock_drag.rs`. Runner forwards stable multi-pointer IDs for touch + tablet tools via `crates/fret-runner-winit/src/lib.rs` (maps touch `FingerId` and tablet `DeviceId` into `fret_core::PointerId`). Docking host suppresses viewport hover + viewport input forwarding during dock drags and prevents competing captures from secondary button presses: `ecosystem/fret-docking/src/dock/space.rs` (`allow_viewport_hover` gating; capture start suppression when `dock_drag`/`viewport_capture`/`divider_drag` are active). Settings-driven modifier inversion is implemented in `fret_runtime::DockingInteractionSettings`: `crates/fret-runtime/src/docking_settings.rs` (used by docking: `ecosystem/fret-docking/src/dock/space.rs`). Runtime provides generic cross-window internal drag routing via `fret_ui::internal_drag`: mechanism API in `crates/fret-ui/src/internal_drag.rs`, consumed by dispatch in `crates/fret-ui/src/tree/dispatch.rs` (cross-window hover routing). Runtime also routes per-pointer capture (`crates/fret-ui/src/tree/mod.rs`, `crates/fret-ui/src/tree/dispatch.rs`) and per-pointer drag sessions (`crates/fret-runtime/src/drag.rs`, `crates/fret-runtime/src/ui_host.rs`). Gaps: (1) true multi-pointer arbitration is still not audited end-to-end across all platforms/backends (non-winit sources may differ); (2) the generalized `viewport tool capture vs overlays` contract in ADR 0049 remains deferred/not audited. |
| [`0073-active-descendant-and-composite-widget-semantics.md`](0073-active-descendant-and-composite-widget-semantics.md) | Accepted | Aligned (with known gaps) | Phase A (schema + bridge) is implemented: semantics schema includes `active_descendant: Option<NodeId>` (`crates/fret-core/src/semantics.rs`), element/host semantics can set it (`crates/fret-ui/src/widget.rs` + `crates/fret-ui/src/declarative/host_widget/semantics.rs`), and snapshots carry it (`crates/fret-ui/src/tree/mod.rs`). AccessKit mapping emits `active_descendant` with reachability gating tests: `crates/fret-a11y-accesskit/src/lib.rs` (see `active_descendant_is_*` tests) + winit glue `crates/fret-runner-winit/src/accessibility.rs`. Gap: cmdk-style component policy wiring (focus stays on input while updating `active_descendant`) is not yet implemented in shadcn surfaces. |
| [`0074-component-owned-interaction-policy-and-runtime-action-hooks.md`](0074-component-owned-interaction-policy-and-runtime-action-hooks.md) | Accepted | Aligned | Runtime action-hook substrate exists in `crates/fret-ui/src/action.rs` (`UiActionHost`, `ActionCx`, `DismissReason`, handler types) and is invoked from declarative dispatch (`crates/fret-ui/src/declarative/host_widget/event/`). Component-layer convenience helpers live in `ecosystem/fret-ui-kit/src/declarative/action_hooks.rs` (e.g. `ActionHooksExt`), and overlay dismissal/outside-press behavior composes hooks in `ecosystem/fret-ui-kit/src/overlay_controller.rs` + `ecosystem/fret-ui-kit/src/window_overlays/` (ADR 0069). |
| [`0075-docking-layering-b-route-and-retained-bridge.md`](0075-docking-layering-b-route-and-retained-bridge.md) | Accepted | Aligned (with known gaps) | Docking UI/policy lives in a dedicated crate: `ecosystem/fret-docking/` (e.g. `ecosystem/fret-docking/src/dock/space.rs`). Runtime mechanism provides generic internal drag routing via `fret_ui::internal_drag`: `crates/fret-ui/src/internal_drag.rs` + `crates/fret-ui/src/tree/dispatch.rs`, and docking refreshes the route anchor during event/layout/paint to support tear-off/cross-window drags: `ecosystem/fret-docking/src/dock/space.rs` + tests `ecosystem/fret-docking/src/dock/tests.rs` (`dock_space_installs_internal_drag_route_anchor`). Transitional retained bridge is feature-gated: `crates/fret-ui/src/lib.rs` (`retained_bridge` behind `unstable-retained-bridge`) + `crates/fret-ui/src/retained_bridge.rs`. Gap: explicit `delete plan` checkpoints for shrinking the retained bridge are not currently tracked here (should be added as follow-up notes as the bridge surface shrinks). |
| [`0076-declarative-layout-performance-hardening.md`](0076-declarative-layout-performance-hardening.md) | Accepted | Not implemented | The old “container-owned persistent TaffyTree” strategy was removed as the repo defaulted to the window-scoped layout engine (ADR 0116). Per-package dev override remains: `Cargo.toml` (`[profile.dev.package.taffy]`). Performance hardening now lives in the window-owned engine: stable `NodeId` identity + incremental node reuse in `crates/fret-ui/src/layout_engine.rs`, flow graph construction in `crates/fret-ui/src/layout_engine/flow.rs`, and per-solve measurement memoization inside `compute_root_with_measure` (`measure_cache` keyed by `MeasureKey` in `crates/fret-ui/src/layout_engine.rs`). |
| [`0077-resizable-panel-groups-and-docking-split-sizing.md`](0077-resizable-panel-groups-and-docking-split-sizing.md) | Accepted | Partially aligned | Runtime primitive exists in `crates/fret-ui/src/resizable_panel_group/mod.rs` (+ tests in `crates/fret-ui/src/resizable_panel_group/tests.rs`) and declarative surface exists (`ElementContext::resizable_panel_group` in `crates/fret-ui/src/elements/cx.rs`, props in `crates/fret-ui/src/element.rs`); shadcn facade forwards into it: `ecosystem/fret-ui-shadcn/src/resizable.rs`. Docking reuses the shared sizing math via the retained bridge helpers (`drag_update_fractions`): `crates/fret-ui/src/retained_bridge.rs` + `ecosystem/fret-docking/src/dock/space.rs`, and implements same-axis nested split stabilization with tests: `ecosystem/fret-docking/src/dock/split_stabilize.rs` + `ecosystem/fret-docking/src/dock/tests.rs`. Gap vs ADR: docking still owns split-handle hit-testing/paint/cursor/capture policy in `ecosystem/fret-docking` (`dock/hit_test.rs`, `dock/paint.rs`) rather than rendering split nodes as the runtime primitive/widget, so behavior can still drift across docking vs shadcn until conformance tests lock the shared contract end-to-end. |
| [`0078-scene-transform-and-clip-composition.md`](0078-scene-transform-and-clip-composition.md) | Accepted | Aligned | Scene op stack exists in `crates/fret-core/src/scene.rs` (`PushTransform`/`PopTransform`, `PushClipRect`/`PushClipRRect`/`PopClip`, `PushOpacity`/`PopOpacity`). Transform composition is left-multiplication (`current * t`) consistent across core and renderer: `crates/fret-core/src/geometry.rs` (`Transform2D::compose`) and `crates/fret-render/src/renderer/render_scene/encode/ops.rs` (`state.transform_stack.push(current * transform)`). Clip evaluation captures the current transform at push time and does not retroactively move when later transforms are pushed: `crates/fret-render/src/renderer/render_scene/encode/clip.rs` (uses `state.current_transform_px()` to derive `inv_px` stored per clip node, linked-list `clip_head`). Renderer uses scissor fast path only for axis-aligned rect clips and falls back to shader clip for non-axis-aligned/rounded: `crates/fret-render/src/renderer/render_scene/encode/clip.rs`. GPU conformance coverage exists: `crates/fret-render/tests/affine_clip_conformance.rs` (clip-local evaluation + clip-before-transform capture + rounded clip). UI authoring emits clip-before-transform (viewport clip) via `PushClip*` wrappers around child paint where desired: `crates/fret-ui/src/declarative/paint_helpers.rs` and `crates/fret-ui/src/declarative/host_widget/paint.rs` (e.g. `VirtualList` row clipping). |
| [`0079-scene-layers-marker-only-v1.md`](0079-scene-layers-marker-only-v1.md) | Accepted | Aligned | Layer ops exist in the core scene contract: `crates/fret-core/src/scene.rs` (`SceneOp::PushLayer { layer }`, `SceneOp::PopLayer`) with stack validation (`LayerUnderflow`) and fingerprint tagging (layer ID mixed but no ordering semantics). Runtime/renderer treat layers as perf/debug boundaries only: `crates/fret-render/src/renderer/render_scene/encode/ops.rs` flushes batches on `PushLayer`/`PopLayer` without changing transform/clip/opacity state; SVG prep ignores them: `crates/fret-render/src/renderer/svg/prepare.rs`. Conformance tests lock that layer markers do not affect transform/clip/opacity semantics: `crates/fret-core/tests/scene_state_stack_conformance.rs` (`layer_markers_do_not_affect_transform_clip_or_opacity`). Reference: Zed/GPUI has a different model where layers participate in z-index tracking (`repo-ref/zed/crates/gpui/src/scene.rs`), which this ADR explicitly avoids. |
| [`0080-vector-path-contract.md`](0080-vector-path-contract.md) | Accepted | Aligned (with known gaps) | Core path contract exists in `crates/fret-core/src/vector_path.rs` (`PathCommand`, `FillRule`, `PathStyle`, `PathConstraints`, `PathMetrics`, `PathService::prepare/release`) and `SceneOp::Path` draws prepared geometry by `PathId`: `crates/fret-core/src/scene.rs` (fingerprint includes `path` + `origin` + `color`, but ops remain strictly ordered). Renderer implements `PathService` and owns caching/tessellation via lyon: `crates/fret-render/src/renderer/services.rs` (ref-counted cache entries) + `crates/fret-render/src/renderer/path.rs` (command robustness: implicit `MoveTo(to)` when not active; `Close` is no-op when inactive; fill rule maps to lyon; stroke clamps `width >= 0` and fixes join/caps to round; conservative `PathMetrics.bounds` expands by `width/2`). `SceneOp::Path` rendering honors transforms/clips/opacity: `crates/fret-render/src/renderer/render_scene/encode/draw/path.rs` (applies transform stack per vertex, intersects with current clip scissor, multiplies alpha by `current_opacity()`). Gap: there are no dedicated renderer conformance tests yet for fill-rule correctness/self-intersections and stroke bounds conservativeness beyond code review. |
| [`0082-draworder-is-non-semantic.md`](0082-draworder-is-non-semantic.md) | Accepted | Aligned | Core contract explicitly documents `DrawOrder` as non-semantic: `crates/fret-core/src/scene.rs` (`DrawOrder` comment referencing ADR 0082). Renderers preserve `Scene.ops` order and do not sort/bucket by `DrawOrder`: encoding is a single pass over ops (`crates/fret-render/src/renderer/render_scene/encode/mod.rs`) and draw encoders do not branch on `DrawOrder` (e.g. `crates/fret-render/src/renderer/render_scene/encode/draw/quad.rs`). `DrawOrder` is mixed into the scene fingerprint to avoid cache collisions without affecting compositing: `crates/fret-core/src/scene.rs` (`mix_scene_op` mixes `order.0`). |
| [`0083-render-transform-hit-testing.md`](0083-render-transform-hit-testing.md) | Accepted | Aligned | Render transform contract exists on `Widget`: `crates/fret-ui/src/widget.rs` (`render_transform`). Paint emits transform stack ops and ignores non-invertible transforms: `crates/fret-ui/src/tree/paint.rs` (`PushTransform`/`PopTransform`, `node_render_transform`); hit-testing maps pointer through inverse transforms: `crates/fret-ui/src/tree/hit_test.rs`; pointer-bearing events map `position` through inverse transforms and map wheel `delta` as a vector: `crates/fret-ui/src/tree/dispatch.rs` (`build_mapped_event_chain`). Cross-frame layout-vs-visual geometry queries exist for elements: `crates/fret-ui/src/elements/queries.rs` (`bounds_for_element`, `visual_bounds_for_element`) + `crates/fret-ui/src/elements/cx.rs` (`last_*_bounds_for_element`); visual bounds include composed ancestor transforms via AABB: `crates/fret-ui/src/tree/paint.rs` + `crates/fret-ui/src/tree/mod.rs` (`rect_aabb_transformed`). Conformance tests cover transforms + hit-testing/coordinates + clips + non-invertible behavior: `crates/fret-ui/src/tree/tests/transforms.rs`, `crates/fret-ui/src/tree/tests/hit_test.rs`, `crates/fret-ui/src/tree/tests/outside_press.rs`, `crates/fret-ui/src/declarative/tests/anchored.rs`. Paint caching is conservatively disabled for transformed subtrees: `crates/fret-ui/src/tree/paint.rs` (matches the ADR note; Zed/GPUI also disables caching for inspection to preserve hit-test hitboxes: `repo-ref/zed/crates/gpui/src/view.rs`). |
| [`0084-multi-window-degradation-policy.md`](0084-multi-window-degradation-policy.md) | Accepted | Aligned | Capability gate exists as `PlatformCapabilities.ui.multi_window` (`crates/fret-runtime/src/capabilities.rs` keys + struct), and web runner clamps it to `false` in effective capabilities (`crates/fret-launch/src/runner/web.rs`); desktop runner reports support on desktop targets and clamps requested by available (`crates/fret-launch/src/runner/desktop/mod.rs`). Dock layout import can degrade unmapped logical windows into in-window floating containers inside a fallback window: `crates/fret-core/src/dock.rs` (`import_layout_for_windows_with_fallback_floatings`) with a regression test `import_layout_degrades_unmapped_windows_into_floating_containers`. Tear-off request degrades to in-window floating (no `WindowRequest::Create`) when multi-window is disabled: `ecosystem/fret-docking/src/runtime.rs` (`handle_dock_op` maps `RequestFloatPanelToNewWindow` -> `FloatPanelInWindow`) with tests `request_float_degrades_to_in_window_when_multi_window_is_disabled`. Docking UI also gates tear-off affordances by caps (`cx.input_ctx.caps.ui.multi_window`): `ecosystem/fret-docking/src/dock/space.rs`. |
| [`0085-virtualized-accessibility-and-collection-semantics.md`](0085-virtualized-accessibility-and-collection-semantics.md) | Accepted | Aligned | P0 portable contract is implemented end-to-end: semantics schema exposes `pos_in_set`/`set_size` on `SemanticsNode` (`crates/fret-core/src/semantics.rs`); authoring surface exists (`SemanticsCx::set_collection_position` in `crates/fret-ui/src/widget.rs`) and declarative host forwards it for common surfaces (e.g. `Pressable` in `crates/fret-ui/src/declarative/host_widget/semantics.rs`). Snapshot production enforces basic invariants and clears invalid values (`crates/fret-ui/src/tree/mod.rs`). Platform bridge maps the metadata to AccessKit (`set_position_in_set`/`set_size_of_set`) with a focused regression test (`list_item_pos_in_set_and_set_size_are_emitted`): `crates/fret-a11y-accesskit/src/lib.rs`. Component policy is in ecosystem as required: stamping helper `ecosystem/fret-ui-kit/src/declarative/collection_semantics.rs`, virtual list adoption + test `list_virtualized_stamps_collection_semantics_on_rows` in `ecosystem/fret-ui-kit/src/declarative/list.rs`, and `active_descendant` correctness under recycling is locked by tests (clear when the active option is not present): `ecosystem/fret-ui-kit/src/primitives/active_descendant.rs`. shadcn surfaces (cmdk/select/etc.) assert `pos_in_set`/`set_size` in their a11y tests (e.g. `ecosystem/fret-ui-shadcn/src/command.rs`, `ecosystem/fret-ui-shadcn/src/select.rs`). P1 platform-native virtualization patterns remain optional and are not implemented (not required for alignment). |
| [`0086-model-handle-lifecycle-and-weak-models.md`](0086-model-handle-lifecycle-and-weak-models.md) | Accepted | Aligned | Strong/weak handle pattern is implemented in `crates/fret-runtime/src/model.rs`: `Model<T>` is ref-counted in the store (`Clone` -> `inc_strong`, `Drop` -> `dec_strong`), and `WeakModel<T>::upgrade()` fails if the entry is missing or strong is zero (`upgrade_strong`). Drop while leased is handled via `pending_drop`: dropping the last strong while `entry.value` is `None` sets `pending_drop`, and removal is deferred until `end_lease` returns the value (`end_lease_shared` removes when `pending_drop && strong == 0`). Drop/removal marks the id as changed (`mark_changed_locked`) so the runtime can invalidate caches keyed by `ModelId` (`take_changed_models`). Regression tests cover the core lifecycle invariants: `strong_handle_drop_removes_entry`, `clone_increments_and_decrements_strong_count`, and `dropping_last_strong_while_leased_defers_removal_until_end_lease` in `crates/fret-runtime/src/model.rs`. Re-exports exist in `crates/fret-runtime/src/lib.rs`. |
| [`0087-models-are-main-thread-only-and-not-send.md`](0087-models-are-main-thread-only-and-not-send.md) | Accepted | Aligned | Compile-time main-thread enforcement is implemented by making `ModelStore` `!Send`/`!Sync` in `crates/fret-runtime/src/model.rs`: the store is backed by `Rc<...>` and includes an explicit `_not_send: PhantomData<Rc<()>>` marker (commented as the threading contract). All handles embed the store (`Model<T>` contains `ModelStore`, `WeakModel<T>` contains `Weak<ModelStoreInner>`), so they inherit the same `!Send`/`!Sync` boundary. This matches the ADR requirement to route cross-thread work through effects/messages rather than moving model handles across threads (ADR 0008). |
| [`0088-overflow-and-clipping-conventions.md`](0088-overflow-and-clipping-conventions.md) | Accepted | Aligned | Layout overflow contract exists (`Overflow::{Visible,Clip}`) in `crates/fret-ui/src/element.rs`. Declarative host maps `Overflow::Clip` to paint + hit-test clipping (`clips_hit_test` + optional rounded radii) in `crates/fret-ui/src/declarative/host_widget/layout.rs` and emits `PushClipRect`/`PushClipRRect` + `PopClip` for paint in `crates/fret-ui/src/declarative/host_widget/paint.rs` + `crates/fret-ui/src/declarative/paint_helpers.rs`; runtime hit-testing respects the same rectangular/rounded clip shape via `Widget::{clips_hit_test, clip_hit_test_corner_radii}` in `crates/fret-ui/src/tree/hit_test.rs` (+ coverage in `crates/fret-ui/src/tree/tests/hit_test.rs`). Focus rings can extend outside chrome: `crates/fret-ui/src/paint.rs` (`paint_focus_ring`) gated by focus-visible: `crates/fret-ui/src/focus_visible.rs` and used by `Pressable` paint in `crates/fret-ui/src/declarative/host_widget/paint.rs` (+ tests in `crates/fret-ui/src/declarative/tests/layout.rs`). Component-layer `surface chrome boundary` helper enforces `Pressable (visible) -> chrome (clip) -> content`: `ecosystem/fret-ui-kit/src/declarative/chrome.rs`. Overlays escape ancestor clips via portal/overlay roots rather than relying on `Overflow::Visible`: `ecosystem/fret-ui-kit/src/overlay_controller.rs` + `ecosystem/fret-ui-kit/src/window_overlays/` (e.g. `ecosystem/fret-ui-shadcn/src/popover.rs`). Zed/GPUI uses a similar content-mask clipping model for hit testing (`with_content_mask` + hit-test intersection): `repo-ref/zed/crates/gpui/src/window.rs`. |
| [`0089-renderer-architecture-v2-scene-compiler.md`](0089-renderer-architecture-v2-scene-compiler.md) | Accepted | Aligned (with known gaps) | Core "scene compiler" shape is implemented as a dedicated encoding pass that preserves `Scene.ops` order while maintaining explicit state stacks (transform/opacity/scissor/clip): `crates/fret-render/src/renderer/render_scene/encode/mod.rs` (`encode_scene_ops_into`) + `crates/fret-render/src/renderer/render_scene/encode/state.rs` (`EncodeState`). The compiler produces an ordered internal draw stream across primitive kinds (`SceneEncoding.ordered_draws: Vec<OrderedDraw>`): `crates/fret-render/src/renderer/types.rs`, matching the adjacency-preserving ordering contract (ADR 0009). Encoded results are cached and reused by a stable key (`SceneEncodingCacheKey`) and allocation reuse is explicit (`scene_encoding_cache`/`scene_encoding_scratch`): `crates/fret-render/src/renderer/render_scene/render.rs` + cache storage in `crates/fret-render/src/renderer/mod.rs`. GPU uploads use a small frames-in-flight ring buffer (`FRAMES_IN_FLIGHT = 3`) to avoid writing into in-flight buffers: `crates/fret-render/src/renderer/resources.rs` (buffer arrays + rotating indices in `render_scene/render.rs`). Performance tooling exists for SVG atlas/budgets and explicit cache lifecycle knobs (`clear_svg_*_cache`, `SvgPerfSnapshot`): `crates/fret-render/src/renderer/config.rs` (stress harness exists at `apps/fret-svg-atlas-stress/src/main.rs`). Gap: the "converge all textured draws into Sprite2D + universal RenderRuns/BatchKey" direction is only partially realized today (quads are instanced and batched; other primitive kinds like text/images/paths still encode as separate streams and can remain one draw per op), and additional cross-primitive coalescing/atlas unification remains incremental work as the ADR notes. |
| [`0090-radix-aligned-headless-primitives-in-fret-components-ui.md`](0090-radix-aligned-headless-primitives-in-fret-components-ui.md) | Proposed | Aligned | The split is implemented as described: `ecosystem/fret-ui-kit` exposes `headless` (pure state machines) and `primitives` (Radix-named facades) in `ecosystem/fret-ui-kit/src/{headless,primitives}/` and documents strict anti-duplication rules in `ecosystem/fret-ui-kit/src/primitives/mod.rs`. The crate remains renderer/platform-agnostic at the Cargo level (`ecosystem/fret-ui-kit/Cargo.toml` depends on `fret-core`/`fret-ui` only). shadcn recipes build on these stable primitives (`use fret_ui_kit::primitives::*` across `ecosystem/fret-ui-shadcn/src/`). Radix mapping/index docs exist and are kept navigable (`docs/radix-primitives-alignment.md`, `docs/ui-closure-map.md`). Overlay orchestration internals are feature-gated while stable facade types are surfaced at the crate root (`ecosystem/fret-ui-kit/src/lib.rs` + `window_overlays` cfg). Follow-up: downshift remaining hover-overlay intent drivers (Tooltip/HoverCard) from shadcn recipes into `fret-ui-kit::headless` so the behavior is unit-testable and shared (tracked in `docs/todo-tracker.md`). |
| [`0091-platform-backends-native-web.md`](0091-platform-backends-native-web.md) | Accepted | Aligned (with known gaps) | The crate split exists as described: portable platform contracts live in `crates/fret-platform/src/lib.rs` (backend-agnostic module surface), with native and web backends in `crates/fret-platform-native` and `crates/fret-platform-web` (Cargo manifests show platform/API deps only; no UI deps). Runner responsibilities remain winit-specific: `crates/fret-runner-winit/Cargo.toml` depends on `winit` + `fret-core` (and `fret-a11y-accesskit`) and does not depend on `fret-ui` / component crates. Launcher glue integrates backends: `crates/fret-launch/src/runner/desktop/mod.rs` wires `fret_platform_native::*` implementations via `fret_platform::*` traits; `crates/fret-launch/src/runner/web.rs` uses `fret_platform_web::WebPlatformServices`. `crates/fret-runner-web/src/lib.rs` is currently a compatibility shim (re-export web on wasm32, error on native), and a dedicated DOM adapter remains future work as the ADR notes. |
| [`0092-keyboard-types-physical-keycodes.md`](0092-keyboard-types-physical-keycodes.md) | Accepted | Aligned | `fret_core::KeyCode` is aligned with `keyboard-types::Code` via a direct re-export (`pub use keyboard_types::Code as KeyCode`): `crates/fret-core/src/input.rs`. Winit runner forwards physical keys without translation (`PhysicalKey::Code(code) => code`, `Unidentified(_) => KeyCode::Unidentified`) and locks it with unit tests: `crates/fret-runner-winit/src/lib.rs` (`map_physical_key`, `physical_key_code_roundtrips`, `physical_key_unidentified_maps_to_unidentified`). Keymap tokens parse using `FromStr` on `KeyCode` (`keys.key.parse()`), matching the "variant name" token strategy: `crates/fret-runtime/src/keymap.rs` (`parse_keys`). |
| [`0093-crate-structure-core-backends-apps.md`](0093-crate-structure-core-backends-apps.md) | Accepted | Aligned (with known gaps) | Workspace structure matches the decision: `Cargo.toml` includes `members = ["crates/*", "ecosystem/*", "apps/*"]` and keeps `apps/*` out of the default build via `default-members = ["crates/*", "ecosystem/*"]`. Core crates (`fret-core`, `fret-runtime`, `fret-app`, `fret-ui`) are backend-agnostic at the Cargo level (e.g. `crates/fret-ui/Cargo.toml` depends only on core/runtime + portable libs). Backend crates (`fret-platform-*`, `fret-runner-winit`) depend on core crates but not on UI/components. The facade crate does not pull in backends by default: `crates/fret/Cargo.toml` uses optional deps + feature bundles (default is `core/app/ui`, while `launch`, `runner-winit`, `platform-*`, `render` are opt-in). `crates/fret-launch` exists as a thin launcher/glue crate (per ADR). |
| [`0094-window-close-and-web-runner-destroy.md`](0094-window-close-and-web-runner-destroy.md) | Unknown | Aligned (with known gaps) | `WindowRequest::Close(AppWindowId)` is implemented as a runner-level "close app window / exit app instance" primitive: desktop runner closes the target OS window and exits when appropriate (`crates/fret-launch/src/runner/desktop/mod.rs` handles `Effect::Window(WindowRequest::Close(..))`); web runner treats `Close` as exiting the runner instance for the primary window (`crates/fret-launch/src/runner/web.rs`). Docking "close" is modeled as dock ops, not window close (`DockOp::ClosePanel` in `crates/fret-core/src/dock_op.rs` and applied in docking runtime), and overlay dismissal is expressed as overlay policy/model updates in the component layer (e.g. `ecosystem/fret-ui-kit/src/window_overlays/`), matching the ownership boundary. The web host-controlled teardown API exists as `fret_launch::WebRunnerHandle::destroy()` (`crates/fret-launch/src/runner/web.rs`), implemented by setting an exit flag + waking the event loop; the runner exits at a well-defined hook (`maybe_exit`) and drops DOM listeners/resources. Gap: the ADR document itself does not currently declare an explicit `Status: ...` line (this matrix leaves it as `Unknown` until the ADR metadata is updated). |
| [`0095-menu-open-modality-and-entry-focus.md`](0095-menu-open-modality-and-entry-focus.md) | Proposed | Aligned | Per-window modality signal exists as `InputModality` in `crates/fret-ui/src/input_modality.rs` and is updated during dispatch (`crates/fret-ui/src/tree/dispatch.rs` calls `input_modality::update_for_event`). Menu overlay policy gates initial focus by modality (pointer-open focuses the content container to prevent entry focus) in `ecosystem/fret-ui-kit/src/primitives/menu/root.rs` (`dismissible_menu_request_with_modal`). The overlay controller suppresses pointer occlusion while another layer holds pointer capture to keep pointer-open focus deterministic: `ecosystem/fret-ui-kit/src/window_overlays/render.rs`. Coverage exists in unit tests for modality defaults/switching (`crates/fret-ui/src/input_modality.rs`) and shadcn menu behavior (`ecosystem/fret-ui-shadcn/src/dropdown_menu.rs`). |
| [`0096-renderer-perf-snapshot-and-stress-harness.md`](0096-renderer-perf-snapshot-and-stress-harness.md) | Proposed | Partially aligned | Unified renderer-wide perf snapshot API exists (`Renderer::{set_perf_enabled,take_perf_snapshot}` in `crates/fret-render/src/renderer/config.rs`, type `RenderPerfSnapshot` in `crates/fret-render/src/renderer/types.rs`) with P0 counters collected in `crates/fret-render/src/renderer/render_scene/render.rs` (encode/prepare time, draw calls, pipeline/bind group sets, upload bytes, scene encoding cache hit/miss) and emitted by the stress harness `apps/fret-svg-atlas-stress/src/main.rs` (`renderer_perf:`). Pipeline switches can also be broken down by pipeline class (quad/viewport/mask/text_mask/text_color/path/path_msaa/composite/fullscreen/clip_mask) and printed by setting `FRET_RENDERER_PERF_PIPELINES=1` (stress demos emit `renderer_perf_pipelines:` / `headless_renderer_perf_pipelines:`). Subsystem-specific SVG perf snapshot remains available (`SvgPerfSnapshot`). Gaps: counters are best-effort (no GPU timestamp queries) and unified cache telemetry is currently limited to scene encoding hit/miss (does not aggregate per-atlas hit/miss yet). |
| [`0097-plot-widgets-and-crate-placement.md`](0097-plot-widgets-and-crate-placement.md) | Accepted | Aligned | Plot widget lives in the ecosystem layer as a dedicated crate (`ecosystem/fret-plot`, crate name `fret-plot`) and is not part of the kernel. It builds on existing portable primitives (not plot-specific `SceneOp`s): uses `fret_core::PathCommand` and plot geometry helpers (`ecosystem/fret-plot/src/cartesian.rs`, `ecosystem/fret-plot/src/plot/decimate.rs`), and relies on the existing retained/widget substrate via `fret-ui`'s `unstable-retained-bridge` (`ecosystem/fret-plot/Cargo.toml`). The crate does not depend on `wgpu`/`winit`/`fret-render` (Cargo deps), matching the layering requirement. Stable series identity is explicit (`SeriesId(u64)` with deterministic derivation): `ecosystem/fret-plot/src/series.rs`. |
| [`0098-plot3d-rendering-strategy.md`](0098-plot3d-rendering-strategy.md) | Proposed | Aligned | Plot3D follows the "embedded viewport surface" route (B1): the UI widget emits `SceneOp::ViewportSurface` referencing an engine-owned `RenderTargetId` (`ecosystem/fret-plot3d/src/retained.rs`), and forwards interaction via `Effect::ViewportInput(ViewportInputEvent)` with `ViewportMapping`-derived UV/px coordinates (`ecosystem/fret-plot3d/src/retained.rs`). The demo driver owns render-target allocation and records engine command buffers separately from UI using the engine render hook pipeline (`apps/fret-examples/src/plot3d_demo.rs` `record_engine_frame`), proving end-to-end correctness without introducing new mesh/3D scene ops. |
| [`0099-plot-architecture-and-performance.md`](0099-plot-architecture-and-performance.md) | Proposed | Aligned | 2D plots are implemented as retained canvases with composable layers in `ecosystem/fret-plot/src/retained/` (`PlotCanvas<L: PlotLayer>` in `ecosystem/fret-plot/src/retained/canvas/mod.rs`, concrete layers + aliases like `LinePlotCanvas`/`ScatterPlotCanvas`/`HeatmapPlotCanvas` in `ecosystem/fret-plot/src/retained/layers.rs`). Stable identity is enforced via `SeriesId` and ImPlot-style slice/getter adapters (`SeriesData`) in `ecosystem/fret-plot/src/series.rs` and series models in `ecosystem/fret-plot/src/retained/models.rs` (per-series `YAxis` incl. Y2/Y3/Y4). Axis transforms and log-scale sanitization use `f64` data space with explicit `AxisScale::{Linear,Log10}` and `PlotTransform` in `ecosystem/fret-plot/src/cartesian.rs`. LOD/decimation is bounded by viewport pixels (min/max per pixel bucket style) in `ecosystem/fret-plot/src/plot/decimate.rs` and used by layers; caching keys include model revision + viewport/scale/style in `ecosystem/fret-plot/src/retained/{layers.rs,canvas/cache.rs}`. Plot state IO is caller-owned (`PlotState`) with widget-written observation output (`PlotOutput`) in `ecosystem/fret-plot/src/retained/state.rs`. Stress/perf harness exists (`apps/fret-examples/src/plot_stress_demo.rs`). The 3D route uses viewport surfaces and input forwarding via a dedicated crate `ecosystem/fret-plot3d` (`Plot3dCanvas` in `ecosystem/fret-plot3d/src/retained.rs` + demo `apps/fret-examples/src/plot3d_demo.rs`). |
| [`0100-markdown-rendering-streaming-and-injection.md`](0100-markdown-rendering-streaming-and-injection.md) | Accepted | Aligned (with known gaps) | The crate boundary shape exists as described: `ecosystem/fret-syntax` provides UI-agnostic tree-sitter highlighting (no `fret-ui` dependency; feature-gated grammars), `ecosystem/fret-code-view` provides a code block component (depends on `fret-syntax` and UI crates), and `ecosystem/fret-markdown` provides Markdown-to-elements rendering (uses `mdstream` + `pulldown-cmark`, and renders fenced code via `fret-code-view`): `ecosystem/fret-syntax/Cargo.toml`, `ecosystem/fret-code-view/Cargo.toml`, `ecosystem/fret-markdown/Cargo.toml`. Streaming contract is implemented with committed blocks + pending block via `mdstream::DocumentState` and keyed subtree stability (`ElementContext::for_each_keyed` over committed blocks and `cx.keyed(pending.id, ..)`): `ecosystem/fret-markdown/src/lib.rs`. Injection points exist via `MarkdownComponents` (block render overrides, code block actions, link/image hooks) and link activation is delegated to the host (helper emits `Effect::OpenUrl` with a safe-scheme gate): `ecosystem/fret-markdown/src/lib.rs`. Security posture is conservative by default: no implicit I/O for images, and pulldown options do not enable HTML parsing (`pulldown_options_default` does not set `ENABLE_HTML`), leaving HTML handling to explicit overrides. Gap: the ADR describes an `ecosystem/fret-syntax/third_party/` packaging policy; the crate structure exists, but the exact "tracked upstream sources + license texts" layout should be verified/locked as part of a dedicated licensing audit. |
| [`0151-pointer-click-count-and-double-click.md`](0151-pointer-click-count-and-double-click.md) | Proposed | Aligned (with known gaps) | `click_count: u8` is part of `PointerEvent::{Down,Up}` in `crates/fret-core/src/input.rs` and is forwarded through event mapping in `crates/fret-ui/src/tree/dispatch.rs`. Runner-level click counting is implemented in `crates/fret-runner-winit/src/lib.rs` (`ClickTracker` with `CLICK_SLOP_PX` + `MULTI_CLICK_MAX_DELAY`, per-button state, and logical-px distance math). Viewport forwarding carries `click_count` via `ViewportInputKind::{PointerDown,PointerUp}` (`crates/fret-core/src/input.rs`) and is forwarded by docking (`ecosystem/fret-docking/src/dock/space.rs`). Double-click UX is consumed by plot widgets (`*click_count == 2` fit on input-map match) in `ecosystem/fret-plot/src/retained/canvas/mod.rs`. Gap: the wasm cursor polling path (`poll_web_cursor_updates` in `crates/fret-runner-winit/src/lib.rs`) emits `PointerEvent::Move` without calling `click.update_move`, so "moved out of slop then back" may be misclassified as a click on web. |
| [`0101-headless-table-engine.md`](0101-headless-table-engine.md) | Accepted | Aligned (with known gaps) | TanStack-inspired headless table engine exists under `ecosystem/fret-ui-headless/src/table/` and is re-exported as `fret-ui-kit::headless::table` via `ecosystem/fret-ui-kit/src/headless/mod.rs` (the legacy `fret-ui-kit` `table` compatibility feature has been removed). The headless module is deterministic and unit-tested (many `#[test]` in the headless directory) and does not depend on `fret-ui` types (no `use fret_ui` within the headless table module), matching the layering intent. Stable identity types exist (`ColumnId = Arc<str>`: `ecosystem/fret-ui-headless/src/table/column.rs`; `RowKey(u64)`: `ecosystem/fret-ui-headless/src/table/row_model.rs`) and a memoized pipeline substrate exists (`ecosystem/fret-ui-headless/src/table/memo.rs`). The shadcn `DataTable` recipe is wired to the headless pipeline via the shared `table_virtualized` view (`ecosystem/fret-ui-shadcn/src/data_table.rs` calls `ecosystem/fret-ui-kit/src/declarative/table.rs::table_virtualized` with `Model<TableState>`), and recipe controls exist (`ecosystem/fret-ui-shadcn/src/data_table_recipes.rs`: `DataTableToolbar`, `DataTablePagination`). Pagination bounds/clamping are explicit via `TableViewOutput` (`ecosystem/fret-ui-kit/src/declarative/table.rs`) backed by `pagination_bounds` tests (`ecosystem/fret-ui-headless/src/table/pagination.rs`). Gaps: multi-sort key modifiers and column sizing/pinning parity are deferred; there is still no dedicated shadcn table feature gate. |
| [`0102-semantic-theme-keys-and-extensible-token-registry.md`](0102-semantic-theme-keys-and-extensible-token-registry.md) | Accepted | Aligned (with known gaps) | Semantic-first token identity is implemented via canonical key aliasing (`canonicalize_token_key`) for colors/metrics: `crates/fret-ui/src/theme_registry.rs`, and theme lookups go through canonicalization (`Theme::color_by_key`, `Theme::metric_by_key`): `crates/fret-ui/src/theme.rs`. Typed core key sets exist and map to canonical semantic names (`ThemeColorKey` / `ThemeMetricKey` + `canonical_name`), with typed accessors on `Theme` (`Theme::color`, `Theme::metric`): `crates/fret-ui/src/theme_keys.rs`, `crates/fret-ui/src/theme.rs`. Default theme seeds the shadcn/new-york semantic palette (e.g. `background`, `popover-foreground`, `ring-offset-background`) and keeps common dotted aliases working (e.g. `card.background`, `popover.background`, `input.border`) via canonicalization + a regression test ensuring alias presence: `crates/fret-ui/src/theme.rs`. Deterministic invalidation exists as a monotonic `Theme.revision` incremented when config application changes resolved tables (`extra_colors`/`extra_metrics`): `crates/fret-ui/src/theme.rs`. Gap: the full registry model described in the ADR (interned token IDs, layered `extends`, and a separate `SyntaxTheme` subsystem) is not implemented as a first-class `ThemeRegistry`/`ResolvedTheme` type yet; additionally, there is no CI guard preventing typed snapshot reads, and some shadcn/ui-kit code still reads `theme.snapshot().colors.*` for legacy palette keys (e.g. `ecosystem/fret-ui-shadcn/src/select.rs`, `ecosystem/fret-ui-shadcn/src/slider.rs`, `ecosystem/fret-ui-kit/src/recipes/resizable.rs`). |
| [`0103-text-decorations-and-markdown-theme-tokens.md`](0103-text-decorations-and-markdown-theme-tokens.md) | Proposed | Partially aligned | Markdown theme token namespace + fallbacks are implemented in `ecosystem/fret-markdown/src/lib.rs` (`MarkdownTheme::resolve` prefers `fret.markdown.*` then `markdown.*` then semantic fallbacks; `resolve_code_block_ui` handles `markdown.code_block.max_height`). Markdown maps `strong`/`emphasis` via `RichText` runs (weight/slant overrides) using existing `TextSlant::{Normal,Italic,Oblique}` in `crates/fret-core/src/text.rs` (see `render_rich_text_inline` in `ecosystem/fret-markdown/src/lib.rs`). Strikethrough is currently approximated in the fallback path by overlaying a 1px line in an absolute-positioned container (`render_inline_text_token` in `ecosystem/fret-markdown/src/lib.rs`). Gap: the proposed renderer-level decoration contract is not implemented (no `TextDecoration` surface on `TextStyle`/`TextRun`, no decoration emission in the renderer), and link underline is not modeled as a decoration (distinct from the text-area preedit underline). |
| [`0104-layout-driven-anchored-overlays.md`](0104-layout-driven-anchored-overlays.md) | Accepted | Aligned (with known gaps) | `Anchored` primitive exists: `crates/fret-ui/src/element.rs` (`AnchoredProps`), layout-driven placement + render-transform mapping: `crates/fret-ui/src/declarative/host_widget/layout.rs` (`ElementInstance::Anchored`) + `crates/fret-ui/src/declarative/host_widget.rs` (`render_transform`); tests: `crates/fret-ui/src/declarative/tests/anchored.rs`. Gap: ecosystem overlays still mostly compute placement from last-frame bounds (`ecosystem/fret-ui-kit/src/overlay.rs`, `ecosystem/fret-ui-kit/src/primitives/popper.rs`) instead of using intrinsic sizing under `Anchored`. |
| [`0135-node-graph-editor-and-typed-connections.md`](0135-node-graph-editor-and-typed-connections.md) | Proposed | Aligned (with known gaps) | Core crate placement + feature policy matches ADR: `ecosystem/fret-node` exists with `default = ["fret-ui","kit"]` and a headless-safe substrate (`ecosystem/fret-node/Cargo.toml`, `ecosystem/fret-node/src/lib.rs`). Model/rules/ui boundary is implemented as separate modules (`ecosystem/fret-node/src/{core,ops,types,schema,rules,profile,ui}/`). Typed connections exist as a serializable `TypeDesc` enum with `Opaque`/`Union`/`Option`/`Object`/`Var` etc. (`ecosystem/fret-node/src/types/mod.rs`), and edge kinds + port kinds/capacity are explicit (`EdgeKind::{Data,Exec}`, `PortKind::{Data,Exec}`, `PortCapacity::{Single,Multi}` in `ecosystem/fret-node/src/core/model.rs`). Rules-driven connection planning exists as `ConnectPlan` with deterministic op lists (`plan_connect`, `plan_connect_typed`, `plan_reconnect_edge`, `plan_split_edge_by_inserting_node` + tests in `ecosystem/fret-node/src/rules/{mod.rs,tests.rs}`), and profile-driven apply/concretize/validate is modeled by `GraphProfile` + `apply_transaction_with_profile` (`ecosystem/fret-node/src/profile/{mod.rs,pipeline.rs}`; kit `DataflowProfile` in `ecosystem/fret-node/src/kit/profiles/dataflow.rs`). UI canvas provides editor-grade interaction policy (pan/zoom, selection, reconnect, split-edge, context menus/searcher) with semantic-zoom screen-space overlays via a canvas `render_transform` and zoom-compensated sizes (`ecosystem/fret-node/src/ui/canvas/widget.rs`), plus an "internals" output store (derived window-space node/port geometry + transform) for overlays/tooling (`ecosystem/fret-node/src/ui/internals.rs`, `NodeGraphCanvas::with_internals_store` + `update_internals_store`). Deterministic clipboard/paste remapping exists (`GraphFragment` + UUIDv5 `IdRemapper` in `ecosystem/fret-node/src/ops/fragment.rs`) and local undo/redo exists (`GraphHistory` in `ecosystem/fret-node/src/ops/history.rs`, wired by the canvas). Demo coverage exists (`apps/fret-examples/src/node_graph_demo.rs`). Gaps: the ADR’s explicit per-kind migration hook surface and `UnknownNode` wrapper type are not implemented (the model preserves payloads but there is no registry-driven migrator contract yet), and cycle policy remains profile-driven without a built-in data-cycle checker. |
| [`0106-plot-overlays-and-annotations.md`](0106-plot-overlays-and-annotations.md) | Accepted | Aligned | Caller-owned overlays are stored on `PlotState` as `PlotState::overlays: PlotOverlays` in `ecosystem/fret-plot/src/retained/state.rs`, with concrete overlay types matching the ADR (e.g. `InfLineX`, `InfLineY`, `PlotImage`, `TagX`, `TagY`, `PlotText`). The plot canvas renders overlays in plot coordinates by mapping through `PlotTransform` and emitting portable primitives (e.g. `SceneOp::ImageRegion` for `PlotImage`, and quad/text for tags/text) inside the retained canvas renderer: `ecosystem/fret-plot/src/retained/canvas/mod.rs`. Annotation theming is token-driven via `fret.plot.annotation.*` / `plot.annotation.*` fallbacks: `ecosystem/fret-plot/src/retained/canvas/mod.rs`. |
| [`0107-dev-hotpatch-subsecond-and-hot-reload-safety.md`](0107-dev-hotpatch-subsecond-and-hot-reload-safety.md) | Accepted | Aligned | Dev-only hotpatch integration is implemented in the runner glue layer and is feature-gated (`hotpatch-subsecond`) in `crates/fret-launch/Cargo.toml` (pulls `subsecond`/`tungstenite` only when enabled). A function-pointer driver entrypoint exists as `FnDriver` with dev-only hot reload hooks (`hot_reload_global`, `hot_reload_window`) in `crates/fret-launch/src/runner/common.rs`. Desktop runner wiring supports safe hot reload boundaries by canceling in-flight drag, calling driver hot-reload hooks, and resetting per-window cached state: `crates/fret-launch/src/runner/desktop/mod.rs` (`hot_reload_all_windows`). Trigger mechanisms exist for both a file marker (`.fret/hotpatch.touch` default; env-configurable) and Subsecond/devserver notifications, with regression tests for the file trigger: `crates/fret-launch/src/runner/desktop/hotpatch.rs`. Dev loop tooling is kept outside library crates via `apps/fretboard` (see ADR 0108). |
| [`0108-ecosystem-bootstrap-ui-assets-and-dev-tools.md`](0108-ecosystem-bootstrap-ui-assets-and-dev-tools.md) | Accepted | Aligned | Ecosystem golden-path startup crate exists as `ecosystem/fret-bootstrap` and composes `fret-launch` without changing kernel contracts (e.g. `BootstrapBuilder` wraps `fret_launch::WinitAppBuilder`, applies `.fret/settings.json`, registers icon packs, and can enable hotpatch env wiring behind a feature): `ecosystem/fret-bootstrap/src/lib.rs` + `ecosystem/fret-bootstrap/Cargo.toml`. The UI render assets convenience layer exists as `ecosystem/fret-ui-assets` and re-exports `ecosystem/fret-asset-cache` under a clearer name: `ecosystem/fret-ui-assets/src/lib.rs`. Dev tooling exists as a standalone CLI app (`apps/fretboard`) that runs demos and supports hotpatch trigger workflows and starter templates (`fretboard init todo`), keeping toolchain concerns out of framework crates: `apps/fretboard/src/main.rs` and docs in `docs/examples/todo-app-golden-path.md`. |
| [`0152-readonly-text-selection-and-clipboard.md`](0152-readonly-text-selection-and-clipboard.md) | Proposed | Aligned | `SelectableText` exists as a baseline read-only surface (`SelectableTextProps/SelectableTextState`): `crates/fret-ui/src/element.rs`. It participates in focus and supports drag selection (incl. double/triple click word/line selection), clipboard copy via `Effect::ClipboardSetText`, and a11y selection updates (`Event::SetTextSelection`): `crates/fret-ui/src/declarative/host_widget/event/selectable_text.rs`, `crates/fret-ui/src/declarative/host_widget/semantics.rs`, `crates/fret-ui-app/src/accessibility_actions.rs`. Command routing uses the existing `text.*` namespace for focused text surfaces (`ElementHostWidget::command` invokes `apply_selectable_text_command`): `crates/fret-ui/src/declarative/host_widget.rs`, `crates/fret-ui/src/text_surface.rs`. Selection highlight painting uses renderer-backed `selection_rects` and hit-testing: `crates/fret-ui/src/declarative/host_widget/paint.rs`, `crates/fret-render/src/text.rs`. A minimal default keymap binds `text.copy`/`text.cut`/`text.paste`/`text.select_all` cross-platform: `crates/fret-app/src/app.rs`. Markdown renders selection-capable blocks via `SelectableTextProps`: `ecosystem/fret-markdown/src/lib.rs`. Interaction behavior is covered by tests (drag autoscroll, double/triple click selection, active selection bookkeeping): `crates/fret-ui/src/declarative/tests/interactions.rs`. |
| [`0109-rich-text-runs-and-text-quality-v2.md`](0109-rich-text-runs-and-text-quality-v2.md) | Proposed | Partially aligned | Rich runs exist as `RichText { text, runs }` + `TextRun { len, color, weight, slant }` in `crates/fret-core/src/text.rs`, with UTF-8 boundary + full coverage validation in the renderer shaper (`layout_text` resolves `ResolvedRun` only when runs exactly cover `text`): `crates/fret-render/src/text.rs`. The UI surface supports rich runs via `StyledTextProps` / `SelectableTextProps` (`rich: RichText`) and routes through `TextService::prepare_rich` / `measure_rich`: `crates/fret-ui/src/element.rs`, `crates/fret-ui/src/declarative/host_widget/{paint,measure}.rs`. Syntax-highlighted code blocks and Markdown produce `RichText` runs (enabling soft wrap across segments and avoiding per-span `Text` elements): `ecosystem/fret-code-view/src/code_block.rs`, `ecosystem/fret-markdown/src/lib.rs`. Text quality work has started: shaping uses `Shaping::Advanced` with 4 subpixel X variants and applies cosmic-text's snapped `(x,y)` from `CacheKey::new` when placing glyph quads: `crates/fret-render/src/text.rs`. Gaps vs ADR: shaped-layout caching currently fingerprints run *colors* (`runs_fingerprint` includes RGBA) and bakes per-run paint into cached blobs (`GlyphQuad.color`), so theme recolors force reshaping/re-caching; there is no run-aware paint/decorations/backgrounds applied at draw time (see also ADR 0103 gaps), and text quality knobs are incomplete (atlas sampler is `Linear`, hinting is disabled, `SubpixelMask` is collapsed to alpha, and there is no gamma/contrast correction shader surface yet): `crates/fret-render/src/text.rs`, `crates/fret-render/src/renderer/shaders.rs`. |
| [`0110-rich-content-selection-and-clipboard.md`](0110-rich-content-selection-and-clipboard.md) | Proposed | Partially aligned | Baseline text selection UX exists via `SelectableText` (drag select, click-count word/line selection, pointer capture + edge autoscroll) and copies a selected UTF-8 slice to the clipboard (`Effect::ClipboardSetText`), with selection geometry derived from the renderer text system (`hit_test_point`, `selection_rects`) and a11y updates (`Event::SetTextSelection`): `crates/fret-ui/src/declarative/host_widget/event/selectable_text.rs`, `crates/fret-ui/src/declarative/host_widget/paint.rs`, `crates/fret-render/src/text.rs`, `crates/fret-ui/src/declarative/host_widget/semantics.rs`. Code blocks are selectable via `SelectableTextProps` over a single `RichText` (highlight spans) in `ecosystem/fret-code-view/src/code_block.rs`. Gaps vs ADR: there is no Markdown-first, surface-owned selection state, no source-indexed selection anchored to the Markdown source string, and no segment model for cross-block selection/painting/clipboard extraction; selection is element-local (window only tracks `ActiveTextSelection { root, element }` for bookkeeping): `crates/fret-ui/src/elements/runtime.rs`. Markdown links are typically rendered as `Pressable` + `Text` (non-selectable) when interactive, so there is no selection/link-arbitration behavior and selection cannot span embedded components: `ecosystem/fret-markdown/src/lib.rs`. Clipboard semantics are limited to plain clipboard text (native uses `arboard`); there is no primary selection support or rich clipboard formats yet: `crates/fret-platform-native/src/clipboard.rs`, `crates/fret-runtime/src/effect.rs`. |
| [`0111-user-facing-crate-surfaces-and-golden-path.md`](0111-user-facing-crate-surfaces-and-golden-path.md) | Accepted | Aligned | The user-facing layering is realized as described: kernel facade crate `crates/fret` provides portable re-exports with opt-in feature bundles (no ecosystem defaults pulled in by default): `crates/fret/Cargo.toml`, `crates/fret/src/lib.rs`. Ecosystem conveniences are split as recommended: `ecosystem/fret-bootstrap` (startup defaults, settings/icons/dev toggles) and `ecosystem/fret-ui-assets` (UI render asset caches/facade), with dev workflows living in tooling (`apps/fretboard`): `ecosystem/fret-bootstrap/src/lib.rs`, `ecosystem/fret-ui-assets/src/lib.rs`, `apps/fretboard/src/main.rs`. The docs explicitly lock the small “what to remember” crate set in `README.md` and `docs/README.md`, and present the starter template workflow (`fretboard init todo`) + golden-path walkthrough: `docs/examples/todo-app-golden-path.md`. |
| [`0112-golden-path-ui-app-driver-and-pipelines.md`](0112-golden-path-ui-app-driver-and-pipelines.md) | Accepted | Aligned | A hotpatch-friendly golden-path driver exists as an ecosystem wrapper over `fret-launch::FnDriver`: `UiAppDriver` in `ecosystem/fret-bootstrap/src/ui_app_driver.rs` (uses `fn` pointers for `init_window` and `view`, centralizes `UiTree` wiring, event/command routing, model/global change propagation, and dev hot reload hooks). It integrates the three-pipeline model explicitly (event -> `UiTree::dispatch_event`, command -> `UiTree::dispatch_command` then app hooks, effect -> `Effect` backflow via runner flush points) and supports multi-window creation hooks (`window_create_spec`, `window_created`) and viewport/docking forwarding. The driver is called through `FnDriver` hooks and can be hotpatched via Subsecond when enabled (`subsecond::HotFn::current(...)` call sites): `ecosystem/fret-bootstrap/src/ui_app_driver.rs`, with the base `FnDriver` and `hot_reload_*` hooks in `crates/fret-launch/src/runner/common.rs` and safe reload boundary in `crates/fret-launch/src/runner/desktop/mod.rs`. `crates/fret-ui-app` provides a backend-agnostic ergonomic alias surface binding `UiTree` to `fret_app::App`: `crates/fret-ui-app/src/lib.rs`. |
| [`0113-ecosystem-integration-contracts.md`](0113-ecosystem-integration-contracts.md) | Accepted | Aligned | The repository structure and existing ecosystem crates follow the guidance: headless-vs-UI tiering is present in practice (e.g. headless table engine under `ecosystem/fret-ui-kit/src/headless/table/` with UI recipes elsewhere; plotting/markdown as ecosystem crates), commands remain string-based (`CommandId`) and are routed through the explicit pipeline glue (golden-path `UiAppDriver` dispatches commands and pushes effects), and styling is token-driven via theme keys (e.g. plot annotation tokens): `ecosystem/fret-ui-kit/src/headless/table/`, `ecosystem/fret-plot/src/retained/canvas/mod.rs`, `ecosystem/fret-bootstrap/src/ui_app_driver.rs`. This ADR is non-binding guidance; alignment here is primarily about layering and exemplars existing in-tree. |
| [`0114-ui-assets-facade-and-golden-path-wiring.md`](0114-ui-assets-facade-and-golden-path-wiring.md) | Accepted | Aligned | The `UiAssets` facade exists in `ecosystem/fret-ui-assets/src/ui_assets.rs` with the documented surface (`UiAssetsBudgets`, `UiAssets::configure`, `UiAssets::handle_event`, stats helpers). Golden-path wiring is implemented in `ecosystem/fret-bootstrap`: `BootstrapBuilder::with_ui_assets_budgets` initializes caches and budgets (`ecosystem/fret-bootstrap/src/lib.rs`), and `UiAppDriver` drives `UiAssets::handle_event` from the event pipeline by default behind the `ui-assets` feature (`ecosystem/fret-bootstrap/src/ui_app_driver.rs`, `drive_ui_assets` toggle). A runnable assets demo entrypoint exists (`apps/fret-demo/src/bin/assets_demo.rs`) as referenced by the ADR (calls into `apps/fret-examples` logic). |
| [`0115-available-space-and-non-reentrant-measurement.md`](0115-available-space-and-non-reentrant-measurement.md) | Proposed | Aligned (with known gaps) | `AvailableSpace` + `LayoutConstraints`: `crates/fret-ui/src/layout_constraints.rs`; non-reentrant `measure_in` with recursion guard: `crates/fret-ui/src/tree/layout.rs` (`measure_stack`); Flex/Grid measure callbacks use `measure_in` (no layout re-entry): `crates/fret-ui/src/declarative/host_widget.rs`, `crates/fret-ui/src/declarative/host_widget/layout/flex.rs`, `grid.rs`; `Fill` only resolves to a definite size when the axis is definite during intrinsic measurement: `crates/fret-ui/src/declarative/host_widget/measure.rs` (`clamp_to_constraints_in_measure`); conformance tests: `crates/fret-ui/src/declarative/tests/layout.rs` (`fill_only_resolves_under_definite_available_space_in_measurement`) + `crates/fret-ui/src/tree/tests/measure_in.rs` (`measure_in_reentrancy_panics_in_debug_builds`). `MaxContent` is exercised by barrier code paths (e.g. Scroll/VirtualList) without `1e9` probes: `crates/fret-ui/src/declarative/host_widget/layout/scrolling.rs`, `crates/fret-ui/src/virtual_list.rs`, with v2 traversal in `crates/fret-ui/src/layout_engine/flow.rs`. Gaps: percent sizing is not exposed as a declarative `Length` yet (only `Fill` maps to `percent(1.0)`); release-mode re-entrancy diagnostics are rate-limited (per-frame window) via `MeasureReentrancyDiagnostics`: `crates/fret-ui/src/tree/mod.rs`, emitted from `crates/fret-ui/src/tree/layout.rs`. |
| [`0116-window-scoped-layout-engine-and-viewport-roots.md`](0116-window-scoped-layout-engine-and-viewport-roots.md) | Proposed | Aligned (with known gaps) | Window-owned engine + viewport roots exist behind `fret-ui/layout-engine-v2`: engine core `crates/fret-ui/src/layout_engine.rs` + flow builder `crates/fret-ui/src/layout_engine/flow.rs`; viewport root registration + independent solves + per-root request/build staging: `crates/fret-ui/src/tree/layout.rs` (layer roots + viewport-root flush loop), with conformance tests in `crates/fret-ui/src/declarative/tests/layout.rs` (viewport roots do not couple fill; request/build keeps node identity stable when compute is skipped) and docking integration tests in `ecosystem/fret-docking/src/dock/tests.rs` (notably `bounds_for_element_is_window_scoped_across_multi_window_docking`, `overlay_placement_must_use_window_local_anchor_bounds`). Translation-only and clean subtrees skip compute while still remaining alive in the engine graph (`mark_seen_if_present` + Final-only `end_frame`). Gaps vs ADR: request/build is still orchestrated per-root inside `UiTree::layout_all` (not a single window-wide prepaint stage), and some legacy/manual layout paths still bypass the engine (intentional barriers or non-element/custom roots). |
| [`0117-trigger-composition-and-no-slot-aschild.md`](0117-trigger-composition-and-no-slot-aschild.md) | Proposed | Aligned | No general Slot/`asChild` mechanism is implemented; triggers are modeled as typed elements (typically `Pressable`) with children-based visuals across shadcn surfaces (e.g. `ecosystem/fret-ui-shadcn/src/popover.rs`, `tooltip.rs`, `select.rs`). A11y/relations are stamped via narrow helper functions (e.g. `ecosystem/fret-ui-kit/src/primitives/trigger_a11y.rs` + `apply_*_trigger_a11y` call sites). Layout engine wrapper skipping is explicit and restricted (not prop merging): `crates/fret-ui/src/layout_engine/flow.rs` (`passthrough_wrapper_child`). |
| [`0118-renderer-architecture-v3-render-plan-and-postprocessing-substrate.md`](0118-renderer-architecture-v3-render-plan-and-postprocessing-substrate.md) | Accepted | Aligned (with known gaps) | `RenderPlan` compile + execute exists (`crates/fret-render/src/renderer/render_plan.rs`, `crates/fret-render/src/renderer/render_scene/render.rs`) with pooled intermediates and explicit per-frame targets (`crates/fret-render/src/renderer/intermediate_pool.rs`, `crates/fret-render/src/renderer/frame_targets.rs`). Gaps: execution is still a linear sequence (no DAG scheduler), and there is no pass cache-key reuse yet. |
| [`0119-effect-layers-and-backdrop-filters-scene-semantics-v1.md`](0119-effect-layers-and-backdrop-filters-scene-semantics-v1.md) | Accepted | Aligned (with known gaps) | Public scene ops exist (`SceneOp::PushEffect/PopEffect` in `crates/fret-core/src/scene.rs`) and are compiled/executed via the renderer v3 substrate (`crates/fret-render/src/renderer/render_scene/encode/ops.rs`, `crates/fret-render/src/renderer/render_plan_effects.rs`) with GPU conformance tests (`crates/fret-render/tests/effect_*_conformance.rs`) and a runnable demo (`apps/fret-examples/src/effects_demo.rs`). Declarative authoring wrapper exists in `fret-ui` (`EffectLayerProps` in `crates/fret-ui/src/element.rs`, paint emission in `crates/fret-ui/src/declarative/host_widget/paint.rs`). Gaps: `EffectStep::Dither` remains a no-op (debug-only postprocess exists separately); higher-level effect recipes live in demos/docs rather than a stabilized UI kit catalog. |
| [`0120-renderer-intermediate-budgets-and-effect-degradation-v1.md`](0120-renderer-intermediate-budgets-and-effect-degradation-v1.md) | Accepted | Aligned (with known gaps) | Per-window intermediate budgets are configurable (`crates/fret-render/src/renderer/config.rs`) and enforced during effect compilation/execution (`crates/fret-render/src/renderer/render_plan_effects.rs`, `crates/fret-render/src/renderer/render_scene/render.rs`) with deterministic quality tier selection (`EffectQuality`) and target reuse/eviction (`crates/fret-render/src/renderer/intermediate_pool.rs`). Observability is available via perf snapshots (`crates/fret-render/src/renderer/types.rs`). Gaps: cost modeling is still coarse (primarily byte-based), and there is no public “degradation report” surfaced to apps yet. |
| [`0121-streaming-images-and-video-surfaces.md`](0121-streaming-images-and-video-surfaces.md) | Proposed | Not audited |  |
| [`0122-offscreen-rendering-frame-capture-and-readback.md`](0122-offscreen-rendering-frame-capture-and-readback.md) | Proposed | Not audited |  |
| [`0123-streaming-upload-budgets-and-backpressure-v1.md`](0123-streaming-upload-budgets-and-backpressure-v1.md) | Proposed | Partially aligned | The runners apply deterministic latest-wins coalescing per `(ImageId, stream_generation)` (ADR 0126) and enforce soft per-window budgets: updates are queued across frames (delay strategy), drained under `streaming_upload_budget_bytes_per_frame`, and bounded by `streaming_staging_budget_bytes` (drop oldest pending updates first) in `crates/fret-launch/src/runner/streaming_upload.rs`. Desktop/web runners integrate this queue at drain points: `crates/fret-launch/src/runner/desktop/mod.rs`, `crates/fret-launch/src/runner/web.rs`. Budget accounting uses conservative CPU->GPU upload estimates including row-alignment repacking (still budgeted as RGBA for YUV updates even when the experimental NV12 GPU path is enabled). Observability: the runner can update `fret_core::StreamingUploadPerfSnapshot` as an app global when enabled (`WinitRunnerConfig.streaming_perf_snapshot_enabled`) and/or log debug counters gated by `FRET_STREAMING_DEBUG`; the snapshot reports both `upload_bytes_budgeted` (estimate used for budget decisions) and `upload_bytes_applied` (actual CPU->GPU uploads performed). Note: to avoid pre-render drains overwriting the snapshot to zeros, the runner only refreshes the snapshot when the current drain turn observes streaming activity (non-zero counters). Gaps vs ADR: there is no per-stream fairness cap and no explicit per-image drop counters exposed in a stable renderer perf snapshot surface. |
| [`0124-renderer-capabilities-and-optional-zero-copy-imports.md`](0124-renderer-capabilities-and-optional-zero-copy-imports.md) | Proposed | Partially aligned | A stable per-session capability snapshot exists as `fret_render::RendererCapabilities` (`crates/fret-render/src/capabilities.rs`) and is computed from the selected `WgpuContext` (adapter info + key format support). Desktop/web runners publish it as an app global at GPU initialization time (`crates/fret-launch/src/runner/desktop/app_handler.rs`, `crates/fret-launch/src/runner/web.rs`). The streaming NV12 GPU conversion fast path is capability-gated (`streaming_images.nv12_gpu_convert`) and can be enabled via config (`WinitRunnerConfig.streaming_nv12_gpu_convert_enabled`) or a debug env override (`FRET_STREAMING_GPU_YUV`). Gaps vs ADR: there is no public capability surface for external texture imports yet, no negotiated “register external image” contract, and most fields from the ADR sketch (sample counts/readback constraints/binding arrays) are not reported yet. |
| [`0125-renderer-extensibility-materials-effects-and-sandboxing-v1.md`](0125-renderer-extensibility-materials-effects-and-sandboxing-v1.md) | Accepted | Aligned (with known gaps) | Tier A is implemented via `RenderTargetId` + coordinated submission (`docs/adr/0007-viewport-surfaces.md`, `docs/adr/0038-engine-render-hook-and-submission-coordinator.md`). Tier B is currently implemented as a small, framework-owned `EffectStep` surface (`crates/fret-core/src/scene.rs`) compiled by the renderer (`crates/fret-render/src/renderer/render_plan.rs`). Gaps: no public `MaterialId`/`EffectId` registries yet; untrusted shader DSL/node graph is not implemented. |
| [`0126-streaming-image-update-effects-and-metadata-v1.md`](0126-streaming-image-update-effects-and-metadata-v1.md) | Proposed | Partially aligned | Streaming update effects are implemented with coalescing + budgets: `Effect::{ImageUpdateRgba8,ImageUpdateNv12,ImageUpdateI420}` exist in `crates/fret-runtime/src/effect.rs`, the runner drains them through a cross-frame latest-wins queue keyed by `(ImageId, stream_generation)` with per-window upload/staging budgets (ADR 0123) in `crates/fret-launch/src/runner/streaming_upload.rs`, and RGBA8 updates are applied via `queue.write_texture` dirty-rect writes with runner-owned `wgpu::Texture` storage (`crates/fret-launch/src/runner/desktop/mod.rs`, `crates/fret-launch/src/runner/web.rs`). YUV updates are supported via a CPU fallback conversion to RGBA8 at the runner apply stage (shared conversion helpers in `crates/fret-launch/src/runner/yuv.rs`). There is also an experimental NV12 GPU conversion path behind `FRET_STREAMING_GPU_YUV=1` that uploads NV12 planes and runs a tiny conversion pass into RGBA8 sRGB storage (`crates/fret-launch/src/runner/{yuv_gpu.rs,yuv_nv12_convert.wgsl}`; wired in `crates/fret-launch/src/runner/{desktop/mod.rs,web.rs}`). Metadata types exist and are plumbed through effects (`fret_core::ImageColorInfo`, `fret_core::AlphaMode` in `crates/fret-core/src/image.rs`): RGBA8 uses `ImageColorInfo.encoding` to select sRGB vs linear formats, and `AlphaMode` is stored in the image registry and used by the viewport/image shader to avoid double-premultiplying premultiplied sources (`crates/fret-render/src/images.rs`, `crates/fret-render/src/renderer/shaders.rs`). Optional update acknowledgements are capability-gated by config (`WinitRunnerConfig.streaming_update_ack_enabled`) via `Event::{ImageUpdateApplied,ImageUpdateDropped}` (`crates/fret-core/src/input.rs`). Note: `fret_core::StreamingUploadPerfSnapshot` is only refreshed when the current drain observes streaming activity, so app-side reads in render callbacks do not get “zeroed” by pre-render drains (`crates/fret-launch/src/runner/{desktop/mod.rs,web.rs}`); it reports both `upload_bytes_budgeted` (estimate) and `upload_bytes_applied` (actual) and tracks CPU-side YUV preparation time via `yuv_convert_us`. Gaps vs ADR: there are no zero-copy YUV imports, GPU conversion is env-gated (not capability surfaced), and the renderer does not yet use the rest of `ImageColorInfo` (primaries/transfer/chroma siting) for conversion. |
| [`0127-frame-capture-options-and-determinism-v1.md`](0127-frame-capture-options-and-determinism-v1.md) | Proposed | Not audited |  |
| [`1128-delinea-headless-chart-engine.md`](1128-delinea-headless-chart-engine.md) | Accepted | Aligned | Headless chart engine lives as a dedicated ecosystem crate `ecosystem/delinea` (crate name `delinea`) and is renderer-agnostic (depends on `fret-core` only; no `wgpu`/`winit`/`fret-render` deps): `ecosystem/delinea/Cargo.toml`. Stable IDs and revisions exist as explicit newtypes (`SeriesId`, `AxisId`, `DatasetId`, `FieldId`, `Revision`): `ecosystem/delinea/src/ids.rs`. The engine exposes dataset/encode concepts via `DatasetSpec.fields` and `SeriesEncode` mapping channels to fields: `ecosystem/delinea/src/spec/mod.rs` (and validated model layer under `ecosystem/delinea/src/engine/model/`). Incremental/progressive work is modeled by a fixed `WorkBudget` with point/label/mark budgets: `ecosystem/delinea/src/scheduler/budget.rs`. Output is a stable `MarkTree` backed by an arena with a monotonic `revision` bump (`MarkTree::clear`): `ecosystem/delinea/src/marks/mod.rs`. UI bridge exists as `ecosystem/fret-chart` (depends on `delinea` + `fret-ui` retained bridge) and translates marks into portable scene primitives (`SceneOp::{Path,Quad,Text}` with `PathCommand` generation) inside a retained canvas: `ecosystem/fret-chart/src/retained/canvas.rs`. |
| [`1129-delinea-transform-pipeline-and-datazoom-semantics.md`](1129-delinea-transform-pipeline-and-datazoom-semantics.md) | Proposed | Partially aligned | Transform pipeline pieces exist and are revision-driven, but not yet a general ECharts-class graph. DataZoom is modeled as a first-class spec/model node (`DataZoomXSpec { id, axis, filter_mode }` + `FilterMode::{Filter,None,WeakFilter,Empty}`; extended semantics tracked by ADR 1150): `ecosystem/delinea/src/spec/mod.rs`, validated as X-only and **one per axis** (`data_zoom_x_by_axis` rejects duplicates): `ecosystem/delinea/src/engine/model/mod.rs`. Runtime state is axis-scoped (`ChartState.data_zoom_x: BTreeMap<AxisId, DataZoomXState>`) and mutated through explicit actions (`SetDataWindowX*`, `SetDataWindowXFilterMode`) with a monotonic `state.revision` bump: `ecosystem/delinea/src/engine/mod.rs`, `ecosystem/delinea/src/action/mod.rs`. The DataZoom transform is an explicit node (`DataZoomXNode`) that computes an X policy; when a mode is filtering (`Filter` / `WeakFilter`), monotonic-X heuristics can represent the filtered view as a `RowSelection::Range`, while non-monotonic inputs fall back to applying the X filter predicate per-point (marks/axisPointer sampling), with an indices-backed selection cache available as an optimization carrier for large non-monotonic views (`DataViewStage`) and applied to the live view via `FilterProcessorStage` when ready: `ecosystem/delinea/src/transform/data_zoom_x.rs`, `ecosystem/delinea/src/engine/window_policy.rs`, `ecosystem/delinea/src/transform_graph/data_view.rs`, `ecosystem/delinea/src/engine/stages/filter_processor.rs`, `ecosystem/delinea/src/engine/mod.rs`. View rebuild is revision-based and consumes the transform to compute per-series view selection/policy (`ViewState::{sync_inputs,rebuild}`): `ecosystem/delinea/src/view/mod.rs`, with regression tests covering filter-mode semantics (including non-monotonic X axisPointer sampling): `ecosystem/delinea/src/engine/tests.rs`. Gaps vs ADR: there is no generalized transform graph with per-node cached outputs/derived columns yet. Order-sensitive multi-dim filtering is only implemented as a v1 subset: `FilterProcessorStage` executes an explicit per-grid step loop (`XYWeakFilter` -> `XIndices` -> `YIndices`) to support “X-before-Y” indices materialization in the same frame when sparse selection carriers are used. |
| [`1130-delinea-axis-scales-and-coordinate-mapping.md`](1130-delinea-axis-scales-and-coordinate-mapping.md) | Proposed | Not audited |  |
| [`1131-delinea-marks-identity-and-renderer-contract.md`](1131-delinea-marks-identity-and-renderer-contract.md) | Proposed | Not audited |  |
| [`1132-delinea-large-data-and-progressive-rendering.md`](1132-delinea-large-data-and-progressive-rendering.md) | Proposed | Not audited |  |
| [`1133-delinea-interaction-and-hit-testing-contract.md`](1133-delinea-interaction-and-hit-testing-contract.md) | Proposed | Aligned (with known gaps) | Core interaction state/actions live in the headless model (`ecosystem/delinea/src/engine/mod.rs`, `ecosystem/delinea/src/engine/hit_test/mod.rs`) and the UI adapter maps input to actions + renders outputs (retained: `ecosystem/fret-chart/src/retained/canvas.rs`; declarative: `ecosystem/fret-chart/src/declarative/panel.rs` + overlays `ecosystem/fret-chart/src/declarative/{legend_overlay.rs,tooltip_overlay.rs}`). Tooltip formatter hooks are available in both adapters (retained `ChartCanvas::set_tooltip_formatter`; declarative `ChartCanvasPanelProps.tooltip_formatter`). Gaps: declarative keyboard/gesture parity is still evolving. |
| [`1134-delinea-multi-axis-and-layout-contract.md`](1134-delinea-multi-axis-and-layout-contract.md) | Proposed | Not audited |  |
| [`1135-delinea-axis-interaction-locks-and-shortcuts.md`](1135-delinea-axis-interaction-locks-and-shortcuts.md) | Accepted | Not audited |  |
| [`1136-delinea-datazoom-y-and-2d-semantics.md`](1136-delinea-datazoom-y-and-2d-semantics.md) | Accepted | Aligned (with known gaps) | Y view windows are first-class axis-scoped state (`ChartState.data_window_y: BTreeMap<AxisId, DataWindowY>`) and are mutated through explicit actions (`SetDataWindowYFromZoom`, `SetDataWindowY`, plus paired 2D writes via `SetViewWindow2DFromZoom`) with lock/span gating in the engine: `ecosystem/delinea/src/action/mod.rs`, `ecosystem/delinea/src/engine/mod.rs`, tests: `ecosystem/delinea/src/engine/tests.rs`. Multi-axis routing for wheel/drag on axis bands and plot-region 2D box zoom is implemented in the UI adapter (`ecosystem/fret-chart/src/retained/canvas.rs`) and validated via the multi-axis conformance harness (`apps/fret-examples/src/chart_multi_axis_demo.rs`). Per the ADR amendments, `DataZoomYSpec` gained an opt-in `filter_mode` to explore ECharts-style 2D semantics; the current v1 subset can materialize sparse indices selections and/or Empty masks only for non-stacked cartesian series under size caps (see ADR 1150 and `ecosystem/delinea/src/transform/data_zoom_y.rs`, `ecosystem/delinea/src/engine/stages/filter_processor.rs`, `ecosystem/delinea/src/view/mod.rs`, `ecosystem/delinea/src/transform_graph/data_view.rs`). Gaps: durable persisted Y window defaults (rangeMode/persistence), full ECharts order-sensitive multi-dim filtering semantics, and broader Empty/weakFilter parity across series kinds and coordinate systems. |
| [`1137-delinea-row-selection-and-filtering-contract.md`](1137-delinea-row-selection-and-filtering-contract.md) | Accepted | Aligned (with known gaps) | `RowSelection` is implemented as a first-class participation contract with stable raw-index identity (`RowSelection::{All,Range,Indices}` + `get_raw_index`/`view_len`) in `ecosystem/delinea/src/transform/selection.rs`. X selection derives from DataZoomX and prefers a contiguous `RowRange` when monotonic, otherwise falls back to filter predicates with optional indices views as an optimization carrier, materialized in `DataViewStage` and applied to the live view in `FilterProcessorStage` when ready (`ecosystem/delinea/src/transform/data_zoom_x.rs`, `ecosystem/delinea/src/transform_graph/data_view.rs`, `ecosystem/delinea/src/engine/stages/filter_processor.rs`). Optional Y filtering can materialize sparse `RowSelection::Indices` for non-stacked series under a size cap and apply it to the live view in `FilterProcessorStage` (`ecosystem/delinea/src/transform/data_zoom_y.rs`, `ecosystem/delinea/src/engine/stages/filter_processor.rs`, `ecosystem/delinea/src/view/mod.rs`). Consumers iterate indices-backed selections via `get_raw_index`; segmented Empty-mode line-family mark emission consumes indices-backed selections via `RowSelection::get_raw_index` (`ecosystem/delinea/src/engine/stages/marks.rs`, `ecosystem/delinea/src/engine/lod/minmax_per_pixel.rs`). Coverage: `ecosystem/delinea/src/engine/tests.rs` (y-filtered indices selection and Empty-mode segment emission). Gaps: generalized multi-dim transform ordering and full ECharts-class `weakFilter/empty` parity remain tracked by ADR 1129/1150. |
| [`1138-delinea-datazoom-component-composition-and-span-policy.md`](1138-delinea-datazoom-component-composition-and-span-policy.md) | Accepted | Not audited |  |
| [`1139-delinea-time-axis-ticks-and-labels.md`](1139-delinea-time-axis-ticks-and-labels.md) | Proposed | Not audited |  |
| [`0153-renderer-effect-clip-masks-and-soft-clipping-v1.md`](0153-renderer-effect-clip-masks-and-soft-clipping-v1.md) | Accepted | Aligned | The renderer v3 plan includes a tiered clip mask substrate: `PlanTarget::{Mask0,Mask1,Mask2}` + `RenderPlanPass::ClipMask`: `crates/fret-render/src/renderer/render_plan.rs` (including budget/tier selection and conformance tests like `compile_for_scene_backdrop_color_adjust_emits_mask_target_when_budget_allows` and `compile_for_scene_backdrop_blur_caps_clip_mask_tier_when_forced_to_quarter`). Mask targets are budgeted intermediates with explicit lifetimes: `crates/fret-render/src/renderer/frame_targets.rs`, and the clip mask pass is executed by the scene renderer: `crates/fret-render/src/renderer/render_scene/render.rs`. |
| [`0154-window-styles-and-utility-windows.md`](0154-window-styles-and-utility-windows.md) | Proposed | Not implemented | ADR only; no portable window-style/utility-window surface wired yet. |
| [`0136-undo-redo-infrastructure-boundary.md`](0136-undo-redo-infrastructure-boundary.md) | Proposed | Aligned (with known gaps) | App-owned history substrate exists in `ecosystem/fret-undo` (`UndoHistory`, `UndoService`, `UndoRecord`, `ValueTx`, `CoalesceKey`) and is wired in demos: `apps/fret-examples/src/gizmo3d_demo.rs` records coalesced gizmo edits and routes `edit.undo`/`edit.redo` through `UiTree::dispatch_command` first (ADR 0020), then falls back to the active document history. Gaps: there is no shared editor-level document registry/service in `fret-app` yet (demo owns window/document mapping), and history persistence is not implemented. |
| [`0137-canvas-widgets-and-interactive-surfaces.md`](0137-canvas-widgets-and-interactive-surfaces.md) | Proposed | Partially aligned | `ecosystem/fret-canvas` exists with policy-light substrate helpers (`view::PanZoom2D`, `scale`, `drag`, `text::TextCache`) and is wired into `ecosystem/fret-node/src/ui/canvas/widget.rs` (pan/zoom mapping + `render_transform`) and `ecosystem/fret-chart/src/retained/canvas.rs` (text caching via `TextCacheGroup`). |
| [`0138-tooltip-scroll-dismissal.md`](0138-tooltip-scroll-dismissal.md) | Accepted | Aligned | Scroll dismissal: `UiTree::set_layer_scroll_dismiss_elements` (`crates/fret-ui/src/tree/layers.rs`) + dispatch in `crates/fret-ui/src/tree/dispatch.rs`; wiring in `ecosystem/fret-ui-kit/src/window_overlays/render.rs` and `ecosystem/fret-ui-shadcn/src/tooltip.rs` (plus tests). |
| [`0139-viewport-gizmos-engine-pass-and-ui-overlay-boundary.md`](0139-viewport-gizmos-engine-pass-and-ui-overlay-boundary.md) | Proposed | Aligned (with known gaps) | Engine-pass gizmo rendering is demonstrated without introducing new `SceneOp` primitives: `ecosystem/fret-gizmo` provides update/draw + ray/project math (`ecosystem/fret-gizmo/src/{gizmo.rs,math.rs}`) and `apps/fret-examples/src/gizmo3d_demo.rs` renders gizmo draw lists with depth-tested geometry alongside the scene. A minimal render-pass overlay extension point exists in `crates/fret-render/src/viewport_overlay.rs`. A runner/app boundary hook service exists in `crates/fret-launch/src/runner/common.rs` (`ViewportOverlay3dHooksService`) and is exercised by the demo to record gizmo overlays into the viewport pass. Input flows via the explicit-units `ViewportInputEvent` (ADR 0147; legacy in ADR 0025) and drag phases map to begin/update/commit/cancel (ADR 0024/0136 direction). Alignment audit vs reference gizmos: `docs/audits/gizmo-imguizmo-transform-gizmo-alignment.md`. Gaps: the demo still owns its wgpu pipelines, there is no standardized 2D overlay composition for tool UI (labels, snapping HUD, etc.) yet, and per-viewport overlay registration is still app-owned (no shared editor service). |
| [`1140-delinea-dataset-storage-and-indices.md`](1140-delinea-dataset-storage-and-indices.md) | Accepted | Not audited |  |
| [`1141-delinea-missing-values-and-segment-policy.md`](1141-delinea-missing-values-and-segment-policy.md) | Proposed | Partially aligned | Line-family marks preserve gaps by segmenting at missing values (and `filterMode='empty'` out-of-window masking): `ecosystem/delinea/src/engine/lod/minmax_per_pixel.rs`, `ecosystem/delinea/src/engine/stages/marks.rs`, tests `line_missing_values_break_into_segments` and `band_missing_upper_breaks_and_preserves_pairing` in `ecosystem/delinea/src/engine/tests.rs`. Gaps: `connect_nulls` flag is not implemented yet and tooltip/axisPointer missing-value semantics are not fully audited. |
| [`0142-fret-chart-theme-tokens-and-style-resolution.md`](0142-fret-chart-theme-tokens-and-style-resolution.md) | Accepted | Not audited |  |
| [`1143-delinea-derived-dimensions-and-stack-transform.md`](1143-delinea-derived-dimensions-and-stack-transform.md) | Accepted | Not audited |  |
| [`1144-delinea-brush-selection-and-output-contract.md`](1144-delinea-brush-selection-and-output-contract.md) | Accepted | Not audited |  |
| [`1145-delinea-brush-selection-to-row-selection-fast-path.md`](1145-delinea-brush-selection-to-row-selection-fast-path.md) | Accepted | Not audited |  |
| [`1146-delinea-link-events-for-brush-selection.md`](1146-delinea-link-events-for-brush-selection.md) | Accepted | Not audited |  |
| [`1147-delinea-visualmap-and-data-driven-styling.md`](1147-delinea-visualmap-and-data-driven-styling.md) | Proposed | Aligned (with known gaps) | Implemented v1 continuous bucket mapping for scatter marks: `ecosystem/delinea/src/spec/mod.rs`, `ecosystem/delinea/src/engine/stages/marks.rs`, adapter paint resolution in `ecosystem/fret-chart/src/retained/canvas.rs`. Gaps: controller UI, piecewise mode, additional channels, and LOD-aware mapping. |
| [`1148-delinea-tooltip-formatting-contract.md`](1148-delinea-tooltip-formatting-contract.md) | Proposed | Aligned (with known gaps) | Engine emits structured tooltip payloads (`ecosystem/delinea/src/tooltip.rs`) and wires axis-trigger sampling + snapping through `AxisPointerOutput` (`ecosystem/delinea/src/engine/mod.rs`). The adapter formats and renders via `TooltipFormatter` (`ecosystem/fret-chart/src/retained/tooltip.rs`) and supports app overrides in retained via `ChartCanvas::set_tooltip_formatter` (`ecosystem/fret-chart/src/retained/canvas.rs`). Tooltip overlays exist in declarative (`ecosystem/fret-chart/src/declarative/tooltip_overlay.rs`) and shared column splitting lives in `ecosystem/fret-chart/src/tooltip_layout.rs`. Gaps: rich text/HTML formatter parity and a declarative formatter hook surface. |
| [`1149-delinea-appenddata-and-incremental-caches.md`](1149-delinea-appenddata-and-incremental-caches.md) | Proposed | Not audited | Not audited yet. Implementation anchors (per ADR): append-only APIs `ecosystem/delinea/src/data/mod.rs` (`append_row_f64`, `append_columns_f64`), incremental scan caches `ecosystem/delinea/src/transform_graph/data_view.rs`, `ecosystem/delinea/src/engine/stages/{ordinal_index,nearest_x_index}.rs`. |
| [`1150-delinea-filtermode-weakfilter-empty.md`](1150-delinea-filtermode-weakfilter-empty.md) | Proposed | Partially aligned | `FilterMode` includes `WeakFilter` / `Empty` (`ecosystem/delinea/src/spec/mod.rs`) and the X transform policy is wired (`ecosystem/delinea/src/transform/data_zoom_x.rs`, `ecosystem/delinea/src/engine/window_policy.rs`). `WeakFilter` is v1-equivalent to `Filter` (ADR 1150); `Empty` preserves the base row selection (tests: `ecosystem/delinea/src/engine/tests.rs`). Remaining gap: full ECharts-class multi-dimensional `weakFilter` behavior and explicit segment-break semantics for line-family marks under interleaved missing samples. |
| [`1151-element-identity-debug-paths-and-frame-staged-element-state.md`](1151-element-identity-debug-paths-and-frame-staged-element-state.md) | Accepted | Aligned (with known gaps) | Identity debug registry exists behind `fret-ui/diagnostics` (`WindowElementState::{record_debug_root,record_debug_child,debug_path_for_element}`) and keying diagnostics include callsite location + best-effort element path (`ElementContext::for_each_unkeyed`). Duplicate-key detection exists for keyed lists and virtual lists (debug-only): `crates/fret-ui/src/elements/cx.rs`. Element state is staged across frames with a `next_state`/`rendered_state` + lag ring derived from `gc_lag_frames`: `crates/fret-ui/src/elements/runtime.rs`, used by `crates/fret-ui/src/elements/{cx.rs,access.rs}`. Gap: cached-subtree execution still needs to inherit dependency/state access sets on cache hits (tracked by GPUI parity workstream). |
| [`1152-view-cache-subtree-reuse-and-state-retention.md`](1152-view-cache-subtree-reuse-and-state-retention.md) | Accepted | Aligned (with known gaps) | View-cache subtree reuse is implemented for declarative `ViewCache`: cache hits are decided from `UiTree` invalidation state (`UiTree::should_reuse_view_cache_node` in `crates/fret-ui/src/tree/mod.rs`), the render closure is skipped and state keys are touched (`ElementContext::view_cache` + `WindowElementState::*view_cache*` in `crates/fret-ui/src/elements/{cx.rs,runtime.rs}`), mount reuses the existing retained subtree while re-collecting scroll bindings (`crates/fret-ui/src/declarative/mount.rs`), and cache-hit frames inherit recorded model/global observations so model changes continue to invalidate without re-running render (`crates/fret-ui/src/elements/{runtime.rs,cx.rs,access.rs}` + subtree inherit in `crates/fret-ui/src/declarative/mount.rs`). Covered by `crates/fret-ui/src/declarative/tests/view_cache.rs` (`view_cache_inherits_model_observations_on_cache_hit_layout`). Gap: this remains an experimental mechanism-only path; additional per-frame registries beyond scroll-handle bindings may need explicit reuse support as they are introduced. |
| [`1152-cache-roots-and-cached-subtree-semantics-v1.md`](1152-cache-roots-and-cached-subtree-semantics-v1.md) | Proposed | Aligned (with known gaps) | Cache roots are represented by `node.view_cache.enabled` and are the only nodes eligible for paint-cache replay in view-cache mode (`crates/fret-ui/src/tree/mod.rs`, `crates/fret-ui/src/tree/paint.rs`; tests `crates/fret-ui/src/tree/tests/view_cache.rs`). Observations are uplifted to the nearest cache root when view-cache is active (`UiTree::collapse_*_observations_to_view_cache_roots_if_needed` in `crates/fret-ui/src/tree/mod.rs`; test `view_cache_uplifts_observations_to_nearest_root_and_invalidates_ancestor_roots`). Nested cache roots propagate invalidation to ancestor roots (`UiTree::mark_invalidation` in `crates/fret-ui/src/tree/mod.rs`; test `view_cache_nested_boundaries_invalidate_ancestor_cache_roots`). Gaps: the ADR’s proposed explicit cache-root parent/ownership maps and per-root hit/miss counters are not standardized; the author-facing `CachedSubtree` helper API remains ecosystem work (tracked in GPUI parity workstream). |
| [`1157-input-dispatch-phases-prevent-default-and-action-availability-v2.md`](1157-input-dispatch-phases-prevent-default-and-action-availability-v2.md) | Proposed | Partially aligned | V2 phase primitives exist: `InputDispatchPhase::{Preview,Capture,Bubble}` is modeled in `crates/fret-runtime/src/input.rs` and surfaced to widgets via `EventCx.input_ctx` (`crates/fret-ui/src/widget.rs`). Preview (observer) dispatch is routed through the observer-only contract (`Widget::event_observer` + `ObserverCx`) so outside-press/click-through policies cannot mutate focus/capture/propagation/default actions (evidence: `crates/fret-ui/src/tree/dispatch.rs`, `crates/fret-ui/src/widget.rs`, tests in `crates/fret-ui/src/tree/tests/outside_press.rs`). Capture-phase dispatch is wired as a first-class pass (root → target) for key down/up and pointer events (down/up, wheel/pinch/cancel, and move when buttons are pressed or capture is active), with bubble running target → root and capture able to suppress bubble via `stop_propagation` (evidence: `crates/fret-ui/src/tree/dispatch.rs`, tests in `crates/fret-ui/src/tree/tests/dispatch_phase.rs`). `prevent_default` is available to widgets (`EventCx::prevent_default` in `crates/fret-ui/src/widget.rs`) and the runtime executes a minimal default action set including `DefaultAction::FocusOnPointerDown` (`crates/fret-ui/src/tree/dispatch.rs`, tests in `crates/fret-ui/src/tree/tests/prevent_default.rs`). Dispatch-path action availability is implemented as a best-effort pure query: `Widget::command_availability` + `UiTree::{command_availability,is_command_available,is_action_available}` (`crates/fret-ui/src/widget.rs`, `crates/fret-ui/src/tree/commands.rs`). Declarative authoring can participate in availability without new core widget types via the policy hook `ElementContext::command_on_command_availability_for` (evidence: `crates/fret-ui/src/elements/cx.rs`, `crates/fret-ui/src/declarative/host_widget.rs`). A data-only runner bridge exists via `WindowCommandActionAvailabilityService` (`crates/fret-runtime/src/window_command_action_availability.rs`) and consumption through `WindowCommandGatingSnapshot` (`crates/fret-runtime/src/window_command_gating.rs`). Availability snapshots are intended to be published by app/runner layers on demand (menus, command palette): bootstrap publishes a command-scoped snapshot (`ecosystem/fret-bootstrap/src/ui_app_driver.rs`) and `UiTree::publish_window_command_action_availability_snapshot` exists as a retained-runtime helper. Gaps vs ADR: availability coverage is still incremental (only a subset of widgets report availability today; callers should treat `NotHandled` / missing snapshot entries as unknown). |
| [`1154-scroll-handle-revision-invalidation-contract.md`](1154-scroll-handle-revision-invalidation-contract.md) | Accepted | N/A (superseded) | Superseded by ADR 1156. The implementation now distinguishes offset-only vs layout-affecting handle changes. |
| [`1156-scroll-offset-children-transform-and-scrollhandle-invalidation-v2.md`](1156-scroll-offset-children-transform-and-scrollhandle-invalidation-v2.md) | Accepted | Aligned | Scroll offset is modeled as a children-only transform (`Widget::children_render_transform`) so offset changes avoid layout solves, while still mapping hit-testing/event coordinates/semantics bounds/visual bounds consistently: `crates/fret-ui/src/{widget.rs,tree/{hit_test.rs,dispatch.rs,mod.rs}}`, Scroll wiring in `crates/fret-ui/src/declarative/host_widget/layout/scrolling.rs`. Scroll-handle revision invalidation is fine-grained: offset-only -> `Invalidation::HitTestOnly`, viewport/content/unknown -> `Invalidation::Layout`: `crates/fret-ui/src/declarative/frame.rs`, `crates/fret-ui/src/tree/layout.rs`. Coverage: `crates/fret-ui/src/declarative/tests/layout.rs`, `crates/fret-ui/src/tree/tests/scroll_into_view.rs`. |
| [`1155-cache-root-tracing-contract-v1.md`](1155-cache-root-tracing-contract-v1.md) | Proposed | Aligned (with known gaps) | Cache-root tracing spans are present for layout and paint (`ui.cache_root.layout` / `ui.cache_root.paint`) and include core fields (`node`, `pass` for layout, `view_cache_active`, `contained_layout`, `invalidated`): `crates/fret-ui/src/tree/{layout.rs,paint.rs}`. Mount/reuse tracing exists for cache-root nodes (`ui.cache_root.mount` / `ui.cache_root.reuse`, includes `element`, `node`, `cache_hit`, `contained_layout`): `crates/fret-ui/src/declarative/mount.rs`. The per-frame and per-phase spans exist in the bootstrap UI app driver (`fret.frame`, `fret.ui.layout`, `fret.ui.paint`) and Tracy export is supported behind `fret-bootstrap/tracy` (`ecosystem/fret-bootstrap/src/{lib.rs,ui_app_driver.rs}`; docs: `docs/debugging-playbook.md`, `docs/tracy.md`). Gap: invalidation reason taxonomy is not standardized yet (model/global/scroll/child-root/etc.). |
| [`0155-custom-gizmo-plugins-and-handle-contract.md`](0155-custom-gizmo-plugins-and-handle-contract.md) | Proposed | Aligned (with known gaps) | The plugin contract + manager exist in `ecosystem/fret-gizmo/src/plugin.rs` (`GizmoPlugin`, `GizmoPluginManager`), and handle IDs are namespaced by plugin via `HandleId::from_parts` in `ecosystem/fret-gizmo/src/gizmo/types.rs`. The transform gizmo is integrated through the plugin surface (`TransformGizmoPlugin` in `ecosystem/fret-gizmo/src/transform_plugin.rs`), and real custom plugins exist (`RingScaleGizmoPlugin` in `ecosystem/fret-gizmo/src/ring_scale_plugin.rs`, `LightRadiusGizmoPlugin` in `ecosystem/fret-gizmo/src/light_radius_plugin.rs`). Non-transform edits are supported via `GizmoUpdate.custom_edits` in `ecosystem/fret-gizmo/src/gizmo/runtime.rs`. Gaps: complex gizmos still lack Godot-style pick acceleration (collision geometry registry + BVH), and there is no standardized editor service for routing `custom_edits` into undo/redo across documents/viewports yet. |
| [`0167-gizmo-host-property-source-readonly-v1.md`](0167-gizmo-host-property-source-readonly-v1.md) | Proposed | Aligned (with known gaps) | Read-only host property access is implemented via `GizmoPropertySource` in `ecosystem/fret-gizmo/src/plugin.rs` and threaded through `GizmoPluginContext` (`properties`). The plugin manager forwards the property source to plugins from `GizmoPluginManager::{update,draw}` (`ecosystem/fret-gizmo/src/plugin.rs`). A shipped custom plugin consumes host values (`LightRadiusGizmoPlugin` in `ecosystem/fret-gizmo/src/light_radius_plugin.rs`) and the end-to-end demo provides a host implementation (`apps/fret-examples/src/gizmo3d_demo.rs`). Gaps: only scalar reads exist (no typed vectors/transforms), and writes remain host-owned via `GizmoCustomEdit` (no direct property write API). |
| [`0168-viewport-tooling-host-helpers-and-arbitration-v1.md`](0168-viewport-tooling-host-helpers-and-arbitration-v1.md) | Proposed | Partially aligned | A shared, policy-light input mapping + tool protocol exists as `ecosystem/fret-viewport-tooling/src/lib.rs` (`ViewportToolInput`, `ViewportRect`, `ViewportTool{Id,Priority,Result}`) and is reused by `ecosystem/fret-gizmo` (re-export + math types). Host routing helpers exist as `ecosystem/fret-ui-kit/src/viewport_tooling.rs`, including a trait-object router (`ViewportToolArbitrator`) and a callback router (`ViewportToolRouterState`, `ViewportToolEntry`, `route_viewport_tools`) with captured-button latching to avoid inconsistent move `buttons` state and explicit cancellation entry points (`ViewportToolArbitrator::cancel_active_and_clear_hot`, `cancel_active_viewport_tools`). Active routing is now pointer-local (latches `PointerId` on capture and ignores other pointers while active) with cancel forwarding (`ViewportInputKind::PointerCancel`) covered by unit tests in the same module. Engine-pass overlay helpers exist in `crates/fret-launch/src/runner/viewport_overlay_immediate_3d.rs` (`install_viewport_overlay_3d_immediate`, `upload_viewport_overlay_3d_immediate`), and the runner hook service supports multiple hooks (`ViewportOverlay3dHooksService::push`). Gaps: hot/hover routing is still single-pointer, concurrent active interactions are not supported, and end-to-end multi-pointer behavior is not audited across all backends. |
| [`0169-ecosystem-crate-taxonomy-glue-and-ui-kit-split-v1.md`](0169-ecosystem-crate-taxonomy-glue-and-ui-kit-split-v1.md) | Proposed | Partially aligned | A first pass split exists: headless modules live in `ecosystem/fret-ui-headless` (re-exported from `ecosystem/fret-ui-kit/src/headless/mod.rs` to preserve `fret-ui-kit::headless::*`), and UI-runtime wiring primitives are starting to move into `ecosystem/fret-ui-primitives` (with `fret-ui-kit` keeping compatibility shims). Gaps: `dismissible_layer` remains a compatibility shim in `fret-ui-kit::headless`, and the broader taxonomy (more glue layers + a finalized split between primitives vs kits) is not finalized. |
| [`0156-declarative-canvas-element-and-painter.md`](0156-declarative-canvas-element-and-painter.md) | Proposed | Aligned (with known gaps) | Declarative `Canvas` leaf is implemented in `crates/fret-ui/src/element.rs` and wired through the declarative host (`crates/fret-ui/src/declarative/{mount.rs,frame.rs,host_widget/paint.rs}`). A hosted `CanvasPainter` exists in `crates/fret-ui/src/canvas.rs` and caches + prepares + releases `TextBlobId`/`PathId`/`SvgId` via `UiServices::{text(),path(),svg()}`; teardown is deterministic via `Widget::cleanup_resources` (`crates/fret-ui/src/declarative/host_widget.rs`) and covered by `crates/fret-ui/src/declarative/tests/canvas.rs`. SVG lifecycle safety depends on ref-counted `SvgService::unregister_svg` semantics (implemented in `crates/fret-render/src/renderer/services.rs`). Gaps: additional Canvas API surface (e.g. safe clip guards / transforms helpers) is still minimal. |
| [`0159-canvas-pan-zoom-input-mapping-v1.md`](0159-canvas-pan-zoom-input-mapping-v1.md) | Proposed | Aligned (with known gaps) | A recipe surface exists behind the `fret-ui-kit` `recipes` feature: `ecosystem/fret-ui-kit/src/recipes/canvas_pan_zoom.rs` (`pan_zoom_canvas_surface_panel`, `PanZoomInputPreset::DefaultSafe`). It reuses the policy-free substrate wrapper `ecosystem/fret-ui-kit/src/declarative/canvas_surface.rs` and stores view state in `Model<PanZoom2D>` via `use_controllable_model`. Pinch gestures are supported via `OnPinchGesture` (`crates/fret-ui/src/action.rs`) and declarative pointer region routing (`crates/fret-ui/src/declarative/host_widget/event/pointer_region.rs`). Gaps: Space-to-pan chords are not implemented. |
| [`0157-text-system-v2-parley-attributed-spans-and-quality-baseline.md`](0157-text-system-v2-parley-attributed-spans-and-quality-baseline.md) | Proposed | Aligned (with known gaps) | Core v2 mechanisms exist: attributed spans in `crates/fret-core/src/text.rs` (`TextInput`, `TextSpan`), Parley shaper + wrapper in `crates/fret-render/src/text_v2/{parley_shaper,wrapper}.rs`, and renderer integration in `crates/fret-render/src/text.rs` (`prepare_input`, `wrap_with_constraints`). Gaps: UI authoring surfaces are not fully migrated (some elements still build uniform text), and the full quality baseline (gamma/contrast tuning + conformance strings) remains tracked in `docs/workstreams/text-system-v2-parley.md`. |
| [`0158-text-layout-cache-boundary-and-glyph-residency.md`](0158-text-layout-cache-boundary-and-glyph-residency.md) | Proposed | Aligned (with known gaps) | Layout vs residency split is implemented in the renderer text system: layout caches produce glyph instances without UVs, and glyph residency is ensured frame-driven via `crates/fret-render/src/text.rs` (`TextSystem::prepare_for_scene`, atlas ensure/evict paths). Gaps: observability/debug snapshot surface is minimal; continue tracking in the text workstream. |
| [`0160-unified-glyph-key-and-subpixel-rendering-policy.md`](0160-unified-glyph-key-and-subpixel-rendering-policy.md) | Proposed | Aligned (with known gaps) | Renderer-owned `GlyphKey` + stable `FontFaceKey` registry exist in `crates/fret-render/src/text.rs` (glyph key types + `font_face_key_by_fontique` mapping). Subpixel binning and atlas split (`Mask/Color/Subpixel`) are implemented. Gaps: shader-side gamma/contrast correction and any variable-font variation key/flags expansion remain follow-ups. |
| [`0161-text-input-v2-attributed-spans-core-contract.md`](0161-text-input-v2-attributed-spans-core-contract.md) | Proposed | Aligned | Core types exist in `crates/fret-core/src/text.rs` (`TextInput`, `TextInputRef`, `TextSpan`, `TextShapingStyle`, `TextPaintStyle`), and the renderer/service surface consumes them (`crates/fret-render/src/text.rs`, `crates/fret-render/src/renderer/services.rs`). |
| [`0162-font-stack-bootstrap-and-textfontstackkey-v1.md`](0162-font-stack-bootstrap-and-textfontstackkey-v1.md) | Proposed | Aligned (with known gaps) | Web/WASM bootstrap fonts are bundled via `crates/fret-fonts` (Inter + JetBrains Mono subsets; optional emoji/CJK bundles), and runners seed and propagate font-family config via `apply_font_catalog_update` (`crates/fret-runtime/src/font_bootstrap.rs`, `crates/fret-launch/src/runner/web.rs`, `crates/fret-launch/src/runner/desktop/mod.rs`). Gaps: finalize the persistence story (store family lists/config, not numeric IDs) and standardize cross-runner invalidation/diagnostics per ADR 0162 + `docs/todo-tracker.md`. |
| [`0163-component-ecosystem-authoring-conventions-v1.md`](0163-component-ecosystem-authoring-conventions-v1.md) | Proposed | Not audited | New conventions ADR; start with the companion author guide `docs/component-author-guide.md`. |
| [`0164-headless-dnd-toolbox-and-ui-integration.md`](0164-headless-dnd-toolbox-and-ui-integration.md) | Proposed | Aligned (with known gaps) | Headless policy toolbox exists as `ecosystem/fret-dnd` (multi-pointer activation sensor in `ecosystem/fret-dnd/src/activation.rs`, collision strategies + stable tie-break in `ecosystem/fret-dnd/src/collision.rs`, modifiers in `ecosystem/fret-dnd/src/modifier.rs`, auto-scroll requests in `ecosystem/fret-dnd/src/scroll.rs`). `fret-ui-kit` integration layer exists as a window+scope partitioned registry + per-kind controller (`ecosystem/fret-ui-kit/src/dnd.rs`), and the sortable recipe reuses it (`ecosystem/fret-ui-kit/src/recipes/sortable_dnd.rs`). Docking reuses the deterministic activation contract for cross-window tear-off drags (`ecosystem/fret-docking/src/dock/space.rs`). Gaps: cross-window `DragSession` bridging is docking-specific today (no shared helper crate yet), and more complex droppable shapes (ports/paths) remain out of scope for the core snapshot format. |
| [`0165-pointer-identity-and-multi-pointer-capture.md`](0165-pointer-identity-and-multi-pointer-capture.md) | Proposed | Aligned (with known gaps) | Pointer identity is defined as `PointerId` in `crates/fret-core/src/ids.rs` and is carried by pointer/internal-drag events (`crates/fret-core/src/input.rs`). UI capture is now per-pointer (`crates/fret-ui/src/tree/mod.rs`, `crates/fret-ui/src/tree/dispatch.rs`). Gaps: runners currently provide `PointerId(0)` for most sources (mouse/touch not yet disambiguated), and some ecosystem tests/examples still assume a single pointer by convention. |
| [`0166-multi-pointer-drag-sessions-and-routing-keys.md`](0166-multi-pointer-drag-sessions-and-routing-keys.md) | Proposed | Aligned (with known gaps) | Drag kind is extensible via `DragKindId` and drag sessions are keyed by `PointerId`: `crates/fret-runtime/src/drag.rs`, `crates/fret-runtime/src/ui_host.rs`, `crates/fret-app/src/app.rs`. Internal drag events now include `pointer_id` (`crates/fret-core/src/input.rs`) and routing/capture participates per-pointer in `crates/fret-ui/src/tree/dispatch.rs`. Gap: true multi-pointer internal drag requires runner pointer identity mapping (currently most paths use `PointerId(0)`). |
| [`0170-docking-tab-dnd-contract.md`](0170-docking-tab-dnd-contract.md) | Proposed | Aligned (with known gaps) | Docking delays cross-window drag session creation until activation is satisfied (pending state in `ecosystem/fret-docking/src/dock/space.rs`), suppresses viewport hover/wheel forwarding while pending (test: `ecosystem/fret-docking/src/dock/tests.rs` `pending_dock_drag_suppresses_viewport_hover_and_wheel_forwarding`), and uses headless collision selection for drop hover targets (`dock_drop_target_via_dnd` in `ecosystem/fret-docking/src/dock/space.rs`). Gap: the “cross-window `DragSession` ↔ headless DnD registry” bridge remains docking-specific (no shared helper yet). |
| [`0150-node-graph-canvas-middleware.md`](0150-node-graph-canvas-middleware.md) | Proposed | Aligned (with known gaps) | Middleware surface exists (`ecosystem/fret-node/src/ui/canvas/middleware.rs`) and is wired into `NodeGraphCanvas` for event/command interception (`handle_event`, `handle_command`) and as the pre-commit tx gate (`before_commit`): `ecosystem/fret-node/src/ui/canvas/widget.rs` (`NodeGraphCanvasWith::with_middleware`, `Widget::event`, `Widget::command`, `commit_transaction`). Coverage exists in `ecosystem/fret-node/src/ui/canvas/widget/tests/middleware_conformance.rs`. Gaps: middleware is only non-bypassable for edits originating in the canvas (external callers can still dispatch transactions directly via `NodeGraphStore`), and there is no `after_commit` hook (consistent with ADR open questions). |
| [`0147-viewport-input-forwarding-explicit-units.md`](0147-viewport-input-forwarding-explicit-units.md) | Accepted | Aligned | Explicit-units viewport input types exist in `crates/fret-core/src/input.rs` (`ViewportInputEvent`, `ViewportInputGeometry`) and the effect surface exists in `crates/fret-runtime/src/effect.rs` (`Effect::ViewportInput`). Runners forward the effect to the driver hook (`viewport_input`) in `crates/fret-launch/src/runner/{common.rs,desktop/mod.rs,web.rs}`. Viewport widgets reuse `ViewportInputEvent::from_mapping_window_point*` via the retained helper (`crates/fret-ui/src/retained_bridge.rs`) and the declarative panel wrapper (`ecosystem/fret-ui-kit/src/declarative/viewport_surface.rs`). Tooling glue is provided by `ecosystem/fret-gizmo/src/viewport_input.rs` (`ViewportToolInput`) and is exercised by the end-to-end viewport gizmo demo (`apps/fret-examples/src/gizmo3d_demo.rs`). |
| [`0148-gizmo-picking-primitives-and-precision-policy.md`](0148-gizmo-picking-primitives-and-precision-policy.md) | Proposed | Partially aligned | A dedicated pick-primitive layer exists in `ecosystem/fret-gizmo/src/picking.rs` (2D circle/capsule/quad) and is exported via `ecosystem/fret-gizmo/src/lib.rs`. Core handle selection paths use these primitives (translate/scale/bounds/rotate) in `ecosystem/fret-gizmo/src/gizmo.rs`, reducing reliance on ad-hoc per-handle math and easing future custom gizmo/plugin work. Gaps: picking is still mostly per-mode and screen-space oriented (no shared primitive registry/BVH); and while `fret-gizmo/f64-math` provides opt-in f64 projection/unprojection for picking-critical math, most gizmo update math remains f32 and the broader large-world precision policy is still incomplete. |
| [`0149-effect-recipes-and-tier-selection-v1.md`](0149-effect-recipes-and-tier-selection-v1.md) | Proposed | Partially aligned | Tier B mechanism exists via `EffectLayerProps` (`crates/fret-ui/src/element.rs`) emitting `SceneOp::PushEffect/PopEffect` (ADR 0119) during paint (`crates/fret-ui/src/declarative/mount.rs`). Ecosystem recipes exist for glass + pixelate (`ecosystem/fret-ui-kit/src/recipes/{glass,pixelate}.rs`) with declarative wrappers (`ecosystem/fret-ui-kit/src/declarative/{glass,pixelate}.rs`) and user-facing authoring guidance (`docs/effects-authoring.md`). Tier A embedding is available as a declarative leaf via `ViewportSurfaceProps` (`crates/fret-ui/src/element.rs`) emitting `SceneOp::ViewportSurface` (ADR 0007). For input forwarding, `fret-ui-kit` provides `viewport_surface_panel` (`ecosystem/fret-ui-kit/src/declarative/viewport_surface.rs`); retained widgets can reuse `handle_viewport_surface_input` (`crates/fret-ui/src/retained_bridge.rs`); and docking viewport panels forward viewport input consistently via `ViewportInputEvent::from_mapping_window_point*` (`ecosystem/fret-docking/src/dock/viewport.rs`). |
