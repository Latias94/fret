# TODO

Status: closed

- [x] Add protocol constants and payload shape for `platform.capabilities`.
- [x] Publish launch-time filesystem sidecars from `fret-bootstrap`.
- [x] Include the same source in DevTools `environment.sources.get_ack`.
- [x] Teach campaign manifests to parse `platform_capabilities` predicates.
- [x] Teach campaign admission to skip unsatisfied platform posture requirements with
  `check.environment.json`.
- [x] Add `imui-p3-wayland-real-host` as the first consumer campaign.
- [x] Update the docking Wayland runbook to prefer the host-admitted campaign wrapper.
