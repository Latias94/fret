# Handoff

Status: Closed
Last updated: 2026-05-28

This lane is closed. ChipSet remains a Material3 recipe container that uses existing Fret roving
mechanisms and delegates per-chip primary/trailing focus behavior to the individual chip recipes.

Do not move ChipSet into `fret-ui-kit` from this packet alone. Start a kit-policy follow-on only if
another design-system crate needs the same reusable roving group container.
