# ImUi Table Sortable Demo Proof v1 - Milestones

Status: closed

## M1 - App-Owned Sortable Demo Slice

Exit criteria:

- `imui_shadcn_adapter_demo` shows a sortable inspector table header.
- The demo owns row ordering through local state.
- The source marker test proves the demo uses `TableResponse::header(...)` rather than a hidden
  generic sorting helper.

Result:

- Complete in `M1_APP_OWNED_SORTABLE_DEMO_SLICE_2026-04-29.md`.

## M2 - Closeout

Exit criteria:

- Future diagnostics scripts, richer recipes, multi-sort examples, engines, resize, and persistence
  are explicitly routed to separate follow-ons.

Result:

- Complete in `CLOSEOUT_AUDIT_2026-04-29.md`.
