# M3PH-060 - Material Test Support Extraction

Status: DONE_WITH_CONCERNS
Date: 2026-05-27

## Summary

Extracted reusable Material test support from `radio_alignment.rs` into
`ecosystem/fret-ui-material3/tests/support/`:

- `host.rs`: `TestHost` and `FakeUiServices`
- `theme.rs`: Material theme helpers
- `layout.rs`: layout/search helpers and `with_padding`
- `events.rs`: pointer/key/timer helpers
- `goldens.rs`: scene snapshot, overlay frame, and golden assertion helpers

No component test bodies were intentionally rewritten.

## Validation

Passed:

```powershell
rustfmt --edition 2024 ecosystem/fret-ui-material3/tests/radio_alignment.rs ecosystem/fret-ui-material3/tests/support/mod.rs ecosystem/fret-ui-material3/tests/support/host.rs ecosystem/fret-ui-material3/tests/support/events.rs ecosystem/fret-ui-material3/tests/support/layout.rs ecosystem/fret-ui-material3/tests/support/theme.rs ecosystem/fret-ui-material3/tests/support/goldens.rs
git diff --check -- ecosystem/fret-ui-material3/tests/radio_alignment.rs ecosystem/fret-ui-material3/tests/support
cargo test -p fret-ui-material3 --test radio_alignment --no-run
```

Failed with a pre-existing controls golden mismatch:

```powershell
cargo nextest run -p fret-ui-material3 --test radio_alignment material3_headless_controls_suite_goldens_v1
```

The same nextest command also fails in a clean detached `HEAD` worktree at
`F:\SourceCodes\Rust\fret-worktrees\m3ph060-head-check`, before this extraction, with the same
`goldens/material3-headless/v1/material3-controls.scale1_0.dark.tonal_spot.json` mismatch. Treat the
golden failure as pre-existing Material controls drift, not an extraction regression.

## Next

Run M3PH-070 only after deciding whether to first handle the Material controls golden drift or to use
a narrower preserved-behavior gate for the first component-family split.
