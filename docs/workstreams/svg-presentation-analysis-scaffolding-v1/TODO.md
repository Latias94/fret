# TODO

Status: Closed
Last updated: 2026-04-09

- [x] Confirm that this is a narrow follow-on rather than a reopening of the closed Iconify
  suggestion/report lanes.
- [x] Freeze the public surface as one explicit `icons suggest svg-dir-presentation-overrides`
  helper.
- [x] Reuse generator SVG-dir naming normalization so suggested `icon_name` values match import.
- [x] Emit only per-icon `original-colors` overrides and keep `default_render_mode` unset.
- [x] Add an optional versioned report that records evidence, warnings, and parse failures.
- [x] Update docs/source-policy gates so the helper is taught as advisory scaffolding only.
- [x] Run the lane gates and close the follow-on.
