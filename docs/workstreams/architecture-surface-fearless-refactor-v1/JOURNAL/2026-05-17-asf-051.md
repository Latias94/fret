# ASF-051 — Carousel Direct Headless Recipe Path

Date: 2026-05-17
Status: Complete

## Scope

Migrated one recipe surface to consume the finalized headless owner directly instead of using broad
`fret-ui-kit::headless` compatibility shims.

## Shipped State

- `ecosystem/fret-ui-shadcn/src/carousel.rs` imports
  `fret_ui_headless::{carousel, embla, snap_points}` directly.
- `docs/audits/shadcn-carousel.md` records the headless engine owner path.
- ADR 0154 alignment records carousel as the recipe-surface proof for ASF-051.

## Validation

Passed:

- `cargo fmt --package fret-ui-shadcn`
- `cargo check -p fret-ui-shadcn --locked -j 1`
- `cargo test -p fret-ui-shadcn --locked --test carousel_loop_downgrade_without_embla_engine -j 1`
- `python tools/check_layering.py`
- `python tools/check_workstream_catalog.py`
- `git diff --check`

## Next

Continue with ASF-060. Prefer a concrete menu/select behavior with repeated recipe-local policy and
a small owner-module unit test.
