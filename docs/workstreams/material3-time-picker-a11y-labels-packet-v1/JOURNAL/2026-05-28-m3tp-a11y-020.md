# M3TP A11Y 020 - Implementation

Updated `time_picker.rs` so display selectors use `RadioButton` semantics with selected state and
spoken values. Dial item labels now use the same hour/minute spoken strings while preserving their
value-derived part ids.

Added parent period ids for dial and input modes so automation can target the `Select AM or PM`
group outcome directly.
