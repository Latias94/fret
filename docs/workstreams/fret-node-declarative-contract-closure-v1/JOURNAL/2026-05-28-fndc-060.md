# FNDC-060 Paint-only Orchestration Split - 2026-05-28

## Summary

- Extracted `plan_paint_only_interaction_frame` into `paint_only/frame_plan.rs`.
- Moved panning, marquee-active, node-drag phase, hover, and effective-selection derivation out of
  host-bound `prepare_surface_frame`.
- Added a pure snapshot-state regression test and extended the paint-only source policy scan to
  cover the new plan file.
- Kept the helper in `fret-node` because it depends on node-graph marquee, node-drag, and
  selection semantics.

## Validation

- PASS `cargo nextest run -p fret-node paint_only_interaction_frame_plan declarative_paint_only_runtime node_graph_surface cache paint_only` (110 tests)
- PASS `cargo fmt --check`

## Notes

- Broader cache/scene-plan extraction should be split into a narrower adapter lane if future
  evidence shows a domain-neutral helper can serve more than `fret-node`.
