# FNDC-030 Store Dispatch Commit Path - 2026-05-28

## Summary

- Collapsed `NodeGraphStore::dispatch_transaction` and
  `NodeGraphStore::dispatch_transaction_with_profile` through one private
  `dispatch_transaction_impl` path.
- Kept public APIs and public error types unchanged.
- Added an external-profile dispatch regression test to prove middleware ordering, history
  availability, patch payload, and graph event publication still match the shared commit pipeline.

## Validation

- PASS `cargo nextest run -p fret-node store_dispatch store_middleware store_rejects` (7 tests)
- PASS `cargo nextest run -p fret-node --no-default-features` (133 tests)
- PASS `cargo fmt --check`
- PASS `git diff --check`

## Notes

- Undo/redo profile/non-profile duplication was left as a follow-on candidate because FNDC-030 is
  scoped to dispatch commit-path consolidation.
