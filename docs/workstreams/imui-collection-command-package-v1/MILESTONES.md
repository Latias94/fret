# ImUi Collection Command Package v1 - Milestones

## M0. Baseline frozen

- Re-read the modularization closeout, the priority refresh, and the current collection proof.
- Freeze that the next move is broader app-owned command-package depth, not new helper growth.

Status: complete

## M1. Duplicate-selected slice

- Land `Primary+D` duplicate-selected.
- Route the same command through one explicit button and one context-menu entry.
- Add one proof-local command status line plus focused unit/surface gates.

Status: complete

## M2. Rename trigger parity slice

- Route the existing inline rename command through one explicit button in addition to `F2` and the
  existing context-menu entry.
- Keep the render-path trigger wiring demo-local inside `collection.rs`.
- Reuse the existing inline rename session/status models instead of inventing a shared command
  helper.

Status: complete

## M3. Closeout decision

- Decide against a third command verb in this folder.
- Close the lane on the bounded duplicate-selected plus explicit rename-trigger package.
- Move the default next priority to the second proof surface follow-on.

Status: complete
