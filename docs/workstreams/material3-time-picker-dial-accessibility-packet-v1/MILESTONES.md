# Milestones - Material 3 TimePicker Dial Accessibility Packet v1

## M1 - Packet Opened

Exit criteria:

- The workstream states source truth, scope, non-goals, and layer ownership.
- The first executable task is value-derived dial label ids.

## M2 - Dial Label Surface Shipped

Exit criteria:

- TimePicker hour dial labels expose stable ids derived from the caller base id.
- TimePicker minute dial labels expose stable ids derived from the caller base id.
- Existing parent and modal selector ids remain stable.
- A focused automation gate proves the selectors are live.

## M3 - Matrix and Evidence Updated

Exit criteria:

- The component alignment matrix records the new ids and artifact.
- The picker packet residual risk no longer lists per-dial-label ids as open.
- Exact validation commands are recorded.

