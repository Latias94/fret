# ImUi Table Header Label Policy v1 - Milestones

Status: closed
Last updated: 2026-04-28

## M0 - Tracking

Exit criteria:

- the lane is a narrow follow-on of `imui-label-identity-ergonomics-v1`,
- repo-level indexes point to the new workstream,
- and the scope stays limited to table header visible-label policy.

## M1 - Header Visible Labels

Result:

- table headers hide `##` / `###` suffixes from rendered text,
- existing table layout and test-id behavior remains unchanged,
- and `fret-imui` authoring proof coverage names the table case.

## M2 - Closeout

Result:

- final gates are recorded,
- column identity / sortable table state remains explicitly deferred,
- and `WORKSTREAM.json` now points future table identity work to a follow-on.
