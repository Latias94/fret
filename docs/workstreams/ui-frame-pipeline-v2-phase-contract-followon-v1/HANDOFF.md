# UI Frame Pipeline v2 Phase Contract Follow-On v1 - Handoff

Updated: 2026-05-25

## Current State

This is an active follow-on to the closed Frame Pipeline v2 lane. No code has moved yet.

## Next Step

Select one proof surface outside the original closeout pair and write down:

- which phase owns the state;
- which dirty flag triggers it;
- what cache key allows reuse;
- which focused test or diagnostic gate proves the contract.
