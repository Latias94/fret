---
title: Fret Mechanism Harness v1 Findings 2026-05-12
status: active
date: 2026-05-12
---

# Findings 2026-05-12

## F4: WindowMetricsService environment changes did not reach view-cache environment dependencies

Layer: `crates/fret-ui` declarative runtime / environment mechanism

Status: fixed

What was found:

- Existing environment view-cache tests mostly wrote directly into `ElementRuntime`, while real
  desktop/web runners write platform environment updates into `WindowMetricsService`.
- `render_root` only synchronized viewport bounds, scale factor, safe-area insets, and occlusion
  insets from `WindowMetricsService` into `ElementRuntime`.
- As a result, view-cache subtrees that queried color scheme, reduced motion, text scale, reduced
  transparency, accent color, contrast preference, or forced-colors mode through the real
  `WindowMetricsService` path could incorrectly stay cache-hit after the environment changed.

How it was exposed:

- Added a fixture-driven environment view-cache harness:
  `crates/fret-ui/src/declarative/tests/fixtures/environment_view_cache_invalidation_v1.json`
- Added a thin declarative runner:
  `crates/fret-ui/src/declarative/tests/environment_view_cache_harness.rs`
- The first run failed before the runtime fix:
  - `renders.env_cache` stayed at `1` instead of `2`.
  - `observed.env_unique_values` stayed at `1` instead of `2`.
  - Failures covered reduced motion, color scheme, contrast preference, forced colors, text scale,
    reduced transparency, and accent color.

What was fixed:

- `crates/fret-ui/src/declarative/mount.rs` now commits all known `WindowMetricsService`
  environment values into `ElementRuntime` before declarative rendering:
  - prefers reduced motion,
  - text scale factor,
  - prefers reduced transparency,
  - accent color,
  - color scheme,
  - contrast preference,
  - forced colors mode,
  - plus the existing scale factor, safe-area, and occlusion paths.

Regression coverage:

- Fixture suite:
  `crates/fret-ui/src/declarative/tests/fixtures/environment_view_cache_invalidation_v1.json`
- Fixture runner: `crates/fret-ui/src/declarative/tests/environment_view_cache_harness.rs`
- Existing focused environment tests: `crates/fret-ui/src/declarative/tests/environment_queries.rs`
- Runtime commit point: `crates/fret-ui/src/declarative/mount.rs`
- Real source path: `fret_core::window::WindowMetricsService`

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_environment_view_cache_invalidation_matches_oracles -- --nocapture`
  - Before fix: failed for 7 cases.
  - After fix: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib environment_ -- --nocapture`
  - Result: passed, 12 tests.
- `cargo test --profile dev-fast -p fret-ui --lib view_cache -- --nocapture`
  - Result: passed, 58 tests.
- `cargo fmt --package fret-ui --check`
  - Result: passed.

Next slice recommendation:

- Move to hit-test/routing or overlay/focus next. The environment slice shows the harness is now
  capable of finding real mechanism bugs when it drives the same entry point as runners instead of
  direct internal state.

## F5: Pointer occlusion and captured-pointer routing needed fixture-level observability

Layer: `crates/fret-ui` tree input routing / overlay mechanism

Status: covered, no runtime fix required

What was found:

- Existing focused tests covered pointer occlusion, outside-press observers, pointer-move observers,
  modal barrier wheel scoping, and captured-pointer behavior.
- The coverage was procedural and spread across multiple files, so the workstream coverage map still
  could not answer whether pointer occlusion and captured-pointer routing were covered as a
  reviewable matrix.
- The first fixture run also exposed a harness schema constraint: `domains` is a fixed mechanism
  enum, not a free-form tag list. Specific subdomains such as pointer occlusion and pointer capture
  must be represented by suite/case ids and docs unless the shared schema is intentionally extended.

What was added:

- Fixture suite: `crates/fret-ui/src/tree/tests/fixtures/pointer_occlusion_routing_v1.json`
- Thin tree runner: `crates/fret-ui/src/tree/tests/pointer_occlusion_routing_harness.rs`

The fixture matrix covers:

- `BlockMouseExceptScroll` suppressing underlay move/down dispatch while allowing wheel routing,
- `BlockMouse` suppressing underlay wheel dispatch,
- hit-test-inert modal barriers blocking underlay wheel routing,
- outside-press observer dispatch while underlay pointer-down dispatch is suppressed,
- pointer-move observer dispatch while underlay pointer-move dispatch is suppressed,
- captured underlay pointers suppressing unrelated overlay pointer-move observers,
- captured overlay pointers continuing to receive moves while uncaptured pointers remain occluded.

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_pointer_occlusion_routing_matches_oracles -- --nocapture`
  - First run: failed due to invalid fixture `domains` values.
  - After schema-aligned fixture update: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib pointer_occlusion -- --nocapture`
  - Result: passed, 11 tests.
- `cargo test --profile dev-fast -p fret-ui --lib pointer_move_layers -- --nocapture`
  - Result: passed, 3 tests.
- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_hit_test_routing_matches_oracles -- --nocapture`
  - Result: passed, 1 test.

Next slice recommendation:

- Continue into overlay/focus. The pointer routing slice shows that event-routing facts need
  first-class mechanism metrics; overlay/focus should use the same pattern for focus trap, restore,
  roving focus, active descendant, modal barrier, and nested overlay interactions.

## F6: Focus barrier routing needed a fixture-level matrix before policy-level focus traps

Layer: `crates/fret-ui` tree focus routing / overlay mechanism

Status: covered, no runtime fix required

What was found:

- Existing focused tests already covered hit-test-inert focus barriers, focus traversal under a
  focus barrier, and focus scopes.
- The workstream still lacked a fixture matrix for the tree-level focus substrate that recipe-layer
  focus traps and restore policies depend on.
- The first runner attempt exposed a type-boundary issue: fixture command strings cannot be borrowed
  directly into `CommandId::from`, because these command ids are represented by static command
  values in the tested path.

What was added:

- Fixture suite: `crates/fret-ui/src/tree/tests/fixtures/focus_barrier_routing_v1.json`
- Thin tree runner: `crates/fret-ui/src/tree/tests/focus_barrier_routing_harness.rs`
- The runner maps fixture commands through a closed enum before constructing `CommandId` values.

The fixture matrix covers:

- hit-test-inert overlays keeping active focus barriers,
- focus barrier activation preserving existing overlay focus,
- underlay focus attempts being rejected while a focus barrier is active,
- `focus.next` / `focus.previous` traversal staying inside the focus barrier when the input barrier
  is off,
- modal overlays reporting both modal and focus barriers,
- timer dispatch not clearing focus inside a hit-test-inert focus barrier.

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_focus_barrier_routing_matches_oracles -- --nocapture`
  - First run: failed to compile due to borrowed fixture command strings escaping into `CommandId`.
  - After enum-backed command mapping: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib focus_barrier -- --nocapture`
  - Result: passed, 8 tests.
- `cargo test --profile dev-fast -p fret-ui --lib focus_scope -- --nocapture`
  - Result: passed, 6 tests.

Next slice recommendation:

- Move from tree-level focus barrier to declarative focus behavior: focus scope fixture coverage,
  focus restore policy surfaces, roving focus, active descendant semantics, and UI Gallery
  overlay/focus diagnostics gates.

## F7: Semantics relations existed at runtime but were not first-class harness facts

Layer: `crates/fret-mechanism-harness` observation/oracle and `crates/fret-ui` declarative
semantics

Status: covered, no runtime fix required

What was found:

- Existing focused tests already verified text input `controls_element`, text input
  `active_descendant_element`, and `attach_semantics` relation/state behavior.
- `SemanticsSnapshot` already carried active-descendant, labelled-by, described-by, controls,
  disabled, hidden, focus-barrier, and captured-node facts.
- `ObservedTree::from_semantics_snapshot` dropped those facts, so fixture suites and AI-driven
  sweeps could not express accessibility relationship or state oracles. This was a harness
  observability gap, not a runtime behavior defect.

What was added:

- `ObservedNode` now records active-descendant, labelled-by, described-by, controls, disabled, and
  hidden observations.
- `ObservedTree` now records focus-barrier and captured-node ids from semantics snapshots.
- `MechanismPredicate` now supports selector-addressable `semantics_relation_includes` and
  `semantics_flag_is` predicates.
- Fixture suite: `crates/fret-ui/src/declarative/tests/fixtures/semantics_relations_v1.json`
- Thin declarative runner: `crates/fret-ui/src/declarative/tests/semantics_relations_harness.rs`

The fixture matrix covers:

- text-input combobox `controls_element` resolving to a listbox semantics node,
- text-input combobox `active_descendant_element` resolving to a listbox option semantics node,
- `attach_semantics` preserving active-descendant, labelled-by, described-by, controls, disabled,
  and hidden state without introducing a layout wrapper,
- `SemanticsProps` wrapper exposing labelled-by, described-by, controls, disabled, and hidden
  outcomes.

Run results:

- `cargo test --profile dev-fast -p fret-mechanism-harness --lib semantics_relation_and_flag_oracles_match_observed_nodes -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_semantics_relations_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib text_input_semantics_controls_element_is_exposed -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib text_input_semantics_active_descendant_element_is_exposed -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib declarative_attach_semantics_can_override_state_and_relations -- --nocapture`
  - Result: passed, 1 test.

Next slice recommendation:

- Move to declarative focus behavior rather than more tree-level focus: focus scope fixtures, focus
  restore policy surfaces, roving focus, active-descendant interaction state, and UI Gallery
  overlay/focus diagnostics gates.

## F8: Roving focus behavior needed a case-id-addressable interaction matrix

Layer: `crates/fret-ui` declarative focus interaction mechanism

Status: covered, no runtime fix required

What was found:

- Existing focused tests already covered roving focus arrow movement, disabled item skipping,
  descendant focus, pointer-region wrappers, and typeahead.
- The coverage was procedural and not reviewable as a fixture matrix, so future recipe parity work
  would need to copy Rust test logic instead of adding cases.
- No runtime bug was found when the matrix was run.

What was added:

- Fixture suite: `crates/fret-ui/src/declarative/tests/fixtures/roving_focus_interaction_v1.json`
- Thin declarative runner:
  `crates/fret-ui/src/declarative/tests/roving_focus_interaction_harness.rs`
- The runner records the resulting focused semantics node and a `roving.selection.index` mechanism
  metric so both focus movement and active-selection updates are asserted.

The fixture matrix covers:

- arrow-down skipping a disabled middle item,
- arrow-up wrapping from the first item to the last item,
- arrow-up without wrapping preserving the first item focus and selection,
- pointer-region wrapped items still participating in roving item collection.

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_roving_focus_interaction_matches_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib roving_flex -- --nocapture`
  - Result: passed, 5 tests.

Next slice recommendation:

- Extend declarative focus coverage to focus-scope fixture cases and then connect overlay/focus
  recipe gates for dialog, popover, dropdown menu, select, and combobox focus restore.

## F9: Focus scope containment needed fixture coverage before recipe focus-restore gates

Layer: `crates/fret-ui` declarative focus interaction mechanism

Status: covered, no runtime fix required

What was found:

- Existing focused tests already covered focus-scope traversal trapping, pointer focus arbitration,
  stale parent pointer robustness, and layered scopes.
- There was no fixture matrix for the basic declarative focus-scope contract, so recipe-layer focus
  restore gates would not have a compact mechanism substrate to point at.
- The first matrix run did not reveal a runtime bug. It confirmed that `trap_focus=false` allows
  traversal to leave the scope and does not accidentally behave like a trapped scope.

What was added:

- Fixture suite: `crates/fret-ui/src/declarative/tests/fixtures/focus_scope_interaction_v1.json`
- Thin declarative runner:
  `crates/fret-ui/src/declarative/tests/focus_scope_interaction_harness.rs`
- The runner asserts exact focused semantics nodes for command traversal and mechanism metrics for
  pointer activation plus focus containment.

The fixture matrix covers:

- trapped `focus.next` traversal from the first inside item to the second,
- trapped `focus.next` wrapping from the second inside item back to the first,
- trapped `focus.previous` wrapping from the first inside item to the second,
- non-trapped `focus.next` leaving the scope,
- pointer activation outside the scope running while focus remains inside the trapped scope.

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_focus_scope_interaction_matches_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib focus_scope -- --nocapture`
  - Result: passed, 7 tests.

Next slice recommendation:

- Move to recipe-consumer focus restore gates: shadcn dialog/popover/dropdown/select/combobox
  already have focused tests, but the harness lane needs a smaller parity fixture or diagnostics
  gate that exercises the common open -> dismiss -> restore path.

## F10: Focus restore recipe fixture exposed a barrier-filtered focus oracle bug

Layer: `crates/fret-mechanism-harness` oracle, with recipe-consumer coverage in
`ecosystem/fret-ui-shadcn`

Status: fixed and covered

What was found:

- The initial shadcn recipe fixture for dialog/popover/combobox Escape dismissal failed on the
  dialog case.
- Runtime recipe behavior was already correct: the existing focused dialog test restored focus to
  the trigger.
- The mechanism harness oracle was wrong for this state. `UiPredicateV1::FocusIs` selected the
  target through the normal barrier-filtered path. During a close transition, a pointer/modal
  barrier can still be present while focus containment has already been released and focus is
  restored to the trigger. The trigger existed and was focused, but the harness selector filtered it
  out before comparing focus ids.

What was fixed:

- `ObservedTree` now exposes `select_unfiltered` / `select_best_unfiltered` for oracle cases that
  must reason about runtime facts outside the current input barrier.
- `UiPredicateV1::FocusIs` now resolves its target with the unfiltered selector path.
- Normal selector predicates such as `Exists` continue using the barrier-filtered path.
- Added a focused harness unit:
  `focus_oracle_can_match_restored_focus_outside_pointer_barrier`.

What was added:

- Fixture suite: `ecosystem/fret-ui-shadcn/tests/fixtures/focus_restore_recipe_cases_v1.json`
- Thin recipe runner: `ecosystem/fret-ui-shadcn/tests/focus_restore_mechanism_harness.rs`

The initial fixture matrix covered:

- dialog Escape dismissal closing the open model and restoring trigger focus,
- popover Escape dismissal closing the open model and restoring trigger focus,
- combobox Escape dismissal closing the open model and restoring trigger focus.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - First run: failed on the dialog case because `FocusIs` used barrier-filtered selection.
  - After oracle fix: passed, 1 test.
- `cargo test --profile dev-fast -p fret-mechanism-harness --lib focus_oracle_can_match_restored_focus_outside_pointer_barrier -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test dialog_escape_dismiss_focus_restore dialog_escape_closes_and_restores_focus_to_trigger -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test popover_escape_dismiss_focus_restore popover_escape_closes_and_restores_focus_to_trigger -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test combobox_escape_dismiss_focus_restore combobox_escape_closes_and_restores_focus_to_trigger -- --nocapture`
  - Result: passed, 1 test.

Next slice recommendation:

- Extend recipe-consumer parity from Escape restore to outside-press outcomes: dialog/popover/select
  should restore trigger focus where Radix does, while context-menu-like surfaces may intentionally
  clear or move focus depending on policy.

## F11: Outside-press focus policy is now represented in the recipe fixture matrix

Layer: `ecosystem/fret-ui-shadcn` recipe-consumer parity

Status: covered, no runtime fix required

What was found:

- The first focus-restore recipe matrix covered Escape dismissal only.
- Existing focused tests already proved dialog overlay-click restore and popover click-through
  outside-press behavior.
- The fixture matrix still could not express that outside press is policy-dependent: dialog restores
  trigger focus, while non-modal popover can let the click-through underlay become focused and
  activated.
- No new runtime bug was found.

What was added:

- `dialog_outside_press_restore` fixture case: overlay click closes the dialog and restores focus to
  `dialog-trigger`.
- `popover_outside_press_focuses_underlay` fixture case: click-through outside press closes popover,
  focuses `underlay`, and records `underlay.activated == 1`.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test dialog_overlay_click_dismiss_focus_restore dialog_overlay_click_closes_and_restores_focus_to_trigger -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test popover_outside_click_dismiss_focus_restore popover_outside_click_closes_and_activates_underlay -- --nocapture`
  - Result: passed, 1 test.

Next slice recommendation:

- Add select/dropdown/context-menu outside-press policy cases. This should intentionally distinguish
  restore-to-trigger, focus-underlay, and focus-clear outcomes instead of forcing all overlay
  recipes into one restore rule.

## F12: Select and dropdown-menu Escape restore are now in the recipe fixture matrix

Layer: `ecosystem/fret-ui-shadcn` recipe-consumer parity

Status: covered, no runtime fix required

What was found:

- The recipe focus-restore harness had expanded beyond dialog/popover/combobox, but the workstream
  evidence still did not record select and dropdown-menu Escape restore as first-class coverage.
- Existing focused tests already covered select and dropdown-menu Escape dismissal restoring trigger
  focus.
- No new runtime bug was found.

What was added:

- `select_escape_restore` fixture case: Escape closes the select open model and restores focus to
  `select-trigger`.
- `dropdown_menu_escape_restore` fixture case: Escape closes the dropdown-menu open model and
  restores focus to `menu-trigger`.
- Workstream coverage and gate docs now list the focused select/dropdown gates beside the shared
  fixture runner.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test select_escape_dismiss_focus_restore select_escape_closes_and_restores_focus_to_trigger -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test dropdown_menu_escape_dismiss_focus_restore dropdown_menu_escape_closes_and_restores_focus_to_trigger -- --nocapture`
  - Result: passed, 1 test.

Next slice recommendation:

- Continue with outside-press policy matrices for select, dropdown-menu, and context-menu. The
  fixture schema should distinguish restore-to-trigger, focus-underlay, and focus-clear outcomes so
  policy differences stay explicit instead of being flattened into one overlay rule.

## F13: Recipe outside-press policy differences are now fixture-addressable

Layer: `ecosystem/fret-ui-shadcn` recipe-consumer parity, with policy support in
`ecosystem/fret-ui-kit`

Status: covered; select pointer-open defect confirmed and fixed in F14

What was found:

- The focus-restore fixture could express Escape restore and two outside-press cases, but not the
  broader policy split across combobox, select, dropdown-menu, and context-menu.
- The first select outside-press draft used the trigger pointer-open path and then clicked outside.
  That draft stayed open while existing controlled-open select barrier gates passed. This was
  escalated into F14 and confirmed as a mouse-open guard-cache defect.

What was added:

- `combobox_outside_press_restore`: outside press closes the combobox, activates the click-through
  underlay, and restores focus to `combobox-trigger`.
- `select_outside_press_restore`: controlled-open modal select outside press closes, restores
  `select-trigger` focus, and keeps `underlay.activated == 0`.
- `dropdown_menu_outside_press_clears_focus`: non-modal dropdown outside press closes and leaves
  focus cleared instead of restoring the trigger.
- `context_menu_escape_clears_focus`: context-menu Escape closes and clears focus.
- `context_menu_outside_press_focuses_underlay`: non-modal context-menu outside press closes and
  focuses/activates the underlay.
- The runner now records `focus.none` and per-case `underlay.activated` metrics.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test context_menu_escape_dismiss_focus_clears context_menu_escape_closes_and_clears_focus -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --lib context_menu_click_through_outside_press_closes_and_focuses_underlay -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --lib dropdown_menu_non_modal_outside_press_closes_without_restoring_focus_to_trigger -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --lib select_open_before_first_layout_installs_modal_barrier_and_blocks_underlay -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-kit --lib close_auto_focus_decision_maps_reasons -- --nocapture`
  - Result: passed, 1 test.

Next slice recommendation:

- Move to prevent-default outside-press policy matrices and nested submenu restore. The first
  outside-press matrix now covers the default outcomes.

## F14: Select pointer-open outside press was suppressed by a stale guard decision

Layer: `ecosystem/fret-ui-kit` select primitive policy, exposed through
`ecosystem/fret-ui-shadcn` select recipe harness

Status: fixed and covered

What was found:

- The select outside-press fixture failed when the select was opened through the real trigger
  pointer path, then dismissed by a second pointer id clicking outside.
- The open model stayed `true`: the outside pointer-up was treated as if it were the original
  trigger click-release that should be suppressed.
- Root cause: `SelectMouseOpenGuardState` cached the previous pointer-up decision only by tick id.
  The runtime intentionally tolerates same/adjacent ticks for duplicate routing of the same platform
  pointer-up, but a later outside click can also receive an adjacent tick. Without pointer-id
  scoping, the guard reused the stale `Suppress` decision for a different pointer id.

What was fixed:

- `SelectMouseOpenGuardState` now stores the last pointer-up pointer id.
- Cached pointer-up decisions are reused only when the new event has the same pointer id and the
  same/adjacent tick id.
- The select outside-press recipe fixture now uses the real trigger pointer-open path rather than a
  controlled-open fallback.

Run results:

- `cargo test --profile dev-fast -p fret-ui-kit --lib mouse_open_guard_pointer_up_decision_is_reusable_within_tick -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.

Next slice recommendation:

- Add prevent-default outside-press policy cases. They should assert both default prevention and the
  underlay/focus side effects that still occur for non-modal click-through surfaces.

## F15: Prevent-default outside-press matrix exposed select barrier handler accumulation

Layer: `ecosystem/fret-ui-shadcn` recipe-consumer parity, with confirmed fix in
`ecosystem/fret-ui-kit` select primitive policy

Status: fixed and covered

What was found:

- The first prevent-default matrix covered popover, select, dropdown-menu, and context-menu.
- Non-modal click-through surfaces need to prevent the paired `FocusOutside` request as well as the
  `OutsidePress` request if the intended outcome is "keep the overlay open while the underlay still
  receives focus/activation". This is consistent with the existing focus-outside policy path.
- The select modal prevent-default case failed with `dismiss.calls == 15` instead of `1`.
- Root cause: `select_modal_layer_elements_with_pointer_up_guard_and_dismiss_handler` installed the
  modal barrier dismissal hook with `pressable_add_on_pointer_up` on every rendered open frame. A
  long-lived open select therefore accumulated one handler per frame.

What was fixed:

- The select modal barrier now uses `pressable_on_pointer_up` for its owned pointer-up dismissal
  behavior, replacing the previous accumulating `add` registration.
- The focus-restore recipe fixture now includes prevent-default cases for:
  - non-modal popover keep-open + underlay focus/activation,
  - modal select keep-open + underlay blocking,
  - non-modal dropdown-menu keep-open + underlay focus/activation,
  - modal dropdown-menu keep-open + underlay blocking,
  - non-modal context-menu keep-open + underlay focus/activation.
- The runner records `dismiss.calls` to catch repeated-handler accumulation instead of only checking
  final open state.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --lib select_modal_barrier_dismiss_can_be_prevented_via_dismiss_handler -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --lib popover_outside_press_can_be_intercepted -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --lib dropdown_menu_modal_outside_press_can_be_prevented_via_dismiss_handler -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --lib context_menu_click_through_outside_press_can_be_prevented_and_still_activates_underlay -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-kit --lib mouse_open_guard_pointer_up_decision_is_reusable_within_tick -- --nocapture`
  - Result: passed, 1 test.

Next slice recommendation:

- Move to nested submenu restore and focus-out policy matrices. The prevent-default outside-press
  path is now covered, but focus-outside remains only partially represented in the recipe fixture.

## F16: Focus-outside policy and context-menu submenu restore were coverage gaps, not runtime defects

Layer: `ecosystem/fret-ui-shadcn` recipe-consumer parity, with policy support in
`ecosystem/fret-ui-kit`

Status: covered, no runtime fix required

What was found:

- The recipe focus-restore fixture covered Escape, outside press, and prevent-default outside press,
  but did not explicitly cover pure focus transfers outside a dismissible layer.
- Focus-outside is policy-significant in Fret because it should close by default while preserving
  the external focus target, and `preventDefault` should keep the overlay open without firing the
  handler repeatedly on later frames.
- Dropdown-menu already had a submenu keyboard focus-transfer/ArrowLeft restore focused gate, but
  context-menu only had "ArrowRight renders submenu items" coverage. That left the shared
  Radix-menu submenu restore invariant unverified for context-menu.

What was added:

- `ecosystem/fret-ui-shadcn/tests/fixtures/focus_restore_recipe_cases_v1.json` now includes
  focus-outside default and prevent-default cases for:
  - popover,
  - non-modal dropdown-menu,
  - non-modal context-menu.
- `ecosystem/fret-ui-shadcn/tests/focus_restore_mechanism_harness.rs` now observes pure
  focus-outside flows and records `dismiss.calls` for focus-outside prevent-default cases.
- `ecosystem/fret-ui-shadcn/src/context_menu.rs` now has
  `context_menu_submenu_keyboard_open_transfers_focus_and_arrow_left_restores_focus`.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui-shadcn --lib context_menu_submenu_keyboard_open_transfers_focus_and_arrow_left_restores_focus -- --nocapture`
  - Result: passed, 1 test.
- `python -m json.tool ecosystem/fret-ui-shadcn/tests/fixtures/focus_restore_recipe_cases_v1.json`
  - Result: passed.

Next slice recommendation:

- Promote submenu restore into a small fixture matrix if dropdown-menu, context-menu, menubar, and
  future Material menu recipes start sharing more cases. Otherwise move to active-descendant
  interaction or typeahead because those remain high-risk focus/semantics intersections.

## F17: Roving typeahead needed fixture-level call-count coverage

Layer: `crates/fret-ui` declarative focus interaction mechanism

Status: covered, no runtime fix required

What was found:

- The roving focus fixture covered arrow navigation, disabled skipping, wrapping, and pointer-region
  wrappers, but printable-key typeahead remained only in a focused Rust test.
- Typeahead is a high-risk mechanism because it crosses keyboard routing, component-owned policy
  hooks, focus movement, active-selection updates, and wrapper-preserved roving item collection.
- The harness also lacked a scalar metric that would catch duplicate typeahead handler invocation.

What was added:

- `crates/fret-ui/src/declarative/tests/fixtures/roving_focus_interaction_v1.json` now includes:
  - `typeahead-key-targets-third-item`,
  - `typeahead-none-preserves-current-focus`,
  - `typeahead-through-pointer-region-wrapper-targets-third-item`.
- `crates/fret-ui/src/declarative/tests/roving_focus_interaction_harness.rs` now supports
  printable key cases and records `roving.typeahead.calls`.

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_roving_focus_interaction_matches_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib roving_flex -- --nocapture`
  - Result: passed, 5 tests.
- `python -m json.tool crates/fret-ui/src/declarative/tests/fixtures/roving_focus_interaction_v1.json`
  - Result: passed.

Next slice recommendation:

- Move to active-descendant interaction. The relation is already observable in semantics fixtures,
  but the lane still lacks an interaction fixture that proves keyboard/filtering changes update the
  active descendant that assistive technologies will observe.

## F18: Combobox active-descendant interaction needed a fixture-level matrix before recipe parity

Layer: `crates/fret-ui` declarative semantics / focus interaction mechanism

Status: covered, no runtime fix required

What was found:

- The shared semantics harness already exposed `active_descendant` as a relation, but the lane
  still lacked a query-driven interaction matrix that drove a real text input and verified the
  resulting active descendant, filtered option count, and active option index together.
- Without that matrix, active-descendant drift could hide behind manual screenshots or narrow
  relation-only checks.

What was added:

- Fixture suite:
  `crates/fret-ui/src/declarative/tests/fixtures/combobox_active_descendant_interaction_v1.json`
- Thin declarative runner:
  `crates/fret-ui/src/declarative/tests/combobox_active_descendant_interaction_harness.rs`

The fixture matrix covers:

- prefix query `"nu"` updating the active descendant to the matching `Nuxt` option,
- `combobox.query.len` as a scalar mechanism metric,
- `combobox.visible.count` as the filtered option count,
- `combobox.active.index` as the selected option index,
- the `active_descendant` semantics relation from the combobox input to the matching option.

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_combobox_active_descendant_interaction_matches_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo fmt --package fret-ui --check`
  - Result: passed.
- `python -m json.tool docs/workstreams/fret-mechanism-harness-v1/WORKSTREAM.json | Out-Null`
  - Result: passed.
- `python -m json.tool crates/fret-ui/src/declarative/tests/fixtures/combobox_active_descendant_interaction_v1.json | Out-Null`
  - Result: passed.

Next slice recommendation:

- Move to nested focus scope or stale-parent focus state next. Active-descendant interaction is now
  covered, so the next high-risk gap is cross-scope focus ancestry and recipe-level parity around
  typeahead and submenu behavior.

## F20: Stale-parent focus scope behavior needed a tree-level fixture harness

Layer: `crates/fret-ui` tree focus scope / retained-tree mechanism

Status: covered, no runtime fix required

What was found:

- The stale-parent focus scope risk already existed as a focused Rust test, but the workstream still
  lacked a case-id-addressable tree fixture harness that exercised the same retained-tree parent
  pointer mutation.
- The first harness run exposed a schema constraint rather than a runtime defect: `domains` cannot
  use a free-form `dispatch` tag and must stay within the fixed mechanism enum.

What was added:

- Fixture suite:
  `crates/fret-ui/src/tree/tests/fixtures/focus_scope_stale_parent_interaction_v1.json`
- Thin tree runner:
  `crates/fret-ui/src/tree/tests/focus_scope_stale_parent_harness.rs`

The fixture matrix covers:

- stale parent pointer mutation on the trapped focus scope children,
- child reachability through the dispatch snapshot before pointer routing,
- pointer activation outside the scope still running,
- focus remaining inside the trapped scope after the click path.

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_focus_scope_stale_parent_interaction_matches_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo fmt --package fret-ui --check`
  - Result: passed.
- `python -m json.tool crates/fret-ui/src/tree/tests/fixtures/focus_scope_stale_parent_interaction_v1.json | Out-Null`
  - Result: passed.

Next slice recommendation:

- Move to submenu fixture coverage next. Stale-parent pointer robustness is now fixture-covered, so
  the next uncovered mechanism gap is recipe-level submenu restore parity and, after that, deeper
  typeahead/composition behavior.

## F19: Nested focus scope behavior needed an inner/outer fixture matrix

Layer: `crates/fret-ui` declarative focus interaction mechanism

Status: covered, no runtime fix required

What was found:

- Existing focused tests covered layered focus scopes and innermost-scope preference, and the flat
  focus-scope fixture covered trap/wrap/non-trap/pointer behavior.
- The workstream still lacked a case-id-addressable nested-scope matrix that proved inner and outer
  trapped scopes cooperate under the same declarative scenario surface.
- This gap matters because menu/submenu, popover-inside-dialog, and future editor overlays all rely
  on the innermost active focus scope winning without leaking focus to outer siblings.

What was added:

- Fixture suite:
  `crates/fret-ui/src/declarative/tests/fixtures/focus_scope_nested_interaction_v1.json`
- Existing declarative runner extended in:
  `crates/fret-ui/src/declarative/tests/focus_scope_interaction_harness.rs`

The fixture matrix covers:

- `focus.next` from the first inner item moving to the second inner item,
- `focus.previous` from the first inner item wrapping to the second inner item,
- `focus.next` from an outer-scope sibling wrapping into the inner scope,
- pointer activation outside the outer scope still running while focus remains inside the trapped
  inner scope.

Run results:

- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_nested_focus_scope_interaction_matches_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_focus_scope_interaction_matches_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo fmt --package fret-ui --check`
  - Result: passed.
- `python -m json.tool crates/fret-ui/src/declarative/tests/fixtures/focus_scope_nested_interaction_v1.json | Out-Null`
  - Result: passed.

Next slice recommendation:

- Move to stale-parent focus state or submenu fixture matrices next. Nested focus ancestry is now
  covered at fixture level, but stale parent pointers remain procedural-only and submenu restore is
  still mostly focused-test coverage instead of a shared recipe matrix.

## F21: Dropdown/context submenu restore was a coverage gap, not a runtime defect

Layer: `ecosystem/fret-ui-shadcn` recipe-consumer parity

Status: covered, no runtime fix required

What was found:

- The recipe focus-restore fixture already covered Escape, outside press, focus-outside, and
  prevent-default policy differences, but submenu keyboard open/ArrowLeft restore still lived only
  in focused tests.
- That left the dropdown-menu and context-menu submenu restore invariant observable in code but not
  locked by the shared fixture/oracle runner.
- The missing piece was not a runtime defect; it was fixture-level coverage that could prove the
  open/close sequence and final focus state together.

What was added:

- `ecosystem/fret-ui-shadcn/tests/fixtures/focus_restore_recipe_cases_v1.json` now includes
  dropdown-menu and context-menu submenu keyboard restore cases.
- `ecosystem/fret-ui-shadcn/tests/focus_restore_mechanism_harness.rs` now records
  `submenu.opened` and `submenu.closed` metrics in addition to the final focus state.
- The same `mechanism_harness_focus_restore_recipe_cases_match_oracles` run now covers the submenu
  matrix alongside the existing Escape/outside/focus-outside cases.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo fmt --package fret-ui-shadcn --check`
  - Result: passed.
- `git diff --check`
  - Result: passed.
- `python -m json.tool ecosystem/fret-ui-shadcn/tests/fixtures/focus_restore_recipe_cases_v1.json`
  - Result: passed.

Next slice recommendation:

- Extend submenu fixture coverage to menubar and then continue with recipe-level typeahead parity.

## F22: Menubar submenu restore was a coverage gap, not a runtime defect

Layer: `ecosystem/fret-ui-shadcn` recipe-consumer parity

Status: covered, no runtime fix required

What was found:

- The submenu fixture already covered dropdown-menu and context-menu, but menubar submenu restore
  still lived in focused tests.
- That left the menu-bar path visible in runtime code but not yet locked by the shared
  fixture/oracle runner.
- The missing piece was still a coverage gap rather than a runtime defect.

What was added:

- `ecosystem/fret-ui-shadcn/tests/fixtures/focus_restore_recipe_cases_v1.json` now includes a
  menubar submenu keyboard restore case.
- `ecosystem/fret-ui-shadcn/tests/focus_restore_mechanism_harness.rs` now renders the menubar path,
  records `submenu.opened` and `submenu.closed`, and asserts focus restoration after ArrowLeft.
- The same `mechanism_harness_focus_restore_recipe_cases_match_oracles` run now covers
  dropdown-menu, context-menu, and menubar submenu restore.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test focus_restore_mechanism_harness mechanism_harness_focus_restore_recipe_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo fmt --package fret-ui-shadcn --check`
  - Result: passed.
- `git diff --check`
  - Result: passed.
- `python -m json.tool ecosystem/fret-ui-shadcn/tests/fixtures/focus_restore_recipe_cases_v1.json`
  - Result: passed.

Next slice recommendation:

- Continue with recipe-level typeahead parity, then wire high-risk cases into UI Gallery diagnostics.

## F23: Recipe typeahead parity was a coverage gap, not a runtime defect

Layer: `ecosystem/fret-ui-shadcn` recipe-consumer parity

Status: covered, no runtime fix required

What was found:

- The lower-level roving focus fixture already covered printable-key typeahead dispatch,
  no-match preservation, pointer-region wrappers, and duplicate handler call counts.
- Recipe-level typeahead still lacked a shared fixture matrix above focused component tests, so the
  lane could not prove select trigger typeahead, dropdown-menu open-menu typeahead, and menubar
  open-menu typeahead through the same case-id-addressable harness.
- The first recipe matrix run did not reveal a runtime defect.

What was added:

- New fixture suite:
  `ecosystem/fret-ui-shadcn/tests/fixtures/recipe_typeahead_cases_v1.json`
- New thin recipe runner:
  `ecosystem/fret-ui-shadcn/tests/recipe_typeahead_mechanism_harness.rs`
- The matrix covers:
  - select trigger typeahead selecting `banana`, keeping the popup closed, and firing
    `on_value_change` exactly once,
  - dropdown-menu pointer-open typeahead moving focus to the `Billing` item while preserving open
    state,
  - menubar keyboard-open typeahead moving focus to the `Open` item.

Run results:

- `cargo test --profile dev-fast -p fret-ui-shadcn --test recipe_typeahead_mechanism_harness mechanism_harness_recipe_typeahead_cases_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `python -m json.tool ecosystem/fret-ui-shadcn/tests/fixtures/recipe_typeahead_cases_v1.json | Out-Null`
  - Result: passed.
- `cargo fmt --package fret-ui-shadcn --check`
  - First run: failed on the new harness formatting only.
  - After `cargo fmt --package fret-ui-shadcn`: passed.
- `git diff --check`
  - Result: passed.
- `python tools/check_workstream_catalog.py`
  - Result: passed.

Next slice recommendation:

- Wire high-risk overlay/focus cases into UI Gallery diagnostics once stable demo pages expose focus
  state and barrier selectors. If typeahead grows again, extend this suite with no-match, buffer
  timeout, context-menu, and submenu typeahead cases rather than returning to focused-only tests.

## F24: UI Gallery overlay/focus runtime coverage found a harness precondition gap

Layer: diagnostics harness / `apps/fret-ui-gallery` runtime coverage

Status: covered for default-compatible paths, no runtime mechanism fix required

What was found:

- The first high-risk script, `ui-gallery-modal-barrier-focus-restore`, failed before reaching the
  overlay/focus invariant because it targets the internal Overlay preview page.
- That page is behind the `gallery-dev` feature, so running the script against the default
  `fret-ui-gallery` binary leaves `ui-gallery-nav-overlay` absent from the nav tree.
- Bounded bundle triage confirmed this was a selector/precondition failure, not a modal barrier or
  focus-restore runtime defect.
- The promoted suite command also had to use the actual `diag suite` execution contract: unlike
  `diag run`, the suite path rejects `--pack`, so the gate command records the runnable form.
- Default-compatible public pages already expose enough stable selectors to cover important
  overlay/focus runtime behavior without requiring the dev-only Overlay preview page.

What was added:

- New suite:
  `tools/diag-scripts/suites/fret-mechanism-harness-overlay-focus/suite.json`
- Promoted default-compatible scripts:
  - `tools/diag-scripts/ui-gallery/overlay/ui-gallery-alert-dialog-focus-trap-tab-cycle.json`
  - `tools/diag-scripts/ui-gallery/overlay/ui-gallery-dialog-detached-trigger-focus-restore.json`
  - `tools/diag-scripts/ui-gallery/overlay/ui-gallery-popover-escape-focus-restore.json`
- `ui-gallery-modal-barrier-focus-restore` now documents its `gallery-dev` feature precondition in
  script metadata so future runs do not confuse target drift with a mechanism failure.

Run results:

- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/overlay/ui-gallery-modal-barrier-focus-restore.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- cargo run -p fret-ui-gallery --release`
  - Result: failed at step 3 with `selector.not_found` for `ui-gallery-nav-overlay`.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778538785319-201560/1778539220100-script-step-0003-scroll_into_view-target-not-found`
- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/overlay/ui-gallery-alert-dialog-focus-trap-tab-cycle.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- cargo run -p fret-ui-gallery --release`
  - Result: passed.
- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/overlay/ui-gallery-dialog-detached-trigger-focus-restore.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- cargo run -p fret-ui-gallery --release`
  - Result: passed.
- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/overlay/ui-gallery-popover-escape-focus-restore.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- cargo run -p fret-ui-gallery --release`
  - Result: passed.
- `cargo run -p fretboard-dev -- diag suite fret-mechanism-harness-overlay-focus --dir target/fret-diag-mechanism-harness-runtime --session-auto --launch -- cargo run -p fret-ui-gallery --release`
  - Result: passed; all 3 suite scripts passed.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778539841178-206908/suite.summary.json`

Next slice recommendation:

- Add modal-barrier runtime coverage on a default-compatible public page, or split `gallery-dev`
  suites/features so dev-only scripts can only run with the matching gallery binary.

## F25: Public Dialog modal-barrier runtime coverage was missing from the default suite

Layer: diagnostics harness / `apps/fret-ui-gallery` runtime coverage

Status: covered, no runtime mechanism fix required

What was found:

- F24 proved the internal Overlay preview script was not a good default-binary gate because it
  depended on `gallery-dev`.
- The public Dialog docs page already exposes stable trigger/content/action ids and the diagnostics
  predicate surface already exposes `barrier_roots`, so the modal barrier root lifecycle could be
  covered without adding app code.
- The first run of the public Dialog modal-barrier script did not reveal a runtime defect.

What was added:

- New default-compatible script:
  `tools/diag-scripts/ui-gallery/overlay/ui-gallery-dialog-modal-barrier-focus-restore.json`
- The `fret-mechanism-harness-overlay-focus` suite now runs four default-compatible overlay/focus
  paths:
  - AlertDialog focus trap/tab cycle,
  - Dialog modal barrier/focus-barrier install + clear + focus restore,
  - Dialog detached-trigger focus restore,
  - Popover Escape focus restore.

Run results:

- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/overlay/ui-gallery-dialog-modal-barrier-focus-restore.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- cargo run -p fret-ui-gallery --release`
  - Result: passed.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778540631015-206124/1778541048822`
- `cargo run -p fretboard-dev -- diag suite fret-mechanism-harness-overlay-focus --dir target/fret-diag-mechanism-harness-runtime --session-auto --launch -- cargo run -p fret-ui-gallery --release`
  - Result: passed; all 4 suite scripts passed.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778541074402-183780/suite.summary.json`

Next slice recommendation:

- Add a default-compatible underlay/status probe for modal and non-modal outside-press behavior so
  runtime diagnostics can prove both barrier roots and underlay activation policy on public pages.

## F26: Drawer underlay runtime gate exposed a text instance-buffer binding defect

Layer: `crates/fret-render-wgpu` render scene recorder, discovered through diagnostics harness /
`apps/fret-ui-gallery` runtime coverage

Status: fixed and covered

What was found:

- The default-compatible Drawer outside-press script reached a real runtime path rather than a
  selector precondition.
- The first stable-binary run exposed a confirmed wgpu validation failure:
  `Instance 2454 extends beyond limit 2413 imposed by the buffer in slot 0`.
- The failing path was not a Drawer recipe layout issue. It was a renderer resource binding defect:
  `TextDrawKind::Mask` used `text_vertex_buffer` even though every text pipeline declares slot 0 as
  `TextGlyphInstance` with `VertexStepMode::Instance`.
- This mismatch made the text draw use glyph instance ranges against the wrong vertex buffer.

What was fixed:

- `crates/fret-render-wgpu/src/renderer/render_scene/recorders/scene_draw.rs` now binds
  `text_glyph_instance_buffer` for `TextDrawKind::Mask`, matching the text pipeline vertex layout
  and the other text draw kinds.

What was added:

- `tools/diag-scripts/ui-gallery/drawer/ui-gallery-drawer-outside-press-focus-restore.json` is now
  part of `tools/diag-scripts/suites/fret-mechanism-harness-overlay-focus/suite.json`.
- The promoted suite now covers five default-compatible runtime paths:
  - AlertDialog focus trap/tab cycle,
  - Dialog modal barrier/focus-barrier install + clear + focus restore,
  - Dialog detached-trigger focus restore,
  - Drawer outside-press underlay blocking + focus restore,
  - Popover Escape focus restore.

Run results:

- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/drawer/ui-gallery-drawer-outside-press-focus-restore.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- target/release/fret-ui-gallery.exe`
  - Before fix: app panicked with the wgpu validation failure above; the diagnostics tool timed out
    because the app exited before writing a normal script result.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778542621534-209760`
- `cargo build -p fret-ui-gallery --release`
  - Result: passed.
- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/drawer/ui-gallery-drawer-outside-press-focus-restore.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- target/release/fret-ui-gallery.exe`
  - After fix: passed.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778543537605-211528/1778543540366`
  - AI packet:
    `target/fret-diag-mechanism-harness-runtime/sessions/1778543537605-211528/1778543540366/ai.packet`
- `cargo run -p fretboard-dev -- diag suite fret-mechanism-harness-overlay-focus --dir target/fret-diag-mechanism-harness-runtime --session-auto --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed; all 5 suite scripts passed.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778543642089-204132/suite.summary.json`

Next slice recommendation:

- Add a default-compatible non-modal click-through activation-status runtime gate for popover,
  dropdown-menu, select, or context-menu once a public page exposes stable underlay/status probes.
  The Drawer gate now proves modal underlay blocking and also shows the harness can discover
  renderer-level mechanism defects through normal component interactions.

## F27: Combobox outside-press runtime gate exposed two diagnostics harness precondition gaps

Layer: diagnostics harness / `apps/fret-ui-gallery` runtime coverage

Status: covered, no combobox runtime mechanism fix required

What was found:

- The public Combobox outside-press script already existed, but the conformance demo did not expose
  `ui-gallery-combobox-demo-selected` / `ui-gallery-combobox-demo-query` status probes on the
  rendered page.
- After adding the probes, the script still failed because the `test_id` lived on an unlabeled
  container while `label_contains` only checks the selected node label. The child text label was not
  a valid oracle for the selected `test_id`.
- Once the state label was attached to the `test_id` node, the script reached the click step and
  exposed a second harness precondition gap: `exists` matched the offscreen conformance trigger at
  `y=3435.3`, so the injected click was clamped to the bottom of the window and did not hit the
  trigger.
- The final script did not reveal a Combobox outside-press runtime defect. Outside press closes the
  popup, preserves the unselected value, and restores focus to the trigger on the public page.

What was fixed:

- `apps/fret-ui-gallery/src/ui/snippets/combobox/conformance_demo.rs` now renders selected/query
  status rows for the public conformance demo.
- Combobox status row helpers now attach the current state label to the same node as the stable
  `test_id`, so diagnostics `label_contains` assertions observe the intended state directly.
- `tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-dismiss-outside-press.json` now
  scrolls the conformance trigger into view and asserts `bounds_within_window` before clicking it.

What was added:

- `tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-dismiss-outside-press.json` is now
  part of `tools/diag-scripts/suites/fret-mechanism-harness-overlay-focus/suite.json`.
- The promoted suite now covers six default-compatible runtime paths:
  - AlertDialog focus trap/tab cycle,
  - Dialog modal barrier/focus-barrier install + clear + focus restore,
  - Dialog detached-trigger focus restore,
  - Drawer outside-press underlay blocking + focus restore,
  - Combobox outside-press dismiss + no-selection preservation + focus restore,
  - Popover Escape focus restore.

Run results:

- `cargo fmt -p fret-ui-gallery --check`
  - Result: passed.
- `cargo build -p fret-ui-gallery --release`
  - Result: passed.
- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-dismiss-outside-press.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- target/release/fret-ui-gallery.exe`
  - Before scroll fix: failed at step 15 because the offscreen trigger click was clamped to the
    bottom of the window.
  - Evidence:
    `target/fret-diag-mechanism-harness-runtime/sessions/1778545713773-209396/1778545723289-script-step-0015-wait_until-timeout`
- `cargo run -p fretboard -- diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-dismiss-outside-press.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- target/release/fret-ui-gallery.exe`
  - After fix: passed.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778545819701-166516/1778545821783`
  - AI packet:
    `target/fret-diag-mechanism-harness-runtime/sessions/1778545819701-166516/1778545821783/ai.packet`
- `cargo run -p fretboard-dev -- diag suite fret-mechanism-harness-overlay-focus --dir target/fret-diag-mechanism-harness-runtime --session-auto --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed; all 6 suite scripts passed.
  - Evidence: `target/fret-diag-mechanism-harness-runtime/sessions/1778545884714-211856/suite.summary.json`

Next slice recommendation:

- Promote the same "exists is not clickability" lesson into a reusable diagnostics authoring rule:
  public UI Gallery interaction scripts should use `scroll_into_view` plus `bounds_within_window`
  before clicking any deep docs-page target.
- Continue toward a default-compatible non-modal click-through activation-status gate for popover,
  dropdown-menu, select, or context-menu. The Combobox gate proves close + focus restore, but it
  still does not prove underlay activation side effects.

## F28: Combobox screenshot review exposed trigger chevron layout drift and screenshot gate gaps

Layer: `ecosystem/fret-ui-shadcn` recipe + diagnostics harness

Status: fixed and covered with focused geometry + screenshot evidence

What was found:

- The Combobox suite continued past the previous failures and exposed more diagnostics precondition
  gaps: `ui-gallery-combobox-open-select-focus-restore.json` and
  `ui-gallery-combobox-roving-skips-disabled.json` clicked the conformance trigger after only
  checking `exists`, so the target could still be offscreen.
- `ui-gallery-combobox-demo-open-neutral-dark-screenshot.json` was not independently runnable; it
  waited for the Combobox page without navigating there first.
- Manual review of the normal-width screenshot then exposed a real recipe layout defect: the
  Combobox trigger row did not fill the trigger width on the desktop path, so
  `justify: SpaceBetween` placed the chevron directly after the truncated label instead of at the
  trigger inline end.
- Existing coverage already included Combobox overlay placement fixtures, but the workstream
  coverage table did not explicitly list them, making placement/style progress hard to audit.

What was fixed:

- `ecosystem/fret-ui-shadcn/src/combobox.rs` now gives the desktop trigger row
  `Length::Fill`, so the label lane and chevron lane occupy the full trigger width.
- Added `combobox_trigger_places_chevron_at_inline_end`, a focused geometry regression that checks
  the chevron semantics bounds sit near the trigger inline end.
- Hardened the two interaction scripts with `scroll_into_view`, `bounds_within_window`, and
  `click_stable` before clicking deep docs-page triggers.
- Repaired the neutral-dark open screenshot script so it navigates to the Combobox page, scrolls the
  target into view, and can be run from a fresh gallery launch.
- Updated `COVERAGE_MAP.md`, `TODO.md`, and `EVIDENCE_AND_GATES.md` to make Combobox placement
  fixtures and visual screenshot coverage visible as tracked workstream surfaces.

Run results:

- `cargo test -p fret-ui-shadcn --lib combobox_trigger_places_chevron_at_inline_end -- --nocapture`
  - Result: passed.
- `cargo build -p fret-ui-gallery --release`
  - Result: passed; existing warnings only.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-demo-open-neutral-dark-screenshot.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --include-screenshots --launch -- target/release/fret-ui-gallery.exe`
  - Before script hardening: failed at step 7 because the script did not navigate to the Combobox
    page.
  - After script hardening and chevron fix: passed.
  - Screenshot:
    `target/fret-diag-mechanism-harness-runtime/sessions/1778558055240-49296/screenshots/1778558059695-ui-gallery-combobox-basic-neutral-dark-open/window-4294967297-tick-101-frame-101.png`
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-demo-narrow-open-screenshot.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --include-screenshots --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed.
  - Screenshot:
    `target/fret-diag-mechanism-harness-runtime/sessions/1778557035697-61384/screenshots/1778557037892-ui-gallery-combobox-demo-open-narrow/window-4294967297-tick-59-frame-59.png`
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-open-select-focus-restore.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed after script hardening.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-roving-skips-disabled.json --dir target/fret-diag-mechanism-harness-runtime --session-auto --pack --ai-packet --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed after script hardening.

Next slice recommendation:

- Add runtime placement traces to the primary Combobox screenshot scripts so list position is backed
  by structured overlay data, not only screenshots.
- Promote a visual/style coverage matrix that tracks component, state, theme, viewport, screenshot
  script, geometry predicate, owner layer, and current gap. This should make future Material 3 or
  other component ecosystems auditable the same way as shadcn.

## F29: Combobox popup placement trace exposed Popover content height overestimation

Layer: `ecosystem/fret-ui-shadcn` Popover recipe sizing, with UI Gallery diagnostics coverage

Status: fixed and covered

What was found:

- After the Combobox screenshot gate gained `wait_overlay_placement_trace`, the normal-width
  Combobox run failed at the placement trace step instead of relying on screenshot review alone.
- The failing trace was selector-valid: it recorded `anchor_test_id=ui-gallery-combobox-basic-trigger`
  and `content_test_id=ui-gallery-combobox-basic-listbox`.
- The actual defect was content size estimation. The Popover solver saw `desired.h_px=316.0`,
  while the preferred bottom space was only about `245-247px`, so it correctly flipped the panel to
  `top`. The real command shell should be closer to `204px`, so bottom placement should fit.

What was fixed:

- `ecosystem/fret-ui-shadcn/src/popover.rs` now estimates Popover content size structurally across
  flex, row, column, scroll, container, hover-region, stack, pressable, opacity, and transform
  wrappers instead of adding fixed height and max height as independent shell/body heights.
- The normal-width Combobox screenshot script now requires `diag.overlay_placement_trace` and asserts
  `chosen_side=bottom` plus `flipped=false`; it also captures a layout sidecar before the screenshot.
- `TODO.md`, `COVERAGE_MAP.md`, and `EVIDENCE_AND_GATES.md` now treat Combobox placement trace
  coverage as part of the workstream, not only visual screenshot review.

Run results:

- `cargo build -p fret-ui-gallery`
  - Result: passed.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-demo-open-neutral-dark-screenshot.json --dir target/fret-diag-combobox-check2 --session-auto --pack --ai-packet --include-screenshots --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Trace evidence:
    `target/fret-diag-combobox-check2/sessions/1778566847049-70708/1778566849917/script.result.json`
  - Placement trace after fix: `desired.h_px=204.0`, `chosen_side=bottom`,
    `preferred_fits_without_main_clamp=true`.

Next slice recommendation:

- Extend the same structured placement-trace gate pattern to other high-risk overlay recipes:
  Select, DropdownMenu submenus, Command/Combobox long lists, DatePicker nested selects, and future
  Material 3 menu/autocomplete surfaces.

## F30: DropdownMenu submenu runtime gate exposed a placement observability gap

Layer: shared `ecosystem/fret-ui-shadcn` submenu diagnostics bridge + DropdownMenu UI Gallery diagnostics coverage

Status: fixed and covered

What was found:

- Extending the Combobox placement-trace pattern to
  `ui-gallery-dropdown-menu-submenu-open-smoke.json` initially failed at the first
  `wait_overlay_placement_trace` step.
- The failure was not a submenu visibility failure: the script had already opened the root menu,
  hovered `ui-gallery-dropdown-menu-submenu-invite-users`, and observed the first submenu item.
- The diagnostics evidence had no `overlay_placement_trace` entries for DropdownMenu submenus.
  Select and Popover were already wired to placement diagnostics, but DropdownMenu submenu geometry
  used the menu-sub geometry model without recording a `PlacedRect`.

What was fixed:

- `ecosystem/fret-ui-shadcn/src/menu_submenu_diagnostics.rs` now provides the shared submenu
  `PlacedRect` diagnostics bridge used by DropdownMenu and follow-on menu recipes.
- `ecosystem/fret-ui-shadcn/src/dropdown_menu.rs` now records DropdownMenu submenu geometry as
  `PlacedRect` diagnostics whenever a submenu geometry has been resolved.
- The recorded side is inferred from the actual reference/floating geometry, so the trace reflects
  right/left flips or vertical fallbacks instead of assuming the preferred side.
- Added `submenu_geometry_side_tracks_floating_position`, a focused pure regression for
  the side inference used by the runtime trace bridge.
- `tools/diag-scripts/ui-gallery/dropdown-menu/ui-gallery-dropdown-menu-submenu-open-smoke.json`
  now requires `diag.overlay_placement_trace`, asserts the first and nested submenu anchors choose
  `right`, and captures a layout sidecar.
- `TODO.md`, `COVERAGE_MAP.md`, and `EVIDENCE_AND_GATES.md` now list DropdownMenu nested submenu
  runtime placement tracing as a covered harness surface.

Run results:

- `cargo test -p fret-ui-shadcn --lib submenu_geometry_side_tracks_floating_position -- --nocapture`
  - Result: passed.
- `cargo test -p fret-ui-shadcn --lib dropdown_menu_submenu_keyboard_open_transfers_focus_and_arrow_left_restores_focus -- --nocapture`
  - Result: passed.
- `cargo check -p fret-ui-shadcn`
  - Result: passed.
- `cargo build -p fret-ui-gallery`
  - Result: completed successfully after the shell command timed out; the background cargo process
    finished and updated `target/debug/fret-ui-gallery.exe`.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/dropdown-menu/ui-gallery-dropdown-menu-submenu-open-smoke.json --dir target/fret-diag-dropdown-submenu-trace --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Before the diagnostics bridge: failed at step 13 with
    `wait_overlay_placement_trace_timeout`.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/dropdown-menu/ui-gallery-dropdown-menu-submenu-open-smoke.json --dir target/fret-diag-dropdown-submenu-trace2 --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Trace evidence:
    `target/fret-diag-dropdown-submenu-trace2/sessions/1778568285442-34920/1778568287793/script.result.json`
  - Placement trace after fix: `ui-gallery-dropdown-menu-submenu-invite-users` side `right`,
    `ui-gallery-dropdown-menu-submenu-more-options` side `right`.
  - Layout sidecar:
    `target/fret-diag-dropdown-submenu-trace2/sessions/1778568285442-34920/1778568290949-ui-gallery-dropdown-menu-submenu-open-smoke.layout/layout.taffy.v1.json`

Next slice recommendation:

- Extend the same submenu placed-rect diagnostics bridge to Menubar runtime gallery paths, then add
  RTL submenu trace assertions so logical inline-end behavior is structured data rather than
  screenshot-only evidence.

## F31: ContextMenu submenu gate exposed stale selectors, offscreen activation, and missing placement trace

Layer: shared `ecosystem/fret-ui-shadcn` submenu diagnostics bridge + ContextMenu UI Gallery diagnostics coverage

Status: fixed and covered

What was found:

- Upgrading `ui-gallery-context-menu-submenu-safe-corridor-sweep.json` to require
  `wait_overlay_placement_trace` first exposed a stale diagnostics script rather than a placement
  result.
- The trigger was offscreen in the original script. The right-click target resolved, but the click
  was clamped outside the window, so the script timed out before opening the context menu.
- After adding `scroll_into_view` and `click_stable`, the script still waited for stale ids:
  `ui-gallery-context-menu-submenu-open` and `ui-gallery-context-menu-submenu-rename`. The current
  snippet exposes `ui-gallery-context-menu-submenu-content` and
  `ui-gallery-context-menu-submenu-save-page`.
- Once the script reached the submenu state, it failed at `wait_overlay_placement_trace`: ContextMenu
  root content emitted anchored-panel placement, but submenu geometry had no placed-rect trace.

What was fixed:

- `tools/diag-scripts/ui-gallery/context-menu/ui-gallery-context-menu-submenu-safe-corridor-sweep.json`
  now scrolls the submenu trigger into view, uses stable right-click activation, waits for current
  snippet test ids, asserts the submenu placed-rect side is `right`, and captures a layout sidecar.
- `ecosystem/fret-ui-shadcn/src/context_menu.rs` now records ContextMenu submenu geometry through
  the shared submenu diagnostics bridge.
- The script was added to `tools/diag-scripts/suites/ui-gallery/suite.json` and
  `tools/diag-scripts/suites/ui-gallery-shadcn-conformance/suite.json`, so this runtime path is
  no longer only an ad-hoc repro.
- `TODO.md`, `COVERAGE_MAP.md`, and `EVIDENCE_AND_GATES.md` now list ContextMenu safe-corridor
  placement tracing as a covered harness surface.

Run results:

- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/context-menu/ui-gallery-context-menu-submenu-safe-corridor-sweep.json --dir target/fret-diag-context-menu-submenu-trace-pre --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Before script hardening: failed before trace because the submenu trigger was offscreen.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/context-menu/ui-gallery-context-menu-submenu-safe-corridor-sweep.json --dir target/fret-diag-context-menu-submenu-trace-pre3 --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - After script hardening and selector repair, before the diagnostics bridge: failed at step 19
    with `wait_overlay_placement_trace_timeout`.
- `cargo test -p fret-ui-shadcn --lib submenu_geometry_side_tracks_floating_position -- --nocapture`
  - Result: passed.
- `cargo test -p fret-ui-shadcn --lib context_menu_submenu_keyboard_open_transfers_focus_and_arrow_left_restores_focus -- --nocapture`
  - Result: passed.
- `cargo build -p fret-ui-gallery`
  - Result: passed.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/context-menu/ui-gallery-context-menu-submenu-safe-corridor-sweep.json --dir target/fret-diag-context-menu-submenu-trace --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Trace evidence:
    `target/fret-diag-context-menu-submenu-trace/sessions/1778571251704-73756/1778571253975/script.result.json`
  - Placement trace after fix: root content `chosen_side=right`; submenu anchor
    `ui-gallery-context-menu-submenu-more-tools` side `right`.
  - Layout sidecar:
    `target/fret-diag-context-menu-submenu-trace/sessions/1778571251704-73756/1778571256920-ui-gallery-context-menu-submenu-safe-corridor-sweep.layout/layout.taffy.v1.json`

Next slice recommendation:

- Add Menubar submenu runtime placement traces and at least one RTL submenu placement trace across
  DropdownMenu, ContextMenu, or Menubar so logical inline-end placement is no longer screenshot-only.

## F32: Combobox popup-trigger placement needed structured side-offset coverage

Layer: diagnostics harness query surface + `ecosystem/fret-ui-shadcn` Combobox UI Gallery runtime coverage

Status: fixed and covered

What was found:

- The `ui-gallery-combobox-popup-trigger.json` scenario visually opens the popup above the trigger
  in a 1280x720 window. The structured trace shows this is correct collision behavior, not a
  mechanism placement bug: the trigger sits at `y=669.33` with only `28.67px` of preferred bottom
  space, while the popup wants `204px`.
- The same script previously only checked open/select/close behavior. It could not fail when the
  popup chose the wrong side, when collision flip regressed, or when shadcn `ComboboxContent`
  `sideOffset` drifted by a few pixels.
- The runtime trace already recorded `side_offset_px`, but `wait_overlay_placement_trace` and
  `fretboard diag query overlay-placement-trace` could not query it. That made side-offset parity a
  manual trace inspection instead of a durable gate.
- Source alignment check against `repo-ref/ui/apps/v4/registry/new-york-v4/ui/combobox.tsx` confirms
  shadcn v4 `ComboboxContent` defaults to `sideOffset = 6`.

What was fixed:

- `UiOverlayPlacementTraceQueryV1` now accepts `side_offset_px` and `side_offset_eps_px`.
- The UI diagnostics runner and `fretboard diag query overlay-placement-trace` now filter anchored
  panel traces by side offset, so scripts can directly catch small recipe spacing drift.
- `tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-popup-trigger.json` now requires
  `diag.overlay_placement_trace`, asserts `chosen_side=top`, `flipped=true`, and
  `side_offset_px=6`, and captures an open-state layout sidecar, screenshot, and bundle before
  selecting the item.
- `tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-popup-trigger-bottom-room.json`
  now starts directly on the Popup section in a taller viewport and asserts the same trigger
  keeps `chosen_side=bottom` when there is enough room below it.
- The script metadata now explains that this fixture intentionally keeps the trigger near the
  viewport bottom to validate collision flip behavior.

Run results:

- `cargo test -p fret-diag-protocol overlay_placement_trace_query_serializes_side_offset_gate -- --nocapture`
  - Result: passed.
- `cargo test -p fret-diag query_overlay_placement_trace -- --nocapture`
  - Result: passed; includes `query_overlay_placement_trace_filters_anchored_panel_side_offset`
    and `query_overlay_placement_trace_contract_uses_canonical_flags`.
- `cargo check -p fret-bootstrap -p fret-diag -p fret-diag-protocol`
  - Result: passed.
- `cargo check -p fret-diag-scriptgen`
  - Result: passed.
- `cargo check -p fret-ui-shadcn`
  - Result: passed.
- `cargo build -p fretboard`
  - Result: passed.
- `cargo build -p fret-ui-gallery`
  - Result: command timed out after the binary was produced; `target/debug/fret-ui-gallery.exe`
    updated at 2026-05-12 17:03:37 local time and no cargo/rustc processes remained.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-popup-trigger.json --dir target/fret-diag-combobox-popup-position-side-offset --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Trace evidence:
    `target/fret-diag-combobox-popup-position-side-offset/sessions/1778576696581-76876/1778576699867/script.result.json`
  - Query proof:
    `fretboard diag query overlay-placement-trace ... --chosen-side top --flipped true --side-offset-px 6 --side-offset-eps-px 0.25 --json`
    returned one matching anchored-panel trace.
  - Placement trace after hardening: preferred side `bottom`, chosen side `top`, side offset `6`,
    gap `6`, preferred bottom space `28.67px`, flipped side fits.
  - Layout sidecar:
    `target/fret-diag-combobox-popup-position-side-offset/sessions/1778576696581-76876/1778576702191-ui-gallery-combobox-popup-trigger-open.layout/layout.taffy.v1.json`
  - Screenshot:
    `target/fret-diag-combobox-popup-position-side-offset/sessions/1778576696581-76876/screenshots/1778576702321-ui-gallery-combobox-popup-trigger-open/window-4294967297-tick-83-frame-83.png`

Next slice recommendation:

- Keep the same paired-fixture discipline for submenu overlays: verify both the preferred logical
  side in a roomy viewport and the collision-flipped side in a tight viewport.

## F33: Menubar submenu placement traces exposed a DirectionProvider/scope boundary

Layer: `ecosystem/fret-ui-shadcn` Menubar recipe + UI Gallery diagnostics

Status: fixed and covered

What was found:

- Menubar submenu placement had no UI Gallery runtime `placed_rect` trace, while DropdownMenu and
  ContextMenu already did. That left Menubar logical-side placement as screenshot-only evidence.
- Adding an RTL Menubar submenu gate first failed with `side=right` for
  `ui-gallery-menubar-rtl-more`. The initial trace used a 1120px viewport where the submenu trigger
  sat at `x=135` and the submenu wanted `191px`, so physical left had insufficient room and the
  correct collision behavior was to flip to the right.
- After widening the viewport to make physical left viable, the gate would still have been weak
  without preserving the direction across Menubar's own deferred render scope. The recipe captured
  direction inside `cx.scope`, which can observe the default LTR instead of the caller's
  `DirectionProvider`.

What was fixed:

- `Menubar::into_element(...)` now captures the caller direction before entering its own scope and
  threads that value into each `MenubarMenuEntries`.
- Menubar submenu overlay recording now reuses the shared
  `menu_submenu_diagnostics::record_submenu_placement(...)` bridge, so the same placed-rect query
  surface covers DropdownMenu, ContextMenu, and Menubar.
- Added LTR and RTL Menubar submenu placement scripts:
  `tools/diag-scripts/ui-gallery/menubar/ui-gallery-menubar-submenu-placement-trace.json` and
  `tools/diag-scripts/ui-gallery/menubar/ui-gallery-menubar-rtl-submenu-placement-trace.json`.
- The RTL fixture uses a wide viewport to assert the preferred logical inline-end side (`left`) only
  when collision solving has enough room to preserve it.

Run results:

- `cargo check -p fret-ui-shadcn`
  - Result: passed.
- `cargo build -p fret-ui-gallery`
  - Result: passed after one timeout/retry; no cargo/rustc processes remained after the timed-out
    attempt.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/menubar/ui-gallery-menubar-submenu-placement-trace.json --dir target/fret-diag-menubar-submenu-placement-ltr-final --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Query proof: `anchor_test_id=ui-gallery-menubar-demo-share`, `kind=placed_rect`,
    `chosen_side=right`.
- `target/debug/fretboard.exe diag run tools/diag-scripts/ui-gallery/menubar/ui-gallery-menubar-rtl-submenu-placement-trace.json --dir target/fret-diag-menubar-rtl-submenu-placement-wide --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Query proof: `anchor_test_id=ui-gallery-menubar-rtl-more`, `kind=placed_rect`,
    `chosen_side=left`.

Next slice recommendation:

- Add a companion RTL submenu tight-left fixture that intentionally keeps the submenu trigger near
  the left collision edge and asserts the correct flip back to physical right.
