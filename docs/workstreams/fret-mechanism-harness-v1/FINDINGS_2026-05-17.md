# Fret Mechanism Harness v1 Findings

## F129: Layout primitive synthetic coverage expanded without exposing a new mechanism defect

Layer: declarative layout mechanism harness.

Status: observed and documented.

What was found:

- The existing `layout_primitives_v1.json` suite covered only a narrow slice of layout behavior.
- The largest remaining gap was not a known runtime bug, but missing synthetic proof for several
  self-drawn layout primitives that commonly affect shadcn-style composition: percent sizing,
  min/max clamping, intrinsic measurement with percent constraints, wrapped text height
  propagation, scroll-root child layout preservation, and absolute percent inset placement.
- The baseline `mechanism_harness_layout_primitives_match_oracles` test was already green, so the
  right move was to widen the harness before claiming any new defect.

What was fixed:

- Added new fixture cases for percent sizing, min/max clamping, percent min/max intrinsic
  measurement, wrapped text height propagation, scroll-root child layout preservation, and
  absolute percent inset placement.
- Upgraded the layout primitive test harness with a test-local text service that can model wrap
  width and line growth instead of a fixed 10x10 placeholder.
- Added scalar metric collection for the intrinsic percent min/max case so the fixture can assert
  measurement behavior without overloading geometry assertions.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs`
- `crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
- `docs/workstreams/fret-mechanism-harness-v1/COVERAGE_MAP.md`
- `docs/workstreams/fret-mechanism-harness-v1/EVIDENCE_AND_GATES.md`

Run results:

- Focused synthetic gate:
  `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_layout_primitives_match_oracles -- --nocapture`
  - Result: passed.

Next slice recommendation:

- Add text measurement/paint agreement cases or explicit RTL/writing-mode cases once the direction
  contract is clear enough to exercise as a synthetic mechanism slice.

## F130: Text measurement/paint agreement promoted into the layout primitive harness

Layer: declarative layout mechanism harness.

Status: observed and documented.

What was found:

- Fret already had focused Rust tests proving that text measurement and paint preparation agree on
  wrap width, overflow, and scale factor.
- The durable mechanism harness did not observe those facts, so `layout_primitives_v1.json` could
  prove wrapped layout height but not whether the measure pass and paint pass used the same text
  constraints.
- That is a harness gap for a self-drawn UI engine: a component can appear misaligned when layout
  used one wrap width and paint used another, even if the final node bounds look plausible.

What was fixed:

- Added fixture cases for column text wrap-width agreement, max-width row wrap-width agreement, and
  overflow/scale constraint agreement.
- Extended the layout primitive observer to paint selected scenarios and publish scalar metrics for
  measured-vs-prepared wrap width, scale factor, overflow/wrap equality, prepared painted height,
  and following-sibling placement after painted text height.
- Kept the changes in the mechanism harness layer; no runtime behavior was changed.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs`
- `crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
- `docs/workstreams/fret-mechanism-harness-v1/COVERAGE_MAP.md`

Run results:

- Focused synthetic gate:
  `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_layout_primitives_match_oracles -- --nocapture`
  - Result: passed.

Next slice recommendation:

- Add RTL/writing-mode layout primitive cases only after the mechanism-level direction model is
  explicit enough to avoid baking recipe policy into `fret-ui` oracles.

## F131: View-cache lifecycle promoted into a fixture-driven mechanism harness

Layer: declarative view-cache and layout-query invalidation mechanism.

Status: observed and documented.

What was found:

- No new runtime mechanism defect was reproduced in this slice.
- The useful finding was a harness modeling gap: key `ViewCache` lifecycle guarantees were covered
  by scattered focused tests, but not by a single fixture-driven matrix that future agents can
  extend case-by-case.
- The first layout-query oracle draft also clarified an important mechanism rule: layout-query reads
  are frame-lagged, so a size change must not rerender the dependent cached subtree in the same
  frame; it should rerender after the committed layout-query revision advances.

What was fixed:

- Added `view_cache_lifecycle_v1.json` with stable case ids for clean cache hits, element state
  preservation, explicit cache-key misses, animation-frame invalidation, model dependency
  preservation across cache-hit frames, unrelated model scoping, inspection-mode cache bypass, and
  layout-query next-frame invalidation.
- Added a thin `view_cache_lifecycle_harness.rs` runner that reports scalar render, cache-reason,
  model-observation, state, and layout-query metrics through `fret-mechanism-harness`.
- Kept the change in the mechanism harness layer; no runtime behavior was changed.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/fixtures/view_cache_lifecycle_v1.json`
- `crates/fret-ui/src/declarative/tests/view_cache_lifecycle_harness.rs`
- `crates/fret-ui/src/declarative/tests/mod.rs`
- `docs/workstreams/fret-mechanism-harness-v1/COVERAGE_MAP.md`
- `docs/workstreams/fret-mechanism-harness-v1/EVIDENCE_AND_GATES.md`

Run results:

- Fixture schema:
  `python -m json.tool crates/fret-ui/src/declarative/tests/fixtures/view_cache_lifecycle_v1.json > $null`
  - Result: passed.
- Focused synthetic gate:
  `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_view_cache_lifecycle_matches_oracles -- --nocapture`
  - Result: passed.
- Companion view-cache focused gate:
  `cargo test --profile dev-fast -p fret-ui --lib view_cache -- --nocapture`
  - Result: passed.

Next slice recommendation:

- Move to a runtime-visible scroll/click stability slice or a retained/component semantics mutation
  slice. Add a UI Gallery companion for view-cache lifecycle only if a real surface can mutate
  cached model/layout-query dependencies in a way synthetic fixtures cannot represent.

## F132: ScrollArea content-growth runtime gate promoted into the suite

Layer: UI Gallery diagnostics harness for scroll extent and click stability.

Status: observed and documented.

What was found:

- No ScrollArea mechanism defect reproduced in the focused run.
- The actual gap was harness promotion: `ui-gallery-scroll-area-expand-at-bottom.json` already
  encoded the risky runtime behavior, but it was not a member of the promoted
  `ui-gallery-scroll-area` suite.
- This matters for a self-drawn UI framework because dynamic content growth can pass static layout
  fixtures while still failing at runtime if scroll extents, click targeting, or viewport stability
  drift after the content changes.

What was fixed:

- Added the content-growth script to `tools/diag-scripts/suites/ui-gallery-scroll-area/suite.json`.
- Added a `fret-diag-protocol` roundtrip test so the promoted script stays parseable through the
  diagnostics schema.
- Kept the change in the diagnostics harness layer; no runtime behavior was changed.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scroll-area-expand-at-bottom.json`
- `tools/diag-scripts/suites/ui-gallery-scroll-area/suite.json`
- `crates/fret-diag-protocol/tests/script_json_roundtrip.rs`

Run results:

- Focused runtime gate:
  `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scroll-area-expand-at-bottom.json --dir target/fret-diag-scroll-area-expand-at-bottom-v1 --session-auto --pack --ai-packet --include-screenshots --timeout-ms 360000 --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - AI packet:
    `target/fret-diag-scroll-area-expand-at-bottom-v1/sessions/1778953288892-97788/1778953292783/ai.packet`
  - Share pack:
    `target/fret-diag-scroll-area-expand-at-bottom-v1/sessions/1778953288892-97788/share/1778953292783.zip`

Next slice recommendation:

- Run the promoted `ui-gallery-scroll-area` suite after the manifest update. If it stays green,
  move to a different runtime-visible gap instead of adding more ScrollArea checks without a fresh
  failure signal.

## F133: DataTable page-entry lint moved from candidate debt to strict coverage

Layer: diagnostics registry authoring harness.

Status: observed and documented.

What was found:

- No DataTable component or mechanism defect was reproduced in this slice.
- The earlier candidate audit reported 174 DataTable page-entry violations across 21 promoted
  scripts when DataTable was modeled as having only one owning page root.
- A second audit showed those were harness-modeling false positives: DataTable has several valid
  page/variant roots (`default`, `basic`, `listlike`, `reusable`, `rtl`, and `torture`) that prove
  page entry just as well as the broad page body id.

What was fixed:

- Extended the diagnostics registry page-entry lint to support `entry_ids` per page rule.
- Enabled strict page-entry lint for `ui-gallery-data-table`,
  `ui-gallery-data-table-retained`, and `ui-gallery-data-table-view-cache-torture`.
- Added lint self-tests for DataTable variant-root entry, missing entry rejection, and
  `FRET_UI_GALLERY_START_PAGE=data_table_torture` entry evidence.

Evidence anchors:

- `tools/check_diag_scripts_registry.py`
- `tools/test_check_diag_scripts_registry.py`
- `tools/diag-scripts/suites/ui-gallery-data-table/suite.json`
- `tools/diag-scripts/suites/ui-gallery-data-table-retained/suite.json`
- `tools/diag-scripts/suites/ui-gallery-data-table-view-cache-torture/suite.json`

Run results:

- Candidate audit before the fix:
  - Result: 174 DataTable candidate violations across 21 promoted scripts under a single-root
    model.
- Candidate audit after modeling DataTable entry roots:
  - Result: 0 DataTable candidate violations.
- Lint self-test gate:
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed, 11 tests.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.

Next slice recommendation:

- Extend strict click-visibility to one more promoted long-page family, or move back to a
  runtime-visible component suite where lint already has zero false positives.

## F134: ScrollArea strict click visibility exposed debug-snapshot current-state drift

Layer: diagnostics authoring harness and UI diagnostics debug-snapshot predicates.

Status: observed and fixed.

What was found:

- The promoted `ui-gallery-scroll-area` suite had five long-page content actions that still used
  plain `click` after a visibility scroll. Those scripts were vulnerable to offscreen or moving
  target false failures when the Gallery content viewport changed.
- Enabling ScrollArea as a strict click-visibility suite then exposed a deeper diagnostics harness
  issue in `ui-gallery-scrollbar-drag-multipointer-underlay-touch.json`: current-state debug
  predicates such as `input_pointer_capture_active_is` could match stale entries from the recent
  snapshot ring instead of the latest snapshot.
- After the stale-ring fix, the same script exposed an authoring-model issue: immediate `assert`
  steps were checking cross-frame pointer-capture state before the next debug snapshot was reliably
  available. Failure bundles showed the current frame was correct, so the defect was in the
  diagnostics harness/authoring layer rather than ScrollArea's pointer-capture mechanism.

What was fixed:

- Replaced the ScrollArea long-page content `click` steps with `click_stable`, keeping existing
  `scroll_into_view(require_fully_within_window=true)` guards and adding a fresh reset visibility
  guard in the nested-scroll-routing script.
- Added `ui-gallery-scroll-area` to the strict click-visibility registry lint and taught the lint
  to treat `ui-gallery-scroll-area-*` and `ui-gallery-scrollbar-*` as long-page content targets.
- Restricted debug-snapshot history matching to aggregate predicates only; current-state predicates
  now read the latest debug snapshot.
- Changed the multi-pointer ScrollArea capture script to use `wait_until` for capture state
  convergence after pointer events.

Evidence anchors:

- `tools/check_diag_scripts_registry.py`
- `tools/test_check_diag_scripts_registry.py`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/debug_snapshot_predicates.rs`
- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scroll-area-expand-at-bottom.json`
- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scroll-area-nested-scroll-routing.json`
- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-baseline-content-growth.json`
- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-pointer-cancel-release.json`
- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-multipointer-underlay-touch.json`

Run results:

- Lint self-test gate:
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed, 13 tests.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- Focused debug-snapshot predicate gate:
  `cargo nextest run -p fret-bootstrap --features ui-app-driver,diagnostics current_state_predicates_do_not_match_stale_ring_snapshots input_pointer_capture_active_predicate_reads_debug_snapshot --no-fail-fast`
  - Result: passed, Nextest run id `70f41d32-7ce9-4d1d-9ea9-680d61f909d3`.
- Focused multi-pointer runtime gate after the fix:
  `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-multipointer-underlay-touch.json --dir target/fret-diag-scrollbar-drag-multipointer-underlay-touch-after-wait-until-v1 --session-auto --pack --ai-packet --include-screenshots --timeout-ms 360000 --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed, run id `1778955752839`.
  - AI packet:
    `target/fret-diag-scrollbar-drag-multipointer-underlay-touch-after-wait-until-v1/sessions/1778955749551-106960/1778955752839/ai.packet`
  - Share pack:
    `target/fret-diag-scrollbar-drag-multipointer-underlay-touch-after-wait-until-v1/sessions/1778955749551-106960/share/1778955752839.zip`
- Full ScrollArea suite:
  `target/debug/fretboard-dev.exe diag suite ui-gallery-scroll-area --dir target/fret-diag-scroll-area-suite-strict-click-v2 --session-auto --timeout-ms 360000 --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed, summary
    `target/fret-diag-scroll-area-suite-strict-click-v2/sessions/1778955771522-90268/suite.summary.json`.

Next slice recommendation:

- Continue strict authoring-lint expansion only for suites that already have clear page-entry and
  visibility models. For the next defect-finding slice, prefer a runtime-visible suite with
  structured state predicates so harness failures can separate mechanism defects from script
  timing defects.

## F135: Promoted pointer-state scripts now reject event-adjacent current-state asserts

Layer: diagnostics registry authoring harness.

Status: observed and fixed.

What was found:

- No new ScrollArea component or mechanism defect was reproduced in this slice.
- A promoted-registry audit found 6 adjacent pointer/current-state patterns. Three were already
  correct bounded `wait_until` convergence checks. Three were still immediate `assert` steps in
  `ui-gallery-scrollbar-drag-baseline-content-growth.json` and
  `ui-gallery-scrollbar-drag-pointer-cancel-release.json`.
- Those immediate asserts were harness debt: pointer-capture state is published through the latest
  debug snapshot, so a script should not treat the event-adjacent step as a guaranteed settled
  snapshot.

What was fixed:

- Converted the remaining ScrollArea pointer-capture lifecycle assertions to bounded `wait_until`
  checks for both active capture and owner attribution.
- Added a promoted-script registry lint that rejects pointer-event steps immediately followed by
  current-state `assert` predicates (`input_pointer_capture_active_is`, `captured_is`, and
  `dock_viewport_capture_active_is`).
- Added registry lint self-tests for the rejected immediate-assert shape and the accepted
  `wait_until` shape.

Evidence anchors:

- `tools/check_diag_scripts_registry.py`
- `tools/test_check_diag_scripts_registry.py`
- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-baseline-content-growth.json`
- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-pointer-cancel-release.json`
- `docs/workstreams/fret-mechanism-harness-v1/COVERAGE_MAP.md`
- `docs/workstreams/fret-mechanism-harness-v1/EVIDENCE_AND_GATES.md`

Run results:

- Lint self-test gate:
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed, 15 tests.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- Structured promoted-registry audit:
  - Result: `immediate assert violations=0`, `adjacent wait_until convergence patterns=6`.
- Focused runtime gates:
  `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-baseline-content-growth.json --dir target/fret-diag-scrollbar-drag-baseline-content-growth-wait-until-v1 --session-auto --pack --ai-packet --include-screenshots --timeout-ms 360000 --launch -- target/debug/fret-ui-gallery.exe`
  `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-pointer-cancel-release.json --dir target/fret-diag-scrollbar-drag-pointer-cancel-release-wait-until-v1 --session-auto --pack --ai-packet --include-screenshots --timeout-ms 360000 --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed with run ids `1778956466823` and `1778956486039`.
- Full ScrollArea suite:
  `target/debug/fretboard-dev.exe diag suite ui-gallery-scroll-area --dir target/fret-diag-scroll-area-suite-pointer-current-state-lint-v1 --session-auto --timeout-ms 360000 --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed, summary
    `target/fret-diag-scroll-area-suite-pointer-current-state-lint-v1/sessions/1778956501773-46724/suite.summary.json`.

Next slice recommendation:

- Move away from ScrollArea unless a fresh failure appears. The next useful defect-finding slice is
  a runtime-visible suite with structured state predicates, so failures can separate true mechanism
  defects from authoring or state-publication timing debt.

## F136: Select trigger semantics now expose current value separately from name

Layer: `fret-ui` semantics mechanism plus shadcn Select recipe semantics.

Status: observed and fixed; one separate layout warning remains.

What was found:

- The promoted `ui-gallery-select` suite passed, but lint surfaced repeated
  `semantics.missing_label` warnings on Select trigger nodes. The issue was not script flakiness:
  Select trigger semantics had an accessible-name gap whenever the trigger was not explicitly
  labelled by a field label or `.a11y_label(...)`.
- Composite triggers need two separate facts: the accessible name (`label`) and the current
  selected/displayed text (`value`). Fret's `PressableA11y` had no direct value channel, so the
  Select recipe could not express that distinction on the trigger itself.
- After the fix, `ui-gallery-select` no longer reports `semantics.missing_label`. The remaining
  lint warnings are `layout.zero_size` on `ui-gallery-select-label-trigger` and one known
  scrollable-list `layout.active_item_out_of_window` warning in the wheel-up-from-bottom case.

What was fixed:

- Added `PressableA11y.value` and wired it into host-widget semantics snapshots.
- Updated shadcn Select trigger semantics to publish the displayed placeholder/selected text as
  `value`.
- When the trigger has no explicit accessible name and no `labelled_by` relation, Select now uses
  the same visible trigger text as a fallback `label`.
- Added focused regressions for mechanism-level pressable `label`/`value` separation and Select
  trigger placeholder, selected-label, explicit-label, and raw-value-label-policy outcomes.

Evidence anchors:

- `crates/fret-ui/src/element.rs`
- `crates/fret-ui/src/declarative/host_widget/semantics.rs`
- `crates/fret-ui/src/declarative/tests/semantics.rs`
- `ecosystem/fret-ui-shadcn/src/select.rs`

Run results:

- Focused mechanism gate:
  `cargo test --profile dev-fast -p fret-ui --lib pressable_semantics_exposes_label_and_value_separately -- --nocapture`
  - Result: passed.
- Focused Select gate:
  `cargo test --profile dev-fast -p fret-ui-shadcn --lib select_trigger_exposes_accessible_name_and_current_value -- --nocapture`
  - Result: passed.
- Build gate:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- Full Select runtime suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-select --dir target/fret-diag-select-suite-trigger-value-semantics-v1 --session-auto --timeout-ms 900000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 14 rows.
  - Summary:
    `target/fret-diag-select-suite-trigger-value-semantics-v1/sessions/1778959377920-8012/suite.summary.json`.
  - Lint result:
    `error_issues=0`; `semantics.missing_label` no longer appears. Remaining warnings are
    `layout.zero_size` for `ui-gallery-select-label-trigger` and one
    `layout.active_item_out_of_window` warning in a scrollable Select wheel case.

Next slice recommendation:

- Continue with the `ui-gallery-select-label-trigger` zero-height warning. It is now isolated from
  the accessible-name defect and repeats across most Select scripts, so it is the next likely
  component/layout authoring issue to classify.

## F137: Tailwind flex-1 now uses percent basis semantics in Fret UI kit

Layer: `fret-ui-kit` layout utility policy backed by `fret-ui` layout mechanism fixture coverage.

Status: observed and fixed.

What was found:

- The remaining `ui-gallery-select-label-trigger` zero-size warning was not a Select-only
  accessibility or trigger semantics issue. The upstream shadcn `FieldContent` source uses
  `flex flex-1 flex-col gap-1.5 leading-snug`, while the Fret `LayoutRefinement::flex_1()` helper
  translated `flex-1` as a hard `0px` basis.
- CSS/Tailwind `flex-1` is `flex: 1 1 0%`, not `flex: 1 1 0px`. In an indefinite main axis, the
  percent basis behaves like content/auto, while a hard `0px` basis can collapse content-bearing
  vertical compositions and report zero-size descendants.
- This exposed a harness gap: the Select runtime gate could find the symptom, but the layout
  primitive harness did not yet lock the underlying auto-height vertical flex invariant.

What was fixed:

- Changed `LayoutRefinement::flex_1()` from `LengthRefinement::Px(0px)` to
  `LengthRefinement::Fraction(0.0)` so the utility matches Tailwind/CSS percent basis semantics.
- Added a `fret-ui-kit` unit test that locks the `flex-1` translation to `grow=1`, `shrink=1`,
  `basis=0%`.
- Added a `fret-ui` layout primitive fixture case for an auto-height vertical flex stack with a
  `grow/shrink + 0% basis` content item followed by a fixed-height sibling. The oracle proves the
  content item remains 10px high, the fixed sibling remains 36px high, and the parent stack resolves
  to content + gap + sibling height.

Evidence anchors:

- `ecosystem/fret-ui-kit/src/style/layout.rs`
- `ecosystem/fret-ui-kit/src/declarative/style.rs`
- `crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs`
- `crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
- `target/fret-diag-select-label-trigger-flex1-percent-v2/sessions/1778962354257-40124/1778962362747/ai.packet`
- `target/fret-diag-select-suite-flex1-percent-v1/sessions/1778962404441-107064/suite.summary.json`

Run results:

- Focused utility translation gate:
  `cargo test --profile dev-fast -p fret-ui-kit --lib layout_refinement_flex_1_uses_tailwind_percent_basis -- --nocapture`
  - Result: passed.
- Focused layout mechanism gate:
  `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_layout_primitives_match_oracles -- --nocapture`
  - Result: passed.
- Build gate:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- Focused Select label runtime gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/select/ui-gallery-select-label-click-focus.json --dir target/fret-diag-select-label-trigger-flex1-percent-v2 --session-auto --pack --ai-packet --include-triage --timeout-ms 240000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778962362747`.
  - AI packet reported `tooling_warnings=[]`; no `layout.zero_size` or layout dirty underflow text
    was found in the packet.
- Full Select runtime suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-select --dir target/fret-diag-select-suite-flex1-percent-v1 --session-auto --timeout-ms 900000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 14 rows.
  - Summary:
    `target/fret-diag-select-suite-flex1-percent-v1/sessions/1778962404441-107064/suite.summary.json`.

Residual finding:

- The full Select suite emitted `dispatch/chain: node missing from input snapshot` warnings during
  the typeahead case while still passing. This is a separate mechanism/diagnostics hardening
  candidate and should not be mixed into the `flex-1` fix.

Next slice recommendation:

- Classify the input-snapshot warning as the next Select mechanism slice: decide whether it is
  stale event-chain traversal after overlay/list rebuild, expected transient diagnostics noise, or
  a real input routing contract gap. Add a focused fixture or runtime gate once the owner layer is
  clear.

## F138: Timer dispatch now ignores stale targets on hidden overlay layers

Layer: `fret-ui` event dispatch mechanism.

Status: observed and fixed.

What was found:

- The `ui-gallery-select-typeahead-commit-banana` runtime gate passed visually, but emitted repeated
  `dispatch/chain: node missing from input snapshot` warnings when the Select typeahead clear timer
  fired after the listbox had closed.
- The warning exposed a real mechanism defect: targeted timer dispatch resolved a live attached
  element/node even when that node belonged to a hidden overlay layer. The timer dispatch context is
  intentionally built from visible layers so transition-visible, hit-test-inert layers can still
  receive timers. Hidden layers must not receive targeted timer dispatch.
- In the failing reduced mechanism case, a hidden overlay timer target received the timer and stopped
  propagation, preventing visible timer listeners from seeing the event. That made the issue more
  than logging noise.

What was fixed:

- Timer target resolution now verifies the resolved node is present in the timer dispatch snapshot
  before targeted dispatch. If it is not in the visible-layer snapshot, the target is treated as
  missing and the normal visible-layer broadcast path continues.
- Added a regression test where a hidden overlay owns the stale timer target and the visible base
  layer owns a timer listener. The hidden target no longer receives the timer, and the visible
  listener does.
- Kept the existing visible-but-hit-test-inert layer timer test passing, preserving the transition
  timer contract.

Evidence anchors:

- `crates/fret-ui/src/tree/dispatch/window.rs`
- `crates/fret-ui/src/tree/tests/timer_dispatch.rs`
- `target/fret-diag-select-typeahead-input-snapshot-after-hidden-timer-target-v2/sessions/1778963942817-47376/1778963951450/ai.packet`

Run results:

- Repro before fix:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/select/ui-gallery-select-typeahead-commit-banana.json --dir target/fret-diag-select-typeahead-input-snapshot-repro-v1 --session-auto --pack --ai-packet --include-triage --timeout-ms 300000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778963318187`, but emitted repeated
    `dispatch/chain: node missing from input snapshot` warnings.
- Focused mechanism gate:
  `cargo test --profile dev-fast -p fret-ui --lib timer_dispatch_ignores_target_on_hidden_overlay_layer -- --nocapture`
  - Result: failed before the fix, passed after the fix.
- Existing transition contract gate:
  `cargo test --profile dev-fast -p fret-ui --lib timer_dispatch_uses_visible_layer_snapshot_when_input_layers_are_empty -- --nocapture`
  - Result: passed.
- Existing live-target repair gate:
  `cargo test --profile dev-fast -p fret-ui --lib timer_dispatch_resolves_live_attached_element_target_over_stale_detached_seed -- --nocapture`
  - Result: passed.
- Build gate:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- Runtime regression gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/select/ui-gallery-select-typeahead-commit-banana.json --dir target/fret-diag-select-typeahead-input-snapshot-after-hidden-timer-target-v2 --session-auto --pack --ai-packet --include-triage --timeout-ms 300000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778963951450`.
  - AI packet reported `tooling_warnings=[]`; no `dispatch/chain`, `node missing from input
    snapshot`, `layout.zero_size`, or underflow text was found in the packet.

Next slice recommendation:

- Promote timer-target visibility semantics into a small fixture/harness matrix if more timer-heavy
  overlay families start showing stale-target symptoms: visible hit-test-inert transition layer,
  hidden layer, removed layer, stale element target repaired to visible node, and node-target timer
  on detached/hidden nodes.

## F139: Timer dispatch lifecycle matrix promoted to mechanism harness

Layer: `fret-ui` event dispatch mechanism.

Status: coverage gap closed; no new defect reproduced.

What was found:

- After F138, timer-target visibility behavior still lived mostly in focused procedural tests and a
  Select runtime repro. That made future timer lifecycle expansion harder to review case-by-case.
- The highest-risk uncovered matrix cells were visible base target, visible but hit-test-inert
  overlay target, hidden overlay target, and removed overlay target.

What was fixed:

- Added `timer_dispatch_v1.json` as a fixture-driven mechanism matrix.
- Added a thin `timer_dispatch_harness.rs` runner that records timer targets, dispatches timers,
  and exposes base/overlay hit counts as `mechanism_metric` values.
- Registered the harness in `crates/fret-ui/src/tree/tests/mod.rs`.

Evidence anchors:

- `crates/fret-ui/src/tree/tests/fixtures/timer_dispatch_v1.json`
- `crates/fret-ui/src/tree/tests/timer_dispatch_harness.rs`
- `crates/fret-ui/src/tree/tests/mod.rs`

Run results:

- Focused matrix gate:
  `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_timer_dispatch_matches_oracles -- --nocapture`
  - Result: passed.
- Focused timer family gate:
  `cargo test --profile dev-fast -p fret-ui --lib timer_dispatch -- --nocapture`
  - Result: passed, 7 tests.

Next slice recommendation:

- Continue outside timer dispatch unless a new stale node-target or repeat-timer defect appears.
  The current high-risk Select-discovered hidden-layer target path is now covered by both fixture
  and runtime evidence.

## F140: Command suite promoted into strict diagnostics authoring coverage

Layer: diagnostics harness authoring.

Status: harness gap found and fixed; no new Command component/mechanism defect reproduced.

What was found:

- The promoted `ui-gallery-command` suite was not covered by strict page-entry or long-page
  click-visibility lint even though the page is long and many scripts target page-local
  `ui-gallery-command-*` selectors.
- A candidate strict audit found 4 page-entry violations in the zinc-dark docs-demo screenshot
  script and 20 plain long-page content clicks across promoted Command scripts.
- A second strict pass exposed 6 subtler click-stability gaps where a nearby/root target had been
  made visible but the actual clicked target had no target-level window-visibility proof.
- The real Command runtime suite still passed after hardening, so this slice found diagnostics
  harness authoring debt rather than a Command recipe or `fret-ui` mechanism bug.

What was fixed:

- Added `ui-gallery-command` to strict page-entry and strict click-visibility registry gates.
- Added a Command page-entry rule for `ui-gallery-page-command` /
  `ui-gallery-command-component`, including `FRET_UI_GALLERY_START_PAGE=command`.
- Taught the click-visibility lint that `ensure_visible(within_window=true)` is a valid
  target-level visibility proof before `click_stable`.
- Converted promoted Command long-page content clicks from plain `click` to guarded
  `click_stable`, added missing target-level visibility guards, and added the missing Command page
  root wait to the zinc-dark docs-demo screenshot script.
- Added registry lint tests covering Command page-entry, start-page defaults, and
  `ensure_visible`-guarded stable clicks.

Evidence anchors:

- `tools/check_diag_scripts_registry.py`
- `tools/test_check_diag_scripts_registry.py`
- `tools/diag-scripts/suites/ui-gallery-command/suite.json`
- promoted Command scripts under `tools/diag-scripts/ui-gallery/command/`

Run results:

- Registry lint tests:
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed, 18 tests.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- Runtime Command suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-command --dir target/fret-diag-command-suite-strict-authoring-v1 --session-auto --timeout-ms 900000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 18/18 rows.
  - Summary:
    `target/fret-diag-command-suite-strict-authoring-v1/sessions/1778966586171-88944/suite.summary.json`
  - Evidence aggregate: `scripts_with_evidence=18`, `focus_mismatch_total=0`.

Next slice recommendation:

- Continue strict authoring expansion one promoted long-page family at a time. The next candidate
  should be a suite with stable page-entry semantics and a realistic risk of false mechanism
  triage if clicks or page-local selectors are under-specified.

## F141: DataTable runtime semantics lint defects fixed

Layer: `fret-ui-kit` table mechanism and `fret-ui-shadcn` DataTable recipe diagnostics surface.

Status: real defects found and fixed by the runtime evidence suite.

What was found:

- The `ui-gallery-shadcn-runtime-evidence` suite reached the DataTable pagination semantics gate and
  passed its runtime assertions, then failed suite lint on real semantics output.
- The lint found a duplicate `data-table-toolbar-column-filter-input` `test_id` across four
  same-page DataTable toolbar instances: default, basic, guide, and RTL.
- The same bundle reported 21 `semantics.missing_label` warnings. The meaningful mechanism issue
  was sortable table header pressables that rendered text children but did not export an accessible
  name on the button action owner. This is exactly the kind of visual-pass / semantics-fail issue a
  self-drawn UI harness must catch.

What was fixed:

- Added `DataTableToolbar::test_id_prefix(...)` so recipe-owned child ids can be scoped per toolbar
  instance while preserving the historical single-instance ids by default.
- Scoped UI Gallery DataTable toolbar ids for default, basic, guide, RTL, and torture surfaces.
- Updated retained/view-cache DataTable diagnostic scripts to target the prefixed torture toolbar
  ids.
- Added accessible labels to virtualized table sortable header pressables in both retained and
  non-retained table paths, including sorted-state wording.
- Added focused regression tests for prefixed DataTable toolbar inputs and sortable header button
  labels.

Evidence anchors:

- `ecosystem/fret-ui-shadcn/src/data_table_recipes.rs`
- `ecosystem/fret-ui-kit/src/declarative/table.rs`
- `ecosystem/fret-ui-shadcn/tests/data_table_toolbar_global_filter.rs`
- `apps/fret-ui-gallery/src/ui/snippets/data_table/default_demo.rs`
- `apps/fret-ui-gallery/src/ui/snippets/data_table/basic_demo.rs`
- `apps/fret-ui-gallery/src/ui/snippets/data_table/guide_demo.rs`
- `apps/fret-ui-gallery/src/ui/snippets/data_table/rtl_demo.rs`
- `apps/fret-ui-gallery/src/ui/previews/gallery/data/table_torture.rs`
- `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-retained-column-filter.json`
- `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-retained-global-filter.json`
- `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-retained-reset-filters.json`
- `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-retained-filter-shrink-vlist-inputs-change.json`
- `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-view-cache-filter-shrink-vlist-inputs-change.json`

Run results:

- Focused toolbar id gate:
  `cargo test --profile dev-fast -p fret-ui-shadcn --test data_table_toolbar_global_filter data_table_toolbar_test_id_prefix_scopes_owned_inputs -- --nocapture`
  - Result: passed.
- Focused non-retained table semantics gate:
  `cargo test --profile dev-fast -p fret-ui-kit --lib table_virtualized_sort_header_button_exposes_accessible_label -- --nocapture`
  - Result: passed.
- Focused retained table semantics gate:
  `cargo test --profile dev-fast -p fret-ui-kit --lib table_virtualized_retained_header_debug_ids_click_sort_actions -- --nocapture`
  - Result: passed.
- Build gate:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- Focused DataTable runtime gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-default-pagination-collection-metadata.json --dir target/fret-diag-data-table-default-pagination-lint-fix-v2 --session-auto --pack --ai-packet --include-triage --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778969776320`.
- Runtime evidence suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-shadcn-runtime-evidence --dir target/fret-diag-shadcn-runtime-evidence-after-datatable-lint-v1 --session-auto --timeout-ms 900000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 10/10 rows.
  - Summary:
    `target/fret-diag-shadcn-runtime-evidence-after-datatable-lint-v1/sessions/1778969813032-104196/suite.summary.json`
  - Evidence aggregate: `scripts_with_evidence=10`, `focus_mismatch_total=0`.

Next slice recommendation:

- Continue with another runtime evidence suite where visual behavior can pass while diagnostics
  lint or semantics/action metadata can still fail. Good candidates are promoted overlay/menu
  families with multiple same-page instances, because they stress duplicate ids, labels, relation
  edges, and hidden subtree filtering at the same time.

## F142: Time-sensitive diag scripts need a fixed frame clock

Layer: diagnostics harness authoring policy.

Status: harness defect found and fixed; HoverCard component policy was not implicated by this
slice.

What was found:

- The `ui-gallery-hover-card` suite initially failed the `trigger-delays` script at the delayed
  content assertion.
- The evidence showed the script used a short `wait_frames` interval to stand for a Radix-style
  millisecond delay contract, but the native runner spent roughly hundreds of wall-clock
  milliseconds between those frame-counted steps under the suite launch conditions.
- That means the failure was a false component signal: the script had no deterministic frame clock,
  so `wait_frames=2` did not reliably mean "before the close delay".

What was fixed:

- Added `FRET_DIAG_FIXED_FRAME_DELTA_MS=16` `meta.env_defaults` to promoted scripts whose names
  declare frame-time-sensitive contracts (`fixed-frame-delta` and `trigger-delays`).
- Added a registry lint that rejects future promoted frame-time-sensitive scripts without the fixed
  frame delta environment default.
- Added registry self-tests covering missing fixed-frame metadata, accepted fixed-frame metadata,
  and the HoverCard `trigger-delays` naming pattern.

Evidence anchors:

- `tools/check_diag_scripts_registry.py`
- `tools/test_check_diag_scripts_registry.py`
- `tools/diag-scripts/ui-gallery/hover-card/ui-gallery-hover-card-trigger-delays.json`
- promoted fixed-frame-delta scripts under `tools/diag-scripts/ui-gallery/` and
  `tools/diag-scripts/docking/demo/`

Run results:

- Registry lint tests:
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed, 21 tests.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- Focused HoverCard delay runtime gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/hover-card/ui-gallery-hover-card-trigger-delays.json --dir target/fret-diag-hover-card-trigger-delays-fixed-delta-v1 --session-auto --pack --ai-packet --include-triage --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778971779541`.
  - Evidence:
    `target/fret-diag-hover-card-trigger-delays-fixed-delta-v1/sessions/1778971771145-113412/1778971779541/ai.packet`
  - Launch env proof:
    `explicit_overrides_fret=[FRET_DIAG_FIXED_FRAME_DELTA_MS,FRET_DIAG_REDACT_TEXT,FRET_DIAG_RENDERER_PERF]`.
- HoverCard suite follow-up:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-hover-card --dir target/fret-diag-hover-card-fixed-delta-suite-v1 --session-auto --timeout-ms 600000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: failed, but not on the fixed delay case. The first three rows passed, including
    `ui-gallery-hover-card-trigger-delays.json`; `ui-gallery-hover-card-sides-placement.json`
    failed at step 20 with `move_pointer_no_semantics_match` because the script used
    `ui-gallery-status-last-action` as a leave target and that node was absent from the HoverCard
    page snapshot.
  - Summary:
    `target/fret-diag-hover-card-fixed-delta-suite-v1/sessions/1778971816718-107892/suite.summary.json`

Next slice recommendation:

- Repair the HoverCard `sides-placement` leave-target authoring issue, then rerun the HoverCard
  suite. If the placement script then fails past selector resolution, triage it as a real overlay
  placement or HoverCard policy candidate.

## F143: HoverCard sides placement oracle now matches collision geometry

Layer: diagnostics harness authoring policy and overlay placement evidence.

Status: harness oracle defect found and fixed; no HoverCard component or placement mechanism defect
was confirmed.

What was found:

- After the fixed-frame repair, the `ui-gallery-hover-card` suite advanced to
  `ui-gallery-hover-card-sides-placement.json` and failed at step 20 with
  `move_pointer_no_semantics_match`.
- The script used `ui-gallery-status-last-action` as a pointer-leave target, but the failed
  snapshot did not expose that node. The same suite already had a stable leave target:
  `ui-gallery-nav-search`.
- Once the leave target was fixed, the focused script progressed to real overlay placement
  checking and exposed a second harness oracle defect: the bottom side case expected
  `chosen_side=bottom`, but the actual trigger was near the bottom of the 1280x980 window. The
  trace showed only ~43px of preferred bottom space for a 120px panel, so the correct collision
  behavior is `preferred_side=bottom`, `chosen_side=top`, `flipped=true`.

What was fixed:

- Replaced the unstable leave target with `ui-gallery-nav-search`.
- Changed the bottom placement oracle to explicitly assert the collision flip to top.
- Left the left/top/right side assertions intact, so the script still checks preferred-side
  placement where the geometry supports it and checks flip where the geometry demands it.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/hover-card/ui-gallery-hover-card-sides-placement.json`
- failed suite summary before the fix:
  `target/fret-diag-hover-card-fixed-delta-suite-v1/sessions/1778971816718-107892/suite.summary.json`
- focused fixed run:
  `target/fret-diag-hover-card-sides-placement-flip-oracle-v1/sessions/1778972693726-90000/1778972701843/ai.packet`
- passing suite summary:
  `target/fret-diag-hover-card-suite-after-sides-placement-oracle-v1/sessions/1778972760323-23244/suite.summary.json`

Run results:

- Focused HoverCard sides placement gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/hover-card/ui-gallery-hover-card-sides-placement.json --dir target/fret-diag-hover-card-sides-placement-flip-oracle-v1 --session-auto --pack --ai-packet --include-triage --include-screenshots --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778972701843`.
- HoverCard suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-hover-card --dir target/fret-diag-hover-card-suite-after-sides-placement-oracle-v1 --session-auto --timeout-ms 600000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 6/6 rows.
  - Evidence aggregate:
    `scripts_with_evidence=6`, `focus_mismatch_total=0`,
    `overlay_chosen_side_counts={left:3,right:1,top:5}`.

Next slice recommendation:

- Continue with another overlay/menu family where script geometry and collision oracle quality can
  hide real component failures. Prioritize scripts that mix long-page scrolling, overlay placement,
  and pointer-leave behavior.

## F144: Menubar submenu placement promoted to a focused suite

Layer: diagnostics harness runtime gate coverage.

Status: coverage gap closed; no new Menubar recipe or overlay mechanism defect was reproduced.

What was found:

- Menubar submenu placement already had three useful runtime scripts: LTR submenu opens to the
  physical right, RTL wide submenu opens to the physical left, and RTL tight-left collision flips
  back to the physical right.
- Those scripts were only reachable through broader suites, so the placement sub-axis did not have
  a small durable gate that could be run independently after overlay placement changes.

What was fixed:

- Added `tools/diag-scripts/suites/ui-gallery-menubar-placement/suite.json`.
- Regenerated `tools/diag-scripts/index.json` so the three Menubar placement scripts now have
  `ui-gallery-menubar-placement` suite membership.
- Updated the coverage map to point at the focused suite as the runtime gate for the Menubar
  placement sub-axis.

Evidence anchors:

- `tools/diag-scripts/suites/ui-gallery-menubar-placement/suite.json`
- `tools/diag-scripts/ui-gallery/menubar/ui-gallery-menubar-submenu-placement-trace.json`
- `tools/diag-scripts/ui-gallery/menubar/ui-gallery-menubar-rtl-submenu-placement-trace.json`
- `tools/diag-scripts/ui-gallery/menubar/ui-gallery-menubar-rtl-submenu-tight-left-collision.json`
- `target/fret-diag-menubar-placement-suite-v1/sessions/1778973313432-109176/suite.summary.json`

Run results:

- Registry lint tests:
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed, 21 tests.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- Menubar placement suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-menubar-placement --dir target/fret-diag-menubar-placement-suite-v1 --session-auto --timeout-ms 600000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 3/3 rows.
  - Lint: all three rows report `error_issues=0`, `warning_issues=0`.
  - Evidence aggregate:
    `scripts_with_evidence=3`, `focus_mismatch_total=0`.

Next slice recommendation:

- Continue with another overlay/menu placement family that currently lacks a focused suite. Good
  candidates are DropdownMenu submenu placement and ContextMenu safe-corridor placement, because
  they stress different trigger ownership and pointer-route policies than Menubar.

## F145: DropdownMenu focused suite exposed harness authoring/oracle gaps

Layer: diagnostics harness authoring policy and runtime gate packaging.

Status: harness defects found and fixed; no new DropdownMenu recipe or overlay mechanism defect was
confirmed.

What was found:

- The new `ui-gallery-dropdown-menu` suite initially failed the Basic typeahead script before it
  could prove a DropdownMenu behavior. The original script clicked the `.chrome` decoration node,
  then the first repair clicked the action owner but allowed the trigger to remain near the window
  bottom edge, where `click_stable` could wait until tooling timeout.
- After the visibility precondition was fixed, the script reached the intended interaction and
  proved `b` typeahead plus Enter dispatched the Billing command. The remaining assertion still
  failed because it used `ui-gallery-status-last-action`, an optional status-bar semantics node, as
  the outcome oracle. The app snapshot already had `/shell/last_action = "menu.dropdown.orange"`.

What was fixed:

- Added a focused `ui-gallery-dropdown-menu` suite covering submenu placement, Basic typeahead
  commit, and disabled-but-focusable keyboard suppression.
- Hardened the Basic typeahead script to require the trigger fully within both the Gallery scroll
  container and window before `click_stable`.
- Switched the typeahead commit oracle from an optional status-bar `test_id` label assertion to the
  stable UI Gallery app snapshot field `/shell/last_action`.

Evidence anchors:

- `tools/diag-scripts/suites/ui-gallery-dropdown-menu/suite.json`
- `tools/diag-scripts/ui-gallery/dropdown-menu/ui-gallery-dropdown-menu-basic-typeahead-billing.json`
- `tools/diag-scripts/ui-gallery/dropdown-menu/ui-gallery-dropdown-menu-submenu-open-smoke.json`
- `tools/diag-scripts/ui-gallery/dropdown-menu/ui-gallery-dropdown-menu-focusable-disabled-keyboard-suppression.json`
- failed precondition evidence:
  `target/fret-diag-dropdown-typeahead-action-owner-v1/sessions/1778973863634-110500/script.result.json`
- failed optional-status oracle evidence:
  `target/fret-diag-dropdown-typeahead-visible-trigger-v2/sessions/1778974813651-119448/script.result.json`
- passing focused typeahead evidence:
  `target/fret-diag-dropdown-typeahead-app-snapshot-v1/sessions/1778974958084-114932/1778974969846/ai.packet`
- passing suite summary:
  `target/fret-diag-dropdown-menu-suite-v2/sessions/1778975019728-11100/suite.summary.json`

Run results:

- Build gate:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- Focused DropdownMenu typeahead gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/dropdown-menu/ui-gallery-dropdown-menu-basic-typeahead-billing.json --dir target/fret-diag-dropdown-typeahead-app-snapshot-v1 --session-auto --pack --ai-packet --include-triage --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778974969846`.
- DropdownMenu suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-dropdown-menu --dir target/fret-diag-dropdown-menu-suite-v2 --session-auto --timeout-ms 600000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 3/3 rows.
  - Lint: all three rows report `error_issues=0`, `warning_issues=0`.
  - Evidence aggregate: `scripts_with_evidence=3`, `focus_mismatch_total=0`.

Next slice recommendation:

- Promote the next small overlay/menu suite only when it adds an axis not already covered by
  DropdownMenu/Menubar. ContextMenu safe-corridor is the best candidate because it stresses pointer
  movement policy rather than keyboard typeahead or submenu placement alone.

## F146: ContextMenu corridor evidence promoted to a focused suite

Layer: diagnostics harness runtime gate packaging.

Status: coverage gap closed; no new ContextMenu recipe or hit-test mechanism defect was
reproduced.

What was found:

- ContextMenu already had two high-value runtime scripts: a safe-corridor sweep that proves moving
  toward submenu content keeps the nested submenu open, and a branch/corridor routing script that
  proves enter/return/away movement policy.
- Those scripts were only reachable through broad suites, so pointer-policy regressions did not
  have a small family-specific gate comparable to the new DropdownMenu and Menubar suites.

What was fixed:

- Added `tools/diag-scripts/suites/ui-gallery-context-menu/suite.json`.
- Regenerated `tools/diag-scripts/index.json` so both ContextMenu corridor scripts now have
  `ui-gallery-context-menu` suite membership.
- Updated the workstream TODO and evidence notes to record the focused suite and the actual run.

Evidence anchors:

- `tools/diag-scripts/suites/ui-gallery-context-menu/suite.json`
- `tools/diag-scripts/ui-gallery/context-menu/ui-gallery-context-menu-submenu-safe-corridor-sweep.json`
- `tools/diag-scripts/ui-gallery/context-menu/ui-gallery-context-menu-submenu-branch-corridor-routing.json`
- `target/fret-diag-context-menu-suite-v1/sessions/1778975671673-115352/suite.summary.json`

Run results:

- Registry lint tests:
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed, 21 tests.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- ContextMenu focused suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-context-menu --dir target/fret-diag-context-menu-suite-v1 --session-auto --timeout-ms 600000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 2/2 rows.
  - Lint: both rows report `error_issues=0`, `warning_issues=0`.

Next slice recommendation:

- Move away from overlay menu packaging now that Menubar, DropdownMenu, and ContextMenu have small
  focused suites. The next higher-value slice is another mechanism axis where existing broad
  diagnostics could hide real issues, such as strict authoring coverage for another long-page
  family or a runtime gate that exercises cached overlay source movement across viewport roots if
  a real UI Gallery surface can expose it.

## F147: Overlay/listbox ownership slice closed for v1

Layer: mechanism/runtime ownership coverage.

Status: v1 TODO closed; no new defect was reproduced in the closeout rerun.

What was found:

- The overlay/listbox placement ownership TODO was still open even though its sub-axes were already
  covered: Select item-aligned resize-close, Combobox anchored resize-reposition,
  scroll-container clipping plus RTL ownership, Dialog modal/root-boundary ownership, synthetic
  cross-root coordinate conversion, and the Resizable multi-viewport runtime companion.
- This slice had already found a real mechanism defect earlier: effective root-boundary placement
  used the OS window/owner root instead of the source element's nearest layout viewport root. The
  fixed runtime trace now flips the Combobox popover to `top` against the Resizable panel viewport.

What was fixed:

- Closed the overlay/listbox ownership TODO for v1 rather than adding another redundant menu or
  overlay script.
- Recorded a fresh `ui-gallery-resizable` suite rerun as the closeout evidence.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/resizable/ui-gallery-resizable-multi-viewport-combobox-placement.json`
- `tools/diag-scripts/suites/ui-gallery-resizable/suite.json`
- `target/fret-diag-resizable-suite-ownership-close-v1/sessions/1778976095291-118960/suite.summary.json`

Run results:

- Resizable ownership suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-resizable --dir target/fret-diag-resizable-suite-ownership-close-v1 --session-auto --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 1/1 row.
  - Lint: `error_issues=0`, `warning_issues=0`.

Next slice recommendation:

- Do not add more overlay/listbox ownership scripts unless a real UI Gallery surface can move a
  cached overlay source across viewport roots. Continue with a different mechanism axis, preferably
  either RTL/writing-mode layout primitives after the contract is clarified or cached
  model/layout-query mutation if a real non-synthetic surface exists.

## F148: ViewCache runtime companion found Textarea resize-grip semantics drift

Layer: UI Gallery runtime diagnostics plus shadcn Textarea recipe semantics.

Status: runtime companion added; component semantics defect found and fixed; no ViewCache
mechanism defect reproduced.

What was found:

- The ViewCache page had a runtime observability gap: scripts could click visible controls, but
  there was no structured `/view_cache` app-snapshot payload proving cached-subtree model state.
- The new `ui-gallery-view-cache` suite successfully proved counter mutation and Popover
  open/close state while nested ViewCache was enabled.
- The first strict suite run failed on diagnostics lint rather than ViewCache behavior:
  `semantics.missing_label` pointed at the shadcn Textarea resize grip. The grip is a pointer-only
  resize affordance, but it was exported as a visible Button with `invoke=true` and no accessible
  name.

What was fixed:

- Registered ViewCache page models in the UI Gallery diagnostics driver and published a dedicated
  `/view_cache` snapshot with enabled/cache-shell/cache-content/inner-enabled/popover/counter
  state.
- Added `ui-gallery-view-cache-model-mutation-through-cache.json` and the focused
  `ui-gallery-view-cache` strict-lint suite.
- Extended the registry lint so the new suite must prove ViewCache page entry and target
  visibility before page-local clicks.
- Changed the Textarea resize grip to be non-focusable and hidden from the visible accessibility
  tree, while leaving the TextArea label visible. Added a focused semantics snapshot test for that
  outcome.

Evidence anchors:

- `apps/fret-ui-gallery/src/driver/runtime_driver.rs`
- `apps/fret-ui-gallery/src/driver/window_bootstrap.rs`
- `apps/fret-ui-gallery/src/driver/diag_snapshot.rs`
- `ecosystem/fret-ui-shadcn/src/textarea.rs`
- `tools/check_diag_scripts_registry.py`
- `tools/diag-scripts/ui-gallery/view-cache/ui-gallery-view-cache-model-mutation-through-cache.json`
- `tools/diag-scripts/suites/ui-gallery-view-cache/suite.json`
- `target/fret-diag-view-cache-model-mutation-v2/sessions/1778978131681-113548/suite.summary.json`

Run results:

- Focused Textarea semantics gate:
  `cargo test --profile dev-fast -p fret-ui-shadcn --lib textarea_resize_handle_stays_out_of_visible_accessibility_tree -- --nocapture`
  - Result: passed.
- JSON schema sanity:
  `python -m json.tool tools/diag-scripts/ui-gallery/view-cache/ui-gallery-view-cache-model-mutation-through-cache.json`
  `python -m json.tool tools/diag-scripts/suites/ui-gallery-view-cache/suite.json`
  - Result: passed.
- Registry lint gates:
  `python tools/test_check_diag_scripts_registry.py`
  `python tools/check_diag_scripts_registry.py`
  - Result: passed; registry self-tests ran 21 tests.
- Build gate:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- ViewCache runtime suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-view-cache --dir target/fret-diag-view-cache-model-mutation-v2 --session-auto --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 1/1 row.
  - Lint: `error_issues=0`, `warning_issues=0`.

Next slice recommendation:

- Continue with another runtime-visible component semantics slice rather than adding more
  ViewCache coverage immediately. The ViewCache gate is now proving cached model mutation, and the
  useful new signal came from strict semantics lint catching a component recipe defect.

## F149: Button Group suite promoted to zero-warning diagnostics lint

Layer: UI Gallery Button Group diagnostics harness.

Status: strict lint gate promoted; no new Button Group component or mechanism defect reproduced.

What was found:

- `ui-gallery-button-group` already covered the originally risky Button Group visual/layout axes:
  docs screenshots, icon/text alignment, size variants, separator text, Input Group geometry,
  long text, RTL addon order, file/input fill behavior, accessibility examples, and Select
  examples.
- The suite did not yet declare `lint_policy.max_warning_issues=0`, so future missing-label,
  duplicate-id, zero-size, or related diagnostics lint drift could pass unless another broad suite
  happened to catch it.
- A full candidate run passed all 13 scripts with `focus_mismatch_total=0` and zero lint
  errors/warnings, so the suite is ready to become a strict family gate.

What was fixed:

- Added a zero-warning lint policy to `tools/diag-scripts/suites/ui-gallery-button-group/suite.json`.
- Kept this as a harness-strengthening change only; no component or runtime behavior changed.

Evidence anchors:

- `tools/diag-scripts/suites/ui-gallery-button-group/suite.json`
- `target/fret-diag-button-group-strict-candidate-v1/sessions/1778978465500-116384/suite.summary.json`

Run results:

- Candidate Button Group suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-button-group --dir target/fret-diag-button-group-strict-candidate-v1 --session-auto --timeout-ms 900000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 13/13 rows.
  - Lint: every row reported `error_issues=0`, `warning_issues=0`.
  - Evidence aggregate: `scripts_with_evidence=13`, `focus_mismatch_total=0`.
- Registry lint gates:
  `python tools/test_check_diag_scripts_registry.py`
  `python tools/check_diag_scripts_registry.py`
  - Result: passed; registry self-tests ran 21 tests.

Next slice recommendation:

- Continue with another component-family suite that already has runtime breadth but lacks strict
  lint, or run a broad suite until it exposes the next real semantics/layout defect. Button Group
  itself should now be treated as guarded unless a fresh visual report appears.

## F150: Carousel embla-engine suite promoted as the durable runtime evidence unit

Layer: UI Gallery Carousel diagnostics harness packaging.

Status: focused strict lint gate promoted; no new Carousel mechanism or recipe defect reproduced.

What was found:

- `ui-gallery-carousel-docs-parity` is too wide to be a good single evidence unit. A candidate run
  exceeded the outer command timeout before writing a normal `suite.summary.json`, even though the
  completed rows had clean lint output.
- The stopped candidate left a `running` session state at run id `1778980769233` rather than a
  usable suite summary. This is a harness packaging signal: a gate that cannot reliably summarize
  under normal timeouts is too broad for day-to-day defect discovery.
- The suspected long autoplay stop-on-last-snap script passed independently, so no Carousel
  mechanism defect was confirmed from the timeout.
- The compact `ui-gallery-carousel-embla-engine` sub-suite passed 5/5 with zero lint
  errors/warnings and no focus mismatch, making it the right unit to promote first.

What was fixed:

- Added `lint_policy.max_warning_issues=0` to
  `tools/diag-scripts/suites/ui-gallery-carousel-embla-engine/suite.json`.
- Added Carousel runtime evidence to the workstream coverage map, explicitly treating the
  embla-engine suite as the durable evidence unit for inertia, touch, resize reInit, loop
  continuity, and loop downgrade behavior.
- Left the wide docs-parity suite unpromoted for strict lint until it is split into smaller
  screenshot/state sub-suites or otherwise made to summarize reliably.

Evidence anchors:

- `tools/diag-scripts/suites/ui-gallery-carousel-embla-engine/suite.json`
- `target/fret-diag-carousel-docs-parity-strict-candidate-v1/sessions/1778979513835-124280/script.result.json`
- `target/fret-diag-carousel-last-snap-candidate-v1/sessions/1778981137600-116524/1778981149388/ai.packet`
- `target/fret-diag-carousel-embla-engine-strict-v1/sessions/1778982359710-115076/suite.summary.json`
- `docs/workstreams/fret-mechanism-harness-v1/COVERAGE_MAP.md`

Run results:

- Wide docs-parity candidate:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-carousel-docs-parity --dir target/fret-diag-carousel-docs-parity-strict-candidate-v1 --session-auto --timeout-ms 1200000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: outer command timeout before normal suite summary.
  - Follow-up: stopped the launched diagnostics processes and narrowed the slice.
- Focused autoplay stop-on-last-snap gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/carousel/ui-gallery-carousel-plugin-autoplay-stop-on-last-snap-gate.json --dir target/fret-diag-carousel-last-snap-candidate-v1 --session-auto --pack --ai-packet --include-triage --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778981149388`.
- Carousel embla-engine suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-carousel-embla-engine --dir target/fret-diag-carousel-embla-engine-strict-v1 --session-auto --timeout-ms 900000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 5/5 rows.
  - Lint: every row reported `error_issues=0`, `warning_issues=0`.
  - Evidence aggregate: `scripts_with_evidence=5`, `focus_mismatch_total=0`.

Next slice recommendation:

- Split `ui-gallery-carousel-docs-parity` into smaller strict screenshot/state suites before
  trying to promote it as a zero-warning gate. Continue defect discovery with compact runtime
  families where a normal `suite.summary.json` is guaranteed under routine timeouts.

## F151: Date Picker strict suite exposed scroll/script precondition gaps

Layer: diagnostics harness and UI Gallery Date Picker script authoring.

Status: harness defects fixed; no Date Picker component or layout mechanism defect reproduced.

What was found:

- The mobile Drawer script used a 480px window to force the Date Picker mobile branch, but the UI
  Gallery shell still kept its desktop sidebar visible. That left the content viewport at roughly
  247px wide, so fully containing the 240px trigger with the requested padding was geometrically
  impossible.
- `scroll_into_view` reported that situation as generic `scroll_into_view_stuck_no_progress`,
  which was not actionable enough for future agents: the real issue was an unscrollable horizontal
  containment requirement.
- The range-roving script directly clicked an offscreen trigger and depended on suite-injected
  env vars, so a focused run could fail before opening the calendar even though the Date Picker
  behavior under test was unrelated.

What was fixed:

- Added explicit `scroll_into_view_impossible_unscrollable_x/y_for_container/window` reason codes
  and diagnostics labels.
- Changed the mobile Drawer script to 740px wide, which still exercises the component's mobile
  branch below `md=768` while giving the Gallery content viewport enough room for the trigger.
- Added `require_fully_within_window=true` to the mobile Drawer scroll guard.
- Made the range-roving script independently runnable by adding env defaults, scrolling the range
  trigger into view, and using `click_stable`.
- Promoted the Date Picker suite to zero-warning lint policy after the repaired suite passed.

Evidence anchors:

- `ecosystem/fret-bootstrap/src/ui_diagnostics/script_steps_scroll.rs`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/labels.rs`
- `tools/diag-scripts/ui-gallery/date-picker/ui-gallery-date-picker-dropdowns-mobile-drawer.json`
- `tools/diag-scripts/ui-gallery/date-picker/ui-gallery-date-picker-range-roving-skips-disabled.json`
- `tools/diag-scripts/suites/ui-gallery-date-picker/suite.json`
- `target/fret-diag-date-picker-strict-v3/sessions/1778986003617-126604/suite.summary.json`

Run results:

- Focused harness gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-bootstrap --features ui-app-driver,diagnostics scroll_unscrollable_axis --no-fail-fast`
  - Result: passed, Nextest run id `007a4bef-f826-4667-89bd-1a7cd5d41b12`.
- Focused mobile Drawer gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/date-picker/ui-gallery-date-picker-dropdowns-mobile-drawer.json --dir target/fret-diag-date-picker-mobile-drawer-fix-v1 --session-auto --pack --ai-packet --include-triage --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778984155994`.
- Focused range-roving gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/date-picker/ui-gallery-date-picker-range-roving-skips-disabled.json --dir target/fret-diag-date-picker-range-roving-scroll-fix-v2 --session-auto --pack --ai-packet --include-triage --timeout-ms 420000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778985968416`.
- Full Date Picker suite:
  `target/dev-fast/fretboard-dev.exe diag suite ui-gallery-date-picker --dir target/fret-diag-date-picker-strict-v3 --session-auto --timeout-ms 900000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, 4/4 rows.
- Registry gates:
  `python tools/test_check_diag_scripts_registry.py`
  `python tools/check_diag_scripts_registry.py`
  - Result: passed; registry self-tests ran 21 tests.

Next slice recommendation:

- Continue with a different runtime family rather than adding redundant Date Picker checks. The
  next useful harness improvement is a compact visual-geometry/placement family where screenshots
  or sidecars can reveal layout drift without waiting for user reports.
