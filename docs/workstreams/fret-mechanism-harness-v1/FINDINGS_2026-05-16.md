---
title: Fret Mechanism Harness Findings - 2026-05-16
status: active
date: 2026-05-16
---

# Findings 2026-05-16

## F109: Cross-root anchored fixture found a recipe-layer root-boundary gap

Layer: `fret-ui` mechanism harness, `fret-ui-kit` overlay policy helpers, and shadcn anchored
overlay recipes.

Status: fixed for the synthetic and focused recipe gates. The remaining uncovered surface is a
runtime UI Gallery or focused diagnostics gate that exercises a real secondary viewport or embedded
root.

What was found:

- The new `anchored_cross_root_coordinate_v1.json` fixture proved that core `AnchoredProps` can
  resolve an anchor element from another render root and place against a non-window overlay/root
  boundary. No core `fret-ui` coordinate-conversion defect was reproduced by this synthetic slice.
- The fixture schema rejected the proposed `root-boundary` domain label, so the suite now uses the
  existing `overlay` and `layout` domains.
- A real policy-layer gap was found in `fret-ui-kit::overlay`: the public anchored placement helpers
  documented root-local placement but still derived the collision/clamp outer rect from
  `environment_viewport_bounds`. That is harmless for full-window roots, but it is wrong for
  embedded or secondary render roots where the owner/source root is not the OS window.
- The same pattern existed across shadcn anchored overlay families. Popover, Select, Tooltip,
  HoverCard, DropdownMenu, ContextMenu, and Menubar were relying on the environment viewport for
  anchored collision boundaries rather than the current render root bounds.

What was fixed:

- Added `root_bounds_for_placement` and `outer_bounds_with_window_margin_for_root` to
  `ecosystem/fret-ui-kit/src/overlay.rs`.
- Updated `anchored_panel_bounds_for_element` and `popper_layout_for_element` to use current render
  root bounds while still observing the environment viewport for metric invalidation.
- Switched shadcn Popover, Select, Tooltip, HoverCard, DropdownMenu, ContextMenu, and Menubar
  anchored placement paths to the root-boundary helper.
- Added a focused `fret-ui-kit` gate proving the root-boundary helper uses `ElementContext::bounds`
  for a non-zero root origin.
- Added the `fret-ui` synthetic fixture and harness for cross-root anchor/overlay coordinate
  conversion.

Run results:

- JSON fixture check:
  `python -m json.tool crates/fret-ui/src/declarative/tests/fixtures/anchored_cross_root_coordinate_v1.json`
  - Result: passed.
- Synthetic mechanism gate:
  `cargo test -p fret-ui --lib mechanism_harness_anchored_cross_root_coordinate_matches_oracles -- --nocapture`
  - Result: passed, 1 test.
- UI kit root-boundary gates:
  `cargo nextest run -p fret-ui-kit outer_bounds_with_window_margin --no-fail-fast`
  - Result: passed, 2 tests.
  - Nextest run id: `18deeb53-0fd8-4b25-8df3-af61212780e9`.
- shadcn compile/placement smoke:
  `$env:CARGO_BUILD_JOBS='1'; cargo test -p fret-ui-shadcn --lib popover_first_open_placement_size_prefers_explicit_hint -- --nocapture`
  - Result: passed, 1 test. This also rebuilt `fret-ui-shadcn` after the broader recipe edits.
- shadcn representative focused gates:
  - `hover_card_anchor_override_uses_anchor_bounds_for_placement`: passed.
  - `tooltip_anchor_override_uses_anchor_bounds_for_placement`: passed.
  - `dropdown_menu_portal_escapes_overflow_clip_ancestor`: passed.
  - `context_menu_submenu_keyboard_open_transfers_focus_and_arrow_left_restores_focus`: passed.
  - `menubar_submenu_opens_on_arrow_right_and_closes_on_arrow_left_restoring_focus`: passed.
- Formatting gate:
  `rustfmt --edition 2024 ... --check`
  - Result: passed for touched Rust files.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/fixtures/anchored_cross_root_coordinate_v1.json`
- `crates/fret-ui/src/declarative/tests/anchored_cross_root_coordinate_harness.rs`
- `ecosystem/fret-ui-kit/src/overlay.rs`
- `ecosystem/fret-ui-shadcn/src/popover.rs`
- `ecosystem/fret-ui-shadcn/src/select.rs`
- `ecosystem/fret-ui-shadcn/src/tooltip.rs`
- `ecosystem/fret-ui-shadcn/src/hover_card.rs`
- `ecosystem/fret-ui-shadcn/src/dropdown_menu.rs`
- `ecosystem/fret-ui-shadcn/src/context_menu.rs`
- `ecosystem/fret-ui-shadcn/src/menubar.rs`

Next slice recommendation:

- Add a runtime multi-viewport ownership companion. The gate should mount an anchored overlay source
  in a secondary viewport or embedded root, assert placement trace/root boundary, relation edges,
  and hit-tested selection, then capture layout sidecar evidence. This is the missing real-runtime
  proof for the policy gap fixed here.

## F110: Runtime multi-viewport Combobox gate found a mechanism-layer root-boundary cache gap

Layer: `fret-ui` layout/runtime cache, `fret-ui-kit` overlay policy helpers, shadcn anchored overlay
recipes, and UI Gallery diagnostics.

Status: fixed for the resizable multi-viewport Combobox runtime gate.

What was found:

- The new UI Gallery Resizable multi-viewport Combobox gate reproduced the remaining high-risk
  runtime gap from F109: the trigger lived near the bottom of a Resizable panel viewport root, the
  OS window still had room below it, and correct placement had to flip against the panel viewport
  rather than the window.
- The first run failed at `wait_overlay_placement_trace`: the trace chose `bottom`, reported
  `outer_collision=900x1000@0,0`, and treated the window as the collision boundary.
- The underlying mechanism defect was not `AnchoredProps` coordinate conversion. The runtime
  `ElementContext::root_bounds_for_element` query used `NodeEntry.root` as a declarative owner root.
  For descendants inside `LayoutCx::layout_viewport_root`, that owner root can remain the outer
  render root even though the element's effective boundary is the nearest layout viewport root.

What was fixed:

- Added a per-element effective root-boundary cache in `WindowElementState`.
- Rebuilt that cache after final layout from live element-node mappings and the nearest registered
  `viewport_root`, while leaving `NodeEntry.root` as the declarative owner-root contract.
- Updated both `ElementContext::root_bounds_for_element` and the free
  `elements::root_bounds_for_element` query to prefer the effective per-element boundary before
  falling back to owner root bounds.
- Added a focused mechanism test that keeps `NodeEntry.root` pointed at an outer owner while the
  element's node is under a registered viewport root.
- Added a focused view-cache hit test that reuses the cached declarative subtree while moving the
  same element between viewport roots, proving the effective boundary cache rebuilds even when the
  subtree render function is not re-entered.
- Removed the temporary `FRET_DEBUG_OVERLAY_ROOT_BOUNDS` stderr hook after structured placement
  traces proved the fix.

Run results:

- Before evidence:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/resizable/ui-gallery-resizable-multi-viewport-combobox-placement.json --dir .fret/diag/runs/ui-gallery-resizable-multi-viewport-combobox-placement-before --timeout-ms 240000 --pack --include-triage --include-screenshots --launch target/dev-fast/fret-ui-gallery.exe`
  - Result: failed at step 14, reason `wait_overlay_placement_trace_timeout`.
  - Trace evidence: `chosen_side=bottom`, `outer_collision=900x1000@0,0`.
- Focused mechanism gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui element_root_bounds_cache_rebuilds_on_view_cache_hit_after_viewport_move element_root_bounds_cache_rebuilds_when_element_moves_between_viewport_roots element_root_bounds_cache_uses_nearest_nested_viewport_root element_root_bounds_cache_uses_nearest_viewport_when_owner_root_differs viewport_root_bounds_for_descendant_elements_track_the_panel_bounds --no-fail-fast`
  - Result: passed, 5 tests. Nextest run id `045558b5-2637-4b15-a41f-55d7399d5ad2`.
- UI kit focused gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui-kit outer_bounds_with_window_margin_for_root_uses_current_context_bounds popper_layout_for_element_returns_arrow_layout_when_configured --no-fail-fast`
  - Result: passed, 2 tests. Nextest run id `819d431c-1316-431e-95b8-58dc6e962904`.
- Protocol roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol script_v2_roundtrip_ui_gallery_resizable_multi_viewport_combobox_placement --no-fail-fast`
  - Result: passed, 1 test. Nextest run id `6f146a5c-1f46-4982-b37d-4722a3909e4d`.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- Build gate:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- Runtime gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/resizable/ui-gallery-resizable-multi-viewport-combobox-placement.json --dir .fret/diag/runs/ui-gallery-resizable-multi-viewport-combobox-placement-final2 --timeout-ms 240000 --pack --include-triage --include-screenshots --launch target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778905890519`.
  - Trace evidence: `chosen_side=top`, `preferred_fits_without_main_clamp=false`,
    `outer_collision=336x378@514.67,468.67`.

Evidence anchors:

- `crates/fret-ui/src/elements/runtime.rs`
- `crates/fret-ui/src/elements/cx.rs`
- `crates/fret-ui/src/elements/queries.rs`
- `crates/fret-ui/src/tree/layout/entrypoints.rs`
- `crates/fret-ui/src/declarative/tests/layout/viewport_roots.rs`
- `ecosystem/fret-ui-kit/src/overlay.rs`
- `tools/diag-scripts/ui-gallery/resizable/ui-gallery-resizable-multi-viewport-combobox-placement.json`
- `.fret/diag/runs/ui-gallery-resizable-multi-viewport-combobox-placement-before/share/1778902294871.zip`
- `.fret/diag/runs/ui-gallery-resizable-multi-viewport-combobox-placement-final2/share/1778905890519.zip`

Next slice recommendation:

- Extend the same runtime probe only if a real UI Gallery surface can move a cached overlay source
  between viewport roots. Focused coverage now includes nearest-root precedence, direct same-element
  movement, and the view-cache-hit retained-render reuse route through
  `element_root_bounds_cache_rebuilds_on_view_cache_hit_after_viewport_move`; the remaining gap is a
  promoted runtime companion, not the core mechanism proof.

## F111: Non-modal overlay click-through gates were proxy-only

Layer: UI Gallery diagnostics harness over `fret-ui-kit`/shadcn non-modal overlay policy.

Status: harness strengthened; no new component or mechanism defect reproduced.

What was found:

- Existing Popover and DropdownMenu non-modal outside-press gates asserted dismissal/close plus
  focus on the underlay probe.
- Those were proxy signals. They did not prove that the click actually reached the underlay
  activation handler, so a future regression in outside-press consumption or hit-test pass-through
  could still pass if focus moved while activation was swallowed.

What was fixed:

- Added an independent `ui-gallery-overlay-underlay-activated` status flag in the Overlay preview.
- The underlay button now toggles that flag through its own activation handler, and the reset button
  clears it.
- Upgraded the Popover click-through and DropdownMenu non-modal outside-press scripts to assert
  that activation status, in addition to the existing focus/dismiss checks.
- Added protocol roundtrip coverage for both upgraded scripts.

Run results:

- Build gate:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- Popover runtime gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/overlay/ui-gallery-popover-click-through-outside-press-focus-underlay.json --dir .fret/diag/runs/ui-gallery-popover-click-through-outside-press-focus-underlay-activation --timeout-ms 240000 --pack --include-triage --include-screenshots --launch target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778906489806`.
- DropdownMenu runtime gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/overlay/ui-gallery-dropdown-nonmodal-outside-press-focus-underlay.json --dir .fret/diag/runs/ui-gallery-dropdown-nonmodal-outside-press-focus-underlay-activation --timeout-ms 240000 --pack --include-triage --include-screenshots --launch target/dev-fast/fret-ui-gallery.exe`
  - Result: passed, run id `1778906522304`.
- Roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol script_v2_roundtrip_ui_gallery_popover_click_through_outside_press_focus_underlay script_v2_roundtrip_ui_gallery_dropdown_nonmodal_outside_press_focus_underlay --no-fail-fast`
  - Result: passed, 2 tests. Nextest run id `c563620f-80b3-4933-9da6-48d657c68a38`.
- Registry gate:
  `python tools/check_diag_scripts_registry.py`
  - Result: passed.

Evidence anchors:

- `apps/fret-ui-gallery/src/ui/previews/gallery/overlays/overlay/widgets.rs`
- `apps/fret-ui-gallery/src/ui/previews/gallery/overlays/overlay/flags.rs`
- `tools/diag-scripts/ui-gallery/overlay/ui-gallery-popover-click-through-outside-press-focus-underlay.json`
- `tools/diag-scripts/ui-gallery/overlay/ui-gallery-dropdown-nonmodal-outside-press-focus-underlay.json`
- `.fret/diag/runs/ui-gallery-popover-click-through-outside-press-focus-underlay-activation/share/1778906489806.zip`
- `.fret/diag/runs/ui-gallery-dropdown-nonmodal-outside-press-focus-underlay-activation/share/1778906522304.zip`

Next slice recommendation:

- Move to semantics/accessibility runtime gates rather than adding more click-through cases without
  new parity evidence. The high-value next gap is a retained non-list semantics mutation surface or
  a cross-overlay relation-edge gate where stale semantics can pass visual and focus checks.
