---
title: Fret Mechanism Harness Findings - 2026-05-18
status: active
date: 2026-05-18
---

# Findings - 2026-05-18

## F170: Flex wrap gap layout and definite-width max-content measurement are now covered

Layer: `fret-ui` layout primitive fixture harness.

Status: coverage gap closed; no new flex wrap/gap layout or intrinsic measurement defect
reproduced.

What was found:

- F169 proved single-line flex `gap` layout/measurement consistency, but wrapping rows remained a
  higher-risk split because the same `gap` value contributes to both main-axis item spacing and
  cross-axis line spacing.
- The useful mechanism question was whether a definite-width wrapped flex row reports the same
  envelope through final layout and `measure_in(MaxContent)` after the second line is created.
- The new fixture uses a 68px-wide horizontal flex row with `wrap=true`, `gap=8`, and children
  `30 x 10`, `30 x 12`, and `20 x 14`. The first two children occupy the first line
  (`30 + 8 + 30 = 68`), and the third child wraps to `y=20` (`12 + 8`).

What was changed:

- Added `flex-wrap-gap-measure-matches-layout` to `layout_primitives_v1.json`.
- Added the matching `FlexWrapGapMeasureMatchesLayout` harness scenario and scalar metrics for
  final layout bounds plus definite-width `measure_in(MaxContent)`.
- No runtime behavior change was needed. The current flex layout and measurement paths agree on
  the `68 x 34` wrapped gap envelope.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
- `crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs`
- `crates/fret-ui/src/declarative/host_widget/measure.rs`
- `crates/fret-ui/src/layout/engine/flow.rs`

Run results:

- Layout primitive harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_layout_primitives_match_oracles --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `0353a271-aaaf-49d0-9254-f770af9ba4c1`.
- Formatting:
  `cargo fmt -p fret-ui --check`
  - Result: passed.

Next slice recommendation:

- Move to a different layout/measure/paint/hit-test split instead of adding more flex gap mirrors.
  Good candidates are absolute/sticky positioning interactions with hit-test bounds, transform
  bounds under retained/cached roots, or the deferred direction/writing-mode primitive once the
  mechanism contract is explicit enough for a non-recipe oracle.

## F171: Pressable absolute-only wrapper envelopes now drive layout bounds

Layer: `fret-ui` layout engine and passthrough wrapper measurement.

Status: real mechanism defect found and fixed.

What was found:

- `Pressable` is a behavioral passthrough wrapper, but the layout engine previously modeled an
  auto-sized wrapper with only absolute-positioned children as an ordinary grid/block container.
- Taffy correctly ignores absolute-positioned children for normal flow intrinsic contribution, so
  the wrapper's solved layout bounds collapsed to `0 x 0`.
- The widget-local layout fallback could still place the absolute child and hit-test the visible
  child after later fixes, but `UiTree::debug_node_bounds()` and the parent layout envelope still
  reported the `Pressable` wrapper as `0 x 0`.
- The fixture uses a single absolute child with `left: 25%`, `top: 10%`, and `20 x 10` size. The
  required wrapper envelope is `27 x 12`, which places the child at `(6.75, 1.2)` and keeps a
  near-edge hit-test sample inside the child.

What was changed:

- Added `pressable-fractional-absolute-child-envelope-matches-layout` to
  `layout_primitives_v1.json`.
- Added the matching `PressableFractionalAbsoluteChildEnvelopeMatchesLayout` harness scenario,
  scalar layout/measurement metrics, and near-edge hit-test sample.
- Updated passthrough wrapper measurement so absolute-only wrappers contribute the shared
  absolute-child envelope even during final definite layout probes, not only placeholder or
  intrinsic probes.
- Updated the flow engine so an auto/auto wrapper with only absolute children is measured as a
  leaf for parent flow sizing. This is intentionally scoped to absolute-only auto wrappers and does
  not change fixed containing-block percent inset semantics.

Evidence anchors:

- `crates/fret-ui/src/layout/engine/flow.rs`
- `crates/fret-ui/src/layout/engine.rs`
- `crates/fret-ui/src/declarative/host_widget/measure.rs`
- `crates/fret-ui/src/declarative/host_widget/layout.rs`
- `crates/fret-ui/src/declarative/layout_helpers.rs`
- `crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
- `crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs`

Run results:

- First red layout primitive harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_layout_primitives_match_oracles --no-fail-fast --no-capture`
  - Result: failed; Nextest run id `9aab41b5-8444-4086-a1cb-38307cb1467b`.
  - Symptom: `pressable-fractional-absolute` layout bounds and scalar metrics were `0 x 0`
    instead of `27 x 12`.
- Fixed layout primitive harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_layout_primitives_match_oracles --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `cedf2113-d532-4c84-b69a-728b052ae6a0`.
- Formatting:
  `cargo fmt -p fret-ui --check`
  - Result: passed.

Next slice recommendation:

- Continue probing absolute positioning through a different parent contract: e.g. mixed flow plus
  absolute children where the flow child sizes the wrapper but the absolute child extends hit-test
  or overflow bounds, or a transform/view-cache wrapper where layout, visual, and hit spaces can
  diverge.

## F172: Pressable mixed flow/absolute wrapper envelopes now agree across layout, measure, and hit-test

Layer: `fret-ui` layout engine, passthrough wrapper measurement, and positioned-container layout.

Status: real mechanism defect found and fixed.

What was found:

- F171 fixed the absolute-only case, but a higher-risk mixed case remained: an auto/auto behavioral
  wrapper with one normal flow child and one absolute child extending beyond the flow child.
- `pressable-mixed-flow-absolute-child-envelope-matches-layout` first failed because the wrapper's
  authoritative layout bounds and placeholder measurement stayed at the flow child's `20 x 10`
  size, while the absolute child required a `34 x 12` envelope.
- After the first measure/flow fix, wrapper bounds were correct but final child layout still reused
  a too-small absolute containing block, placing the child at `(5, 1)` with a `20 x 10` width
  instead of `(8.5, 1.2)` with its declared `25 x 10` size. The near-edge hit-test sample landed
  on the parent wrapper instead of the absolute child.
- The fixed containing-block percent inset case stayed protected by the existing
  `absolute-inset-fraction-resolves-against-containing-block` fixture.

What was changed:

- Added `pressable-mixed-flow-absolute-child-envelope-matches-layout` to
  `layout_primitives_v1.json`.
- Added the matching `PressableMixedFlowAbsoluteChildEnvelopeMatchesLayout` harness scenario,
  scalar layout/measurement metrics, and near-edge hit-test sample.
- Updated passthrough wrapper measurement so auto axes include absolute-child envelopes even when a
  normal flow child already contributes a non-zero size.
- Updated auto/auto passthrough flow modeling so wrappers with absolute children are measured as
  leaves for parent flow sizing.
- Updated positioned-container fallback layout so auto wrappers union flow-child size with
  absolute-child envelope size, then place static flow children using their measured size and
  absolute children against the union envelope.

Evidence anchors:

- `crates/fret-ui/src/layout/engine/flow.rs`
- `crates/fret-ui/src/declarative/host_widget/measure.rs`
- `crates/fret-ui/src/declarative/host_widget/layout.rs`
- `crates/fret-ui/src/declarative/host_widget/layout/positioned_container.rs`
- `crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
- `crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs`

Run results:

- First red layout primitive harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_layout_primitives_match_oracles --no-fail-fast --no-capture`
  - Result: failed; Nextest run id `8ca82632-03d0-41b9-969b-127241a687c5`.
  - Symptom: `pressable-mixed-absolute` reported `20 x 10` instead of `34 x 12`, and the
    near-edge absolute-child hit-test missed.
- Fixed layout primitive harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_layout_primitives_match_oracles --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `0f75010e-ebc0-4f4c-b835-aff6c0086b9d`.
- Scoped companion layout tests:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui declarative::tests::layout::basics declarative::tests::layout::interactivity --no-fail-fast`
  - Result: passed; 55/55 tests.
- Formatting:
  `cargo fmt -p fret-ui --check`
  - Result: passed.
- Scoped whitespace check:
  `git diff --check -- crates/fret-ui/src/declarative/host_widget/layout.rs crates/fret-ui/src/declarative/host_widget/layout/positioned_container.rs crates/fret-ui/src/declarative/host_widget/measure.rs crates/fret-ui/src/layout/engine/flow.rs crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
  - Result: passed.

Next slice recommendation:

- Move the absolute-positioning probe out of Pressable and into transform/view-cache wrappers:
  verify that layout, visual, and hit spaces stay coherent when a cached or transformed auto
  wrapper has mixed flow/absolute children and is moved between roots.

## F173: RenderTransform carries mixed flow/absolute wrapper envelopes through visual and hit spaces

Layer: `fret-ui` layout primitive harness; transform visual/hit contract.

Status: coverage added; no new mechanism defect reproduced.

What was found:

- F172 fixed the mixed Pressable flow/absolute envelope in layout, measurement, and hit-testing.
  The next risk was whether the same expanded envelope stays coherent once a `RenderTransform`
  moves the wrapper into a different visual/hit space.
- The new `render-transform-mixed-flow-absolute-envelope-matches-visual-hit` fixture reuses the
  same `34 x 12` mixed wrapper envelope under a `RenderTransform` with a `40px` x-translation.
- The first run passed: layout bounds stayed at `0,0 34 x 12`; visual and hit bounds both moved
  `40px` to the right; placeholder measurement stayed `34 x 12`; the original layout-space
  near-edge point did not hit the absolute child; and the translated visual-space near-edge point
  hit `transformed-mixed-absolute-child`.

What was changed:

- Added `RenderTransformMixedFlowAbsoluteEnvelopeMatchesVisualHit` to the layout primitive harness.
- Generalized the existing transform-tracking helper from one hard-coded test id to a tracked
  `{ element, test_id }` pair.
- Added layout, visual/hit-space, scalar measurement, and hit-test predicates to
  `layout_primitives_v1.json`.
- No `fret-ui` runtime mechanism code needed a change.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
- `crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs`
- `crates/fret-ui/src/declarative/host_widget/layout.rs`
- `crates/fret-ui/src/declarative/host_widget/measure.rs`
- `crates/fret-ui/src/layout/engine/flow.rs`

Run results:

- JSON fixture parse:
  `python -m json.tool crates\fret-ui\src\declarative\tests\fixtures\layout_primitives_v1.json`
  - Result: passed.
- Layout primitive harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_layout_primitives_match_oracles --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `fd0237ae-17ff-435d-a416-b34b2e8f5345`.

Next slice recommendation:

- Move from transform-only to cached/root-movement replay: add a view-cache companion where the
  mixed flow/absolute wrapper is reused after movement between panels or roots, then verify layout,
  visual bounds, hit-test replay, and element visual-bounds queries stay coherent.

## F174: ViewCache clean reuse moves mixed flow/absolute wrapper geometry and hit routing

Layer: `fret-ui` declarative ViewCache and hit-test routing harness.

Status: coverage added; no new mechanism defect reproduced.

What was found:

- F173 covered the transform-only case. The next risk was clean ViewCache reuse: a cached subtree
  can skip child render while its parent layout moves it, so stale layout, visual bounds, or
  interaction replay records could drift apart.
- The new test builds a horizontal row with a parent spacer followed by a ViewCache root. The
  cached subtree is a `Pressable` containing one normal flow child and one fractional-inset
  absolute child requiring the same `34 x 12` envelope from F172.
- Frame 1 uses spacer width `0`; frame 2 uses spacer width `40`. The ViewCache child render
  closure still runs only once, proving the second frame is clean reuse.
- The first draft's red assertion was a harness oracle issue rather than a mechanism defect. The
  old sample point no longer hits the moved absolute child, but after inserting the spacer it
  legitimately lands inside the expanded outer row. The corrected oracle compares fallback and
  runtime-routing hit-test paths, rejects stale absolute-child hits at the old point, and requires
  both paths to hit the moved absolute child at the translated point.

What was changed:

- Added `view_cache_hit_moving_mixed_absolute_wrapper_updates_bounds_and_hit_test` to
  `crates/fret-ui/src/declarative/tests/view_cache.rs`.
- The test locks clean reuse, wrapper layout bounds, element visual bounds, absolute-child layout
  bounds, fallback hit-testing, and runtime routing via `debug_hit_test_routing`.
- No `fret-ui` runtime mechanism code needed a change.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/view_cache.rs`
- `crates/fret-ui/src/tree/prepaint/interaction.rs`
- `crates/fret-ui/src/tree/hit_test.rs`
- `crates/fret-ui/src/tree/paint/node.rs`

Run results:

- First red focused test:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui view_cache_hit_moving_mixed_absolute_wrapper_updates_bounds_and_hit_test --no-fail-fast --no-capture`
  - Result: failed; Nextest run id `ff29d5c0-ec02-45d7-bc41-63956a5020fb`.
  - Symptom: the old point assertion expected `None`, but the point legitimately hit the expanded
    outer row after the spacer moved the cached subtree.
- Corrected focused test:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui view_cache_hit_moving_mixed_absolute_wrapper_updates_bounds_and_hit_test --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `7e88f25a-7ea4-42c2-96d6-781f9da9482d`.
- ViewCache family gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui view_cache --no-fail-fast`
  - Result: passed; Nextest run id `a49ebb69-7e9a-4b66-bc83-7f5f52d35a0e`.
- Formatting:
  `cargo fmt -p fret-ui --check`
  - Result: passed.

Next slice recommendation:

- Move to a different high-risk split: retained relation/action-state mutation or renderer-level
  text/glyph evidence, unless a real runtime surface exposes another stale cached movement path.

## F175: Renderer font trace facts become script-level Combobox long-text evidence

Layer: diagnostics protocol and UI Gallery runtime diagnostics.

Status: diagnostics harness capability added; no new Combobox recipe or renderer defect reproduced.

What was found:

- Visual geometry gates already proved the LTR Combobox selected label stayed before the chevron
  and the popup option label/checkmark geometry remained bounded, but they still did not prove the
  renderer prepared that visible text with ellipsis rather than clipping or fallback tofu.
- Renderer font traces were already available in diagnostics bundles, but `UiPredicateV1` had no
  script-level predicate for matching text trace entries. This left renderer-level glyph/ellipsis
  evidence as a manual bundle-inspection step instead of a durable gate.
- The first runtime drafts failed on a static-page `script_stalled_no_frames` condition after a
  bare `wait_frames`, not on the new renderer predicate. The page had already converged; the
  script was waiting for frame progress that was not required for the assertion.

What was changed:

- Added `UiRenderTextFontTraceFamilyClassV1` and
  `render_text_font_trace_entries_matching_ge` to `crates/fret-diag-protocol`.
- Added the diagnostics evaluator in `ecosystem/fret-bootstrap`, with filters for
  `text_contains`, `font`, `wrap`, `overflow`, `missing_glyphs`, `family_contains`, and
  `family_class`.
- Updated `ui-gallery-combobox-long-text-geometry.json` to enable `FRET_TEXT_FONT_TRACE_ALL=1` and
  assert that the selected "Enterprise Observability" label is renderer-prepared with `font=ui`,
  `wrap=none`, `overflow=ellipsis`, and `missing_glyphs=0`.
- Removed unnecessary static-page frame waits from the script and relied on bounded predicate
  convergence.

Evidence anchors:

- `crates/fret-diag-protocol/src/lib.rs`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/predicates.rs`
- `tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-long-text-geometry.json`
- `docs/workstreams/fret-mechanism-harness-v1/VISUAL_PARITY_MATRIX.md`

Run results:

- Protocol/roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol predicate_render_text_font_trace_entries_matching_ge_serializes_and_deserializes script_v2_roundtrip_ui_gallery_combobox_long_text_geometry --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `88d1c4cf-a5e7-4b17-91bd-998df1857420`.
- Evaluator gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-bootstrap --features ui-app-driver,diagnostics render_text_font_trace_matching_predicate_matches_renderer_text_facts --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `4ec491be-913d-47fa-b1e3-d7e756594342`.
- Runtime diagnostics gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-long-text-geometry.json --dir target/fret-diag-combobox-long-text-geometry-renderer-trace-v4 --session-auto --pack --ai-packet --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed; run id `1779077880731`.

Next slice recommendation:

- Add the RTL Combobox renderer font-trace companion, then broaden renderer-level text evidence to
  Command, Input, and Button Group long-text families only where current geometry/TextInput gates
  cannot prove glyph preparation.

## F176: RTL Combobox long text has renderer-backed ellipsis/no-tofu evidence

Layer: UI Gallery runtime diagnostics.

Status: RTL companion gate added; no new Combobox recipe, RTL layout, or renderer defect reproduced.

What was found:

- F175 closed the LTR Combobox selected-label renderer evidence, but the RTL long-text gate still
  relied on geometry, placement traces, layout sidecar, screenshot, and bundle evidence only.
- The RTL script also retained two bare static-page `wait_frames` steps. The LTR run had already
  shown that this pattern can report `script_stalled_no_frames` after the page converges, so the
  RTL companion had the same harness fragility even though it had not failed in the fresh run.

What was changed:

- Updated `ui-gallery-combobox-rtl-long-text-geometry.json` to enable
  `FRET_TEXT_FONT_TRACE_ALL=1` and assert a renderer font trace entry for the selected
  "Enterprise Observability" label with `font=ui`, `wrap=none`, `overflow=ellipsis`, and
  `missing_glyphs=0`.
- Removed the unnecessary static-page `wait_frames` steps and kept the existing RTL physical-side
  geometry, top-flip placement, layout sidecar, screenshot, and bundle captures.
- Added a focused script roundtrip test for the RTL long-text script.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-rtl-long-text-geometry.json`
- `tools/diag-scripts/ui-gallery-combobox-rtl-long-text-geometry.json`
- `crates/fret-diag-protocol/tests/script_json_roundtrip.rs`
- `docs/workstreams/fret-mechanism-harness-v1/VISUAL_PARITY_MATRIX.md`

Run results:

- RTL script roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol script_v2_roundtrip_ui_gallery_combobox_rtl_long_text_geometry --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `23514d59-c3bc-4985-8c8f-d1047d32e6aa`.
- Runtime diagnostics gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-rtl-long-text-geometry.json --dir target/fret-diag-combobox-rtl-long-text-renderer-trace-v1 --session-auto --pack --ai-packet --include-screenshots --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed; run id `1779078285665`.

Next slice recommendation:

- Broaden renderer-level text evidence to Command, Input, or Button Group long-text gates where
  current geometry/TextInput predicates still cannot prove renderer glyph preparation.

## F177: Command docs-demo long query has renderer-backed clip/no-tofu evidence

Layer: UI Gallery runtime diagnostics.

Status: Command long-query companion gate added; no new Command recipe or renderer defect
reproduced.

What was found:

- The Command docs-demo long-query gate already proved TextInput viewport clipping, horizontal
  overflow/offset, IME cursor containment, text-height coverage, screenshot, and layout sidecar
  evidence.
- It still did not prove that the renderer prepared the long query with the expected UI font and no
  missing glyphs. Unlike the Combobox selected-label gates, Command search input text is clipped
  by the input viewport rather than ellipsized.
- The first runtime drafts failed before the renderer predicate because the script waited for exact
  requested inner sizes. On the current Windows native runner, this path follows the existing
  requested/effective viewport contract: requested `1280x1200` and `760x640` produce effective
  diagnostic window bounds `1280x1220` and `760x660`.

What was changed:

- Updated `ui-gallery-command-docs-demo-long-query-text.json` to enable
  `FRET_TEXT_FONT_TRACE_ALL=1` and assert a renderer font trace entry for the long search query with
  `font=ui`, `wrap=none`, `overflow=clip`, and `missing_glyphs=0`.
- Replaced the remaining static-page waits with bounded predicates for page entry, effective
  window size, and non-zero Command content/input bounds.
- Added a focused script roundtrip test for the Command long-query script.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/command/ui-gallery-command-docs-demo-long-query-text.json`
- `tools/diag-scripts/ui-gallery-command-docs-demo-long-query-text.json`
- `crates/fret-diag-protocol/tests/script_json_roundtrip.rs`
- `docs/workstreams/fret-mechanism-harness-v1/VISUAL_PARITY_MATRIX.md`

Run results:

- Focused script roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol script_v2_roundtrip_ui_gallery_command_docs_demo_long_query_text --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `347bc280-0e4c-4f1d-beed-062fd2e4903f`.
- Full roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol --test script_json_roundtrip --no-fail-fast`
  - Result: passed; Nextest run id `9f94f99d-86c7-417b-9b0f-5f29e4ca5797`.
- Runtime diagnostics gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/command/ui-gallery-command-docs-demo-long-query-text.json --dir target/fret-diag-command-docs-demo-long-query-renderer-trace-v6 --session-auto --pack --ai-packet --include-screenshots --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed; run id `1779080299508`.
  - Artifacts:
    `target/fret-diag-command-docs-demo-long-query-renderer-trace-v6/sessions/1779080286919-178616/script.result.json`,
    `target/fret-diag-command-docs-demo-long-query-renderer-trace-v6/sessions/1779080286919-178616/1779080299508/ai.packet`,
    `target/fret-diag-command-docs-demo-long-query-renderer-trace-v6/sessions/1779080286919-178616/share/1779080299508.zip`,
    `target/fret-diag-command-docs-demo-long-query-renderer-trace-v6/sessions/1779080286919-178616/1779080386435-ui-gallery-command-docs-demo-long-query-text.layout/layout.taffy.v1.json`,
    and
    `target/fret-diag-command-docs-demo-long-query-renderer-trace-v6/sessions/1779080286919-178616/screenshots/1779080386835-ui-gallery-command-docs-demo-long-query-text/window-4294967297-tick-7-frame-7.png`.

Next slice recommendation:

- Continue renderer-level text evidence on the plain Input/File or Button Group long-text gates,
  where current diagnostics still prove viewport clipping but not renderer glyph preparation.

## F178: Plain Input/File long text has renderer-backed clip/no-tofu evidence

Layer: UI Gallery runtime diagnostics.

Status: Input/File companion gate added; no new Input recipe, file-composition, or renderer defect
reproduced.

What was found:

- The Input Basic + File long-text gate already proved direct editable text-field semantics,
  long-value mutation, horizontal overflow/offset, visible viewport clipping, IME cursor
  containment, text-height coverage, screenshot, and layout sidecar evidence.
- It still did not prove that either text value was prepared by the renderer with the UI font and no
  missing glyphs.
- The script also retained cross-page nav-search steps and bare static-page waits. These were not
  failing in the fresh run, but the Combobox and Command renderer-trace slices had already shown
  that static waits are weaker than bounded predicate convergence on settled pages.

What was changed:

- Updated `ui-gallery-input-basic-and-file-long-text.json` to start directly on the Input page with
  `FRET_UI_GALLERY_START_PAGE=input` and to enable `FRET_TEXT_FONT_TRACE_ALL=1`.
- Added renderer font trace waits for the Basic Input long value and the file-composed Input long
  value, both requiring `font=ui`, `wrap=none`, `overflow=clip`, and `missing_glyphs=0`.
- Replaced the initial cross-page navigation and static waits with effective-window and page-root
  convergence predicates. The current Windows native runner reports `760x720` after the script
  requests `760x700`, matching the requested/effective viewport contract from the prior slices.
- Refreshed `tools/diag-scripts/index.json` because the script metadata/env defaults changed.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/input/ui-gallery-input-basic-and-file-long-text.json`
- `tools/diag-scripts/ui-gallery-input-basic-and-file-long-text.json`
- `tools/diag-scripts/index.json`
- `crates/fret-diag-protocol/tests/script_json_roundtrip.rs`
- `docs/workstreams/fret-mechanism-harness-v1/VISUAL_PARITY_MATRIX.md`

Run results:

- Focused script roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol script_v2_roundtrip_ui_gallery_input_basic_and_file_long_text --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `4fc33be1-73de-4abd-b44e-1372c97cbe10`.
- Full roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol --test script_json_roundtrip --no-fail-fast`
  - Result: passed; Nextest run id `29fe790a-4b52-4b96-9d3c-0fb7677a8401`.
- Registry gates:
  `python tools/check_diag_scripts_registry.py` and
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed.
- Runtime diagnostics gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/input/ui-gallery-input-basic-and-file-long-text.json --dir target/fret-diag-input-basic-file-long-text-renderer-trace-v1 --session-auto --pack --ai-packet --include-screenshots --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed; run id `1779081108865`.
  - Artifacts:
    `target/fret-diag-input-basic-file-long-text-renderer-trace-v1/sessions/1779081088220-65512/script.result.json`,
    `target/fret-diag-input-basic-file-long-text-renderer-trace-v1/sessions/1779081088220-65512/1779081108865/ai.packet`,
    `target/fret-diag-input-basic-file-long-text-renderer-trace-v1/sessions/1779081088220-65512/share/1779081108865.zip`,
    `target/fret-diag-input-basic-file-long-text-renderer-trace-v1/sessions/1779081088220-65512/1779081309030-ui-gallery-input-basic-and-file-long-text.layout/layout.taffy.v1.json`,
    and
    `target/fret-diag-input-basic-file-long-text-renderer-trace-v1/sessions/1779081088220-65512/screenshots/1779081309581-ui-gallery-input-basic-and-file-long-text/window-4294967297-tick-23-frame-23.png`.

Next slice recommendation:

- Continue renderer-level text evidence on the Button Group long-text gate, where current
  diagnostics still prove viewport clipping and trailing-control non-overlap but not renderer glyph
  preparation.

## F179: Button Group Input Group long text has renderer-backed clip/no-tofu evidence

Layer: UI Gallery runtime diagnostics.

Status: Button Group Input Group companion gate added; no new Button Group recipe, TextInput, or
renderer defect reproduced.

What was found:

- The Button Group Input Group long-text gate already proved the grouped input accepts a long value,
  keeps bounded root/control geometry, avoids overlap with the trailing voice button, reports
  horizontal overflow, clamps horizontal offset, keeps visible text inside the viewport, keeps the
  IME cursor in bounds, and captures layout/screenshot/bundle evidence.
- It still did not prove that the long value was prepared by the renderer with the UI font and no
  missing glyphs.
- The script also retained bare static waits. The first renderer-trace runtime draft exposed a more
  precise harness issue: a pre-mutation `bounds_within_window` wait on the direct input control
  timed out because the control's semantics bounds were still `0 x 0` before `set_text_value`.
  The owning Button Group root was already present and visible.

What was changed:

- Updated `ui-gallery-button-group-input-group-long-text.json` to enable
  `FRET_TEXT_FONT_TRACE_ALL=1`.
- Added a renderer font trace wait for the grouped input long value requiring `font=ui`,
  `wrap=none`, `overflow=clip`, and `missing_glyphs=0`.
- Replaced static waits with effective-window/page/root convergence predicates. The current Windows
  native runner reports `760x660` after the script requests `760x640`, matching the viewport
  contract observed by the Command renderer-trace gate.
- Kept the existing post-mutation control-size, trailing-button non-overlap, IME cursor,
  overflow/offset, visible-viewport, text-height, layout sidecar, screenshot, and bundle evidence.
- Added a focused script roundtrip test for the redirect path and refreshed
  `tools/diag-scripts/index.json`.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/button-group/ui-gallery-button-group-input-group-long-text.json`
- `tools/diag-scripts/ui-gallery-button-group-input-group-long-text.json`
- `tools/diag-scripts/index.json`
- `crates/fret-diag-protocol/tests/script_json_roundtrip.rs`
- `docs/workstreams/fret-mechanism-harness-v1/VISUAL_PARITY_MATRIX.md`

Run results:

- First runtime draft:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/button-group/ui-gallery-button-group-input-group-long-text.json --dir target/fret-diag-button-group-input-group-long-text-renderer-trace-v1 --session-auto --pack --ai-packet --include-screenshots --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: failed at step 7 with `wait_until_timeout` on
    `bounds_within_window(ui-gallery-button-group-input-group-control)`.
  - Evidence showed a unique `text_field` match for
    `ui-gallery-button-group-input-group-control` with pre-mutation bounds `0 x 0`, so the failure
    was a script precondition issue rather than a Button Group layout defect.
- Focused script roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol script_v2_roundtrip_ui_gallery_button_group_input_group_long_text --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `d656b55e-a80c-4a53-8cb0-98a8c2307872`.
- Full roundtrip gate:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol --test script_json_roundtrip --no-fail-fast`
  - Result: passed; Nextest run id `72429701-d2a0-4d9f-9742-563fc421a36f`.
- Registry gates:
  `python tools/check_diag_scripts_registry.py` and
  `python tools/test_check_diag_scripts_registry.py`
  - Result: passed.
- Runtime diagnostics gate:
  `target/dev-fast/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/button-group/ui-gallery-button-group-input-group-long-text.json --dir target/fret-diag-button-group-input-group-long-text-renderer-trace-v2 --session-auto --pack --ai-packet --include-screenshots --timeout-ms 360000 --launch -- target/dev-fast/fret-ui-gallery.exe`
  - Result: passed; run id `1779082851147`.
  - Artifacts:
    `target/fret-diag-button-group-input-group-long-text-renderer-trace-v2/sessions/1779082839430-157148/script.result.json`,
    `target/fret-diag-button-group-input-group-long-text-renderer-trace-v2/sessions/1779082839430-157148/1779082851147/ai.packet`,
    `target/fret-diag-button-group-input-group-long-text-renderer-trace-v2/sessions/1779082839430-157148/share/1779082851147.zip`,
    `target/fret-diag-button-group-input-group-long-text-renderer-trace-v2/sessions/1779082839430-157148/1779082919330-ui-gallery-button-group-input-group-long-text.layout/layout.taffy.v1.json`,
    and
    `target/fret-diag-button-group-input-group-long-text-renderer-trace-v2/sessions/1779082839430-157148/screenshots/1779082919566-ui-gallery-button-group-input-group-long-text/window-4294967297-tick-6-frame-6.png`.

Next slice recommendation:

- Move from renderer trace closure to the next uncovered mechanism axis: retained relation/action
  state mutation on a real runtime surface, or a new layout/measure/paint/hit-test contract split
  where synthetic fixtures can be paired with UI Gallery evidence.

## F180: FractionalRenderTransform derives visual/hit spaces from laid-out size

Layer: `fret-ui` layout primitive harness; transform visual/hit contract.

Status: synthetic fixture added; no new `fret-ui` mechanism defect reproduced.

What was found:

- The layout primitive fixture already covered fixed-pixel `RenderTransform` visual/hit movement
  and the mixed flow/absolute wrapper envelope under a transform.
- `FractionalRenderTransform` shares the same ADR 0082 contract, but computes its translation from
  the wrapper's laid-out size during layout. That size-derived branch was not locked by the fixture
  suite.

What was changed:

- Added `fractional-render-transform-derives-visual-hit-from-layout-size` to
  `layout_primitives_v1.json`.
- Added the matching
  `FractionalRenderTransformDerivesVisualHitFromLayoutSize` runner scenario.
- The fixture wraps a `20 x 20` Pressable with `FractionalRenderTransform(2.0, 0.5)`, then proves
  layout remains `0,0 20 x 20` while visual and hit spaces translate by `40 x 10`.
- Added layout-space miss and translated visual-space hit samples so bounds evidence and routing
  evidence agree.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/fixtures/layout_primitives_v1.json`
- `crates/fret-ui/src/declarative/tests/layout/mechanism_harness.rs`
- `docs/adr/0082-render-transform-hit-testing.md`

Run results:

- JSON fixture validation:
  `python -m json.tool crates\fret-ui\src\declarative\tests\fixtures\layout_primitives_v1.json`
  - Result: passed.
- Layout primitive harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_layout_primitives_match_oracles --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `8a831141-fd89-4656-be5b-59a3d206bdef`.

Next slice recommendation:

- Continue probing layout/measure/paint/hit-test contract splits only where the mechanism contract
  is already explicit. Avoid RTL/writing-mode primitive oracles until the direction model is
  documented enough to distinguish mechanism behavior from recipe policy.

## F181: MaskLayer bounds are paint-only unless overflow clipping is explicit

Layer: `fret-ui` hit-test routing fixture harness; paint/hit-test contract.

Status: synthetic fixture cases added; no new `fret-ui` mechanism defect reproduced.

What was found:

- ADR 0239 says masks are paint-only by default: mask coverage and mask bounds must not change
  pointer targeting.
- ADR 0273 extends that rule to image masks and clarifies that mask bounds are computation bounds,
  not an implicit clip.
- `hit_test_routing_v1.json` already covered clip, transparent-wrapper, transform, overlay, and
  modal-barrier routing, but it did not enumerate the MaskLayer paint-only versus explicit
  overflow-clip split.

What was changed:

- Added `mask-layer-bounds-do-not-clip-hit-testing-by-default`.
- Added `mask-layer-overflow-clip-suppresses-escaped-child-hit`.
- Added the matching `MaskLayer*` runner scenarios and a shared `mask_layer_with_escaped_pressable`
  helper using an offset escaped child.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/fixtures/hit_test_routing_v1.json`
- `crates/fret-ui/src/declarative/tests/layout/hit_test_mechanism_harness.rs`
- `docs/adr/0239-mask-layers-and-alpha-masks-v1.md`
- `docs/adr/0273-clip-path-and-image-mask-sources-v1.md`

Run results:

- First red hit-test routing harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_hit_test_routing_matches_oracles --no-fail-fast --no-capture`
  - Result: failed; Nextest run id `54d2868e-b176-42cb-a6e2-ad01ce63b497`.
  - Classification: fixture oracle mistake. The original case tried to prove escaped hit-testing
    with a width-overflow absolute child, but the current layout contract legitimately constrained
    that child to the wrapper width.
- JSON fixture validation:
  `python -m json.tool crates\fret-ui\src\declarative\tests\fixtures\hit_test_routing_v1.json`
  - Result: passed.
- Corrected hit-test routing harness:
  `cargo nextest run --cargo-profile dev-fast -p fret-ui mechanism_harness_hit_test_routing_matches_oracles --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `a72e3112-544e-405a-957d-d4d00dfad034`.

Next slice recommendation:

- Continue using explicit ADR-backed paint/hit-test splits for synthetic fixtures. Move to
  semantics/action-state runtime mutation only when a real surface exposes a stale-state risk not
  already covered by retained Tree, DataTable, AI FileTree, Command, or Select gates.
