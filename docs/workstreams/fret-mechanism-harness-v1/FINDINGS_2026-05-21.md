---
title: Fret Mechanism Harness Findings - 2026-05-21
status: active
date: 2026-05-21
---

# Findings - 2026-05-21

## F242: Desktop repeating timers no longer starve diagnostics redraw delivery

Layer: desktop runner scheduling plus launched diagnostics script liveness.

Status: mechanism defect fixed. The broad `ui-gallery-shadcn-runtime-evidence` suite is unblocked
again.

What was found:

- The RadioGroup checked-state slice promoted a new broad-suite member, but the broad suite could
  fail earlier in `ui-gallery-command-retained-active-descendant-action-state.json` with
  `script_stalled_no_frames` while the script was waiting for `scroll_into_view` progress.
- Focused repros showed the script had already injected wheel/pointer work and requested redraws,
  but the launched native runner could remain inside the same effect-drain turn without returning
  to winit to deliver the requested `RedrawRequested`.
- The owning-layer defect was in desktop repeating timer scheduling. `fire_due_timers(now)` rearmed
  repeating timers from the stale drain-turn `now`. If a diagnostics keepalive timer did enough
  work, the timer could already be overdue again before the next fixed-point drain iteration. That
  allowed the same repeating keepalive timer to catch up repeatedly inside one runner tick and
  starve the platform frame it had just requested.

What was changed:

- `TimerEntry` now records `last_fired_tick`, and `fire_due_timers` skips a repeating timer that
  already fired during the current runner tick.
- Repeating timers are rearmed from handler completion time in `finish_fired_timer`, not from the
  stale pre-handler drain timestamp.
- Asset-reload polling timers use the same timer-entry initialization path.
- Diagnostics no-frame keepalive paths now preserve redraw/RAF intent for effect-only,
  keyboard/text/IME, and pointer-move injection paths. `wait_frames` keepalive ticks may drain an
  already-started wait counter without skipping the real next script step.
- Script start and effect-only waits now enqueue an explicit redraw plus RAF, and the UI app driver
  bridges script-injected command flushes back into a post-flush frame request.

Evidence anchors:

- Defect/fix:
  `crates/fret-launch/src/runner/desktop/runner/timers.rs`,
  `crates/fret-launch/src/runner/desktop/runner/window.rs`, and
  `crates/fret-launch/src/runner/desktop/runner/asset_reload.rs`.
- Diagnostics liveness bridge:
  `ecosystem/fret-bootstrap/src/ui_diagnostics/script_engine.rs`,
  `ecosystem/fret-bootstrap/src/ui_diagnostics/script_runner.rs`,
  `ecosystem/fret-bootstrap/src/ui_diagnostics/script_steps.rs`, and
  `ecosystem/fret-bootstrap/src/ui_app_driver.rs`.
- Fresh focused Command pass:
  `target/fret-diag-command-retained-active-descendant-action-state-runner-timer-fresh-20260521/sessions/1779298813208-173816/1779298834262/ai.packet`.
- Fresh focused Command pack:
  `target/fret-diag-command-retained-active-descendant-action-state-runner-timer-fresh-20260521/sessions/1779298813208-173816/share/1779298834262.zip`.
- Fresh broad-suite summary:
  `target/fret-diag-shadcn-runtime-evidence-runner-timer-fresh-20260521/sessions/1779299075645-7824/suite.summary.json`.

Run results:

- Timer regression:
  `cargo test --profile dev-fast -p fret-launch repeating_timer --lib -- --nocapture`
  - Result: passed; 2 tests.
- Diagnostics no-frame regression:
  `cargo test --profile dev-fast -p fret-bootstrap --features ui-app-driver,diagnostics --lib no_frame_keepalive -- --nocapture`
  - Result: passed; 3 tests.
- Build:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery --features gallery-dev`
  - Result: passed. The run still emits the pre-existing unrelated
    `crates/fret-ui/src/declarative/host_widget/paint.rs` unused `start` warning.
- Focused runtime diagnostics:
  `target\dev-fast\fretboard-dev.exe diag run tools\diag-scripts\ui-gallery\command\ui-gallery-command-retained-active-descendant-action-state.json --dir target\fret-diag-command-retained-active-descendant-action-state-runner-timer-fresh-20260521 --session-auto --pack --ai-packet --include-triage --include-screenshots --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; run id `1779298834262`.
- Broad runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-shadcn-runtime-evidence --dir target\fret-diag-shadcn-runtime-evidence-runner-timer-fresh-20260521 --session-auto --timeout-ms 900000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed 13/13; `stage_counts={"passed":13}`; `reason_code_counts={}`.
  - The formerly blocking Command retained-active-descendant script passed with run id
    `1779299206755`.
  - The RadioGroup checked-state mutation script also passed in the broad suite with run id
    `1779299568012`.

Next slice recommendation:

- Keep the timer fix in the runner layer. Do not solve this class by adding renderless
  `scroll_into_view` simulation to diagnostics.
- The next high-value uncovered mechanism slice is a synthetic runner-scheduling stress gate that
  combines overlapping repeating timers, script keepalive, asset-reload polling, redraw/RAF, and a
  slow handler. That would lock the owning invariant without needing a full UI Gallery launch for
  every future scheduling regression.

## F243: Runner timer scheduling now has a synthetic overlap stress gate

Layer: desktop runner scheduling tests.

Status: focused regression coverage added. No additional runtime defect was reproduced after F242;
this slice turns the discovered class into a cheap owning-layer gate.

What was found:

- F242 proved the real launched diagnostics failure through UI Gallery, but the cheapest regression
  test still covered only one repeating timer at a time.
- The risk class is broader: a diagnostics keepalive timer and a windowless asset-reload-style poll
  timer can both be overdue in the same stale drain turn. The invariant must hold for overlapping
  timers, not only for one token.

What was changed:

- Added `overlapping_repeating_timers_do_not_catch_up_inside_one_runner_tick`.
- The test primes two overdue repeating timers: one window-targeted timer representing script
  keepalive and one windowless timer representing asset-reload polling.
- After the first fire, the test deliberately makes both deadlines stale again before advancing the
  runner tick. `fire_due_timers` must return `false` for the second same-tick call, then fire both
  timers again only after `tick_id` advances.

Evidence anchors:

- `crates/fret-launch/src/runner/desktop/runner/timers.rs`.

Run results:

- Focused stress gate:
  `cargo test --profile dev-fast -p fret-launch overlapping_repeating_timers --lib -- --nocapture`
  - Result: passed; 1 test.
- Full runner timer regression filter:
  `cargo test --profile dev-fast -p fret-launch repeating_timer --lib -- --nocapture`
  - Result: passed; 3 tests.

Next slice recommendation:

- With the timer starvation class covered at the owning layer, move back to harness discovery. A
  good next target is retained relation/action-state mutation outside Command/RadioGroup, or a
  layout/paint/hit-test split where cached visuals can pass while semantic/action metadata drifts.

## F244: Switch choice-card checked-state semantics now has a runtime gate

Layer: shadcn Switch recipe-consumer diagnostics and FieldLabel control association.

Status: focused runtime coverage added. No Switch recipe, semantics, or label-association defect
was reproduced in this slice.

What was found:

- Existing Switch runtime coverage already proved read-only action-state suppression, dynamic
  read-only mutation, command-gated action availability, and simple docs-label activation.
- The upstream docs-path Choice Card example has a different risk shape: a card-style
  `FieldLabel::wrap(...)` forwards activation to a nested `Switch` via `ControlId`, while the
  visible card chrome and the control can diverge from exported `checked` semantics if either
  association or model rerendering goes stale.
- The broad shadcn runtime-evidence suite was green after F242/F243, so this was a good moment to
  promote another real non-list form semantics mutation surface into that suite.

What was changed:

- Added `ui-gallery-switch-choice-card-checked-state-mutation.json`.
- The script starts directly on the Switch page, scrolls to Choice Card, asserts initial
  `share=false` and `notifications=true`, proves both controls expose `invoke=true`, clicks both
  card labels to toggle their paired switches independently, clicks the share control itself to
  toggle back, and captures bundle plus screenshot evidence.
- Added the focused `ui-gallery-switch-semantics` suite for cheap reruns.
- Promoted the script into `ui-gallery-shadcn-runtime-evidence`.
- Added direct `fret-diag-protocol` roundtrip coverage and refreshed the script registry.

Evidence anchors:

- Script and suite:
  `tools/diag-scripts/ui-gallery/switch/ui-gallery-switch-choice-card-checked-state-mutation.json`
  and `tools/diag-scripts/suites/ui-gallery-switch-semantics/suite.json`.
- Broad-suite membership:
  `tools/diag-scripts/suites/ui-gallery-shadcn-runtime-evidence/suite.json` and
  `tools/diag-scripts/index.json`.
- Protocol gate:
  `crates/fret-diag-protocol/tests/script_json_roundtrip.rs`.
- Focused runtime AI packet:
  `target/fret-diag-switch-choice-card-checked-state-mutation-v1/sessions/1779301451133-110692/1779301465865/ai.packet`.
- Focused runtime pack:
  `target/fret-diag-switch-choice-card-checked-state-mutation-v1/sessions/1779301451133-110692/share/1779301465865.zip`.
- Dedicated suite summary:
  `target/fret-diag-switch-semantics-suite-v1/sessions/1779301571842-137052/suite.summary.json`.
- Broad-suite summary:
  `target/fret-diag-shadcn-runtime-evidence-switch-choice-card-v1/sessions/1779301689107-175712/suite.summary.json`.

Run results:

- JSON and registry:
  `python -m json.tool tools\diag-scripts\ui-gallery\switch\ui-gallery-switch-choice-card-checked-state-mutation.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-switch-semantics\suite.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-shadcn-runtime-evidence\suite.json > $null`;
  `python tools\check_diag_scripts_registry.py --write`;
  `python tools\check_diag_scripts_registry.py`
  - Result: passed.
- Protocol roundtrip:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol --test script_json_roundtrip script_v2_roundtrip_ui_gallery_switch_choice_card_checked_state_mutation --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `3d7a846c-ec57-4044-8222-e81a4b9f978f`.
- Focused runtime diagnostics:
  `target\dev-fast\fretboard-dev.exe diag run tools\diag-scripts\ui-gallery\switch\ui-gallery-switch-choice-card-checked-state-mutation.json --dir target\fret-diag-switch-choice-card-checked-state-mutation-v1 --session-auto --pack --ai-packet --include-triage --include-screenshots --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; run id `1779301465865`.
- Dedicated runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-switch-semantics --dir target\fret-diag-switch-semantics-suite-v1 --session-auto --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; stage counts `{"passed":1}`; script run id `1779301586186`.
- Broad runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-shadcn-runtime-evidence --dir target\fret-diag-shadcn-runtime-evidence-switch-choice-card-v1 --session-auto --timeout-ms 900000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed 14/14; `reason_code_counts={}`; new Switch row run id
    `1779302260684`.

Next slice recommendation:

- Keep this as a recipe-consumer runtime gate rather than adding lower-level Switch mechanism
  tests: the owning risk is composed FieldLabel/card activation plus exported checked semantics.
- The next high-value semantics slice should either cover another non-list form composition with
  independent label/control state, or move to a retained relation/action-state surface where visual
  reuse can hide stale exported metadata.

## F245: Checkbox disabled action-state semantics now has a runtime gate

Layer: shadcn Checkbox recipe-consumer diagnostics and disabled semantics.

Status: focused runtime coverage added. No Checkbox recipe, semantics, or label-association defect
was reproduced in this slice.

What was found:

- Existing Checkbox runtime coverage already proved that the disabled checkbox does not toggle
  through direct control activation or associated label activation.
- That covered visual/model immutability, but it did not prove the exported semantics contract that
  assistive/runtime consumers need: `disabled=true` and `invoke=false` must remain attached to the
  same control while click and label routes are suppressed.
- This is a useful companion to the Input disabled action-state gate because Checkbox combines a
  checked state, a `FieldLabel` association, and disabled action metadata on a non-text form
  control.

What was changed:

- Added `ui-gallery-checkbox-disabled-action-state.json`.
- The script starts directly on the Checkbox page, scrolls to the Disabled section, proves the
  disabled checkbox is initially checked, exports `disabled=true`, and suppresses `invoke`.
- The script clicks both the disabled control and its associated label, then reasserts that checked
  state remains true and `invoke` remains disabled.
- Added the focused `ui-gallery-checkbox-semantics` suite for cheap reruns.
- Promoted the script into `ui-gallery-shadcn-runtime-evidence`.
- Added direct `fret-diag-protocol` roundtrip coverage and refreshed the script registry.

Evidence anchors:

- Script and suite:
  `tools/diag-scripts/ui-gallery/checkbox/ui-gallery-checkbox-disabled-action-state.json` and
  `tools/diag-scripts/suites/ui-gallery-checkbox-semantics/suite.json`.
- Broad-suite membership:
  `tools/diag-scripts/suites/ui-gallery-shadcn-runtime-evidence/suite.json` and
  `tools/diag-scripts/index.json`.
- Protocol gate:
  `crates/fret-diag-protocol/tests/script_json_roundtrip.rs`.
- Focused runtime AI packet:
  `target/fret-diag-checkbox-disabled-action-state-v1/sessions/1779303551841-64456/1779303569865/ai.packet`.
- Focused runtime pack:
  `target/fret-diag-checkbox-disabled-action-state-v1/sessions/1779303551841-64456/share/1779303569865.zip`.
- Dedicated suite summary:
  `target/fret-diag-checkbox-semantics-suite-v1/sessions/1779303815632-98108/suite.summary.json`.
- Broad-suite summary:
  `target/fret-diag-shadcn-runtime-evidence-checkbox-disabled-v1/sessions/1779304064247-171464/suite.summary.json`.

Run results:

- JSON and registry:
  `python -m json.tool tools\diag-scripts\ui-gallery\checkbox\ui-gallery-checkbox-disabled-action-state.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-checkbox-semantics\suite.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-shadcn-runtime-evidence\suite.json > $null`;
  `python tools\check_diag_scripts_registry.py --write`;
  `python tools\check_diag_scripts_registry.py`
  - Result: passed.
- Protocol roundtrip:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol --test script_json_roundtrip script_v2_roundtrip_ui_gallery_checkbox_disabled_action_state --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `8ace631b-a63d-4a0d-bc9b-55ccc1a64267`.
- Focused runtime diagnostics:
  `target\dev-fast\fretboard-dev.exe diag run tools\diag-scripts\ui-gallery\checkbox\ui-gallery-checkbox-disabled-action-state.json --dir target\fret-diag-checkbox-disabled-action-state-v1 --session-auto --pack --ai-packet --include-triage --include-screenshots --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; run id `1779303569865`.
- Dedicated runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-checkbox-semantics --dir target\fret-diag-checkbox-semantics-suite-v1 --session-auto --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; `stage_counts={"passed":1}`; script run id `1779303834251`.
- Broad runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-shadcn-runtime-evidence --dir target\fret-diag-shadcn-runtime-evidence-checkbox-disabled-v1 --session-auto --timeout-ms 900000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed 15/15; `stage_counts={"passed":15}`; `reason_code_counts={}`;
    Checkbox disabled-action row run id `1779304154355`.

Next slice recommendation:

- Keep this as a recipe-consumer runtime gate. The owning risk is disabled semantics/action export
  plus associated label suppression, not a new Checkbox mechanism defect.
- The next high-value semantics slice should cover either retained relation/action-state metadata
  outside the existing Tree/Command surfaces, or a tri-state/mixed Checkbox gate if the public
  surface promotes indeterminate state into first-party docs.


## F246: Slider numeric action-state gate repaired duplicate thumb test ids

Layer: shadcn Slider recipe-consumer diagnostics, semantics, and recipe-owned `test_id` surface.

Status: focused runtime coverage added and the diagnostics-lint defect found by the new suite was
repaired. No Slider numeric/action semantics defect was reproduced after the repair.

What was found:

- The existing Slider runtime corpus covered drag geometry, scroll-range stability, label focus, and
  `set_slider_value`, but it did not prove the real UI Gallery thumb semantics contract: numeric
  value/min/max/step/jump plus enabled `SetValue`/`Increment`/`Decrement`, and the disabled thumb's
  suppressed action state.
- The first dedicated `ui-gallery-slider-semantics` suite run passed the script assertions but
  failed zero-warning lint. The lint failure exposed duplicate visual thumb `test_id`s on multi-thumb
  Slider examples: `ui-gallery-slider-controlled-thumb`, `ui-gallery-slider-multiple-thumb`, and
  `ui-gallery-slider-range-thumb`.
- The duplicate ids belonged to the recipe chrome, not the semantics thumb nodes: semantic thumbs
  were already indexed as `{prefix}-thumb-{index}`, while the visual thumb containers reused the
  bare `{prefix}-thumb` suffix for every thumb.
- The same lint run also reported `layout.focused_out_of_window` because the script used
  `set_slider_value` on the single slider, then scrolled to the Disabled section while focus stayed
  on the now-offscreen single thumb. That was script hygiene, not a runtime defect.

What was changed:

- Added `ui-gallery-slider-numeric-action-state.json`.
- The script starts directly on the Slider page, asserts enabled thumb numeric metadata and action
  state, changes the single slider value to 80 through diagnostics, then asserts the disabled thumb
  exports `disabled=true` and suppresses `set_value`, `increment`, `decrement`, and `focus`.
- Added the focused `ui-gallery-slider-semantics` suite, promoted the script into
  `ui-gallery-shadcn-runtime-evidence`, refreshed the registry, and added protocol roundtrip
  coverage.
- Repaired Slider's visual thumb chrome ids by deriving `{prefix}-thumb-{index}-chrome` while
  preserving the stable semantic thumb selectors `{prefix}-thumb-{index}` used by existing scripts.
- Added `multi_thumb_slider_derives_unique_thumb_test_ids` so the recipe-owned derived-id contract
  is guarded without launching UI Gallery.
- The script now focuses the visible nav search before the final disabled capture so suite lint does
  not inherit an offscreen focused thumb after scroll.

Evidence anchors:

- Script and suite:
  `tools/diag-scripts/ui-gallery/slider/ui-gallery-slider-numeric-action-state.json` and
  `tools/diag-scripts/suites/ui-gallery-slider-semantics/suite.json`.
- Broad-suite membership:
  `tools/diag-scripts/suites/ui-gallery-shadcn-runtime-evidence/suite.json` and
  `tools/diag-scripts/index.json`.
- Recipe repair:
  `ecosystem/fret-ui-shadcn/src/slider.rs` and `ecosystem/fret-ui-shadcn/src/test_id.rs`.
- Protocol gate:
  `crates/fret-diag-protocol/tests/script_json_roundtrip.rs`.
- Discovery lint artifact:
  `target/fret-diag-slider-semantics-suite-v1/sessions/1779306432063-174216/1779306461660-ui-gallery-slider-numeric-action-state/check.lint.json`.
- Focused runtime AI packet:
  `target/fret-diag-slider-numeric-action-state-v2/sessions/1779307920873-121240/1779307931755/ai.packet`.
- Focused runtime pack:
  `target/fret-diag-slider-numeric-action-state-v2/sessions/1779307920873-121240/share/1779307931755.zip`.
- Dedicated suite summary:
  `target/fret-diag-slider-semantics-suite-v2/sessions/1779307963346-175080/suite.summary.json`.
- Broad-suite summary:
  `target/fret-diag-shadcn-runtime-evidence-slider-numeric-v1/sessions/1779308003840-152848/suite.summary.json`.

Run results:

- JSON, registry, formatting, and diff hygiene:
  `python -m json.tool tools\diag-scripts\ui-gallery\slider\ui-gallery-slider-numeric-action-state.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-slider-semantics\suite.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-shadcn-runtime-evidence\suite.json > $null`;
  `python tools\check_diag_scripts_registry.py --write`;
  `python tools\check_diag_scripts_registry.py`;
  `rustfmt --edition 2024 --check ecosystem\fret-ui-shadcn\src\test_id.rs ecosystem\fret-ui-shadcn\src\slider.rs crates\fret-diag-protocol\tests\script_json_roundtrip.rs`;
  `git diff --check`
  - Result: passed.
- Slider recipe unit gates:
  `cargo test --profile dev-fast -p fret-ui-shadcn multi_thumb_slider_derives_unique_thumb_test_ids --lib -- --nocapture`;
  `cargo test --profile dev-fast -p fret-ui-shadcn slider_set_value_numeric_updates_model_via_accessibility_driver --lib -- --nocapture`
  - Result: passed; one focused test each.
- Protocol roundtrip:
  `cargo nextest run --cargo-profile dev-fast -p fret-diag-protocol --test script_json_roundtrip script_v2_roundtrip_ui_gallery_slider_numeric_action_state --no-fail-fast --no-capture`
  - Result: passed; Nextest run id `1e5e6033-7824-4d00-88c5-f9a857b3e4a8`.
- Build:
  `cargo build --profile dev-fast -p fret-ui-gallery`
  - Result: passed; emitted the pre-existing unrelated `crates/fret-ui/src/declarative/host_widget/paint.rs` unused `start` warning.
- Focused runtime diagnostics:
  `target\dev-fast\fretboard-dev.exe diag run tools\diag-scripts\ui-gallery\slider\ui-gallery-slider-numeric-action-state.json --dir target\fret-diag-slider-numeric-action-state-v2 --session-auto --pack --ai-packet --include-triage --include-screenshots --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; run id `1779307931755`.
- Dedicated runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-slider-semantics --dir target\fret-diag-slider-semantics-suite-v2 --session-auto --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; `stage_counts={"passed":1}`; script run id `1779307974845`.
- Broad runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-shadcn-runtime-evidence --dir target\fret-diag-shadcn-runtime-evidence-slider-numeric-v1 --session-auto --timeout-ms 900000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed 16/16; `stage_counts={"passed":16}`; `reason_code_counts={}`;
    Slider numeric-action row run id `1779308615620`.

Next slice recommendation:

- Keep this as a recipe-consumer runtime gate plus recipe-owned `test_id` contract repair. The
  owning defect was not Slider numeric semantics; it was duplicate derived chrome ids that only a
  linted real gallery run exposed.
- The next high-value semantics slice should move to retained relation/action-state metadata or a
  tri-state/mixed Checkbox gate if the public docs surface promotes indeterminate state.


## F247: Checkbox table mixed checked-state needed an explicit tri-state predicate

Layer: diagnostics protocol/runtime, mechanism-harness semantics oracle, and shadcn Checkbox
recipe-consumer runtime coverage.

Status: focused runtime coverage added. No Checkbox recipe/runtime defect was reproduced.

What was found:

- The Checkbox table example already exposes a real tri-state control: the select-all checkbox is
  indeterminate when only some rows are selected.
- Existing diagnostics scripts could assert the legacy binary `checked` flag via `checked_is` and
  `checked_is_none`, but they could not assert the explicit tri-state `checked_state` value.
- This meant an indeterminate Checkbox gate could accidentally prove only "binary checked is absent"
  rather than proving the accessibility state users and assistive/runtime consumers need:
  `checked_state=mixed`.
- The mechanism-harness oracle was stricter than the runtime predicate for `checked_is_none`; it
  also required `checked_state` to be absent. That made fixture semantics differ from runtime
  diagnostics and hid the need for a separate explicit checked-state predicate.

What was changed:

- Added `UiSemanticsCheckedStateV1` and `checked_state_is` to `fret-diag-protocol`.
- Added builder helpers for `checked_state_is(...)` and `checked_state_is_none(...)`.
- Wired runtime predicate evaluation in `fret-bootstrap` and included the selector in wait-until
  trace recording.
- Wired the same predicate through `fret-mechanism-harness` and aligned `checked_is_none` there with
  the runtime contract: it now checks the legacy binary flag only; explicit tri-state assertions use
  `checked_state_is`.
- Updated `docs/ui-diagnostics-and-scripted-tests.md` so authoring guidance separates binary
  `checked` predicates from explicit tri-state `checked_state_is`.
- Added `ui-gallery-checkbox-table-mixed-state-action.json`, updated the Checkbox semantics suite,
  promoted the new script into the broad shadcn runtime-evidence suite, refreshed the registry, and
  added protocol roundtrip coverage.

Evidence anchors:

- Protocol and builder:
  `crates/fret-diag-protocol/src/lib.rs` and `crates/fret-diag-protocol/src/builder.rs`.
- Runtime and mechanism evaluation:
  `ecosystem/fret-bootstrap/src/ui_diagnostics/predicates.rs`,
  `ecosystem/fret-bootstrap/src/ui_diagnostics/script_steps_wait.rs`,
  `crates/fret-mechanism-harness/src/oracle.rs`, and
  `crates/fret-mechanism-harness/src/lib.rs`.
- Script and suites:
  `tools/diag-scripts/ui-gallery/checkbox/ui-gallery-checkbox-table-mixed-state-action.json`,
  `tools/diag-scripts/suites/ui-gallery-checkbox-semantics/suite.json`,
  `tools/diag-scripts/suites/ui-gallery-shadcn-runtime-evidence/suite.json`, and
  `tools/diag-scripts/index.json`.
- Documentation:
  `docs/ui-diagnostics-and-scripted-tests.md`.
- Focused runtime AI packet:
  `target/fret-diag-checkbox-table-mixed-state-action-v1/sessions/1779310480442-177764/1779310495372/ai.packet`.
- Focused runtime pack:
  `target/fret-diag-checkbox-table-mixed-state-action-v1/sessions/1779310480442-177764/share/1779310495372.zip`.
- Dedicated suite summary:
  `target/fret-diag-checkbox-semantics-suite-table-mixed-v1/sessions/1779310724199-166384/suite.summary.json`.
- Broad-suite summary:
  `target/fret-diag-shadcn-runtime-evidence-checkbox-table-mixed-v1/sessions/1779311169346-151568/suite.summary.json`.

Run results:

- JSON, registry, formatting, and diff hygiene:
  `python -m json.tool tools\diag-scripts\ui-gallery\checkbox\ui-gallery-checkbox-table-mixed-state-action.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-checkbox-semantics\suite.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-shadcn-runtime-evidence\suite.json > $null`;
  `python tools\check_diag_scripts_registry.py --write`;
  `python tools\check_diag_scripts_registry.py`;
  `rustfmt --edition 2024 --check crates\fret-diag-protocol\src\builder.rs crates\fret-diag-protocol\src\lib.rs crates\fret-diag-protocol\tests\script_json_roundtrip.rs crates\fret-mechanism-harness\src\lib.rs crates\fret-mechanism-harness\src\oracle.rs ecosystem\fret-bootstrap\src\ui_diagnostics\predicates.rs ecosystem\fret-bootstrap\src\ui_diagnostics\script_steps_wait.rs`;
  `git diff --check`
  - Result: passed.
- Protocol/bootstrap/mechanism gates:
  `cargo test --profile dev-fast -p fret-diag-protocol predicate_checked_state_is_serializes_and_deserializes --lib -- --nocapture`;
  `cargo test --profile dev-fast -p fret-bootstrap --features ui-app-driver,diagnostics checked_state_is_matches_semantics_checked_state --lib -- --nocapture`;
  `cargo test --profile dev-fast -p fret-mechanism-harness semantics_value_state_actions_and_structured_metadata_are_queryable --lib -- --nocapture`;
  `cargo test --profile dev-fast -p fret-ui mechanism_harness_semantics_relations_match_oracles --lib -- --nocapture`
  - Result: passed.
- Script roundtrip:
  `cargo test --profile dev-fast -p fret-diag-protocol --test script_json_roundtrip script_v2_roundtrip_ui_gallery_checkbox_table_mixed_state_action -- --nocapture`
  - Result: passed.
- Build:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery`
  - Result: passed; emitted pre-existing unrelated warnings from
    `crates/fret-ui/src/declarative/host_widget/paint.rs` and, in test builds,
    `current_effective_opacity`.
- Focused runtime diagnostics:
  `target\dev-fast\fretboard-dev.exe diag run tools\diag-scripts\ui-gallery\checkbox\ui-gallery-checkbox-table-mixed-state-action.json --dir target\fret-diag-checkbox-table-mixed-state-action-v1 --session-auto --pack --ai-packet --include-triage --include-screenshots --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; run id `1779310495372`.
- Dedicated runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-checkbox-semantics --dir target\fret-diag-checkbox-semantics-suite-table-mixed-v1 --session-auto --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed 2/2; `stage_counts={"passed":2}`; Checkbox mixed row run id `1779310910113`.
- Broad runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-shadcn-runtime-evidence --dir target\fret-diag-shadcn-runtime-evidence-checkbox-table-mixed-v1 --session-auto --timeout-ms 900000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed 17/17; `stage_counts={"passed":17}`; `reason_code_counts={}`;
    Checkbox mixed row run id `1779311405413`.

Next slice recommendation:

- Keep this as a diagnostics expressiveness and recipe-consumer runtime gate. The next high-value
  semantics slice should target another state axis that is still inferred indirectly today, such as
  toggle-button `pressed_state`, required/invalid form state, or retained relation/action metadata
  after dynamic detach/reattach.


## F248: Toggle interaction was using selected-state instead of pressed-state

Layer: diagnostics protocol/runtime, mechanism-harness semantics oracle, and shadcn Toggle
recipe-consumer runtime coverage.

Status: focused runtime coverage added. No Toggle recipe/runtime defect was reproduced.

What was found:

- The existing `ui-gallery-toggle-interaction-screenshots.json` gate clicked the Bookmark Toggle and
  asserted `selected_is=true`.
- A probe run failed at that assertion. The bundle showed the runtime was exporting the correct
  Toggle semantics: `role=button`, `focused=true`, and `pressed_state="true"`.
- That means the runtime and recipe were aligned with Radix/shadcn Toggle semantics; the diagnostic
  script was asserting the wrong state axis. Toggle should be gated through `pressed_state`, while
  Tabs/Select-style choices remain on `selected`.
- Diagnostics already exported `pressed_state` in snapshots, but protocol predicates, wait traces,
  and mechanism-harness oracles could not assert it directly.

What was changed:

- Added `UiSemanticsPressedStateV1` and `pressed_state_is` to `fret-diag-protocol`.
- Added builder helpers for `pressed_state_is(...)` and `pressed_state_is_none(...)`.
- Wired runtime predicate evaluation in `fret-bootstrap` and included the selector in wait-until
  trace recording.
- Wired the same predicate through `fret-mechanism-harness` and extended the shared semantics
  structured-metadata test to cover pressed-state oracle evaluation.
- Updated `docs/ui-diagnostics-and-scripted-tests.md` so script authors distinguish
  `pressed_state_is` from `selected_is`.
- Converted `ui-gallery-toggle-interaction-screenshots.json` to a direct-start Toggle semantics
  gate and promoted it into a new `ui-gallery-toggle-semantics` suite plus the broad
  `ui-gallery-shadcn-runtime-evidence` suite.

Evidence anchors:

- Protocol and builder:
  `crates/fret-diag-protocol/src/lib.rs` and `crates/fret-diag-protocol/src/builder.rs`.
- Runtime and mechanism evaluation:
  `ecosystem/fret-bootstrap/src/ui_diagnostics/predicates.rs`,
  `ecosystem/fret-bootstrap/src/ui_diagnostics/script_steps_wait.rs`,
  `crates/fret-mechanism-harness/src/oracle.rs`, and
  `crates/fret-mechanism-harness/src/lib.rs`.
- Script and suites:
  `tools/diag-scripts/ui-gallery/toggle/ui-gallery-toggle-interaction-screenshots.json`,
  `tools/diag-scripts/suites/ui-gallery-toggle-semantics/suite.json`,
  `tools/diag-scripts/suites/ui-gallery-shadcn-runtime-evidence/suite.json`, and
  `tools/diag-scripts/index.json`.
- Documentation:
  `docs/ui-diagnostics-and-scripted-tests.md`.
- Discovery failure AI packet:
  `target/fret-diag-toggle-interaction-selected-probe-v1/sessions/1779313631621-83488/1779313643019/ai.packet`.
- Discovery failure pack:
  `target/fret-diag-toggle-interaction-selected-probe-v1/sessions/1779313631621-83488/share/1779313643019.zip`.
- Focused runtime AI packet:
  `target/fret-diag-toggle-pressed-state-interaction-v1/sessions/1779314794606-180768/1779314805300/ai.packet`.
- Focused runtime pack:
  `target/fret-diag-toggle-pressed-state-interaction-v1/sessions/1779314794606-180768/share/1779314805300.zip`.
- Dedicated suite summary:
  `target/fret-diag-toggle-semantics-suite-v1/sessions/1779314830681-87028/suite.summary.json`.
- Broad-suite summary:
  `target/fret-diag-shadcn-runtime-evidence-toggle-pressed-v2/sessions/1779316094427-64032/suite.summary.json`.

Run results:

- JSON, registry, formatting, and diff hygiene:
  `python -m json.tool tools\diag-scripts\ui-gallery\toggle\ui-gallery-toggle-interaction-screenshots.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-toggle-semantics\suite.json > $null`;
  `python -m json.tool tools\diag-scripts\suites\ui-gallery-shadcn-runtime-evidence\suite.json > $null`;
  `python tools\check_diag_scripts_registry.py --write`;
  `python tools\check_diag_scripts_registry.py`;
  `rustfmt --edition 2024 --check crates\fret-diag-protocol\src\builder.rs crates\fret-diag-protocol\src\lib.rs crates\fret-diag-protocol\tests\script_json_roundtrip.rs crates\fret-mechanism-harness\src\lib.rs crates\fret-mechanism-harness\src\oracle.rs ecosystem\fret-bootstrap\src\ui_diagnostics\predicates.rs ecosystem\fret-bootstrap\src\ui_diagnostics\script_steps_wait.rs`;
  `git diff --check`
  - Result: passed.
- Protocol/bootstrap/mechanism gates:
  `cargo test --profile dev-fast -p fret-diag-protocol predicate_pressed_state_is_serializes_and_deserializes --lib -- --nocapture`;
  `cargo test --profile dev-fast -p fret-bootstrap --features ui-app-driver,diagnostics pressed_state_is_matches_semantics_pressed_state --lib -- --nocapture`;
  `cargo test --profile dev-fast -p fret-mechanism-harness semantics_value_state_actions_and_structured_metadata_are_queryable --lib -- --nocapture`;
  `cargo test --profile dev-fast -p fret-ui mechanism_harness_semantics_relations_match_oracles --lib -- --nocapture`
  - Result: passed.
- Script roundtrip:
  `cargo test --profile dev-fast -p fret-diag-protocol --test script_json_roundtrip script_v2_roundtrip_ui_gallery_toggle_interaction_screenshots -- --nocapture`
  - Result: passed.
- Build:
  `cargo build --profile dev-fast -p fretboard-dev -p fret-ui-gallery`
  - Result: passed; emitted pre-existing unrelated warnings from
    `crates/fret-ui/src/declarative/host_widget/paint.rs` and, in test builds,
    `current_effective_opacity`.
- Focused runtime diagnostics:
  `target\dev-fast\fretboard-dev.exe diag run tools\diag-scripts\ui-gallery\toggle\ui-gallery-toggle-interaction-screenshots.json --dir target\fret-diag-toggle-pressed-state-interaction-v1 --session-auto --pack --ai-packet --include-triage --include-screenshots --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed; run id `1779314805300`.
- Dedicated runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-toggle-semantics --dir target\fret-diag-toggle-semantics-suite-v1 --session-auto --timeout-ms 300000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed 1/1; `stage_counts={"passed":1}`; Toggle row run id `1779314840880`.
- Broad runtime suite:
  `target\dev-fast\fretboard-dev.exe diag suite ui-gallery-shadcn-runtime-evidence --dir target\fret-diag-shadcn-runtime-evidence-toggle-pressed-v2 --session-auto --timeout-ms 1200000 --launch -- target\dev-fast\fret-ui-gallery.exe`
  - Result: passed 18/18; `stage_counts={"passed":18}`; `reason_code_counts={}`;
    Toggle row run id `1779317023787`.

Next slice recommendation:

- Keep this as a diagnostics expressiveness and recipe-consumer runtime gate. The next high-value
  semantics slice should target required/invalid form state or another state axis currently inferred
  indirectly by scripts.
