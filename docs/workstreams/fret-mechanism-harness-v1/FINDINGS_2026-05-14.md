---
title: Fret Mechanism Harness v1 Findings 2026-05-14
status: active
date: 2026-05-14
---

# Findings 2026-05-14

## F61: Hidden semantics were still selectable by default harness oracles

Layer: `crates/fret-mechanism-harness` observation/oracle semantics filtering +
`crates/fret-ui` declarative semantics fixture coverage

Status: fixed and covered

What was found:

- The semantics metadata fixture had already grown beyond the coverage map: value/editing metadata,
  collection metadata, action surfaces, live-region metadata, numeric metadata, and scroll metadata
  were present and passing.
- The remaining real semantics gap was hidden-subtree policy from ADR 0293. `SemanticsSnapshot`
  intentionally keeps hidden nodes for deterministic diagnostics, but default harness selectors
  should model the filtered accessibility tree.
- `ObservedTree::select_best` filtered invisible roots and input barriers, but did not filter
  `SemanticsFlags.hidden` or descendants of hidden nodes. This could let mechanism fixtures
  incorrectly treat `aria-hidden` / hidden diagnostic nodes as user-facing accessibility matches.
- `semantics_flag_is` also used the default filtered path, so a correct hidden-subtree filter would
  have made it impossible to assert the hidden flag on retained diagnostic nodes.

What was fixed:

- `ObservedTree::node_is_selectable` now excludes nodes whose own semantics flag is hidden or whose
  ancestor is hidden.
- `semantics_flag_is` now uses the unfiltered selector path so diagnostics can still assert hidden
  flags and inspect retained nodes inside hidden subtrees.
- Added a focused harness regression:
  `default_selectors_exclude_semantics_hidden_subtrees_but_flags_remain_queryable`.
- Extended `semantics_relations_v1.json` with
  `hidden-subtree-policy-excludes-default-selectors-but-keeps-diagnostics`, backed by a real
  declarative `SemanticsProps` scenario.

Run results:

- `cargo test --profile dev-fast -p fret-mechanism-harness --lib default_selectors_exclude_semantics_hidden_subtrees_but_flags_remain_queryable -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-ui --lib mechanism_harness_semantics_relations_match_oracles -- --nocapture`
  - Result: passed, 1 test.
- `cargo fmt --package fret-mechanism-harness --package fret-ui`
  - Result: passed.

Evidence anchors:

- `crates/fret-mechanism-harness/src/observe.rs`
- `crates/fret-mechanism-harness/src/oracle.rs`
- `crates/fret-mechanism-harness/src/lib.rs`
- `crates/fret-ui/src/declarative/tests/fixtures/semantics_relations_v1.json`
- `crates/fret-ui/src/declarative/tests/semantics_relations_harness.rs`
- `docs/adr/0293-a11y-hidden-semantics-v1.md`

Next slice recommendation:

- Add a UI Gallery a11y diagnostics gate once a stable public page exposes filtered-vs-diagnostic
  semantics, live-region updates, or recipe-level hidden decorative affordances. The synthetic
  semantics harness is now strong enough for mechanism proof, but the lane still lacks a runtime
  a11y diagnostics gate comparable to the overlay/focus and TextInput visual gates.

## F62: Scroll/virtual-list runtime gates needed structured instability oracles

Layer: diagnostics protocol + bootstrap script runner + UI Gallery runtime gates

Status: fixed and covered

What was found:

- The Virtual List small-scroll diagnostics script was not a durable runtime gate. It targeted the
  dev-only Virtual List Torture page without recording the `gallery-dev` precondition, and it
  initially used `ui-gallery-virtual-list-root` where the live scroll semantics were actually on
  `ui-gallery-content-viewport-virtual_list_torture`.
- The existing Checkbox RTL scripts could prove "eventually stable" at a short boundary, but could
  not assert "no-input idle stability for N frames." That left the user's RTL scroll jitter report
  hard to convert into a reusable mechanism harness gate.
- The first implementation of idle-stability evidence exposed a second harness defect: the new
  trace was recorded during the assertion, but `note_step_start_and_scope_evidence` dropped it when
  the following capture step started, so passed script results lost the structured sampling proof.
- Current runtime evidence did not reproduce a real RTL scroll mechanism defect on the default
  Checkbox page. The new gate sampled 45 idle frames with no scroll drift.

What was fixed:

- Added `UiPredicateV1::VirtualListWindowShiftSamplesLenLe` so virtual-list debug telemetry can be
  asserted directly from diagnostics scripts.
- Hardened `ui-gallery-virtual-list-small-scroll-no-window-shifts.json` with feature metadata,
  stable navigation, the correct scroll semantics target, diagnostic resets, and `max=0` window-shift
  assertions before and after small wheel deltas.
- Added `assert_semantics_scroll_idle_stable` to diagnostics script v2. It samples a structured
  semantics scroll field for a fixed no-input frame window and fails when single-frame or total
  drift exceeds configured thresholds.
- Added `UiSemanticsScrollIdleStableTraceEntryV1` evidence and retained it across later capture
  steps so passed scripts keep their final sampling proof.
- Promoted `ui-gallery-checkbox-scroll-to-rtl-field.json` into `ui-gallery-rtl-smoke` and
  `ui-gallery-shadcn-conformance`.

Run results:

- `cargo test --profile dev-fast -p fret-diag-protocol --lib predicate_virtual_list_window_shift_samples_len_le_serializes -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-diag-protocol --lib step_assert_semantics_scroll_idle_stable_deserializes_with_defaults -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-bootstrap --features diagnostics,ui-app-driver --lib step_start_retains_semantics_scroll_idle_stable_trace_for_passed_evidence -- --nocapture`
  - Result: passed, 1 test.
- `cargo test --profile dev-fast -p fret-bootstrap --features diagnostics,ui-app-driver --lib ui_diagnostics -- --nocapture`
  - Result: passed, 96 tests.
- `cargo build -p fret-ui-gallery --release`
  - Result: passed.
- `cargo run -p fretboard-dev -- diag run tools/diag-scripts/ui-gallery/virtual-list/ui-gallery-virtual-list-small-scroll-no-window-shifts.json --dir target/fret-diag-virtual-list-harness-commit-check --session-auto --pack --ai-packet --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed.
- `cargo run -p fretboard-dev -- diag run tools/diag-scripts/ui-gallery/checkbox/ui-gallery-checkbox-scroll-to-rtl-field.json --dir target/fret-diag-rtl-scroll-idle-stability-v2 --session-auto --pack --ai-packet --include-screenshots --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed.
- `python tools/check_diag_scripts_registry.py`
  - Result: passed.

Evidence anchors:

- `crates/fret-diag-protocol/src/lib.rs`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/script_steps_wait.rs`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/script_runner.rs`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/service.rs`
- `tools/diag-scripts/ui-gallery/virtual-list/ui-gallery-virtual-list-small-scroll-no-window-shifts.json`
- `tools/diag-scripts/ui-gallery/checkbox/ui-gallery-checkbox-scroll-to-rtl-field.json`
- `tools/diag-scripts/suites/ui-gallery-rtl-smoke/suite.json`
- `tools/diag-scripts/suites/ui-gallery-shadcn-conformance/suite.json`
- Virtual-list evidence:
  `target/fret-diag-virtual-list-harness-commit-check/sessions/1778697640180-135472/1778697641371/script.result.json`
- Checkbox RTL idle-stability evidence:
  `target/fret-diag-rtl-scroll-idle-stability-v2/sessions/1778699811444-47076/1778699812656/script.result.json`
  - Trace: `sample_count=45`, `baseline_value=2495.999755859375`,
    `frame_delta=0.0`, `total_delta=0.0`.

Next slice recommendation:

- Add idle-stability companions for ScrollArea and Table/DataTable RTL surfaces, then add a
  boundary-crossing virtual-list runtime gate that intentionally scrolls far enough to shift the
  retained window and asserts the recorded reason/apply mode instead of `max=0`.

## F63: ScrollArea/Table RTL needed idle-stability runtime companions

Layer: diagnostics scripts + UI Gallery runtime gates

Status: covered; no mechanism defect reproduced on these surfaces

What was found:

- The existing Checkbox RTL idle-stability gate covered only the default page content viewport after
  scrolling to an RTL section. That was useful, but it did not cover a nested RTL scroll root where
  inner scroll offset, outer page scroll offset, and RTL layout direction can interact.
- The existing ScrollArea RTL diagnostics script was screenshot-only. It proved the demo could be
  reached, but it did not assert that scrolling the nested RTL viewport settled after scripted input.
- The Table page had stable RTL title/row anchors from `ui-gallery-table-docs-smoke`, but no
  idle-stability gate that sampled the outer Gallery content viewport after scrolling to the RTL
  table section.
- The user's fresh "scroll to a place in RTL and the page keeps moving up/down" report therefore
  still had an obvious harness gap even though the Checkbox RTL gate was green.

What was fixed:

- Added `ui-gallery-scroll-area-rtl-idle-stability.json`, which navigates to the Scroll Area page,
  scrolls the RTL ScrollArea into view, scrolls the nested RTL viewport, then samples both the inner
  `ui-gallery-scroll-area-rtl-viewport` and the outer `ui-gallery-content-viewport` for 60 no-input
  frames.
- Added `ui-gallery-table-rtl-idle-stability.json`, which navigates to the Table page, scrolls the
  RTL table section into view, waits for the RTL title and final invoice row to settle, then samples
  the outer `ui-gallery-content-viewport` for 60 no-input frames.
- Promoted the gate into both `ui-gallery-rtl-smoke` and `ui-gallery-shadcn-conformance`.
- Regenerated `tools/diag-scripts/index.json`.

Run results:

- `cargo build -p fret-ui-gallery --release`
  - Result: passed.
- `cargo test --profile dev-fast -p fret-diag-protocol --lib step_assert_semantics_scroll_idle_stable_deserializes_with_defaults -- --nocapture`
  - Result: passed, 1 test.
- `cargo run -p fretboard-dev -- diag run tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scroll-area-rtl-idle-stability.json --dir target/fret-diag-scroll-area-rtl-idle-stability --session-auto --pack --ai-packet --include-screenshots --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed.
  - Trace proof: nested RTL viewport stayed at `y=480.0` for 60 samples with
    `frame_delta=0.0` and `total_delta=0.0`; outer content viewport stayed at `y=1220.0` for
    60 samples with `frame_delta=0.0` and `total_delta=0.0`.
- `cargo run -p fretboard-dev -- diag run tools/diag-scripts/ui-gallery/table/ui-gallery-table-rtl-idle-stability.json --dir target/fret-diag-table-rtl-idle-stability --session-auto --pack --ai-packet --include-screenshots --launch -- target/release/fret-ui-gallery.exe`
  - Result: passed.
  - Trace proof: outer content viewport stayed at `y=1260.0` for 60 samples with
    `frame_delta=0.0` and `total_delta=0.0`.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scroll-area-rtl-idle-stability.json`
- `tools/diag-scripts/ui-gallery/table/ui-gallery-table-rtl-idle-stability.json`
- `tools/diag-scripts/suites/ui-gallery-rtl-smoke/suite.json`
- `tools/diag-scripts/suites/ui-gallery-shadcn-conformance/suite.json`
- `target/fret-diag-scroll-area-rtl-idle-stability/sessions/1778705030563-134220/1778705031873/script.result.json`
- `target/fret-diag-scroll-area-rtl-idle-stability/sessions/1778705030563-134220/share/1778705031873.zip`
- `target/fret-diag-table-rtl-idle-stability/sessions/1778705380996-128024/1778705382221/script.result.json`
- `target/fret-diag-table-rtl-idle-stability/sessions/1778705380996-128024/share/1778705382221.zip`

Next slice recommendation:

- Add a DataTable RTL idle-stability companion or capture the exact user-observed RTL route.
  The current Checkbox, ScrollArea, and Table gates did not reproduce the reported jitter, so the next
  defect-discovery step should broaden the surface rather than tune thresholds on passing gates.

## F64: Virtual-list boundary ownership was split across event, prepaint, and retained reconcile paths

Layer: `crates/fret-ui` virtual-list event handling, layout-state scroll consumption, prepaint
window attribution, retained host reconcile, plus `fret-workspace` render-time model writes that
polluted view-cache diagnostics.

Status: fixed and covered for focused mechanism gates; retained runtime suite wrapper clean-exit
still needs a follow-up check.

What was found:

- The boundary-crossing virtual-list diagnostics exposed a real owner split. Non-retained
  `VirtualList` wheel handling called `cx.notify()` when the visible range escaped the rendered
  window, so render could commit the new window before prepaint observed the old rendered window plus
  new offset transition. That made prepaint ownership and reason attribution unreliable.
- Retained virtual-list reconcile preferred cached prepaint/render windows even when a programmatic
  scroll-handle offset change scheduled reconcile without a preceding prepaint pass. That could keep
  stale retained rows after a boundary-crossing scroll.
- Final scroll-handle baseline consumption still needed a non-retained cache-root dirty fallback
  for revision-only or paint-before-prepaint paths where no later prepaint owner would invalidate the
  stale rendered window.
- Prepaint reason attribution could report `Unknown` when an earlier binding pass had already copied
  the new offset into `VirtualListState`, even though the stale render window no longer covered the
  current visible range.
- `WorkspacePaneContentFocusTarget` and `WorkspaceCommandScope` performed render-time no-op
  `ModelStore::update` calls. Because `ModelStore::update` always dirties the model, these writes
  polluted view-cache/root-boundary diagnostics and obscured actual cache owner changes.

What was fixed:

- Non-retained virtual-list wheel events no longer notify the view-cache root on boundary escape;
  prepaint owns the stale-window plus new-offset observation. Retained hosts still mark retained
  reconcile from the event path.
- Retained virtual-list reconcile now reuses a cached window only while it covers the current
  visible range; otherwise it derives a new retained window from the current scroll offset.
- Final scroll-handle baseline consumption directly dirties the non-retained cache root when it is
  the last owner path available.
- `VirtualListPrepaintWindowUpdate` now carries the current visible range, and prepaint falls back
  to `ScrollOffset` reason attribution when the rendered window no longer covers that visible range.
- Workspace focus registry/scope components now read first and only update models when the stored
  value actually changes.

Run results:

- `cargo nextest run -p fret-ui prepaint_attributes_window_escape_to_scroll_offset_when_state_offset_was_synced retained_virtual_list_host_updates_window_without_rerendering_view_cache_root view_cache_virtual_list_revision_only_bump_after_internal_offset_update_marks_window_update prepaint_updates_virtual_list_window_and_marks_cache_root_dirty_on_escape prepaint_detects_render_window_insufficient_for_overscan_policy`
  - Result before the latest `main` merge: passed, 5 tests.
  - Nextest run id: `c0313ca0-8760-447a-a008-5c63eb4b136d`.
- `cargo nextest run -p fret-ui prepaint_attributes_window_escape_to_scroll_offset_when_state_offset_was_synced retained_virtual_list_host_updates_window_without_rerendering_view_cache_root`
  - Result after merging latest `main`: passed, 2 tests.
  - Nextest run id: `d9225669-03a6-4150-b763-3e3d5f6d6485`.
- `cargo nextest run -p fret-workspace`
  - Result: passed, 81 tests.
  - Nextest run id: `bf6c74ed-101b-40a5-8727-2ccc596d6011`.
- `cargo run -p fretboard-dev -- diag suite ui-gallery-vlist-window-boundary --dir target/fret-diag-vlist-window-boundary-after-reason-fix --session-auto --launch -- cargo run -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
  - Evidence: `target/fret-diag-vlist-window-boundary-after-reason-fix/sessions/1778718360331-96416/suite.summary.json`
- `$env:FRET_UI_GALLERY_VLIST_RETAINED='1'; cargo run -p fretboard-dev -- diag suite ui-gallery-vlist-window-boundary-retained --dir target/fret-diag-vlist-window-boundary-retained-after-owner-fix --session-auto --launch -- cargo run -p fret-ui-gallery --features gallery-dev`
  - Result: the script result artifact was written after the retained path passed far enough to
    capture evidence, but the outer suite command returned non-zero and did not write a
    `suite.summary.json`. Treat this as an unresolved suite-wrapper clean-exit gap, not as a fully
    closed runtime gate.
  - Evidence: `target/fret-diag-vlist-window-boundary-retained-after-owner-fix/sessions/1778718929118-137636/script.result.json`
- `python tools/check_workstream_catalog.py`
  - Result after latest `main` merge: passed, 369 dedicated directories and 47 standalone markdown
    files.
- `python tools/check_diag_scripts_registry.py`
  - Result after latest `main` merge: passed.

Evidence anchors:

- `crates/fret-ui/src/declarative/host_widget/event/scroll.rs`
- `crates/fret-ui/src/declarative/mount.rs`
- `crates/fret-ui/src/tree/layout/state.rs`
- `crates/fret-ui/src/tree/prepaint/mod.rs`
- `crates/fret-ui/src/tree/prepaint/virtual_list.rs`
- `crates/fret-ui/src/tree/prepaint/tests.rs`
- `ecosystem/fret-workspace/src/command_scope.rs`
- `ecosystem/fret-workspace/src/pane_content_focus.rs`
- `tools/diag-scripts/ui-gallery/virtual-list/ui-gallery-virtual-list-window-boundary-scroll.json`
- `tools/diag-scripts/ui-gallery/virtual-list/ui-gallery-virtual-list-window-boundary-scroll-retained.json`
- `tools/diag-scripts/suites/ui-gallery-vlist-window-boundary/suite.json`
- `tools/diag-scripts/suites/ui-gallery-vlist-window-boundary-retained/suite.json`
- `target/fret-diag-vlist-window-boundary-after-reason-fix/sessions/1778718360331-96416/suite.summary.json`
- `target/fret-diag-vlist-window-boundary-retained-after-owner-fix/sessions/1778718929118-137636/script.result.json`

Next slice recommendation:

- Close the retained suite-wrapper clean-exit gap so retained boundary-crossing coverage has the
  same `suite.summary.json` proof as non-retained coverage. After that, add a DataTable retained
  window-boundary/RTL idle-stability companion to exercise nested component consumers rather than
  only the dev-only Virtual List page.

## F65: Retained virtual-list keep-alive reuse was a harness/gate drift, not a mechanism defect

Layer: `fret-diag` suite finalization and retained virtual-list streaming post-run gates, plus the
UI Gallery retained virtual-list diagnostics script.

Status: fixed and covered.

What was found:

- The retained boundary suite failure after F64 was not evidence that retained `VirtualList`
  keep-alive reuse was broken. The script scrolled across the boundary only once, so it could prove
  attach/detach and keep-alive insertion but did not bounce back to reuse old rows.
- After adding the bounce, the live script predicate observed reuse successfully, but the post-run
  streaming gate still reported `keep_alive_reuse_frames=0`. The runtime bundle contained current
  schema telemetry under `debug.retained_virtual_list_reconciles[].reused_from_keep_alive_items`,
  while the streaming reader still looked for the older
  `retained_virtual_list_reconcile_records[].reused_keep_alive_items` shape.
- The streaming reader also had a hard-coded early stop at three reuse frames even though suite
  policy could request another minimum. That made the gate less trustworthy as a general harness
  primitive.

What was fixed:

- The retained Virtual List Torture script now performs a true bounce: it scrolls down across the
  retained window boundary, asserts attach/detach, scrolls back, and asserts
  `reused_from_keep_alive_items_min=1`.
- The streaming keep-alive reuse gate now reads both current
  `retained_virtual_list_reconciles[].reused_from_keep_alive_items` telemetry and the legacy record
  shape, accepting numeric and historical boolean reuse values.
- The streaming gate now stops according to the requested `min_keep_alive_reuse_frames` rather than
  the old hard-coded threshold.
- The retained window-boundary suite default reuse threshold is now `1`, matching the single-bounce
  proof. Broader perf-style bounce stress should use a dedicated repeated-bounce suite instead of
  overloading this owner/reuse correctness gate.

Run results:

- `cargo nextest run -p fret-diag check_bundle_for_retained_vlist_keep_alive_reuse_min_streaming_reads_current_and_legacy_schema finalize_suite_success_tail_error_writes_summary_and_preserves_passed_row finalize_suite_script_success_tail_records_row_when_lint_and_post_run_skip`
  - Result: passed, 3 tests.
  - Nextest run id: `b31eeccd-9ff3-46fd-ba11-55847797da4e`.
- `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- `cargo run -p fretboard-dev -- diag suite ui-gallery-vlist-window-boundary-retained --dir target/fret-diag-vlist-window-boundary-retained-bounce-script-final --session-auto --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Evidence: `target/fret-diag-vlist-window-boundary-retained-bounce-script-final/sessions/1778726366803-140844/suite.summary.json`.
  - Bundle proof: frame 54 recorded an `escape` retained reconcile with
    `reused_from_keep_alive_items=9` and `keep_alive_pool_len_after=9`.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/virtual-list/ui-gallery-virtual-list-window-boundary-scroll-retained.json`
- `crates/fret-diag/src/stats/retained_vlist_gates_streaming/keep_alive_reuse_min.rs`
- `crates/fret-diag/src/diag_suite.rs`
- `crates/fret-diag/src/tests.rs`
- `target/fret-diag-vlist-window-boundary-retained-bounce-script-final/sessions/1778726366803-140844/suite.summary.json`

Next slice recommendation:

- Add a retained-host synthetic reconcile fixture metric surface so keep-alive attach/detach/reuse
  can be checked without a UI Gallery process, then add a DataTable retained window-boundary or RTL
  idle-stability companion to exercise a nested component consumer path.

## F66: Retained-host reconcile fixture closed a synthetic harness gap, and exposed frame-scoped sampling

Layer: `crates/fret-ui` mechanism harness test surface.

Status: fixed and covered.

What was found:

- The retained keep-alive invariant was covered by a focused Rust test and a UI Gallery diagnostics
  suite, but not by a fixture-driven synthetic harness case. That meant future cases could only be
  added by writing more procedural Rust or launching the Gallery.
- The first fixture runner draft incorrectly captured reconcile debug records at the end of a
  multi-frame step. `UiTree` clears debug records when a new debug frame begins, so records from
  the first frame of a scroll step were silently dropped before the runner evaluated its oracle.
- After fixing the runner sampling window, the synthetic fixture observed retained keep-alive insert
  and reuse correctly. This did not expose a retained virtual-list mechanism defect.

What was fixed:

- Added `retained_virtual_list_reconcile_v1.json`, a fixture-driven bounce scenario that scrolls a
  retained virtual list down across the window boundary and then back to the top.
- Added `retained_virtual_list_reconcile_harness.rs`, a thin runner using the real declarative
  retained virtual-list host and scalar mechanism metrics for reconcile records.
- The runner now accumulates retained reconcile records immediately after each frame, before the
  next frame can clear frame-scoped debug telemetry.
- The fixture asserts keep-alive insertion on the downward scroll, reuse on the return scroll,
  no fresh attaches on the return path, and cache-root render stability after warmup.

Run results:

- `cargo nextest run -p fret-ui mechanism_harness_retained_virtual_list_reconcile_matches_oracles`
  - Result: passed, 1 test.
  - Nextest run id: `0ab07b84-dfed-4198-8a55-70754688b874`.

Evidence anchors:

- `crates/fret-ui/src/declarative/tests/fixtures/retained_virtual_list_reconcile_v1.json`
- `crates/fret-ui/src/declarative/tests/retained_virtual_list_reconcile_harness.rs`
- `crates/fret-ui/src/declarative/tests/virtual_list/retained.rs`

Next slice recommendation:

- Add prepaint prefetch/detail synthetic cases for viewport/content/items-revision updates, then
  add a DataTable or component-consumer RTL idle-stability companion so virtualization/scroll
  coverage is not limited to the dev-only Virtual List page.

## F67: Prepaint dirty cache-root attribution lost viewport/items-revision details

Layer: `crates/fret-ui` prepaint virtual-list mechanism and mechanism harness coverage.

Status: fixed and covered.

What was found:

- The prepaint virtual-list path already emitted detailed debug telemetry for window shifts:
  `ScrollOffset`, `ViewportResize`, `ItemsRevision`, and `ScrollToItem` could be distinguished in
  `UiDebugVirtualListWindow`.
- The actual cache-root dirty attribution did not reuse that detailed classification. For
  non-retained paths, `mark_nearest_view_cache_root_needs_rerender` recomputed detail from only the
  coarse shift kind (`Prefetch` or `Escape`), so viewport-resize prefetches and items-revision
  escapes were marked with generic details.
- The new fixture first failed on the viewport-resize and items-revision cases: prepaint debug
  metrics were specific, but the cache-root dirty reason metrics were missing.

What was fixed:

- Added `virtual_list_window_update_v1.json`, a synthetic fixture suite for prepaint virtual-list
  window-shift kind/reason/detail outcomes.
- Added `prepaint_virtual_list_window_update_harness.rs`, a thin runner that builds a minimal
  `UiTree`, seeds `VirtualListState`, calls the real prepaint window-update entry point, and
  exposes scalar metrics for prepaint debug records plus dirty cache-root reason.
- Refactored `prepaint/virtual_list.rs` so debug telemetry and actual non-retained cache-root dirty
  attribution use the same `classify_virtual_list_window_shift` result. The dirty reason now keeps
  `ScrollHandleViewportResizeWindowUpdate`, `ScrollHandleItemsRevisionWindowUpdate`, and
  `ScrollHandleScrollToItemWindowUpdate` where applicable.

Run results:

- `cargo nextest run -p fret-ui mechanism_harness_prepaint_virtual_list_window_update_matches_oracles`
  - First result: failed, 1 test.
  - Nextest run id: `7f2de95b-4436-4bd7-ad2e-44e818e389b4`.
  - Failing cases: `viewport-resize-prefetch-non-retained` and
    `items-revision-escape-non-retained` lacked the expected cache-root dirty reason.
- `cargo nextest run -p fret-ui mechanism_harness_prepaint_virtual_list_window_update_matches_oracles`
  - Result after fix: passed, 1 test.
  - Nextest run id: `faf3bf59-56a5-4d09-9bf2-cc1a4c1694a4`.
- `cargo nextest run -p fret-ui prepaint_detects_render_window_insufficient_for_overscan_policy prepaint_marks_scroll_to_item_window_updates_with_distinct_invalidation_detail prepaint_attributes_window_escape_to_scroll_offset_when_state_offset_was_synced prepaint_updates_virtual_list_window_and_marks_cache_root_dirty_on_escape virtual_list_window_shift_detail_classifies_items_revision`
  - Result after fix: passed, 5 tests.
  - Nextest run id: `2d4873d1-1e3f-4f09-a4d5-bb67fc2118a5`.

Evidence anchors:

- `crates/fret-ui/src/tree/prepaint/tests/fixtures/virtual_list_window_update_v1.json`
- `crates/fret-ui/src/tree/prepaint/tests/prepaint_virtual_list_window_update_harness.rs`
- `crates/fret-ui/src/tree/prepaint/virtual_list.rs`
- `crates/fret-ui/src/tree/debug/virtual_list.rs`
- `crates/fret-ui/src/tree/debug/invalidation.rs`

Next slice recommendation:

- Add content-extent and length-shrink window-update cases, then add a DataTable or nested
  component-consumer RTL idle-stability companion so scroll/virtualization coverage exercises a
  realistic table consumer in addition to the dev-only Virtual List page.

## F68: Length-shrink virtual-list inputs were not classified as input changes

Layer: `crates/fret-ui` prepaint virtual-list mechanism and invalidation detail taxonomy.

Status: fixed and covered.

What was found:

- Extending the prepaint fixture to simulate a filtered/data-shrunk list exposed another mechanism
  attribution gap. The current list length shrank from the rendered window's old `count`, but
  prepaint did not classify the resulting window shift as `InputsChange`.
- Without a distinct detail, diagnostics could conflate data/filter-driven window corrections with
  generic scroll-window updates. That weakens future DataTable and virtualized table triage.

What was fixed:

- The prepaint window-update fixture now allows the stale `render_window.count` to differ from the
  current `len` and adds `length-shrink-clamps-offset-and-classifies-inputs-change`.
- `classify_virtual_list_window_shift` now checks render-window input mismatch before offset deltas
  and emits `UiDebugVirtualListWindowShiftReason::InputsChange`.
- Added `UiDebugInvalidationDetail::ScrollHandleInputsChangeWindowUpdate` and included it in the
  view-dirty invalidation detail set so cache-root dirty attribution can preserve the data/input
  cause.

Run results:

- `cargo nextest run -p fret-ui mechanism_harness_prepaint_virtual_list_window_update_matches_oracles`
  - First result after adding the case: failed, 1 test.
  - Nextest run id: `3d32b0e7-e520-4f6a-94d3-241d01903c1c`.
  - Failing case: `length-shrink-clamps-offset-and-classifies-inputs-change` did not expose
    `prepaint.window_shift.reason.inputs_change`.
- `cargo nextest run -p fret-ui mechanism_harness_prepaint_virtual_list_window_update_matches_oracles`
  - Result after fix: passed, 1 test.
  - Nextest run id: `7fd6de79-fd32-421c-88f4-7844cc05ea2f`.
- `cargo nextest run -p fret-ui prepaint_detects_render_window_insufficient_for_overscan_policy prepaint_marks_scroll_to_item_window_updates_with_distinct_invalidation_detail prepaint_attributes_window_escape_to_scroll_offset_when_state_offset_was_synced prepaint_updates_virtual_list_window_and_marks_cache_root_dirty_on_escape virtual_list_window_shift_detail_classifies_items_revision view_cache_virtual_list_revision_only_bump_after_internal_offset_update_marks_window_update`
  - Result after fix: passed, 6 tests.
  - Nextest run id: `3f91c35e-f1ff-4e06-8b13-155dc289a493`.

Evidence anchors:

- `crates/fret-ui/src/tree/prepaint/tests/fixtures/virtual_list_window_update_v1.json`
- `crates/fret-ui/src/tree/prepaint/tests/prepaint_virtual_list_window_update_harness.rs`
- `crates/fret-ui/src/tree/prepaint/virtual_list.rs`
- `crates/fret-ui/src/tree/debug/invalidation.rs`
- `crates/fret-ui/src/tree/ui_tree_invalidation_walk/mark.rs`

Next slice recommendation:

- Add a component-consumer virtualization companion, preferably DataTable RTL idle-stability or
  filter/length-shrink diagnostics, so the synthetic `InputsChange` proof is exercised through a
  realistic shadcn/table surface rather than only a prepaint unit harness.

## F69: DataTable retained filter shrink exercised the real component consumer path

Layer: diagnostics predicate surface + `crates/fret-ui` virtual-list layout/prepaint mechanism +
`ecosystem/fret-ui-kit` retained DataTable row ordering + UI Gallery runtime gate

Status: fixed and covered

What was found:

- The synthetic length-shrink fixture from F68 proved the prepaint mechanism invariant, but it did
  not prove that a real shadcn-style table consumer could expose the same defect without manual
  screenshot feedback.
- The retained DataTable path recomputed retained row entries from the raw data length and local
  sorting, instead of reusing the filtered `FlatRowOrderCache`. After typing a global filter such as
  `Process 123`, the retained virtualized path could still behave as though it had 50,000 rows
  instead of the filtered 111-row order.
- The layout-time virtual-list debug path still had local reason/detail classification logic. It
  did not treat render-window or previous-window `count`/`overscan` mismatches as a first-class
  `InputsChange` transition in the same way as the prepaint mechanism path.
- Diagnostics predicates could filter virtual-list windows by shift kind, reason, apply mode, and
  source, but not by `invalidation_detail`. That made future non-retained input-change/runtime
  gates less precise even though this retained gate intentionally leaves invalidation detail unset.

What was fixed:

- `VirtualListWindowShiftSamplesMatchingGe` and `VirtualListWindowsMatchingGe` now accept an
  optional `invalidation_detail` filter.
- Layout and prepaint virtual-list paths now share `classify_virtual_list_window_shift`, including
  structural input-change detection from render-window and previous-window `count`/`overscan`
  mismatches.
- Retained hosts still report `apply_mode=retained_reconcile` without fabricating a non-retained
  cache-root dirty detail.
- Retained DataTable now builds its retained row entries from `FlatRowOrderCache`, so sorting,
  column filters, and global filters feed the same row order used by the non-retained table path.
- Added
  `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-retained-filter-shrink-vlist-inputs-change.json`
  and promoted it into `ui-gallery-data-table-retained`.

Run results:

- `cargo fmt --package fret-ui --package fret-diag-protocol --package fret-bootstrap --package fret-ui-kit`
  - Result: passed.
- `cargo nextest run -p fret-ui virtual_list_window_shift_classification mechanism_harness_prepaint_virtual_list_window_update_matches_oracles mechanism_harness_retained_virtual_list_reconcile_matches_oracles --no-fail-fast`
  - Result: passed, 4 tests.
- `cargo nextest run -p fret-diag-protocol predicate_virtual_list_window --no-fail-fast`
  - Result: passed, 3 tests.
- `cargo nextest run -p fret-bootstrap --features "ui-app-driver diagnostics" virtual_list_window_shift_samples_matching_predicate_counts_ring_snapshots virtual_list_windows_matching_predicate_counts_ring_snapshots --no-fail-fast`
  - Result: passed, 2 tests.
- `cargo nextest run -p fret-ui-kit table_virtualized_retained --no-fail-fast`
  - Result: passed, 5 tests.
- `python tools/check_diag_scripts_registry.py`
  - Result: passed.
- `cargo build -p fret-ui-gallery --features gallery-dev`
  - Result: passed.
- `$env:FRET_UI_GALLERY_DATA_TABLE_RETAINED='1'; target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-retained-filter-shrink-vlist-inputs-change.json --dir target/fret-diag-datatable-filter-shrink-inputs-change-after-layout-prev-input-fix --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Evidence:
    `target/fret-diag-datatable-filter-shrink-inputs-change-after-layout-prev-input-fix/sessions/1778743224380-134968/1778743227800`
  - Share pack:
    `target/fret-diag-datatable-filter-shrink-inputs-change-after-layout-prev-input-fix/sessions/1778743224380-134968/share/1778743227800.zip`

Evidence anchors:

- `crates/fret-diag-protocol/src/lib.rs`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/debug_snapshot_predicates.rs`
- `crates/fret-ui/src/tree/debug/virtual_list.rs`
- `crates/fret-ui/src/declarative/host_widget/layout/scrolling.rs`
- `crates/fret-ui/src/tree/prepaint/virtual_list.rs`
- `ecosystem/fret-ui-kit/src/declarative/table.rs`
- `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-retained-filter-shrink-vlist-inputs-change.json`
- `tools/diag-scripts/suites/ui-gallery-data-table-retained/suite.json`

Next slice recommendation:

- Add a DataTable RTL idle-stability companion and a non-retained DataTable filter-shrink companion
  that asserts `scroll_handle_inputs_change_window_update` through `invalidation_detail`. This keeps
  the consumer-level virtualization gates balanced across retained/non-retained and LTR/RTL paths.

## F70: DataTable non-retained input-change detail requires an explicit view-cache runtime

Layer: UI Gallery diagnostics gate + diagnostics registry + `crates/fret-ui` virtual-list
classification telemetry

Status: covered; no new mechanism defect found

What was found:

- The F69 retained DataTable gate proved the real retained consumer path, but did not exercise the
  non-retained cache-root dirty detail because retained hosts intentionally do not fabricate
  non-retained invalidation details.
- A first draft non-retained DataTable gate failed when run against the default Gallery runtime. The
  bundle showed a correct frame-26 layout record:
  `source=layout`, `kind=escape`, `reason=inputs_change`, `apply_mode=non_retained_rerender`,
  with `window_count=111` and `prev_count=50000`.
- That failed run also showed `view_cache_active=false`, so the missing
  `scroll_handle_inputs_change_window_update` detail was not a mechanism defect. The script was
  asserting a view-cache-only detail without declaring or enabling its view-cache precondition.
- Running the same scenario with `FRET_UI_GALLERY_VIEW_CACHE=1` passed and proved the
  non-retained DataTable filter-shrink path emits `scroll_handle_inputs_change_window_update`.

What was fixed:

- Added
  `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-view-cache-filter-shrink-vlist-inputs-change.json`.
- Added the dedicated dev-only suite
  `tools/diag-scripts/suites/ui-gallery-data-table-view-cache-torture/suite.json`.
- The script metadata now explicitly records the `FRET_UI_GALLERY_VIEW_CACHE=1` precondition, and
  the registry includes the new suite membership.

Run results:

- `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-filter-shrink-vlist-inputs-change.json --dir target/fret-diag-datatable-filter-shrink-nonretained-inputs-change --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Result: failed, as expected after triage, because `view_cache_active=false` leaves
    `window_shift_invalidation_detail` unset.
  - Evidence:
    `target/fret-diag-datatable-filter-shrink-nonretained-inputs-change/sessions/1778744865979-149372/1778744869344/ai.packet`
- `$env:FRET_UI_GALLERY_VIEW_CACHE='1'; target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-view-cache-filter-shrink-vlist-inputs-change.json --dir target/fret-diag-datatable-view-cache-filter-shrink-inputs-change --session-auto --pack --ai-packet --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Evidence:
    `target/fret-diag-datatable-view-cache-filter-shrink-inputs-change/sessions/1778745510540-145220/1778745514577`
  - Share pack:
    `target/fret-diag-datatable-view-cache-filter-shrink-inputs-change/sessions/1778745510540-145220/share/1778745514577.zip`

Evidence anchors:

- `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-view-cache-filter-shrink-vlist-inputs-change.json`
- `tools/diag-scripts/suites/ui-gallery-data-table-view-cache-torture/suite.json`
- `tools/diag-scripts/index.json`
- `crates/fret-ui/src/tree/debug/virtual_list.rs`
- `crates/fret-ui/src/declarative/host_widget/layout/scrolling.rs`

Next slice recommendation:

- Add the DataTable RTL idle-stability companion next. The retained/non-retained LTR filter-shrink
  paths now cover input-change reason/detail; the remaining high-value consumer virtualization gap
  is scroll stability in RTL under a realistic table surface.

## F71: DataTable RTL idle-stability companion did not reproduce scroll jitter

Layer: UI Gallery diagnostics runtime gate + shadcn DataTable consumer surface

Status: covered; no mechanism defect reproduced

What was found:

- Checkbox, ScrollArea, and plain Table already had RTL idle-stability gates, but DataTable was the
  most realistic remaining table consumer surface because it combines a docs page, table chrome,
  toolbar/footer, direction provider, and row/cell layout.
- The DataTable RTL snippet already exposed stable anchors:
  `ui-gallery-data-table-rtl-root`, `ui-gallery-data-table-rtl-table`, and
  `ui-gallery-data-table-rtl-footer`.
- Running the new gate did not reproduce the user's reported RTL scroll jitter. The outer Gallery
  content viewport stayed stable for the no-input sampling window after scrolling to the DataTable
  RTL section.

What was fixed:

- Added `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-rtl-idle-stability.json`.
- Promoted it into `ui-gallery-rtl-smoke` and `ui-gallery-shadcn-conformance`.
- Regenerated `tools/diag-scripts/index.json`.

Run results:

- `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-rtl-idle-stability.json --dir target/fret-diag-data-table-rtl-idle-stability --session-auto --pack --ai-packet --include-screenshots --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Evidence:
    `target/fret-diag-data-table-rtl-idle-stability/sessions/1778746329247-149040/1778746333478`
  - Share pack:
    `target/fret-diag-data-table-rtl-idle-stability/sessions/1778746329247-149040/share/1778746333478.zip`

Evidence anchors:

- `tools/diag-scripts/ui-gallery/data-table/ui-gallery-data-table-rtl-idle-stability.json`
- `tools/diag-scripts/suites/ui-gallery-rtl-smoke/suite.json`
- `tools/diag-scripts/suites/ui-gallery-shadcn-conformance/suite.json`
- `apps/fret-ui-gallery/src/ui/snippets/data_table/rtl_demo.rs`

Next slice recommendation:

- Move from RTL scroll stability to either pointer occlusion/capture runtime diagnostics or an a11y
  diagnostics gate. The virtualization/DataTable consumer loop now covers retained filter shrink,
  view-cache non-retained input-change detail, and RTL idle stability.

## F72: Combobox RTL long-text was covered, but the workstream still listed it as a gap

Layer: workstream harness bookkeeping + UI Gallery diagnostics asset audit.

Status: covered; no mechanism or recipe defect reproduced.

What was found:

- The current TODO and coverage map still listed "Combobox RTL long-text companion" as an uncovered
  visual/style gap.
- The repo already contained
  `tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-rtl-long-text-geometry.json`, a
  redirect at `tools/diag-scripts/ui-gallery-combobox-rtl-long-text-geometry.json`, and suite
  memberships in `ui-gallery-combobox`, `ui-gallery-rtl-smoke`, and
  `ui-gallery-shadcn-conformance`.
- A fresh audit run passed. That means the active defect-discovery system did not need a duplicate
  script; it needed accurate coverage bookkeeping so future agents do not keep selecting a stale
  gap.

What was fixed:

- Marked the TODO item complete and recorded the actual result.
- Added an explicit RTL long-text row to `VISUAL_PARITY_MATRIX.md`.
- Updated `COVERAGE_MAP.md` so Recipe visual/style coverage includes RTL long-text geometry and no
  longer lists the RTL companion as a current gap.
- Added the rerun command and evidence anchors to `EVIDENCE_AND_GATES.md`.
- Added milestone M32 to make this audit visible in the lane history.

Run results:

- `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-rtl-long-text-geometry.json --dir target/fret-diag-combobox-rtl-long-text-audit --session-auto --pack --ai-packet --include-screenshots --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Evidence:
    `target/fret-diag-combobox-rtl-long-text-audit/sessions/1778747708234-13552/1778747711700/script.result.json`
  - Share pack:
    `target/fret-diag-combobox-rtl-long-text-audit/sessions/1778747708234-13552/share/1778747711700.zip`
  - Layout sidecar:
    `target/fret-diag-combobox-rtl-long-text-audit/sessions/1778747708234-13552/1778747713455-ui-gallery-combobox-rtl-long-text-open.layout/layout.taffy.v1.json`
  - Screenshot:
    `target/fret-diag-combobox-rtl-long-text-audit/sessions/1778747708234-13552/screenshots/1778747713519-ui-gallery-combobox-rtl-long-text-open/window-4294967297-tick-34-frame-34.png`
  - Trace proof:
    `chosen_side=top`, `side_offset_px=6.0`.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/combobox/ui-gallery-combobox-rtl-long-text-geometry.json`
- `tools/diag-scripts/ui-gallery-combobox-rtl-long-text-geometry.json`
- `tools/diag-scripts/suites/ui-gallery-combobox/suite.json`
- `tools/diag-scripts/suites/ui-gallery-rtl-smoke/suite.json`
- `tools/diag-scripts/suites/ui-gallery-shadcn-conformance/suite.json`
- `apps/fret-ui-gallery/src/ui/snippets/combobox/rtl_long_text.rs`
- `docs/workstreams/fret-mechanism-harness-v1/VISUAL_PARITY_MATRIX.md`

Next slice recommendation:

- Move to a real uncovered mechanism runtime gap instead of adding more Combobox long-text gates:
  pointer occlusion/capture UI Gallery diagnostics is the best next target because synthetic
  pointer routing fixtures exist, but the coverage map still lacks a stable runtime gate for
  overlay underlay/observer/capture paths.

## F73: Context-menu pointer occlusion gate did not prove wheel pass-through

Layer: diagnostics harness/runtime gate.

Status: fixed in the harness gate; no mechanism or recipe defect reproduced.

What was found:

- `ui-gallery-context-menu-occlusion-wheel-pass-through.json` opened the context menu and captured
  screenshots/bundles, but it did not assert that wheel input actually changed an underlay scroll
  container.
- The first strengthened draft failed at the post-wheel scroll assertion because it used the wrong
  wheel direction from the top of the content viewport. This was a script-precondition issue, not a
  runtime mechanism failure.
- After switching the wheel to the existing UI Gallery convention (`delta_y=-720` for scrolling
  down from `scroll.y=0`), the gate passed and proved the intended behavior structurally.

What was fixed:

- Added `meta.required_capabilities` to the script so the registry exposes its runtime needs.
- Added pre-wheel assertions that `ui-gallery-content-viewport` has a finite, non-zero vertical
  scroll range and starts at `scroll.y=0`.
- Changed the wheel target from the left nav item to the overlay page's underlay
  `ui-gallery-overlay-reset` button, which sits outside the open context menu but inside the page
  scroll viewport.
- Added post-wheel assertions that `ui-gallery-content-viewport.scroll.y != 0` and
  `ui-gallery-context-content` still exists.
- Refreshed `tools/diag-scripts/index.json`.

Run results:

- `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/overlay/ui-gallery-context-menu-occlusion-wheel-pass-through.json --dir target/fret-diag-context-menu-occlusion-wheel-structured-v2 --session-auto --pack --ai-packet --include-screenshots --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Evidence:
    `target/fret-diag-context-menu-occlusion-wheel-structured-v2/sessions/1778749334444-150476/script.result.json`
  - Bundle:
    `target/fret-diag-context-menu-occlusion-wheel-structured-v2/sessions/1778749334444-150476/1778749343795-ui-gallery-context-menu-occlusion-wheel-pass-through/bundle.schema2.json`
  - Share pack:
    `target/fret-diag-context-menu-occlusion-wheel-structured-v2/sessions/1778749334444-150476/share/1778749337977.zip`
  - Screenshot after wheel:
    `target/fret-diag-context-menu-occlusion-wheel-structured-v2/sessions/1778749334444-150476/screenshots/1778749343662-ui-gallery-context-menu-occlusion-wheel-pass-through-after/window-4294967297-tick-49-frame-49.png`
  - Trace proof:
    `pointer_occlusion=block_mouse_except_scroll`, `pointer_capture_active=false`, and
    `ui-gallery-content-viewport.scroll.y=535.3333740234375`.

Evidence anchors:

- `tools/diag-scripts/ui-gallery/overlay/ui-gallery-context-menu-occlusion-wheel-pass-through.json`
- `tools/diag-scripts/ui-gallery-context-menu-occlusion-wheel-pass-through.json`
- `tools/diag-scripts/suites/ui-gallery-overlay-steady/suite.json`
- `tools/diag-scripts/index.json`

Next slice recommendation:

- Continue with captured-pointer runtime diagnostics. The synthetic tree fixtures cover captured
  pointer routing, but the UI Gallery runtime side still needs a stable page that exposes pointer
  capture owner, underlay probe, and release/cancel evidence.

## F74: Pointer-capture runtime lifecycle was observable in snapshots but not script-gated

Layer: diagnostics harness/runtime gate.

Status: fixed in the harness gate; no mechanism or recipe defect reproduced.

What was found:

- `UiTreeDebugSnapshotV1.debug.input_arbitration.pointer_capture_active` already existed, and
  focused tree tests covered captured-pointer routing.
- `UiPredicateV1` had no runtime predicate for that field, so UI Gallery scripts could not directly
  assert "capture is active during drag" or "capture releases after pointer-up".
- The existing ScrollArea scrollbar drag baseline script proved scroll progress, but it could pass
  without proving the pointer-capture lifecycle.

What was fixed:

- Added `input_pointer_capture_active_is` to `UiPredicateV1`.
- Evaluated the predicate from recent debug snapshots through
  `debug.input_arbitration.pointer_capture_active`.
- Marked it as an off-window-safe diagnostics predicate.
- Strengthened `ui-gallery-scrollbar-drag-baseline-content-growth.json` so it asserts capture is
  active immediately after `pointer_down`, remains active during drag, and is false after
  `pointer_up`.
- Refreshed `tools/diag-scripts/index.json`.

Run results:

- `target/debug/fretboard-dev.exe diag run tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-baseline-content-growth.json --dir target/fret-diag-scrollbar-drag-pointer-capture --session-auto --pack --ai-packet --include-screenshots --launch -- target/debug/fret-ui-gallery.exe`
  - Result: passed.
  - Evidence:
    `target/fret-diag-scrollbar-drag-pointer-capture/sessions/1778752007390-149896/1778752011220/script.result.json`
  - AI packet:
    `target/fret-diag-scrollbar-drag-pointer-capture/sessions/1778752007390-149896/1778752011220/ai.packet`
  - Share pack:
    `target/fret-diag-scrollbar-drag-pointer-capture/sessions/1778752007390-149896/share/1778752011220.zip`

Evidence anchors:

- `crates/fret-diag-protocol/src/lib.rs`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/debug_snapshot_predicates.rs`
- `ecosystem/fret-bootstrap/src/ui_diagnostics/service.rs`
- `tools/diag-scripts/ui-gallery/scroll-area/ui-gallery-scrollbar-drag-baseline-content-growth.json`
- `tools/diag-scripts/index.json`

Next slice recommendation:

- Add captured-pointer owner/test-id attribution and underlay blocking probes to runtime scripts.
  The lifecycle gate proves active/release state, but it does not yet prove which node owns capture
  or that underlay input is blocked while capture is held.
