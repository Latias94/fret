---
title: Shadcn Parity Discovery Harness v2 TODO
status: active
date: 2026-05-11
---

# TODO

- [x] Keep the v1 lock surfaces in the v2 coverage manifest as permanent regression anchors.
- [x] Turn the first uncovered overlay surface into a parity fixture with upstream DOM evidence.
- [x] Turn the second uncovered overlay surface into a parity fixture with upstream DOM evidence.
- [x] Add the first new mobile or responsive sweep target to the same manifest-driven flow.
- [x] Classify the first new non-passing row by layer and promote it into a reusable gate.
- [x] Add the baseline modal Dialog docs-demo slice and promote its app-demo composition fix into
      the v2 suite.
- [x] Expand the sweep to menubar once the initial overlay surfaces are stable.
- [x] Expand the sweep to input OTP once the Menubar recipe lane is locked.
- [x] Add the next input-heavy or table-heavy slice after the Input OTP static geometry lane.
- [x] Add a policy-heavy DataTable slice for sorting/filtering/pagination or column visibility.
