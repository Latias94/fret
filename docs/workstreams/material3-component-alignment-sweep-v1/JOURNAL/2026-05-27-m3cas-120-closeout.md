# M3CAS-120 Closeout

Status: done
Date: 2026-05-27

## Summary

M3CAS-120 closed the Material 3 component alignment sweep with explicit follow-ons. The closeout
audit confirms that the matrix has no unclassified rows and that remaining work is split into
narrow packets.

During closeout, ModalNavigationDrawer was found to still expose a legacy hyphenated scrim id. That
was corrected into the dotted root/scrim/scrim.chrome/panel contract and covered by
`automation_surface`.

## Layer Result

- `material_recipe`: owns ModalNavigationDrawer root/scrim/panel part IDs and NavigationDrawer
  item chrome.
- `kit_policy`: still owns modal overlay containment, scrim dismissal, focus trap, and focus
  restore.
- `diagnostics/test_harness`: owns the selector proof and future navigation golden classification.
- `mechanism`: no new gap found.

## Follow-Ons

Use new narrow lanes for drawer visual evidence, named canvas draw-region diagnostics,
SearchView full behavior, picker accessibility depth, rich tooltip interactivity, bottom-sheet
chrome aliases, and further family-by-family test splits.
