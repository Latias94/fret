# TODO

Status: Closed
Last updated: 2026-05-04

- [x] Add a feature-gated cookbook example for `fret::imui::editor`.
- [x] Re-export any support nouns that the example needs from the editor facade instead of making
  cookbook import raw internal crates.
- [x] Register the example in cookbook metadata and docs.
- [x] Register the example in `fretboard` cookbook feature hints so the recommended dev command
  auto-enables `cookbook-imui`.
- [x] Add source-policy tests that lock the public facade teaching path.
- [x] Run the documented gates and record the final evidence.

Follow-on candidates, not part of this lane:

- deeper TextField draft/commit policy beyond existing editor controls,
- richer color picker popup parity,
- typed slider/drag scalar breadth,
- multi-window editor proof breadth.
