# Archived: Delinea bootstrap ADR drafts (historical)

This folder contains early ADR drafts for the `delinea` chart engine that originally lived under
`docs/adr/` with sequence numbers `0111`–`0117`.

They were moved into `docs/archive/` to avoid colliding with the repository's current ADR numbering.

Notes:

- These documents are kept for historical context only.
- The authoritative chart-engine contracts should live in the current ADR sequence (see `docs/adr/`).

