# Local generated state (captures, layouts, debug outputs).
.fret/
target/

# Safety net: avoid accidentally committing ad-hoc dump artifacts at repo root.
fret_dump_pass_state_json.fallback.response.json
