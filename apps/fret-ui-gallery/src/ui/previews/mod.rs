pub(super) mod material3;
pub(super) mod pages;

// Placeholder module. This will host extracted preview implementations to keep `ui/mod.rs`
// readable and to make future diag/perf-focused pages easier to isolate.
