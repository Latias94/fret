[package]
name = "fret-ui-assets"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
description = "UI render asset loading/caching helpers for Fret (images, SVGs, and resource registries)."

[features]
default = []
# Optional integration helpers that wire `UiAssets` into `fret-app`-based applications.
app-integration = ["dep:fret-app"]
# Optional `ElementContext` sugar (ViewCache-safe observation), consistent with other ecosystem crates.
ui = ["dep:fret-ui"]
# Optional integration with `fret-query` (ergonomics only; baseline image loading must not require it).
query-integration = ["dep:fret-query"]
image-decode = ["dep:image"]
image-metadata = ["dep:fret-ui-kit"]

[dependencies]
fret-executor = { version = "0.1.0", path = "../fret-executor" }
fret-canvas = { version = "0.1.0", path = "../fret-canvas" }
fret-app = { version = "0.1.0", path = "../../crates/fret-app", optional = true }
fret-core = { version = "0.1.0", path = "../../crates/fret-core" }
fret-runtime = { version = "0.1.0", path = "../../crates/fret-runtime" }
fret-ui = { version = "0.1.0", path = "../../crates/fret-ui", optional = true }
fret-query = { version = "0.1.0", path = "../fret-query", optional = true }
fret-ui-kit = { version = "0.1.0", path = "../fret-ui-kit", optional = true }
image = { workspace = true, optional = true }
tracing.workspace = true

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = "0.3"
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
web-sys = { version = "0.3", features = ["Window", "Response"] }

[dev-dependencies]
slotmap.workspace = true
