//! Internal helpers for Radix-style popper arrows (diamond arrows).
//!
//! The implementation lives in `fret-ui-kit::primitives::popper_arrow` so non-shadcn consumers can
//! reuse the same primitive surface. This module keeps shadcn call sites stable.

pub(crate) use fret_ui_kit::primitives::popper_arrow::*;
