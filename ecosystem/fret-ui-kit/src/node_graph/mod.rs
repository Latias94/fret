//! Node-graph related policy helpers and token mappings.
//!
//! This module is intentionally UI-policy oriented (not mechanism): it defines paint-only preset
//! schemas and theme-derived mappings that higher-level editors (like `fret-node`) can consume.

pub mod presets;
